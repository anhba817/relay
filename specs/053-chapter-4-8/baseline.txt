BASELINE — feature 053, chapter 4.8, "the log a customer can search"
Lane: RELAY_POSTGRES_PORT=15432, ClickHouse 8123 relay/relay. Opened 2026-09-16.

═══════════════════════════════════════════════════════════════════════════════
PHASE 1 — PREMISES AND OPENINGS (T001-T012).
═══════════════════════════════════════════════════════════════════════════════

T001 — THE QUICKSTART, ALL EIGHT STEPS, AND STEP 2 CAUGHT STEP 1
  Step 1 is `docker compose up -d`; step 2's positive control answered
  `curl: (52) Empty reply from server`. **ClickHouse was up and not yet answering**, and the
  control is what said so — the quickstart's own instrument working the first time it was run
  as written. CI uses `--wait` and the quickstart does not. Polled to ready, then `SELECT 1`
  -> `1`.

T007 — THE TABLE, FROM THE SERVER
    ORDER BY (environment_id, ts, request_id)
    TTL     toDateTime(ts) + toIntervalDay(30)

  FR-ANL-07's thirty days, not DR-09's ninety, exactly as `0005_connection_events.sql:4` says:
  *"one TTL clause cannot express two."*

T004 — THE THREE SHARES
    total rows                    11,683
    environment_id IS NULL         7,062   60.45%
    attributed                     4,621   39.55%

    of the attributed
      /internal                    1,656   35.84%
      /v1                          1,857   40.19%
      other                        1,108   23.98%
      endpoint absent                 22
    endpoint absent, WHOLE TABLE       32   ← the figure T015c's filter has to reach

T005 — PER-TENANT DISTRIBUTION
    median 10 · p95 155 · max 208 · 152 tenants
    busiest  af628f4e-8be5-497b-8793-9a8a8a011455, 208 rows

T006 — THE TIE RATE
    distinct (environment_id, ts) pairs   11,636
    pairs holding more than one row           42
    rows inside a tied pair                   89    0.76%
    worst pair                                 3

T007a — PARTS AND DUPLICATE KEYS, AND THE PROBE'S EFFECT IS VISIBLE
    rows 11,683 · distinct keys 11,683 · duplicates 0 · active parts 1

  **This is the state analysis pass 2's probe left**, not the state it found. That pass measured
  11,684 rows against 11,683 keys over 2 parts — one duplicate — and then ran
  `OPTIMIZE TABLE … FINAL` while testing the TTL question, which merged the table and removed
  it. The finding is recorded and no longer reproducible here, which is why T035a plants its
  own.

T008 — `quantile` AT THE SIZES A PER-TENANT-PER-HOUR BUCKET ACTUALLY HAS
    n        quantile(0.95)   quantileExact(0.95)    error
       4            3.85              4            3.7500%
      10            9.55             10            4.5000%
      25           23.80             24            0.8333%
      50           47.55             48            0.9375%
     100           95.05             96            0.9896%
     200          190.05            191            0.4974%
     500          475.05            476            0.1996%

  **R2 measured 0.9896% at n=100 and called that the worst. The real worst in this range is
  4.50%, at n=10** — and the median tenant holds ten rows in total, let alone per hour. The
  error does not fall monotonically with size either: 3.75% at four samples, 4.50% at ten,
  0.83% at twenty-five. `quantileExact` is the decision and this table is why.

T009 — `message_events`
    rows 0 · files under services/ naming it 1 — `metering.itest.ts`, a cleanup statement

  Unchanged from 4.7's close. The only code in the services tree that names the table is code
  that deletes from it.

T010 — `deliver.ts`, QUOTED RATHER THAN SUMMARISED
    82:  const started = Date.now();      ← immediately before the fetch
    104: const latencyMs = Date.now() - started;

  The endpoint's response time and nothing else. R1 holds.

T011 — THE FENCED FILES THIS FEATURE TOUCHES, COUNTED
    services/api/src/app.module.ts                  en 11 · vi 11
    packages/protocol/src/codes.ts                  en 12 · vi 12
    vitest.coverage.config.mts                      en 11 · vi 11
    services/api/src/isolation/targets.ts           en  6 · vi  6
    packages/outsider/src/integrate.itest.ts        en  1 · vi  1
    services/api/src/metering/clickhouse.ts         en  0 · vi  0
    services/api/src/request-log/{event,middleware} en  0 · vi  0

T003 — THE FENCE CHAIN AT THE OPENING, AND ONE FILE IS NOT LIKE THE OTHERS
    110 problems — APPLY 74 · HEAD 36
    HEAD splits 25 `differs at line` · 11 `does not exist in relay-platform`

  **FOUR OF THE FIVE FENCED FILES THIS FEATURE EDITS ARE ALREADY HEAD PROBLEMS, AND THE FIFTH
  IS NOT.**

    packages/protocol/src/codes.ts          differs at line 209   edit is invisible
    services/api/src/app.module.ts          differs at line  20   edit is invisible
    services/api/src/isolation/targets.ts   differs at line 130   edit is invisible
    vitest.coverage.config.mts              differs at line  29   edit is invisible
    packages/outsider/src/integrate.itest.ts        CLEAN         **an edit costs the chain**

  Seventeen analysis passes named the four invisible ones — T028d, T029c and T072 each say so —
  and **none of them named the fifth**, which is the only one the checker can see. T035d edits
  it. The cost is one, it is owed a `diff` hunk in phase 7, and it is recorded here rather than
  discovered at the ratchet.

T002 — EVERY GATE AT THE OPENING, ENUMERATED RATHER THAN COUNTED
    relay-platform
      pnpm build · lint · typecheck · test                      GREEN
      api integration lane          6 failed | 636 passed (642)
      gateway integration lane                225 passed (225)
      ingester integration lane                19 passed  (19)
      test-harness integration lane            41 passed  (41)
    relay-tutorial
      check:docs · check:srs · check:figures · check:errors     GREEN, none skipped
      pnpm build                                                GREEN
      check:fences                                              RED — 110, the inherited baseline

  **AND THE SKIP-REFUSAL RAN AND HAD NOTHING TO REFUSE**, which is the positive control for it:
  four of the six tutorial gates exit 0 when their corpus is absent, and each was checked for
  the word `skipping` rather than for its exit code alone.

T012 — THE API LANE'S INHERITED REDS, BY NAME
    request-log.itest.ts   records a served request, against a non-zero floor
    request-log.itest.ts   records an unmatched route with no endpoint, and says why
    request-log.itest.ts   records the rate limiter's 429 at all, and names the operation
    request-log.itest.ts   returns this tenant's rows and ZERO tenantless ones
    request-log.itest.ts   tells a guard refusal from a handler response at the same status
    limits.itest.ts        does not count the dispatcher, which reaches every environment

  Six, all inherited: five are 050-8 (no ingester draining) over the table this chapter reads,
  and the sixth has been red since before Part 4.

T001a/T001b/T001c/T001d — CI, AND THE CORRECTION PASS 12 MADE TO PASS 9
  All three jobs read. The `platform` job — which runs `pnpm test:integration`, `pnpm coverage`
  and the error-registry gate — provided `postgres:18-alpine`, `redis:8-alpine`,
  `nats:2.12-alpine` and **no ClickHouse**. The `outsider` job runs `docker compose up -d
  --wait`, and compose's `clickhouse` carries no `profiles:` key, so it starts there. **Pass 9
  published "CI has no ClickHouse" from a grep of `ci.yml`; the word lives in `compose.yaml`.**
  A grep for a word is not a check for a capability.

  Applied:
    + a `clickhouse/clickhouse-server:25.3` service on the `platform` job, credentials and
      `CLICKHOUSE_DB` matching `compose.yaml`, health-checked with a real query rather than
      `/ping` (chapter 4.2's lesson)
    + `RELAY_CLICKHOUSE_HOST: localhost` in the job env
    + `node analytics/apply.mjs` after the Postgres migration — `CLICKHOUSE_DB` makes the
      database and not its tables
    ~ `check-error-codes.mjs` moved ABOVE `pnpm test:integration`. It used to close the job,
      after a step that has been red since 4.4 and carries no `continue-on-error`, so the gate
      that compares `ERROR_CODES` against the published reference **never executed**
    ~ `pnpm coverage` gained `if: always()`, for the same reason

  **WHAT CI STILL CANNOT DO (T001d).** `pnpm test:integration` is
  `turbo run … --concurrency=1` and stops scheduling at the first failure: it plans 18 tasks,
  attempts 9, and prints `Tasks: 7 successful, 9 total`. One red suite still hides every lane
  ordered after it, in CI exactly as locally (`gaps.md` 051-3). Provisioning the store does not
  fix that and this line exists so nobody reads it as fixed.

═══════════════════════════════════════════════════════════════════════════════
PHASE 2 — THE QUERY CONTRACT, WITH NO STORE IN SIGHT (T013-T022).
═══════════════════════════════════════════════════════════════════════════════

T020 — THE HOSTILE WINDOW, RUN RED, AND THEN ASKED OF THE SERVER
  The phase's own test was written first and run against a version that validates the
  window as a string and interpolates it — which is what *"it is validated upstream"*
  looks like when the validation is `z.string()`. **10 of 10 red**, every payload intact
  in the statement:

    SELECT ... WHERE ts >= '2026-09-16T00:00:00Z' OR 1=1 --' AND ts < '...'
    SELECT ... WHERE ts >= '' UNION ALL SELECT * FROM system.users --' AND ts < '...'

  **THEN THE STATEMENTS WERE PUT TO THE SERVER, BECAUSE "REACHES THE PARSER" IS A WEAKER
  CLAIM THAN THE ONE AVAILABLE.** Against the lane's busiest tenant:

    scoped, honest window (1 hour)                       0 rows
    same statement, `from` carrying `' OR 1=1 --`   11,683 rows   ← the whole table
    the whole table, for comparison                 11,683 rows
    distinct environment_id under the payload          152        ← every tenant in the lane
    the tenant's own honest day                         37 rows

  **The injection does not merely widen the window — it defeats the `environment_id`
  predicate**, because `OR` binds looser than the `AND` chain the scope is written in. That
  is constitution I broken by a query parameter, and it is the figure the chapter carries.

    ' UNION ALL SELECT name FROM system.users --  →  relay, then the request ids
                                                     ^ the ClickHouse account name, read
                                                       out through a customer's log page

  **AND ONE PAYLOAD IS REFUSED BY SOMETHING ELSE, WHICH IS WORTH SAYING RATHER THAN
  CLAIMING THE CREDIT.** The `; DROP TABLE …; --` half cannot execute over this transport:

    Code: 62. DB::Exception: Syntax error (Multi-statements are not allowed)

  That is chapter 4.2's finding doing work as a second wall. The read payloads are single
  statements and it does not touch them, so a surface defended by it alone would be
  defended against the loud half of the attack and not the quiet one.

  **THE REMEDY IS A TYPE, NOT AN ESCAPE.** `z.iso.datetime({offset:true}).transform(v => new
  Date(v))` means what leaves the schema is a `Date`; `clickHouseInstant(at: Date)` is the
  only function that turns one into SQL. There is no path from a query string to a
  statement, so there is nothing to escape and nothing to forget to escape.

T014/T018 — WHAT THE SCHEMA REFUSES, AND THE ONE IT MIRRORS DOES NOT DO WHAT ITS CHAPTER SAYS
  `historyQuerySchema`'s form and bounds copied: `z.strictObject`, 1..200 default 50,
  `older`/`newer`. Measured against the real schema:

    {}                OK   {direction:"older", limit:50}
    {limit:"500"}     REFUSED  too_big
    {limit:"0"}       REFUSED  too_small
    {limt:"200"}      REFUSED  unrecognized_keys

  **AND `limit=500` IS A 400, WHERE CHAPTER 2.4 PUBLISHES A CLAMP.** The lead-in says *"the
  page cap clamps rather than rejects, so a client asking for 500 gets 200 and a cursor
  instead of a lecture"*, and the code comment inside its `diff` fence says `limit` CLAMPS
  in capitals. `.max(200)` on a zod number is a validation, not a transform. Nothing in the
  tree asserts either behaviour — `grep limit` over the messages suites finds no test above
  200 — so the refusal has been unpinned and undocumented since 2.4.

  **THE COMMENT IS IN EIGHT FENCES ACROSS FOUR CHAPTERS IN EACH LOCALE.** 2.4 adds it;
  3.11, 3.17 and 3.18 carry it as `diff` CONTEXT. Changing the tree's comment alone
  unanchors six hunks. Scheduled as **T069a** in phase 7, where the chain is measured.
  The words are what get fixed, not the code: the refusal is what clients have been written
  against for two parts, and this chapter's own surface refuses `limit` for a stated reason.

T015/T019 — WHY ONE FIELD IS CLAMPED AND THE OTHER REFUSED
  `from` is clamped to `now - 30 days` and reported back; `limit` outside 1..200 is a 400.
  The asymmetry is a decision: `limit`'s bound is published in the contract, so a caller
  outside it can see their mistake, and the retention edge moves every second and is
  published as a guarantee rather than as a bound. A caller cannot know it when they write
  the request. **FR-ANL-08 says ninety days and FR-ANL-07 retains thirty**, so a caller
  reading the requirements asks for a window the store cannot answer.

  A window ENTIRELY older than the edge resolves to `empty` rather than to a refusal or a
  bare zero — the caller reads `window` and `retention_edge` and learns the log is gone
  rather than that nothing happened (R8). The reader skips the store for it.

T016/T017 — THE CURSOR, AND ITS OWN REFUSAL BLOCK CAUGHT ITS FIRST BOUND
  `rl:<epochMs>:<uuid>`, base64url, decode refusing anything it did not produce. The pair
  is what T006's 42 tied `(environment_id, ts)` pairs need; a `ts`-only cursor skips or
  repeats all 89 of those rows.

  **`Number.isSafeInteger` IS NOT A BOUND ON AN INSTANT.** The first draft used it, and the
  block of ten refusals written beside it went red on `rl:999999999999999:<uuid>` — fifteen
  digits, a perfectly safe integer, **the year 33658**, headed for a `DateTime64(3)`
  comparison the column cannot express. Both ends are the column's own facts now: the floor
  is the DDL's `CHECK ts > toDateTime64('2020-01-01 …')` and the ceiling is `DateTime64`'s
  representable maximum, 2299-12-31.

  AND THE COMPARISON STAYS IN SQL. `request_id` is a `UUID` column and ClickHouse does not
  order UUIDs by their canonical text, so the token carries the text and the store compares
  it as a `UUID` — comparing them as strings in the statement would draw a page boundary
  the table's own `ORDER BY` disagrees with.

T021 — THE PINS, AND BOTH HALVES OF THE PROBE
    services/api/src/request-log/cursor.ts              100 / 100 / 100 / 100, twice
    services/api/src/request-log/request-log.schema.ts  100 / 100 / 100 / 100, twice

  **PINNED AT 100 AFTER THE BRANCH REPORT FOUND TWO UNCOVERED LINES THAT WERE UNCOVERED FOR
  OPPOSITE REASONS.** `cursor.ts` first read 89.47 / 83.33 naming lines 76 and 79. Line 76's
  guard could never fire — `Number.isSafeInteger` behind a `\d{1,15}` pattern that cannot
  produce an unsafe integer — and was deleted, which is chapter 4.6's finding one file over.
  Line 79's could and never had been asked: the splitting pattern admits `[0-9a-fA-F-]{36}`,
  so thirty-six dashes reach the UUID check, and only the second check knows where the
  dashes go. One arm went each way, which is what makes the 100 worth having.

  **THE PIN SWEEP: 55 literal pins, 0 unbindable**, with a positive control asserting the
  sweep read the threshold block at all (049's rule — a control that proves the checker
  works says nothing about whether it looked).

  **BOTH HALVES OF THE THRESHOLD PROBE, RE-RUN (045's trap, third time):**

    an impossible pin on a REAL file      ERROR: Coverage for lines (100%) does not meet
                                          "src/request-log/cursor.ts" threshold (101%)
    a pin on a path that does not exist   silent — no error, no warning, nothing

T022 — THE GATES
    pnpm lint        green   1 pre-existing warning (`session.perf.itest.ts:120`), 0 errors
    pnpm typecheck   14 of 14
    pnpm test        12 of 12 lanes; api 33 files / 350 tests (+2 files, +59 tests)

  Nothing in this phase touches a store, a database or a broker. That is what it was
  separated for, and it is why the two coverage numbers are the same under the unit lane and
  under the workspace one.

═══════════════════════════════════════════════════════════════════════════════
PHASE 3 — THE READER AND THE ROUTE (T023-T033).
═══════════════════════════════════════════════════════════════════════════════

T023a-e — THE DEADLINE, BOTH HALVES, MEASURED BEFORE BEING BUILT
    SETTINGS max_execution_time = 1, long scan   HTTP 408 in 1.002 s
                                                 Code: 159 … (TIMEOUT_EXCEEDED)
    SELECT nope FROM api_requests                HTTP 404
                                                 Code:  47 … (UNKNOWN_IDENTIFIER)

  **The status already told the two cases apart and only the client was losing it.**
  `createAnalyticalStore` kept `text.trim().split("\n")[0]` and threw the rest away, so a
  caller needing to tell a timeout from a bad query had to string-match `Code: 159` — a
  parse of a server error message to recover a fact the response carried. It throws
  `AnalyticalStoreError` with `status` now; **status 0 means no answer at all** — an abort,
  a refused connection — so a caller maps one value rather than three strings.

  The client half is `AbortSignal.timeout(3000)`, opt-in on `createAnalyticalStore` so
  chapter 4.7's reconciler keeps the behaviour it was measured with. The server half is
  `max_execution_time = 2`, **shorter on purpose**, so the server's refusal wins the race
  and the route receives a code it can map instead of an `AbortError` carrying nothing.

  AND THE 503 MAPS ON WHOSE FAULT IT IS. 408 and 0 are the analytical pipeline being
  unavailable while the api is up — constitution III's second clause exactly — and the
  client should retry. **404 and 400 are our statement being wrong**, and telling a customer
  to retry a query that will never work is worse than telling them nothing. Those stay
  `internal_error`.

T025 — TWO EXTRA COLUMNS SHIP AND ONE DOES NOT, AND THE ONE THAT DOES NOT IS THE INFERRED ONE
  FR-ANL-07 names six fields; the table carries four more.

    principal_kind      RETURNED — the only handle a customer has on the `/internal/*`
                        rows this surface decided to show them
    limited_operation   RETURNED — the answer to "why was I 429'd", and the limiter STAMPS
                        it, so the value is observed
    refused_at          NOT RETURNED — two of its four arms are INFERRED. Chapter 4.4
                        measured that a guard refusal and a handler response are identical
                        from the producer's vantage point, so `handler` is an inference
                        from silence, and a customer acting on a plausibly-wrong value in a
                        column nothing flags is worse off than one who never saw it
    environment_id      NOT RETURNED — the caller is the tenant

T026a/T026c — ABSENCE IS NOT A STRING, AND THE COMMON COLUMN IS THE ONE THAT WOULD HAVE HURT
    endpoint            NULL on    31 real rows (23 rate-limited, 8 unmatched) + 1 fixture
    limited_operation   NULL on 11,660 of 11,683 — 99.8% of the table

  `SELECT endpoint, limited_operation … WHERE status = 429` answers `\N<TAB>rest`. A reader
  taking the value column alone reports the string `"\N"`, which is truthy and
  indistinguishable from a route name downstream. The statement selects `<column> IS NULL`
  as a column of its own and the presence column decides — chapter 4.7's `count()` move
  against the same class one table over.

  **RUN RED (T026b), 3 of 10**, against a reader taking the value column alone:
  `AssertionError: expected '\N' to be null`. And the suite carries the control for its own
  claim: it asks the store directly and asserts it really does answer `\N`, so the tests
  are about the reader rather than about a store that happens never to produce it.

  THE 32 ENDPOINT-LESS ROWS, SPLIT, WHICH T004 COUNTED AND DID NOT CHARACTERISE:

    429 · limited_operation set · refused_at middleware   23   (rest 14, send 7, signup 2)
    404 · limited_operation NULL · refused_at unmatched    8
    200 · refused_at handler · request_id 7777…7777        1   a planted fixture, not a row
                                                               the producer made

T028a-c — THE DERIVATION FOUND THE ROUTE BEFORE THE LIST DID, FOR THE SEVENTH TIME
    before the entry   43 derived, 42 classified   unclassified: ["GET /v1/request-log"]
    after the entry    9 of 9 green
    second direction   an entry matching no target: stale: ["GET /v1/request-log-renamed"],
                       and `CLASSIFICATIONS.length` 44 against 43

  **AND THE SUITE ENFORCES A THIRD DIRECTION THE TASKS DID NOT NAME.** With the entry added
  and no attack written, `gauntlet.itest.ts` failed with *"classified but never attacked:
  GET /v1/request-log"*. Classifying a route is not covering it; the accounting test at the
  foot of that file is what makes the difference a failure.

  THE ATTACK PLANTS ITS OWN ROWS, WHICH NO OTHER ONE IN THAT FILE HAS TO. `compose.yaml`
  runs no ingester (050-8), so both tenants' logs are empty on a fresh lane — and an empty
  log passes a leak check for the same reason an empty page does. Without the plant the
  test could not fail for its own reason. It plants three rows: the attacker's, the
  victim's, and **one with a NULL `environment_id`**, so the attack shows two things rather
  than one — no other tenant's row comes back, and no tenantless row does either.

  57 of 57 green.

T028c — A DERIVED LIST WAS THE WRONG FIX, AND THE EXISTING TEST SAID SO
  `rowsOf` in `attack.ts` knew two body shapes — a bare array and `{ data }` — and this
  chapter serves a third, `{ requests }`. The first repair replaced the lookup with *"the
  first array-valued property"*, on this project's own reflex that a hand-maintained table
  cannot be checked. **`rowsOf({ items: [1,2,3] })` must equal `[]` is an assertion that
  file already carried**, and it went red.

  The reflex has a precondition and this table meets it: the shape set is pinned by that
  assertion AND by the `count > 0` control every `list` attack carries, so an unknown shape
  fails loudly rather than passing. The derived version was worse in the direction that
  matters — it would count ANY array in the body as rows, which is a false pass where an
  unrecognised shape is a failure. `{ requests }` added by name; a new pin,
  `rowsOf({ cursors: [1,2,3], data: [1] })` → length 1, records why.

  **What the named set costs is the failure MESSAGE**: an unrecognised shape reads as *"the
  attacker's own listing came back empty"*, which names a symptom. The pin test is what
  turns that into a sentence about the recogniser.

T029a — THE CODE, REGISTERED FIVE PHASES BEFORE THE GATE THAT WOULD HAVE CAUGHT IT
  `analytics_unavailable` is the **first 503 in `ERROR_CODES`** and the first code that
  names a subsystem rather than a mistake. `check:errors` after the edit:
  **28 codes, 28 sections**, both directions. The throw names it, because
  `ProtocolErrorFilter`'s ladder covers 400/401/403/404 only and a 503 falls past it into
  `internal_error`.

T030-T032 — THE SUITE, AND ITS OWN ASSERTION WAS SCOPED TOO WIDE
  `query.itest.ts`, 10 tests, green. `beforeAll` asks `SELECT 1` before trusting anything
  else the store says; every statement names this suite's own environment ids (FR-024); the
  cleanup polls until `count() = 0` for each and throws if it does not reach it.

  **A FILTER TEST FAILED FOR ANOTHER TEST'S REASON, EIGHT LINES UP.** It asserted the
  endpoint filter returned exactly `["/healthz"]` and got `["/healthz", "/healthz"]` —
  because the `FINAL` dedup test above it plants a second `/healthz` row and the two share
  a tenant. What a filter promises is that nothing ELSE comes back; the count of what does
  is the plant's business. Asserted as a set now.

T033 — THE GATES
    pnpm lint                 green, 0 errors
    pnpm typecheck            14 of 14
    pnpm build                exit 0
    check:errors              28 codes, 28 sections
    api integration lane      6 failed | 647 passed (653) — the six are T012's, by name,
                              and the +11 are this phase's 10 plus the gauntlet's attack
    check:fences              110 -> 112, delta +2

  **THE +2 IS THE TWO CLEAN FENCED FILES THIS PHASE HAD TO EDIT**, predicted before the
  edit and named by the checker after it: `services/api/src/isolation/attack.ts` at line 135
  and `services/api/src/isolation/gauntlet.itest.ts` at line 10. Phase 1 found one such file
  (`packages/outsider/src/integrate.itest.ts`); there are three. Every other file this phase
  touched — `codes.ts` (12 fences/locale), `app.module.ts` (11), `targets.ts` (6),
  `fixtures.ts` (3) — is ALREADY a HEAD problem, so those edits are invisible to the gate.
  T072 collects them and T069/T070 owe two `diff` hunks.

═══════════════════════════════════════════════════════════════════════════════
PHASE 4 — WHAT THE LOG CAN AND CANNOT SHOW (T034-T045).  MVP.
═══════════════════════════════════════════════════════════════════════════════

T040b — HOW FAR BEHIND THE LOG ACTUALLY IS, WITH AN INGESTER RUNNING
    sample 1  0.29 s      n=6/6
    sample 2  1.61 s      min  0.29 s
    sample 3  1.60 s      p50  1.60 s
    sample 4  1.33 s      max  1.61 s
    sample 5  1.61 s
    sample 6  1.60 s      2.7% of FR-ANL-04's 60 seconds

  Request made through the api, its `X-Request-Id` read off the response, then the surface
  polled at 250 ms until that id came back. The clustering at 1.60 s is the ingester's
  two-second batch window; sample 1 caught a flush that was already about to happen.

  **AND IT IS UNDER FR-DSH-02's TWO SECONDS TODAY**, which is the clause T074a files
  forward: the dashboard requirement says 2 s and FR-ANL-04 says 60, a 30x gap, and this
  lane sits inside both. That makes the tension a question about production scale rather
  than a contradiction anyone can see here — which is the honest form of the gap.

  **WHAT STARTING AN INGESTER COST THE LANE, SAID RATHER THAN LEFT IMPLICIT.** It drained
  `ANALYTICS` for everything: **188 rows over 15 batches**, and the first batch alone wrote
  137 — a backlog that had been sitting on the stream. `api_requests` went 11,704 -> 11,892,
  so **T004's opening figures have moved and T073a re-takes them at the close**. The
  process was stopped when the measurement finished.

T035c/d/e — THE SEALED SUITE, STOOD UP, AND IT HAD NEVER PASSED
  CI's `outsider` job copied rather than paraphrased: stores up, migrate from `dist`,
  `--profile services build`, `up -d --wait`, seed captured from stdout. The two new tests
  passed on the first run. **A third test failed, and it was not this feature's:**

    expected 'https://relay.example/docs/error-reference#not_found' to contain '/not_found'

  `integrate.itest.ts` asserted a PATH per code under a comment ending *"Asserted as this
  platform actually answers."* It was not. `docsUrl` returns `${base}#${code}`, and the
  commit that made it an anchor (`0381d54f`) landed **before** the commit that wrote this
  suite (`2c65bbeb`) — checked with `git merge-base --is-ancestor`, both on 2026-09-10.
  **So the sealed suite has never passed since it was written**, and nothing said so
  because it needs a running platform no lane starts.

  The platform is right and the test was wrong: `docs/08-error-reference.md` is ONE page
  with a section per code, so a path per code would 404 for every refusal this platform
  sends. Corrected; **18 of 18 green**, the first time.

  What the new tests assert: the documented envelope, both cursors declared rather than
  absent, the 400 with `field: "limit"` — and **that the log is empty**, after this suite
  has made a dozen calls with that credential. The platform ships no ingester, so the
  records are published and wait. That is FR-028's gap where a customer meets it.

T041/T036b — A PAGE READS THE WHOLE TABLE, AND R6's 8,194 IS SUPERSEDED
    50-row page, 24h window, one tenant     read 11,695   returned 51
    the same page without FINAL             read 11,695   returned 51
    a 31-day window                         read 11,695   returned 51
    filtered by endpoint                    read 11,695   returned 51
    filtered by status                      read 11,695   returned 24
    both filters                            read 11,695   returned 23

  **Every query reads the entire table, whatever the window and whatever the filter.**
  `EXPLAIN indexes=1` says `PrimaryKey … Granules: 1/1`: the condition is right, the key is
  right, and there is exactly one granule to select.

T041a — WHY, AND IT IS THE PART TYPE RATHER THAN THE GRANULARITY
    SHOW CREATE              SETTINGS allow_nullable_key = 1, index_granularity = 8192
    system.parts             part_type Compact · rows 11,695 · marks 2
    mergeTreeIndex()         2 entries — one granule and the terminal mark

  **8,192 IS DECLARED AND IS NOT IN FORCE.** Built by construction rather than argued: two
  scratch tables, same 11,695 rows, same `index_granularity = 8192`, differing only in
  `min_bytes_for_wide_part`:

    granule_probe_compact   Compact   11,695 rows   marks 2   → 1 granule
    granule_probe_wide      Wide      11,695 rows   marks 3   → 2 granules

  So the part type decides, the table is Compact because it is under 10 MiB, and the
  per-tenant filter skips nothing until the table crosses that line. **R6's 8,194 was a
  measurement of a Wide part** — one granule of 8,192 — and reading it as "the engine's
  floor" generalised a number that depends on how much data there is.

  AND WHAT `FINAL` COSTS, AGAINST A TABLE WITH PARTS. `SYSTEM STOP MERGES`, six inserts of
  200 rows for a probe environment, then three runs a side:

    7 parts, 12,895 rows     FINAL 3 ms   plain 2 ms   read_rows identical, 12,895 both

  Three runs each, identical every time. The analysis probe's 2.43 vs 2.02 was measured on
  a single-part table — the best case, and it proves nothing about the shape. Merges
  restarted and the probe rows deleted in the same command, verified at 0.

T042 — FR-ANL-08's NINETY DAYS CANNOT BE REACHED IN THIS TABLE AT ANY VOLUME
    two rows planted for one environment   at now-60d and now-1d
    survivors straight after the INSERT     1
    survivors after OPTIMIZE TABLE FINAL    1      the survivor is 1 day old

  The 30-day TTL removes rows AT INSERT (4.2's finding, reproduced), so no fixture can put
  a row older than 30 days in this table — the clause is unreachable by construction rather
  than by the lane being small. Handed to FR-016 rather than dressed up with a p95 from the
  208-row tenant.

T043 — THE PINS, AND TWO OF THREE MOVEMENTS WERE REAL GAPS
    request-log/cursor.ts               100 / 100   / 100 / 100     twice
    request-log/request-log.schema.ts   100 / 100   / 100 / 100     twice
    request-log/request-log.controller.ts 100 / 100 / 100 / 100     twice
    request-log/reader.ts               100 /  86.79 / 100 / 100    twice

  `reader.ts` read **79.24 → 83.01 → 86.79**, and only the last step was rounding:

    79.24   `refuse()`'s re-throw arm and the malformed cursor — two decisions this
            chapter argued for in a comment and never drove. `reader.test.ts` drives them.
    83.01   **`direction: newer` had never run.** In the contract and the schema since
            phase 2, and every test used the default. Two expressions flip with it — the
            cursor comparison and the sort — and a page that got one and not the other
            returns the right rows in the wrong order.
    86.79   eleven `??` defaults `noUncheckedIndexedAccess` forces over a ten-column TSV.

  AND THE CONTROLLER'S 403 IS AT 100 BECAUSE A UNIT TEST DRIVES IT, NOT BECAUSE THE ROUTE
  CAN REACH IT. `@Accepts("application")` refuses every other class at the door and an
  application credential always carries an environment, so FR-011's refusal is unreachable
  over HTTP today. Kept rather than deleted — the difference from 4.6's dead branches is
  that a requirement asks for this one.

  **AND THE RATCHET CAUGHT A REGRESSION OF MINE.** `metering/clickhouse.ts` fell 94.73 ->
  91.3 against a pin of 93: the new `catch` around `fetch` types `cause` as `unknown`, so
  `cause instanceof Error ? … : …` has an else the runtime never takes. That is the second
  arm of exactly the kind 4.7 already recorded at line 115. Lowered to 91 with both named,
  because driving either means mocking `fetch` — a green number from testing a stub.

T044/T044a — THE SWEEPS, AND ONE CHECKER HAD STILL NEVER LOOKED
    pin sweep                   57 literal pins, 0 unbindable, positive control present
    threshold probe, half A     ERROR: Coverage for lines … threshold (101%)
    threshold probe, half B     silent — a pin on a path that does not exist says nothing

  **`check-lane-scope.py` STILL POINTED AT A WORKTREE 045 DELETED.** Line 28 read
  `/home/dong/work/relay/tmp/part3-refactor`, which does not exist, so the glob matched
  nothing and the script exited 0 with all ten of its own controls firing:

    before   controls: 10 of 10 fired · 0 integration files, 0 unscoped read(s)
    after    controls: 10 of 10 fired · 55 integration files, 0 unscoped read(s)

  **049 measured the retarget and never landed it** (049-3 records "50 files, 0 unscoped
  reads"), so the same line was still here two features later. Retargeted off the file's
  own location, and given the repair 045-81 wrote for the fence checker: a run that reads
  no files now **REFUSES with exit 2** rather than reporting zero. Both halves checked —
  exit 2 on a dead path, exit 0 on the real tree.

T034-T040a — WHAT THE ASSERTIONS ADDED UP TO
  `query.itest.ts` 19 tests, `route.itest.ts` 14, `reader.test.ts` 5,
  `request-log.controller.test.ts` 4, plus phase 2's 39 — 78 in `request-log/` in total.

  **THE DEDUP TEST RUN RED WITHOUT `FINAL`**: 1 of 19, `expected [ …, … ] to have a length
  of 1 but got 2`. It stops merges, asserts the physical count is 2 before asserting the
  surface returns 1, and its `finally` restores merges AND deletes its own probe rows —
  049's whole shape rather than the half that names the hazard.

  AND THE `/internal/*` DECISION IS ASSERTED AS BEHAVIOUR: the rows come back, and the
  caller can filter them out. Same for the REST budget — two reads, `x-ratelimit-remaining`
  down by one — which makes the loop the chapter's rather than an oversight.

T045 — THE LANES
    pnpm lint                  green, 0 errors
    pnpm typecheck             14 of 14
    pnpm test (unit)           12 of 12 lanes
    api integration            6 failed | 670 passed (676) — T012's six, by name
    gateway integration        1 failed | 224 passed (225) — `typing.itest.ts`, A FLAKE:
                               run alone twice, green once and red once
    dispatcher integration     16 of 16
    ingester integration       19 of 19
    test-harness integration   41 of 41
    outsider (sealed)          18 of 18 — the first time
    pnpm test:integration      Tasks: 8 successful, 10 total — it plans 18 and attempts 10,
                               so 051-3 still holds and the number moved from 7 of 9
    pnpm coverage              7 failed | 1684 passed (1691); 3 threshold errors, of which
                               `shape.ts` 95.12 vs 100 is inherited (052-2) and
                               `clickhouse.ts` was mine, now pinned at 91
    check:fences               112 -> 113, delta +1 — `integrate.itest.ts` at line 654,
                               the third and last of the clean fenced files this feature
                               edits. Total from the 110 opening: +3, all three named.

═══════════════════════════════════════════════════════════════════════════════
PHASE 5 — THE PERCENTILE REQUIREMENT, RESOLVED RATHER THAN APPROXIMATED (T046-T054).
═══════════════════════════════════════════════════════════════════════════════

T046/T047 — R1's THREE READINGS, VERIFIED IN THE TREE RATHER THAN COPIED
    deliver.ts:82    const started = Date.now();            immediately before the fetch
    deliver.ts:104   const latencyMs = Date.now() - started; immediately after it
    deliver.ts:115   latency_ms: latencyMs                   and again at :126

  So `webhook_attempts.latency_ms` is **the endpoint's response time and nothing else** —
  no commit, no outbox, no JetStream, no claim, no retry gap. The schema said so when it
  was written and nothing had checked since: `0003_webhook_attempts.sql:20` reads *"How
  long the ENDPOINT took to answer. NOT `message_events.delivery_latency_ms`, which is how
  long a message took to reach a client and still has no producer."*

T048 — THE READING CHOSEN, AND THE TWO NOT CHOSEN
  **Socket fan-out: commit → the frame written to a subscriber's socket.** It is the
  real-time path constitution II is about, and it is the reading
  `message_events.delivery_latency_ms` was created for — the schema comment above names it
  by that description.

  *Webhook delivery* is the last leg only and already has a column and a name; relabelling
  it would publish a number under a name that means something else, which is what chapter
  4.6 avoided by defining a connection-minute before billing one. *A REST history read* has
  no delivery in it — a read nobody made has no latency, and it is pull rather than push.

  Written into FR-ANL-10. The revision row is phase 6's (T060), so the clause moves once
  for this whole feature rather than twice.

T051/T053 — THE PRODUCER DOES NOT EXIST, AND 048-2 IS RESTATED RATHER THAN CLOSED
    message_events                       0 rows · 0 rows carrying a latency
    occurrences under services/          3 — and 4.7's re-measurement said 0

  **THE THREE ARE NOT PRODUCERS.** All three are in `services/ingester/src/metering.itest.ts`:
  a comment saying the table has no producer, a cleanup `DELETE`, and a test's own `INSERT`.
  So the item holds and its wording does not: *"0 occurrences"* is now false and *"0
  producers"* is what was always meant. **Re-measured rather than copied, which is what
  found it** — six features have now carried this item.

  **AND THE ONE THING THAT WRITES THE TABLE WRITES NULL INTO THAT COLUMN ON PURPOSE.**
  `scripts/scale/load-analytics.mjs` names `delivery_latency_ms` in its column list and
  supplies `CAST(NULL AS Nullable(UInt32))` for it, in both halves of its `UNION ALL`. That
  is stronger evidence than an absence: the loader had the chance and declined.

  What building it would cost: both instants carried across a service boundary — the commit
  is the api's and the socket write is the gateway's — on the busiest path in the platform.
  The same cost chapter 4.6 weighed and deferred for a different column.

T049 — `quantile` HAS NO EXACT REGIME, AND ON A SKEWED SAMPLE THE ERROR GROWS WITH n
  Uniform 1..n, against the server:

    n        p50 err%   p95 err%   p99 err%
    4        16.6667     3.7500     0.7500
    10        8.3333     4.5000     0.9000
    20        4.5455     4.7500     0.9500
    50        1.9231     0.9375     0.9800
    100       0.9804     0.9896     0.9900     ← R2's single published figure
    155       0          0.4730     0.3506
    171       0          0.3067     0.4118
    1,000     0.0998     0.0999     0.0999
    10,000    0.8998     0.1216     0.0900

  **It is not monotone.** The p50 error falls to exactly 0 at n=155 and rises again by
  n=10,000, so "it gets better with more data" is false and the zeros are a coincidence of
  that distribution rather than a threshold.

  And on a bounded skewed sample — which is what a latency is — it is worse and it climbs:

    n        p50 err%   p95 err%   p99 err%
    4         2.6162     0.8200     0.1640
    20        3.4785     7.8642     1.5728
    100       4.7263     9.0398     9.4206
    1,000     4.7579     9.0404     9.4211
    10,000    9.5158     9.5163     9.5163

  **THIS IS THE OPPOSITE SHAPE FROM 4.7's `uniq`**, which is exact below 65,536 and wrong
  above. There is no n above which `quantile` is safe.

  AND THE SHARPEST FIGURE IS THE REAL ONE. `webhook_attempts`, the only latency sample this
  platform holds, n=64:

    quantile(0.50)  2          quantileExact(0.50)      2
    quantile(0.95)  2001       quantileExact(0.95)   2001
    quantile(0.99)  4961.26    quantileExact(0.99)  10000    ← 50.39% LOW

  Only the p99 diverges, and it diverges by half — **in the direction that matters**,
  because an alert threshold set on that number never fires. The distribution is why: 43 of
  64 attempts answer in 0–2 ms and one took 10,000, so the tail is one row and an
  approximation that smooths it reports a platform that is fine.

T050 — NOT TAKEN, AND THE BUCKET SIZE IS WHY IT WOULD NOT HAVE HELPED
  Per tenant per hour over the request log, which is the bucket FR-ANL-10 asks for:

    buckets 178 · rows 4,760 · median 10 · p95 155 · max 171
    buckets holding fewer than   5 rows    73 of 178
    buckets holding fewer than  20 rows   125 of 178
    buckets holding fewer than 100 rows   163 of 178

  **A p99 over four samples is a maximum wearing a percentile's name**, and 73 of 178
  buckets are in that state. The clause's own grain is what makes the number weak, which is
  independent of which function computes it — so any implementation publishes `n` beside
  every percentile, and the amended clause says so.

T052 — NOTHING PUBLISHES A PERCENTILE UNDER THIS CLAUSE'S NAME, CHECKED
  `grep FR-ANL-10` over `relay-platform/` returns two hits, both comments in `analytics/`
  SQL, neither computing anything. The percentiles that do exist —
  `scripts/scale/measure.mjs` and `session.perf.itest.ts` — are send latency and connect
  latency, each labelled as what it measures. **The phase publishes no percentile at all**,
  which is the only way to satisfy the requirement while the producer is absent.

  AND CHAPTER 4.2 PREDICTED OTHERWISE IN PRINT. Its prose reads *"will keep writing NULL
  until FR-ANL-10's chapter builds one"* — and FR-ANL-10's chapter is this one, which does
  not. Filed as **T058b** for phase 6; the prose costs the chain nothing, and the identical
  sentence inside a fence is the platform's own file and moves with the clause.

T054 — THE LANES
    pnpm lint         green
    pnpm typecheck    14 of 14
    pnpm test         12 of 12 lanes
    api integration   6 failed | 670 passed (676) — T012's six, unchanged

  This phase changed no platform code. Its product is a clause and four tables of numbers,
  which is what a chapter can do about a requirement whose quantity was never defined.
