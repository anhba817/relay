{
"single": {
"  // Chapter 3.8. The fifth, and the only one that is not a store: Mailpit": "  // The fifth, and the only one that is not a store: Mailpit",
"  /** Chapter 3.2: the suite needs a real credential now, and it mints one the": "  /** The suite needs a real credential now, and it mints one the",
"     * present now that the asserted header is gone (chapter 3.2). */": "     * present now that the asserted header is gone. */",
"  /** Set an environment's quota policy (chapter 3.10).": "  /** Set an environment's quota policy.",
"      // Chapter 3.3: the api's relay needs the broker's address. Forwarded,": "      // The api's relay needs the broker's address. Forwarded,",
"      // Chapter 3.5: the api decrypts webhook signing secrets and authenticates": "      // The api decrypts webhook signing secrets and authenticates",
"      // Chapter 3.8: the failed-authentication threshold and the counter's key": "      // The failed-authentication threshold and the counter's key",
"    // Chapter 3.3: the api children run WITHOUT the outbox relay. This journey": "    // The api children run WITHOUT the outbox relay. This journey",
"    // Chapter 3.8: and no notification relay either, for the same reason. This": "    // The rate-limit chapter: and no notification relay either, for the same reason. This",
"    // Chapter 3.4: no event consumer in these children either, for the reason": "    // No event consumer in these children either, for the reason",
"    // Chapter 3.5: nor the delivery relay, for the third time and the same": "    // The webhook dispatcher chapter: nor the delivery relay, for the third time and the same",
"  /** Chapter 3.2: the harness cannot sign a token any more, and that is the": "  /** The harness cannot sign a token any more, and that is the",
"        // Chapter 3.2: the REST assertions present a credential, not a header.": "        // The REST assertions present a credential, not a header.",
"// Invariant 14, first half (chapter 3.5): with the dispatcher absent, end users": "// Invariant 14, first half: with the dispatcher absent, end users",
"    // THIS TEST WAS RED FOR TWO CHAPTERS AND NOBODY SAW IT (chapter 3.17, T065).": "    // THIS TEST WAS RED FOR TWO CHAPTERS AND NOBODY SAW IT (T065).",
"  /** T033 (chapter 3.24). ATTACHMENTS THROUGH THE SHIPPED BINARY.": "  /** T033. ATTACHMENTS THROUGH THE SHIPPED BINARY.",
"  it(\"holds five connections and is refused a sixth with 4004 (FR-RTM-09 (3.22))\", async () => {": "  it(\"holds five connections and is refused a sixth with 4004 (FR-RTM-09)\", async () => {",
"// T010 (chapter 3.24). THE EXACT KEY SET AND EVERY REFUSAL, on `codes.test.ts`'s": "// T010. THE EXACT KEY SET AND EVERY REFUSAL, on `codes.test.ts`'s",
"describe(\"the attachment shape (FR-002 (3.24), FR-003b (3.24), FR-020 (3.24))\", () => {": "describe(\"the attachment shape (FR-002, FR-003b, FR-020)\", () => {",
"  it(\"accepts the three kinds and refuses a fourth (FR-002 (3.24))\", () => {": "  it(\"accepts the three kinds and refuses a fourth (FR-002)\", () => {",
"describe(\"the scheme rule is not z.url() (FR-004 (3.24), R7)\", () => {": "describe(\"the scheme rule is not z.url() (FR-004, R7)\", () => {",
"  it(\"publishes the allowed set, so a caller can read it (FR-004 (3.24))\", () => {": "  it(\"publishes the allowed set, so a caller can read it (FR-004)\", () => {",
"describe(\"the bounds (FR-005 (3.24), FR-023 (3.24))\", () => {": "describe(\"the bounds (FR-005, FR-023)\", () => {",
"  it(\"accepts exactly ten in a list and refuses eleven (FR-005 (3.24))\", () => {": "  it(\"accepts exactly ten in a list and refuses eleven (FR-005)\", () => {",
"describe(\"the media arm refuses and SAYS SO (FR-003 (3.24), FR-003a (3.24))\", () => {": "describe(\"the media arm refuses and SAYS SO (FR-003, FR-003a)\", () => {",
"  it(\"leaves room for \u00a74.14 rather than requiring a new discriminator (FR-020 (3.24))\", () => {": "  it(\"leaves room for \u00a74.14 rather than requiring a new discriminator (FR-020)\", () => {",
"describe(\"the text-and-attachments pair rule (FR-019 (3.24), FR-019b (3.24))\", () => {": "describe(\"the text-and-attachments pair rule (FR-019, FR-019b)\", () => {",
"  it(\"accepts an attachments-only message with an empty text (FR-019 (3.24))\", () => {": "  it(\"accepts an attachments-only message with an empty text (FR-019)\", () => {",
"  it(\"refuses neither text nor attachments, and names a field (FR-019b (3.24))\", () => {": "  it(\"refuses neither text nor attachments, and names a field (FR-019b)\", () => {",
"/** THE ATTACHMENT SHAPE (chapter 3.24, FR-MSG-11's P2 half).": "/** THE ATTACHMENT SHAPE (FR-MSG-11's P2 half).",
"  it(\"names the non-author refusal separately from the generic 403 (chapter 3.23)\", () => {": "  it(\"names the non-author refusal separately from the generic 403\", () => {",
"    // FR-022 (3.23). The registry's own rule is that a specific code beats the generic": "    // FR-022. The registry's own rule is that a specific code beats the generic",
"      // Chapter 3.16. A banned user is refused at connect, so the socket says this": "      // A banned user is refused at connect, so the socket says this",
"  // Chapter 3.2. The SRS singles this out as the most common first-integration": "  // The SRS singles this out as the most common first-integration",
"  // Chapter 3.11. The socket's half of a quota refusal: an error frame carrying": "  // The socket's half of a quota refusal: an error frame carrying",
"  // Chapter 3.12. The refusal beside `wrong_credential_type`, one dimension over:": "  // The refusal beside `wrong_credential_type`, one dimension over:",
"  // Chapter 3.17. THE THIRD IN THE SAME FAMILY, one dimension further over. Its two": "  // THE THIRD IN THE SAME FAMILY, one dimension further over. Its two",
"  // Chapter 3.12. FR-CHN-07's ceiling: a channel holds at most 1,000 members and": "  // FR-CHN-07's ceiling: a channel holds at most 1,000 members and",
"  // Chapter 3.15. Archiving prevents new messages and preserves history, and this": "  // Archiving prevents new messages and preserves history, and this",
"  // Chapter 3.16. A ban is tenant-scope: no connecting and no sending anywhere in": "  // A ban is tenant-scope: no connecting and no sending anywhere in",
"  //   not_found            a lie with a witness. FR-011 (3.23) keeps deleted messages in": "  //   not_found            a lie with a witness. FR-011 keeps deleted messages in",
"  /** MEDIA THAT DOES NOT EXIST YET, AND ITS OWN CODE (chapter 3.24, FR-003, FR-003a).": "  /** MEDIA THAT DOES NOT EXIST YET, AND ITS OWN CODE (FR-003, FR-003a).",
"/** Whether a string the api sent is a code this registry defines (chapter 3.15).": "/** Whether a string the api sent is a code this registry defines.",
" * does not resolve, and two codes \u2014 `rate_limited` (3.8) and `quota_exceeded`": " * does not resolve, and two codes \u2014 `rate_limited` and `quota_exceeded`",
"  // Chapter 3.24: REQUIRED on `messageSchema`, so this fixture says. `[]` rather than a": "  // REQUIRED on `messageSchema`, so this fixture says. `[]` rather than a",
"  // T015 (chapter 3.23). THE ONE PINNED PLACE A PAYLOAD CHANGE MOVES in this file, and": "  // T015. THE ONE PINNED PLACE A PAYLOAD CHANGE MOVES in this file, and",
"      // Chapter 3.8: the fourth field, required rather than optional. The": "      // The fourth field, required rather than optional. The",
"  // Chapter 3.21. THE `user` REJECTION IS THE SECURITY PROPERTY, not a schema": "  // THE `user` REJECTION IS THE SECURITY PROPERTY, not a schema",
"// T015 and T016 (chapter 3.23). THE EXACT KEY SET, on `codes.test.ts`'s precedent: an": "// T015 and T016. THE EXACT KEY SET, on `codes.test.ts`'s precedent: an",
"describe(\"the message payload's exact key set (chapter 3.24, FR-022 (3.24))\", () => {": "describe(\"the message payload's exact key set (FR-022)\", () => {",
"    // The other half of FR-022 (3.24). An OPTIONAL field would accept this, and a": "    // The other half of FR-022. An OPTIONAL field would accept this, and a",
"describe(\"the deleted frame carries an identity and no text (chapter 3.23)\", () => {": "describe(\"the deleted frame carries an identity and no text\", () => {",
"  it(\"refuses an ATTACHMENTS field, for the same reason it refuses text (FR-013 (3.24))\", () => {": "  it(\"refuses an ATTACHMENTS field, for the same reason it refuses text (FR-013)\", () => {",
"describe(\"the frame union's membership (chapter 3.21)\", () => {": "describe(\"the frame union's membership\", () => {",
"  /** REQUIRED, AND NOT OPTIONAL, and that is the whole of FR-022 (3.24).": "  /** REQUIRED, AND NOT OPTIONAL, and that is the whole of FR-022.",
"   * FR-007 (3.24): a message with none carries `[]` rather than an absent key,": "   * FR-007: a message with none carries `[]` rather than an absent key,",
"/** CLIENT \u2192 SERVER: \"I am typing in this channel\" (chapter 3.21, FR-001).": "/** CLIENT \u2192 SERVER: \"I am typing in this channel\" (FR-001).",
"/** Chapter 3.23. The deleted frame's payload is the one that is NOT a `Message`, so it": "/** The deleted frame's payload is the one that is NOT a `Message`, so it",
"// The analytics grammar (chapter 3.6). Held for the same reason as the one above": "// The analytics grammar. Held for the same reason as the one above",
"describe(\"the usage report (chapter 3.11)\", () => {": "describe(\"the usage report\", () => {",
"  /** REQUIRED, because this payload carries a message and FR-022 (3.24) says every": "  /** REQUIRED, because this payload carries a message and FR-022 says every",
"// Event subjects (chapter 3.4, ADR-02).": "// Event subjects (ADR-02).",
"  /** Chapter 3.8. The two limits the gateway enforces, resolved from the": "  /** The two limits the gateway enforces, resolved from the",
"/** The deliveries stream (chapter 3.5), and its subject grammar.": "/** The deliveries stream, and its subject grammar.",
"// Analytical events (chapter 3.6, constitution III).": "// Analytical events (constitution III).",
"// The dispatch contract (chapter 3.5, constitution IV).": "// The dispatch contract (constitution IV).",
"/** The usage report (chapter 3.11, FR-RTL-05).": "/** The usage report (FR-RTL-05).",
" * (chapter 3.20, FR-RTM-05, FR-RTM-10).": " * (FR-RTM-05, FR-RTM-10).",
" * (chapter 3.19, ADR-19).": " * (ADR-19).",
"// T018d (chapter 3.23). THE SUBJECT STRING AND THE PAYLOAD'S EXACT KEYS, on": "// T018d. THE SUBJECT STRING AND THE PAYLOAD'S EXACT KEYS, on",
"  // Chapter 3.24. THE COMPILER DID NOT FIND THIS ONE: `revisionFabricSchema.parse`": "  // THE COMPILER DID NOT FIND THIS ONE: `revisionFabricSchema.parse`",
"describe(\"the revision subject (chapter 3.23, ADR-24)\", () => {": "describe(\"the revision subject (ADR-24)\", () => {",
"describe(\"the revision fabric payload (chapter 3.23)\", () => {": "describe(\"the revision fabric payload\", () => {",
" * (chapter 3.21, FR-RTM-05, FR-RTM-08).": " * (FR-RTM-05, FR-RTM-08).",
"        // Chapter 3.8: the fourth field constitution V has asked for since 1.3.": "        // The fourth field constitution V has asked for since 1.3.",
"  // bait 5: the four usage tables, ONE ROW EACH (chapter 3.13, FR-036).": "  // bait 5: the four usage tables, ONE ROW EACH (FR-036).",
"  // bait 6: `read_positions`, the guard's tenth table (chapter 3.16).": "  // bait 6: `read_positions`, the guard's tenth table.",
"// Chapter 3.15. Registered here for the reason `ChannelsModule` is: without this": "// Registered here for the reason `ChannelsModule` is: without this",
"    // then its allowance. The limiter is LAST and that is forced (chapter 3.8):": "    // then its allowance. The limiter is LAST and that is forced:",
"// The credential's own rules (chapter 3.2), Docker-free: format, prefix,": "// The credential's own rules, Docker-free: format, prefix,",
"// The API key as a string, and the arithmetic behind it (chapter 3.2).": "// The API key as a string, and the arithmetic behind it.",
"// Authentication's home (chapter 3.2), beside messages/, internal/ and": "// Authentication's home, beside messages/, internal/ and",
"  // Chapter 3.8: the failed-authentication counter. Imported rather than built": "  // The failed-authentication counter. Imported rather than built",
"/** The internal platform credentials (chapter 3.5, extended by 3.11).": "/** The internal platform credentials (extended by the connection-metering chapter).",
" * retyped beside it (chapter 3.12, FR-044).": " * retyped beside it (FR-044).",
"// The refusals, over real HTTP against the compose Postgres (chapter 3.2).": "// The refusals, over real HTTP against the compose Postgres.",
"    // Chapter 3.8. This suite submits bad credentials ON PURPOSE \u2014 that is what": "    // This suite submits bad credentials ON PURPOSE \u2014 that is what",
"    // A BOT IN EACH ENVIRONMENT (chapter 3.17). A key send names one, and the FOREIGN": "    // A BOT IN EACH ENVIRONMENT. A key send names one, and the FOREIGN",
"    // it needs its own bot (chapter 3.17).": "    // it needs its own bot.",
"  // \u2550\u2550 FR-USR-02: A USER ROW ON FIRST AUTHENTICATION (chapter 3.15) \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550": "  // \u2550\u2550 FR-USR-02: A USER ROW ON FIRST AUTHENTICATION \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550",
"    // A BOT CANNOT OBTAIN A TOKEN (chapter 3.17, FR-005, T040, T041).": "    // A BOT CANNOT OBTAIN A TOKEN (FR-005, T040, T041).",
"// What authentication produces (chapter 3.2). Never persisted, never sent \u2014": "// What authentication produces. Never persisted, never sent \u2014",
"/** The platform acting for itself (chapter 3.5).": "/** The platform acting for itself.",
"/** Chapter 3.8. Set by `AuthenticateMiddleware` when this address has already": "/** Set by `AuthenticateMiddleware` when this address has already",
"  /** Chapter 3.18: the id `RequestContextMiddleware` generated for this request.": "  /** The id `RequestContextMiddleware` generated for this request.",
"  /** Chapter 3.8: set when this source address has spent its": "  /** Set when this source address has spent its",
"// End-user tokens (chapter 3.2), Docker-free. The environment's signing secret": "// End-user tokens, Docker-free. The environment's signing secret",
"// End-user tokens (chapter 3.2, FR-AUT-06/07/08). HS256 over the": "// End-user tokens (FR-AUT-06/07/08). HS256 over the",
"    // THE CONTROLLER, NOT THE SERVICE (chapter 3.20, FR-004). `ChannelsService`'s": "    // THE CONTROLLER, NOT THE SERVICE (FR-004). `ChannelsService`'s",
"  /** One channel by id (chapter 3.15, FR-003a).": "  /** One channel by id (FR-003a).",
"  /** Archive and unarchive (chapter 3.15, FR-020, FR-020a).": "  /** Archive and unarchive (FR-020, FR-020a).",
"  /** One member's role (chapter 3.15, FR-011, FR-011a).": "  /** One member's role (FR-011, FR-011a).",
"  /** Remove members, up to a hundred, reporting each (chapter 3.15, FR-006, FR-007).": "  /** Remove members, up to a hundred, reporting each (FR-006, FR-007).",
"  /** The user-initiated half of FR-CHN-03 (chapter 3.15).": "  /** The user-initiated half of FR-CHN-03.",
"    // Chapter 3.15: a private channel, a member, a non-member of the SAME tenant,": "    // A private channel, a member, a non-member of the SAME tenant,",
"    it(\"refuses a JOIN that would exceed it, with the same code (chapter 3.15)\", async () => {": "    it(\"refuses a JOIN that would exceed it, with the same code\", async () => {",
"      // A key send names a bot (chapter 3.17). The refusal under test is the ARCHIVE's,": "      // A key send names a bot. The refusal under test is the ARCHIVE's,",
"  // BOTH, AND ONLY NOW (chapter 3.15, FR-009).": "  // BOTH, AND ONLY NOW (FR-009).",
"/** FR-CHN-04's three, and NOT `memberships`' three (chapter 3.15, FR-011).": "/** FR-CHN-04's three, and NOT `memberships`' three (FR-011).",
"/** The `PATCH` body for one member's role (chapter 3.15, FR-011).": "/** The `PATCH` body for one member's role (FR-011).",
"  /** Chapter 3.15. What role the member holds AFTER the call \u2014 read back, not": "  /** What role the member holds AFTER the call \u2014 read back, not",
"  /** One channel by id, with the caller's membership (chapter 3.15, FR-003a, FR-003).": "  /** One channel by id, with the caller's membership (FR-003a, FR-003).",
"  /** Remove members by EXTERNAL id, reporting each (chapter 3.15, FR-006, FR-007).": "  /** Remove members by EXTERNAL id, reporting each (FR-006, FR-007).",
"  /** Set one member's role by external id (chapter 3.15, FR-011).": "  /** Set one member's role by external id (FR-011).",
"  /** A user joining a channel themselves (chapter 3.15, FR-CHN-03).": "  /** A user joining a channel themselves (FR-CHN-03).",
"// The consumer, against a real broker and a real database (chapter 3.4).": "// The consumer, against a real broker and a real database.",
"// The consumer's home (chapter 3.4). It runs INSIDE the api service, and that": "// The consumer's home. It runs INSIDE the api service, and that",
"// What a handler is, and \u2014 more importantly \u2014 what it is not (chapter 3.4).": "// What a handler is, and \u2014 more importantly \u2014 what it is not.",
"// The first consumer (chapter 3.4) \u2014 and it is a SCAFFOLD, with a named": "// The first consumer \u2014 and it is a SCAFFOLD, with a named",
"// dispatcher (3.5), the analytics ingester and the media worker (Part 4), the": "// dispatcher, the analytics ingester and the media worker (Part 4), the",
"// The decision table, Docker-free (chapter 3.4). Ten of this chapter's twelve": "// The decision table, Docker-free. Ten of this chapter's twelve",
"// The consumer runtime (chapter 3.4). Fetch, decide, acknowledge \u2014 and": "// The consumer runtime. Fetch, decide, acknowledge \u2014 and",
"// rows a database happens to hold (chapter 3.12, FR-040).": "// rows a database happens to hold (FR-040).",
"   * keys and not only single links (chapter 3.23). Every name here is itself a": "   * keys and not only single links. Every name here is itself a",
"      -- REACHABILITY, NOT ADJACENCY (chapter 3.23). The rule this check states": "      -- REACHABILITY, NOT ADJACENCY. The rule this check states",
"describe(\"the database refuses a bot with no description (chapter 3.17, FR-003)\", () => {": "describe(\"the database refuses a bot with no description (FR-003)\", () => {",
"    // A BOT, AND THAT IS THE WHOLE POINT NOW (chapter 3.17, FR-019a). This test is the": "    // A BOT, AND THAT IS THE WHOLE POINT NOW (FR-019a). This test is the",
"describe(\"the listing's keyset survives a shared last_activity_at (chapter 3.15)\", () => {": "describe(\"the listing's keyset survives a shared last_activity_at\", () => {",
"// **CHAPTER 3.23 BUILT THE WRITER** (`repository.deleteMessage`, FR-006 (3.23)), so the": "// **The revisions chapter BUILT THE WRITER** (`repository.deleteMessage`, FR-006), so the",
"describe(\"the listing's tombstone rule and its clamp (chapter 3.15)\", () => {": "describe(\"the listing's tombstone rule and its clamp\", () => {",
"  // FR-011 (3.23) and SC-003 (3.23). The history read must return a tombstone in its": "  // FR-011 and SC-003. The history read must return a tombstone in its",
"  it(\"returns a tombstone in its original position, with the run unbroken (FR-011 (3.23), SC-003 (3.23))\", async () => {": "  it(\"returns a tombstone in its original position, with the run unbroken (FR-011, SC-003)\", async () => {",
"describe(\"the repository's own refusals (chapter 3.15)\", () => {": "describe(\"the repository's own refusals\", () => {",
"    // PLANTED, BECAUSE NOTHING CAN WRITE ONE ANY MORE (chapter 3.17, T014a, FR-014).": "    // PLANTED, BECAUSE NOTHING CAN WRITE ONE ANY MORE (T014a, FR-014).",
"    // THE ARM IS NOT DEAD, AND ITS SUBJECT HAS CHANGED (chapter 3.17, T055, FR-014).": "    // THE ARM IS NOT DEAD, AND ITS SUBJECT HAS CHANGED (T055, FR-014).",
"// \u2550\u2550 EDITING A MESSAGE (chapter 3.23, US1) \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550": "// \u2550\u2550 EDITING A MESSAGE (US1) \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550",
"describe(\"editMessage (chapter 3.23)\", () => {": "describe(\"editMessage\", () => {",
"    // and FR-014 (3.15) decided what that means: a message. Correcting a typo is not a": "    // and FR-014 decided what that means: a message. Correcting a typo is not a",
"    // rows \u2014 the archive sets a timestamp (FR-020 (3.15)) and a user deletion is a": "    // rows \u2014 the archive sets a timestamp (FR-020) and a user deletion is a",
"// \u2550\u2550 DELETING A MESSAGE (chapter 3.23, US2) \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550": "// \u2550\u2550 DELETING A MESSAGE (US2) \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550",
"describe(\"deleteMessage (chapter 3.23)\", () => {": "describe(\"deleteMessage\", () => {",
"// T004 (3.24) \u2014 THE READER TEST, RUN AGAINST UNCHANGED CODE.": "// T004 \u2014 THE READER TEST, RUN AGAINST UNCHANGED CODE.",
"// FR-019 (3.24) decides that an attachments-only message stores `text = \"\"` rather than": "// FR-019 decides that an attachments-only message stores `text = \"\"` rather than",
"describe(\"an empty text is a live message on every read path (FR-019 (3.24))\", () => {": "describe(\"an empty text is a live message on every read path (FR-019)\", () => {",
"// T023 and T024 (chapter 3.24). THE WRITER, AND THE EMPTY TEXT IT NOW ACCEPTS.": "// T023 and T024. THE WRITER, AND THE EMPTY TEXT IT NOW ACCEPTS.",
"describe(\"sendMessage writes attachments (FR-001 (3.24), FR-006 (3.24))\", () => {": "describe(\"sendMessage writes attachments (FR-001, FR-006)\", () => {",
"  it(\"stores the same url twice as two attachments (FR-021 (3.24))\", async () => {": "  it(\"stores the same url twice as two attachments (FR-021)\", async () => {",
"describe(\"an attachments-only message is written and is not a tombstone (FR-019 (3.24))\", () => {": "describe(\"an attachments-only message is written and is not a tombstone (FR-019)\", () => {",
"// T030a (chapter 3.24). BOTH BRANCHES OF `listMessages`, WHICH IS A TERNARY.": "// T030a. BOTH BRANCHES OF `listMessages`, WHICH IS A TERNARY.",
"describe(\"listMessages returns attachments on BOTH branches (FR-009 (3.24))\", () => {": "describe(\"listMessages returns attachments on BOTH branches (FR-009)\", () => {",
"  it(\"returns [] and not null on both branches for a message with none (FR-007 (3.24))\", async () => {": "  it(\"returns [] and not null on both branches for a message with none (FR-007)\", async () => {",
"// T053 (chapter 3.24). THE READ SHAPES THAT DO NOT CHANGE, ASSERTED.": "// T053. THE READ SHAPES THAT DO NOT CHANGE, ASSERTED.",
"describe(\"the read shapes that do NOT carry attachments (FR-009 (3.24))\", () => {": "describe(\"the read shapes that do NOT carry attachments (FR-009)\", () => {",
"// Credentials (chapter 3.2). Part of the ADMIN surface, and that placement is": "// Credentials. Part of the ADMIN surface, and that placement is",
" * (chapter 3.8, FR-RTL-04, research R26).": " * (FR-RTL-04, research R26).",
"/** Credit a batch of usage reports (chapter 3.11, FR-RTL-05/FR-RTL-05/FR-RTL-05).": "/** Credit a batch of usage reports (FR-RTL-05/FR-RTL-05/FR-RTL-05).",
" * (chapter 3.10, FR-RTL-05).": " * (FR-RTL-05).",
"/** The outbox drain, a FOURTH time (chapter 3.10) \u2014 after 3.3's events, 3.5's": "/** The outbox drain, a FOURTH time \u2014 after the outbox chapter's events, the webhook dispatcher chapter's",
"// The outbox drain (chapter 3.3, ADR-06). Part of the ADMIN surface for the": "// The outbox drain (ADR-06). Part of the ADMIN surface for the",
"// The disablement notifications (chapter 3.8, FR-WHK-07 to FR-WHK-07). THE OUTBOX A": "// The disablement notifications (FR-WHK-07 to FR-WHK-07). THE OUTBOX A",
" * transaction** (chapter 3.5, research R2).": " * transaction** (research R2).",
" * transaction** (chapter 3.5).": " * transaction**.",
" * gone on long enough (chapter 3.6, FR-006, FR-007).": " * gone on long enough (FR-006, FR-007).",
"/** Create the one delivery a test event needs (chapter 3.6, FR-013, research R8).": "/** Create the one delivery a test event needs (FR-013, research R8).",
"  // A TEST EVENT IS THE EXCEPTION, and it is the only one (chapter 3.6, FR-013).": "  // A TEST EVENT IS THE EXCEPTION, and it is the only one (FR-013).",
"// The consumer's deduplication ledger (chapter 3.4, SAD risk R5). Admin surface": "// The consumer's deduplication ledger (SAD risk R5). Admin surface",
"/** Signup (chapter 3.1, FR-TEN-01/02). The admin surface's second entrance:": "/** Signup (FR-TEN-01/02). The admin surface's second entrance:",
"    // belongs to (chapter 3.2). A key written outside this transaction could": "    // belongs to. A key written outside this transaction could",
"  /** When it was last edited, or `null` (chapter 3.23, FR-003). Optional on this": "  /** When it was last edited, or `null` (FR-003). Optional on this",
"/** An edited message, as the edit path returns it (chapter 3.23, FR-001, FR-003).": "/** An edited message, as the edit path returns it (FR-001, FR-003).",
"/** A write refused because the channel is archived (chapter 3.15, FR-020, FR-021).": "/** A write refused because the channel is archived (FR-020, FR-021).",
"/** A write or a connect refused because the user is banned (chapter 3.15, FR-031).": "/** A write or a connect refused because the user is banned (FR-031).",
"/** An application credential named a person (chapter 3.17, FR-007, FR-007a).": "/** An application credential named a person (FR-007, FR-007a).",
"/** The message id does not name a message of this channel (chapter 3.23, FR-014).": "/** The message id does not name a message of this channel (FR-014).",
"/** The caller did not write this message (chapter 3.23, FR-013, FR-018, FR-022).": "/** The caller did not write this message (FR-013, FR-018, FR-022).",
"/** An edit was asked for on a tombstone (chapter 3.23, FR-010).": "/** An edit was asked for on a tombstone (FR-010).",
"  // Webhook endpoints (chapter 3.5). Scoped like everything else on this class:": "  // Webhook endpoints. Scoped like everything else on this class:",
"        // (chapter 3.6, FR-017). The hour is measured from the NEXT failure, not": "        // (FR-017). The hour is measured from the NEXT failure, not",
"        // `createUser` CANNOT MAKE A BOT, and that is deliberate (chapter 3.17). Its": "        // `createUser` CANNOT MAKE A BOT, and that is deliberate. Its",
"   * THREE OUTCOMES, NOT A BOOLEAN (chapter 3.13, R14a). Until then this returned": "   * THREE OUTCOMES, NOT A BOOLEAN (R14a). Until then this returned",
"  /** Chapter 3.20. THIS METHOD HAD NO TRANSACTION AND NOW HAS ONE, which is a": "  /** THIS METHOD HAD NO TRANSACTION AND NOW HAS ONE, which is a",
"  /** Archive and unarchive, both idempotent (chapter 3.15, FR-020, FR-020a).": "  /** Archive and unarchive, both idempotent (FR-020, FR-020a).",
"  /** Set a member's role (chapter 3.15, FR-011).": "  /** Set a member's role (FR-011).",
"  /** Chapter 3.20. **NO OUTBOX ROW, AND NO FABRIC PUBLISH.** `membership.changed`'s": "  /** **NO OUTBOX ROW, AND NO FABRIC PUBLISH.** `membership.changed`'s",
"   * (chapter 3.15, FR-006, FR-007, FR-008).": "   * (FR-006, FR-007, FR-008).",
"  /** Chapter 3.20. THIS ONE HAD NO TRANSACTION EITHER, and it was already two": "  /** THIS ONE HAD NO TRANSACTION EITHER, and it was already two",
"        // THE DEFAULT APPLIES HERE AND NOWHERE ELSE (chapter 3.17, FR-002b, T019a).": "        // THE DEFAULT APPLIES HERE AND NOWHERE ELSE (FR-002b, T019a).",
"    // A KIND CHANGE IS REPORTED, NOT PERFORMED (chapter 3.17, FR-002a, FR-002d).": "    // A KIND CHANGE IS REPORTED, NOT PERFORMED (FR-002a, FR-002d).",
"    // FIX (chapter 3.17). The first version of this branch read the row back and threw if": "    // FIX. The first version of this branch read the row back and threw if",
"  /** Ban and unban a user, tenant-wide (chapter 3.15, FR-031, FR-032).": "  /** Ban and unban a user, tenant-wide (FR-031, FR-032).",
"  /** Chapter 3.20. A BAN WRITES ONE `channel.member_removed` PER CHANNEL, and the": "  /** A BAN WRITES ONE `channel.member_removed` PER CHANNEL, and the",
"  /** Delete a user, keeping the row (chapter 3.15, FR-027, FR-028, FR-029).": "  /** Delete a user, keeping the row (FR-027, FR-028, FR-029).",
"      // (chapter 3.17, FR-004a, T043b).": "      // (FR-004a, T043b).",
"  /** Write a user's profile (chapter 3.15, FR-023, FR-024).": "  /** Write a user's profile (FR-023, FR-024).",
"  /** Record a read position (chapter 3.15, FR-017, FR-018).": "  /** Record a read position (FR-017, FR-018).",
"  /** Mark a user deleted, keeping the row (chapter 3.15, FR-017).": "  /** Mark a user deleted, keeping the row (FR-017).",
"      /** THE SENDER MUST BE A BOT (chapter 3.17, FR-007, T030, T032).": "      /** THE SENDER MUST BE A BOT (FR-007, T030, T032).",
"      /** Chapter 3.3: the sender as a CONSUMER will see them. Threaded from the": "      /** The sender as a CONSUMER will see them. Threaded from the",
"      // EVERY SEND IS ATTRIBUTED NOW (chapter 3.17, FR-MSG-15). The gate that used to": "      // EVERY SEND IS ATTRIBUTED NOW (FR-MSG-15). The gate that used to",
"      // that stopped meaning anything**: `addMember`'s `rowCount ?? 0` (3.12), and": "      // that stopped meaning anything**: `addMember`'s `rowCount ?? 0`, and",
"      // `upsertUser`'s second throw and `(row.metadata ?? {})` (3.16). Tightening a": "      // `upsertUser`'s second throw and `(row.metadata ?? {})`. Tightening a",
"          // Chapter 3.15. `channels.type` has been a `\"public\" | \"private\"` column": "          // `channels.type` has been a `\"public\" | \"private\"` column",
"          // Chapter 3.15. Declared in chapter 2.1 and read by NOTHING until here:": "          // Declared in chapter 2.1 and read by NOTHING until here:",
"      // THE SENDER ATTRIBUTES; IT DOES NOT AUTHORISE (chapter 3.17, FR-019).": "      // THE SENDER ATTRIBUTES; IT DOES NOT AUTHORISE (FR-019).",
"      // THE SENDER'S KIND, LAST OF THE FIVE (chapter 3.17, FR-007, T032).": "      // THE SENDER'S KIND, LAST OF THE FIVE (FR-007, T032).",
"      // THE CAP, CHECKED BEFORE THE MESSAGE IS WRITTEN (chapter 3.10, FR-RTL-08).": "      // THE CAP, CHECKED BEFORE THE MESSAGE IS WRITTEN (FR-RTL-08).",
"      // AND `lastActivityAt` MOVES IN THE SAME STATEMENT (chapter 3.15, FR-014).": "      // AND `lastActivityAt` MOVES IN THE SAME STATEMENT (FR-014).",
"      // THE EVENT COMMITS WITH THE MESSAGE (chapter 3.3, ADR-06).": "      // THE EVENT COMMITS WITH THE MESSAGE (ADR-06).",
"      // THE MONTH'S USAGE COMMITS WITH THE MESSAGE (chapter 3.10, FR-RTL-05).": "      // THE MONTH'S USAGE COMMITS WITH THE MESSAGE (FR-RTL-05).",
"      // EVERY SEND IS ATTRIBUTED, SO EVERY SEND COUNTS (chapter 3.17). The gate here": "      // EVERY SEND IS ATTRIBUTED, SO EVERY SEND COUNTS. The gate here",
"  /** Change what a message says (chapter 3.23, FR-001, FR-002, FR-003, FR-004).": "  /** Change what a message says (FR-001, FR-002, FR-003, FR-004).",
"      // THE EVENT COMMITS WITH THE EDIT (chapter 3.23, FR-019, ADR-06). Same argument": "      // THE EVENT COMMITS WITH THE EDIT (FR-019, ADR-06). Same argument",
"  /** Turn a message into a tombstone (chapter 3.23, FR-006, FR-006a, FR-009).": "  /** Turn a message into a tombstone (FR-006, FR-006a, FR-009).",
"  /** A message's edit history, oldest first (chapter 3.23, FR-023).": "  /** A message's edit history, oldest first (FR-023).",
"  /** Does this message exist in this channel of this tenant (chapter 3.23)?": "  /** Does this message exist in this channel of this tenant?",
"  /** Refuse the send if a hard cap is already met (chapter 3.10, FR-RTL-08).": "  /** Refuse the send if a hard cap is already met (FR-RTL-08).",
"    /** REQUIRED, following `sendMessage`'s parameter (chapter 3.17, T012). Its only": "    /** REQUIRED, following `sendMessage`'s parameter (T012). Its only",
"    /** Whether the sender is a person (chapter 3.17, FR-018a, T047b). Threaded from the": "    /** Whether the sender is a person (FR-018a, T047b). Threaded from the",
"   * beyond FR-CHN-01's four (chapter 3.15, FR-003a).": "   * beyond FR-CHN-01's four (FR-003a).",
"  /** Whether this user is a member of this channel (chapter 3.15).": "  /** Whether this user is a member of this channel.",
"  /** Whether this channel exists AND this caller may see it (chapter 3.15, FR-003).": "  /** Whether this channel exists AND this caller may see it (FR-003).",
"      /** Who is reading (chapter 3.15, FR-002, FR-003).": "      /** Who is reading (FR-002, FR-003).",
"    // MEMBERSHIP FIRST, WHEN A USER IS READING (chapter 3.15, FR-002, FR-003).": "    // MEMBERSHIP FIRST, WHEN A USER IS READING (FR-002, FR-003).",
"      // WHEN IT WAS LAST EDITED, OR NULL (chapter 3.23, FR-003). Null for every": "      // WHEN IT WAS LAST EDITED, OR NULL (FR-003). Null for every",
"// The tenancy hierarchy (chapter 3.1). Everything from here to `members`": "// The tenancy hierarchy. Everything from here to `members`",
"// DECISION (chapter 3.1): SAD \u00a76.1 defines `environments` and everything under": "// DECISION: SAD \u00a76.1 defines `environments` and everything under",
"    // Chapter 3.8: per-environment rate limits (FR-RTL-04, FR-RTL-04).": "    // Per-environment rate limits (FR-RTL-04, FR-RTL-04).",
"// DECISION (chapter 3.2): the SRS states the requirements this table serves": "// DECISION: the SRS states the requirements this table serves",
"    // A DELETED USER KEEPS THIS ROW (chapter 3.15, FR-USR-05, research R7).": "    // A DELETED USER KEEPS THIS ROW (FR-USR-05, research R7).",
"    // WHAT KIND OF THING THIS USER IS (chapter 3.17, FR-USR-07).": "    // WHAT KIND OF THING THIS USER IS (FR-USR-07).",
"    // WHAT THE SOFTWARE IS, AND WHY IT POSTS (chapter 3.17, FR-USR-07).": "    // WHAT THE SOFTWARE IS, AND WHY IT POSTS (FR-USR-07).",
"    // WHEN THIS CHANNEL LAST TOOK A MESSAGE (chapter 3.15, FR-014).": "    // WHEN THIS CHANNEL LAST TOOK A MESSAGE (FR-014).",
"// WHAT A MESSAGE USED TO SAY (chapter 3.23, FR-MSG-07). Published in SAD \u00a76.1": "// WHAT A MESSAGE USED TO SAY (FR-MSG-07). Published in SAD \u00a76.1",
"// APPEND ONLY (FR-004, chapter 3.23). Nothing updates or deletes a row here. A": "// APPEND ONLY (FR-004). Nothing updates or deletes a row here. A",
"    // A USER'S ROLE IN A CHANNEL (chapter 3.15, FR-CHN-04), default `member`.": "    // A USER'S ROLE IN A CHANNEL (FR-CHN-04), default `member`.",
"// HOW FAR EACH USER HAS READ IN EACH CHANNEL (chapter 3.15, FR-017, research R6).": "// HOW FAR EACH USER HAS READ IN EACH CHANNEL (FR-017, research R6).",
"// The outbox (chapter 3.3, ADR-06). For the first time in Part 3 this table is": "// The outbox (ADR-06). For the first time in Part 3 this table is",
"// (FR-WHK-03/06, chapter 3.5). This relay retries by not marking a row done.": "// (FR-WHK-03/06). This relay retries by not marking a row done.",
"    // DECISION (chapter 3.3): SAD \u00a76.1 defines this table but no index for it.": "    // DECISION: SAD \u00a76.1 defines this table but no index for it.",
"// The consumer's deduplication ledger (chapter 3.4).": "// The consumer's deduplication ledger.",
"// DECISION (chapter 3.4): no source document defines a table for this. SAD risk": "// DECISION: no source document defines a table for this. SAD risk",
"// Webhooks (chapter 3.5). Three tables, and all three carry `environment_id`.": "// Webhooks. Three tables, and all three carry `environment_id`.",
"// DECISION (chapter 3.5): 3.3's `outbox` and 3.4's `consumed_events` each": "// DECISION: the outbox chapter's `outbox` and the broker chapter's `consumed_events` each",
"// DECISION (chapter 3.5): no source document defines this table. FR-WHK-01 and": "// DECISION: no source document defines this table. FR-WHK-01 and",
"// An API key (3.2) is VERIFIED: a caller presents it, we hash what arrived and": "// An API key is VERIFIED: a caller presents it, we hash what arrived and",
"    // THE FAILURE RUN (chapter 3.6, FR-006). The current unbroken sequence of": "    // THE FAILURE RUN (FR-006). The current unbroken sequence of",
"    // The SWEEP's only query (chapter 3.6, research R1): enabled endpoints with": "    // The SWEEP's only query (research R1): enabled endpoints with",
"// DECISION (chapter 3.5, research R1, MEASURED): the obvious implementation is": "// DECISION (research R1, MEASURED): the obvious implementation is",
"    // WHAT THE ENDPOINT ACTUALLY SAID on the most recent attempt (chapter 3.6).": "    // WHAT THE ENDPOINT ACTUALLY SAID on the most recent attempt.",
"    // A TEST EVENT's delivery (chapter 3.6, FR-013). Three decisions branch on": "    // A TEST EVENT's delivery (FR-013). Three decisions branch on",
"// DECISION (chapter 3.5): FR-WHK-04 requires exhausted events to be retained": "// DECISION: FR-WHK-04 requires exhausted events to be retained",
"// DECISION (chapter 3.6, FR-011, research R7): one row per automatic": "// DECISION (FR-011, research R7): one row per automatic",
"// The api as a PUBLISHER (chapter 3.18). Everything here asserts on a real Redis": "// The api as a PUBLISHER. Everything here asserts on a real Redis",
"   * an application credential (chapter 3.17) and `idempotency_key` must be a": "   * an application credential and `idempotency_key` must be a",
"    // (chapter 3.17). An application credential may send only as a bot user.": "    //. An application credential may send only as a bot user.",
"    // speak only as a bot user (chapter 3.17) and the internal route speaks as": "    // speak only as a bot user and the internal route speaks as",
"    //    bot of its tenant (chapter 3.17): 403 `sender_not_permitted`.": "    //    bot of its tenant: 403 `sender_not_permitted`.",
"/** The api's half of the live fan-out (chapter 3.18, FR-004).": "/** The api's half of the live fan-out (FR-004).",
"// Chapter 3.2: the end user's own token, forwarded by the gateway, rather than": "// The end user's own token, forwarded by the gateway, rather than",
" *     FR-016a (3.23) settled it the other way: resume stays ordered by the channel": " *     FR-016a settled it the other way: resume stays ordered by the channel",
" *     platform is already the first shape, and FR-016b (3.23) asks for that to be": " *     platform is already the first shape, and FR-016b asks for that to be",
"  /** Chapter 3.2: the gateway forwards the user's own token now, so the suite": "  /** The gateway forwards the user's own token now, so the suite",
"  it(\"replays two attachments in the order they were sent (FR-010 (3.24), SC-005 (3.24))\", async () => {": "  it(\"replays two attachments in the order they were sent (FR-010, SC-005)\", async () => {",
"  it(\"replays a message with none as an empty list (FR-007 (3.24))\", async () => {": "  it(\"replays a message with none as an empty list (FR-007)\", async () => {",
"  // \u2550\u2550 WHAT A CLIENT THAT WAS AWAY CAN AND CANNOT LEARN (chapter 3.23, US4) \u2550\u2550": "  // \u2550\u2550 WHAT A CLIENT THAT WAS AWAY CAN AND CANNOT LEARN (US4) \u2550\u2550",
"// The dispatcher's only road to state (chapter 3.5, constitution IV).": "// The dispatcher's only road to state (constitution IV).",
"// FR-044 (chapter 3.12): the CLASS was never enough. Two platform credentials": "// FR-044: the CLASS was never enough. Two platform credentials",
"      // decision (chapter 3.6, research R5, constitution III).": "      // decision (research R5, constitution III).",
"      // Chapter 3.3: and the external id travels too, because the event this": "      // The outbox chapter: and the external id travels too, because the event this",
"  /** Chapter 3.2: the gateway forwards the END USER'S token instead of": "  /** The gateway forwards the END USER'S token instead of",
"    // Chapter 3.15: the socket's route reaches the same `sendMessage`, so the": "    // The socket's route reaches the same `sendMessage`, so the",
"    // Chapter 3.20. Registered here for the reason the comment below `UsageController`": "    // Registered here for the reason the comment below `UsageController`",
"    // Chapter 3.11. Registered HERE and not in `app.module.ts`, which carries": "    // Registered HERE and not in `app.module.ts`, which carries",
"    // Chapter 3.6: the attempt record's way onto the analytical path. Its own": "    // The attempt record's way onto the analytical path. Its own",
"// `GET /internal/memberships` (chapter 3.20, FR-017) \u2014 REVIVED, not invented.": "// `GET /internal/memberships` (FR-017) \u2014 REVIVED, not invented.",
"// `POST /internal/session` (chapter 3.2) \u2014 the route that replaced": "// `POST /internal/session` \u2014 the route that replaced",
"    // A BOT MAY NOT OPEN A SOCKET EITHER (chapter 3.17, FR-005b, T040a).": "    // A BOT MAY NOT OPEN A SOCKET EITHER (FR-005b, T040a).",
"    // Chapter 3.8: the gateway's limits, resolved here because the gateway has no": "    // The gateway's limits, resolved here because the gateway has no",
"    // Chapter 3.11. THE CAP IS ENFORCED AT THE DOOR, because a connection is the": "    // THE CAP IS ENFORCED AT THE DOOR, because a connection is the",
"// What the cap costs at the door (chapter 3.11, , NFR-PERF-01).": "// What the cap costs at the door (NFR-PERF-01).",
"// leaves behind (chapter 3.11).": "// leaves behind.",
"/** The four attacks, one per shape (chapter 3.12, NFR-SEC-09).": "/** The four attacks, one per shape (NFR-SEC-09).",
"/** The indistinguishability oracle (chapter 3.12).": "/** The indistinguishability oracle.",
"/** Two tenants, so every attack has a victim and an attacker (chapter 3.12).": "/** Two tenants, so every attack has a victim and an attacker.",
"  /** This tenant's own bot (chapter 3.17). A key send must name one. */": "  /** This tenant's own bot. A key send must name one. */",
"  // A BOT PER TENANT (chapter 3.17). Every attack in the gauntlet presents a KEY, and a": "  // A BOT PER TENANT. Every attack in the gauntlet presents a KEY, and a",
"  /** A bot of this tenant (chapter 3.17). The control's sender: an application": "  /** A bot of this tenant. The control's sender: an application",
"  // description and the member-add path has nowhere to put one (chapter 3.17).": "  // description and the member-add path has nowhere to put one.",
"          // (chapter 3.17). The control must keep proving the credential works.": "          //. The control must keep proving the credential works.",
"    // \u2550\u2550 THE SENDER (chapter 3.17, T035, T036, SC-005) \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550": "    // \u2550\u2550 THE SENDER (T035, T036, SC-005) \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550",
"    describe(\"a foreign bot and a bot that exists nowhere (chapter 3.17)\", () => {": "    describe(\"a foreign bot and a bot that exists nowhere\", () => {",
"/** The gauntlet's target list (chapter 3.12, NFR-SEC-09).": "/** The gauntlet's target list (NFR-SEC-09).",
"  // Chapter 3.15. A `list` and not a `read`: the attack on a listing is that a": "  // A `list` and not a `read`: the attack on a listing is that a",
"  // Chapter 3.15. The bulk upsert and the deletion. Both `write`: the upsert's attack is": "  // The bulk upsert and the deletion. Both `write`: the upsert's attack is",
"  // Chapter 3.15. The ban pair, both `write`. The attack is a foreign external id: a": "  // The ban pair, both `write`. The attack is a foreign external id: a",
"  // Chapter 3.15. The profile pair. `read` for the GET; the PATCH is a `write` whose": "  // The profile pair. `read` for the GET; the PATCH is a `write` whose",
"  // Chapter 3.15. The route that names TWO tenant-owned identifiers, which is why": "  // The route that names TWO tenant-owned identifiers, which is why",
"// (chapter 3.8, FR-AUT-12, research R3).": "// (FR-AUT-12, research R3).",
"// The fixed-window arithmetic (chapter 3.8, research R1). Pure: no store, no": "// The fixed-window arithmetic (research R1). Pure: no store, no",
"// The fixed-window arithmetic (chapter 3.8, research R1).": "// The fixed-window arithmetic (research R1).",
"// Whose failure was that? (chapter 3.8, FR-AUT-12, research R14.)": "// Whose failure was that? (FR-AUT-12, research R14.)",
"// (chapter 3.8, research R3). Pure apart from the map it owns; every call takes": "// (research R3). Pure apart from the map it owns; every call takes",
"// (chapter 3.8, research R3).": "// (research R3).",
"// (chapter 3.8). Each `it` mints what it needs; the suite's own environments": "//. Each `it` mints what it needs; the suite's own environments",
"    // THE BOT IS SEEDED HERE AND NOT IN `send` (chapter 3.17, T060).": "    // THE BOT IS SEEDED HERE AND NOT IN `send` (T060).",
"    // own bot (chapter 3.17). Same name, so the send below reads the same as the others.": "    // own bot. Same name, so the send below reads the same as the others.",
"// The counter store's home (chapter 3.8).": "// The counter store's home.",
"// configurable (chapter 3.8).": "// configurable.",
"// on (chapter 3.8, research R26).": "// on (research R26).",
"// The tenant limiter (chapter 3.8, FR-RTL-01\u202604).": "// The tenant limiter (FR-RTL-01\u202604).",
"// The counter store (chapter 3.8, research R1).": "// The counter store (research R1).",
"  // (chapter 3.3, research R9).": "  // (research R9).",
"  // Chapter 3.8: the disablement notifications chapter 3.6 wrote and nothing": "  // The disablement notifications the retry-and-disable chapter wrote and nothing",
"  // And the second relay (chapter 3.5): the same loop over a different table,": "  // And the second relay: the same loop over a different table,",
"  // And the first thing that reads what the relay publishes (chapter 3.4).": "  // And the first thing that reads what the relay publishes.",
"// ONE MODULE, TWO IMPORTERS (chapter 3.20).": "// ONE MODULE, TWO IMPORTERS.",
"// The membership publisher, Docker-free (chapter 3.20).": "// The membership publisher, Docker-free.",
"// The membership fabric's api half (chapter 3.20, FR-004).": "// The membership fabric's api half (FR-004).",
"// A REAL SENDER FOR THE WHOLE SUITE (chapter 3.17, FR-MSG-15). History is about": "// A REAL SENDER FOR THE WHOLE SUITE (FR-MSG-15). History is about",
"// A REAL SENDER, BECAUSE `sendMessage` REQUIRES ONE (chapter 3.17, FR-MSG-15).": "// A REAL SENDER, BECAUSE `sendMessage` REQUIRES ONE (FR-MSG-15).",
"  it(\"a retry returns the original's attachments and writes no second row (FR-011 (3.24))\", async () => {": "  it(\"a retry returns the original's attachments and writes no second row (FR-011)\", async () => {",
"  it(\"recovers a TOMBSTONE with an empty list and nothing published (FR-011 (3.24), FR-012 (3.24))\", async () => {": "  it(\"recovers a TOMBSTONE with an empty list and nothing published (FR-011, FR-012)\", async () => {",
"// chapter 3.24. They are two schemas validated at two controllers \u2014 this route": "// They are two schemas validated at two controllers \u2014 this route",
"// DECLARED, NOT INHERITED FROM A FALLBACK (chapter 3.17, T027a). Until now this class": "// DECLARED, NOT INHERITED FROM A FALLBACK (T027a). Until now this class",
"    // Chapter 3.18. INJECTED HERE AND NOT INTO THE SERVICE, because two callers": "    // INJECTED HERE AND NOT INTO THE SERVICE, because two callers",
"    // WHO IS SENDING, resolved here (chapter 3.15, FR-001, T031a).": "    // WHO IS SENDING, resolved here (FR-001, T031a).",
"    // THE SENDER, RESOLVED PER CREDENTIAL CLASS (chapter 3.17, FR-010, FR-008).": "    // THE SENDER, RESOLVED PER CREDENTIAL CLASS (FR-010, FR-008).",
"      // THE SENDER IT USED (chapter 3.17, FR-009a). A caller now required to name one": "      // THE SENDER IT USED (FR-009a). A caller now required to name one",
"  /** Change what a message says (chapter 3.23, FR-001, FR-005, FR-013, FR-013a).": "  /** Change what a message says (FR-001, FR-005, FR-013, FR-013a).",
"  /** Remove what a message says (chapter 3.23, FR-006, FR-007, FR-009, FR-012).": "  /** Remove what a message says (FR-006, FR-007, FR-009, FR-012).",
"  /** What a message used to say (chapter 3.23, FR-023, FR-023a).": "  /** What a message used to say (FR-023, FR-023a).",
"    // controller (chapter 3.15, T041a). Both dropped the caller; the send path was": "    // controller (T041a). Both dropped the caller; the send path was",
"  // Chapter 3.2: the tenant arrives as a CREDENTIAL now, not as a header. The": "  // The tenant arrives as a CREDENTIAL now, not as a header. The",
"  // KEY SENDS NAME `courier`, THE TENANT'S BOT (chapter 3.17, T059). Each caller passes": "  // KEY SENDS NAME `courier`, THE TENANT'S BOT (T059). Each caller passes",
"  // T035 through T040 (chapter 3.24). THE BOUND, THE KINDS, THE SCHEMES, AND THE ONE": "  // T035 through T040. THE BOUND, THE KINDS, THE SCHEMES, AND THE ONE",
"    it(\"refuses eleven and writes no row (FR-005 (3.24), SC-002 (3.24))\", async () => {": "    it(\"refuses eleven and writes no row (FR-005, SC-002)\", async () => {",
"    it(\"accepts exactly ten and returns all ten (FR-005 (3.24))\", async () => {": "    it(\"accepts exactly ten and returns all ten (FR-005)\", async () => {",
"    it(\"stores the same url twice, twice (FR-021 (3.24))\", async () => {": "    it(\"stores the same url twice, twice (FR-021)\", async () => {",
"    it(\"refuses a kind outside the three (FR-002 (3.24))\", async () => {": "    it(\"refuses a kind outside the three (FR-002)\", async () => {",
"    ])(\"refuses %s through the route (T038, FR-004 (3.24), SC-004 (3.24))\", async (_label, bad) => {": "    ])(\"refuses %s through the route (T038, FR-004, SC-004)\", async (_label, bad) => {",
"    it(\"answers a media_id with its own code and a 422 (FR-003a (3.24))\", async () => {": "    it(\"answers a media_id with its own code and a 422 (FR-003a)\", async () => {",
"  // T029, T031 and T032a (chapter 3.24). THE REST DOOR, END TO END.": "  // T029, T031 and T032a. THE REST DOOR, END TO END.",
"  describe(\"attachments through the send and history routes (SC-001 (3.24))\", () => {": "  describe(\"attachments through the send and history routes (SC-001)\", () => {",
"    it(\"reads back an empty list, not an absent field (FR-007 (3.24))\", async () => {": "    it(\"reads back an empty list, not an absent field (FR-007)\", async () => {",
"    it(\"shows a non-member nothing, and therefore no attachment (FR-014 (3.24))\", async () => {": "    it(\"shows a non-member nothing, and therefore no attachment (FR-014)\", async () => {",
"  // T020b (chapter 3.24). THE REFUSAL'S `field`, NOT ONLY ITS CODE.": "  // T020b. THE REFUSAL'S `field`, NOT ONLY ITS CODE.",
"  describe(\"the refusals name a field (FR-019b (3.24))\", () => {": "  describe(\"the refusals name a field (FR-019b)\", () => {",
"    it(\"names `attachments` when there are eleven (FR-005 (3.24))\", async () => {": "    it(\"names `attachments` when there are eleven (FR-005)\", async () => {",
"    it(\"names the index and the key for a bad kind at position 3 (FR-002 (3.24))\", async () => {": "    it(\"names the index and the key for a bad kind at position 3 (FR-002)\", async () => {",
"  // \u2550\u2550 THE SENDER (chapter 3.17, US2) \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550": "  // \u2550\u2550 THE SENDER (US2) \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550",
"    // (chapter 3.15, FR-019b).": "    // (FR-019b).",
"describe(\"PATCH /v1/channels/:channelId/messages/:messageId (chapter 3.23)\", () => {": "describe(\"PATCH /v1/channels/:channelId/messages/:messageId\", () => {",
"  // T043, T045, T046b and T047 (chapter 3.24). THE TOMBSTONE AND THE EDIT.": "  // T043, T045, T046b and T047. THE TOMBSTONE AND THE EDIT.",
"    it(\"leaves attachments untouched through an edit, in order (FR-016 (3.24))\", async () => {": "    it(\"leaves attachments untouched through an edit, in order (FR-016)\", async () => {",
"    it(\"reports the same two on the edit's own answers, in order (FR-015 (3.24))\", async () => {": "    it(\"reports the same two on the edit's own answers, in order (FR-015)\", async () => {",
"    it(\"says nothing about attachments in the edit history (FR-016 (3.24))\", async () => {": "    it(\"says nothing about attachments in the edit history (FR-016)\", async () => {",
"    it(\"returns a tombstone as an empty list through the history route (FR-012 (3.24), SC-003 (3.24))\", async () => {": "    it(\"returns a tombstone as an empty list through the history route (FR-012, SC-003)\", async () => {",
"/** Chapter 3.18. The api publishes to the live fan-out from the send path, so": "/** The api publishes to the live fan-out from the send path, so",
"    /** WHO IS SENDING (chapter 3.17, FR-MSG-15, FR-008).": "    /** WHO IS SENDING (FR-MSG-15, FR-008).",
"/** The edit body (chapter 3.23, FR-001).": "/** The edit body (FR-001).",
"/** The edit body (chapter 3.23, FR-001). ITS OWN BOUND, AND NO LONGER THE SEND'S.": "/** The edit body (FR-001). ITS OWN BOUND, AND NO LONGER THE SEND'S.",
"    /** Chapter 3.3: the same person as a CONSUMER will see them. The event": "    /** The same person as a CONSUMER will see them. The event",
"    /** Whether the caller is an application credential (chapter 3.17, FR-007, T030).": "    /** Whether the caller is an application credential (FR-007, T030).",
"      // THE SENDER IS RESOLVED BEFORE HERE (chapter 3.17, FR-008). The controller does": "      // THE SENDER IS RESOLVED BEFORE HERE (FR-008). The controller does",
"      // 403 `sender_not_permitted`, AND NOT `forbidden` (chapter 3.17, FR-007a, T032a).": "      // 403 `sender_not_permitted`, AND NOT `forbidden` (FR-007a, T032a).",
"        // ONE THROW, AND IT IS THE ONLY ONE (chapter 3.10, FR-RTL-08).": "        // ONE THROW, AND IT IS THE ONLY ONE (FR-RTL-08).",
"  /** Change what a message says (chapter 3.23, FR-001, FR-013, FR-014).": "  /** Change what a message says (FR-001, FR-013, FR-014).",
"  /** Turn a message into a tombstone (chapter 3.23, FR-006, FR-009, FR-012, FR-013).": "  /** Turn a message into a tombstone (FR-006, FR-009, FR-012, FR-013).",
"    /** Who is reading (chapter 3.15, FR-002). Threaded for the same reason `send`": "    /** Who is reading (FR-002). Threaded for the same reason `send`",
"    // VISIBILITY, NOT EXISTENCE (chapter 3.15, FR-003). `channelExists` answered": "    // VISIBILITY, NOT EXISTENCE (FR-003). `channelExists` answered",
"      /** A SCHEMA MAY NAME ITS OWN REFUSAL (chapter 3.24, FR-003a).": "      /** A SCHEMA MAY NAME ITS OWN REFUSAL (FR-003a).",
"// What the message SAYS, decided without a server (chapter 3.8, FR-WHK-07). The": "// What the message SAYS, decided without a server (FR-WHK-07). The",
"// The disablement notification (chapter 3.8, FR-WHK-07, FR-WHK-07).": "// The disablement notification (FR-WHK-07, FR-WHK-07).",
"// The notification relay (chapter 3.8, FR-WHK-07 to FR-WHK-07).": "// The notification relay (FR-WHK-07 to FR-WHK-07).",
"// The notification relay's home (chapter 3.8). Same shape as the outbox": "// The notification relay's home. Same shape as the outbox",
"// The envelope (chapter 3.3), Docker-free. What a consumer eventually receives": "// The envelope, Docker-free. What a consumer eventually receives",
"  // Chapter 3.24. TWO, because FR-006 says order holds on every path that returns a": "  // TWO, because FR-006 says order holds on every path that returns a",
"describe(\"a legacy senderless message in the webhook payload (chapter 3.17)\", () => {": "describe(\"a legacy senderless message in the webhook payload\", () => {",
"// Chapter 3.20. The second and third event types FR-WHK-02 names, and the boundary": "// The second and third event types FR-WHK-02 names, and the boundary",
"/** A tombstone as a consumer receives it (chapter 3.23). No `text` key \u2014 that is": "/** A tombstone as a consumer receives it. No `text` key \u2014 that is",
"describe(\"messageUpdatedEvent and messageDeletedEvent (chapter 3.23)\", () => {": "describe(\"messageUpdatedEvent and messageDeletedEvent\", () => {",
"  it(\"leaves the edit's payload identical to a creation's (FR-008a, FR-015 (3.24))\", () => {": "  it(\"leaves the edit's payload identical to a creation's (FR-008a, FR-015)\", () => {",
"  it(\"carries the attachments themselves, in order, on both (FR-006 (3.24))\", () => {": "  it(\"carries the attachments themselves, in order, on both (FR-006)\", () => {",
"    \"reads a %s written before this chapter and yields an empty list (FR-007 (3.24))\",": "    \"reads a %s written before this chapter and yields an empty list (FR-007)\",",
"// The event envelope (chapter 3.3). Built in ONE place, complete, inside the": "// The event envelope. Built in ONE place, complete, inside the",
"/** A DELETION as a consumer receives it (chapter 3.23, FR-019, FR-020).": "/** A DELETION as a consumer receives it (FR-019, FR-020).",
"/** A membership change as a CONSUMER receives it (chapter 3.20, FR-WHK-02).": "/** A membership change as a CONSUMER receives it (FR-WHK-02).",
"/** An edit, built inside the transaction that wrote it (chapter 3.23, FR-019).": "/** An edit, built inside the transaction that wrote it (FR-019).",
"/** The envelope as a CONSUMER receives it (chapter 3.4).": "/** The envelope as a CONSUMER receives it.",
"  // CHAPTER 3.23. The union is exhaustive over `OUTBOX_EVENT_TYPES`, and this file's own": "  // The union is exhaustive over `OUTBOX_EVENT_TYPES`, and this file's own",
"// The one adapter that knows what a broker is (chapter 3.3, ADR-02).": "// The one adapter that knows what a broker is (ADR-02).",
"/** The analytics stream's own bound (chapter 3.6). The same seven days as": "/** The analytics stream's own bound. The same seven days as",
"/** The ANALYTICS stream (chapter 3.6, constitution III).": "/** The ANALYTICS stream (constitution III).",
"// The outbox, against the real database (chapter 3.3). Invariants 1-4, 7-8 and": "// The outbox, against the real database. Invariants 1-4, 7-8 and",
"      // SCOPED TO THE MESSAGE'S EVENT (chapter 3.20). This counted every row in the": "      // SCOPED TO THE MESSAGE'S EVENT. This counted every row in the",
"// Chapter 3.20. The membership rows, and the transaction that has to hold them.": "// The membership rows, and the transaction that has to hold them.",
"describe(\"the membership rows (chapter 3.20, FR-WHK-02)\", () => {": "describe(\"the membership rows (FR-WHK-02)\", () => {",
"// The relay's home (chapter 3.3). It lives INSIDE the api service because": "// The relay's home. It lives INSIDE the api service because",
"      // Chapter 3.24: `MessageCreatedData` requires the field, so this fixture says.": "      // `MessageCreatedData` requires the field, so this fixture says.",
"// The port (chapter 3.3). ADR-06's quieter payoff is that the outbox is \"the": "// The port. ADR-06's quieter payoff is that the outbox is \"the",
"// The relay (chapter 3.3, ADR-06): the loop that moves committed events to the": "// The relay (ADR-06): the loop that moves committed events to the",
"// The CHECK constraint, against a live database (chapter 3.11, T010a).": "// The CHECK constraint, against a live database (T010a).",
"describe(\"the third dimension, and the two gates it has to pass (3.11)\", () => {": "describe(\"the third dimension, and the two gates it has to pass\", () => {",
"    // Chapter 3.11. This is the key the comment below predicted, and adding it": "    // This is the key the comment below predicted, and adding it",
"// Connection-minutes, credited (chapter 3.11, US1 and US2).": "// Connection-minutes, credited (US1 and US2).",
"/** What a usage report is worth, in one function (chapter 3.11, FR-RTL-05/FR-RTL-05).": "/** What a usage report is worth, in one function (FR-RTL-05/FR-RTL-05).",
"describe(\"minuteOf (chapter 3.11)\", () => {": "describe(\"minuteOf\", () => {",
"describe(\"periodOfMinute agrees with periodOf (chapter 3.11)\", () => {": "describe(\"periodOfMinute agrees with periodOf\", () => {",
"describe(\"the third dimension's copy (chapter 3.11)\", () => {": "describe(\"the third dimension's copy\", () => {",
"  // Chapter 3.11. Hyphenated, as the customer-facing name \u2014 the column is": "  // Hyphenated, as the customer-facing name \u2014 the column is",
" * needs (chapter 3.11).": " * needs.",
" * (chapter 3.10, FR-RTL-07).": " * (FR-RTL-07).",
"// THE OUTBOX PATTERN, A FOURTH TIME (chapter 3.10) \u2014 after 3.3's events, 3.5's": "// THE OUTBOX PATTERN, A FOURTH TIME \u2014 after the outbox chapter's events, the webhook dispatcher chapter's",
"describe(\"the message names the right noun and the right operation (3.11)\", () => {": "describe(\"the message names the right noun and the right operation\", () => {",
"    // THIS TEST'S SUBJECT NO LONGER EXISTS (chapter 3.17). It read \"counts an": "    // THIS TEST'S SUBJECT NO LONGER EXISTS. It read \"counts an",
"// The quota relay's home (chapter 3.10). Same shape as the notification": "// The quota relay's home. Same shape as the notification",
"// The authorization-code flow, by hand (chapter 3.1).": "// The authorization-code flow, by hand.",
"// What a provider is allowed to say back (chapter 3.1). Two responses cross": "// What a provider is allowed to say back. Two responses cross",
"// Invariants 5 and 6 (chapter 3.1), in the Docker-free lane: the CSRF binding": "// Invariants 5 and 6, in the Docker-free lane: the CSRF binding",
"// Signup (chapter 3.1, FR-TEN-01/02). Two routes, both necessarily": "// Signup (FR-TEN-01/02). Two routes, both necessarily",
"// Signup against the real database (chapter 3.1) \u2014 invariants 1\u20134 and 7.": "// Signup against the real database \u2014 invariants 1\u20134 and 7.",
"    // Chapter 3.2: there is no header left to forge here. The property is": "    // There is no header left to forge here. The property is",
"// The CSRF binding for the OAuth flow (chapter 3.1).": "// The CSRF binding for the OAuth flow.",
"// Signup's home (chapter 3.1). Deliberately thin, and deliberately NOT": "// Signup's home. Deliberately thin, and deliberately NOT",
"/** The user surface (chapter 3.15).": "/** The user surface.",
"    // Chapter 3.20. The controller for the reason `channels.controller.ts` states:": "    // The controller for the reason `channels.controller.ts` states:",
"  /** A read position for the user the path names (chapter 3.15, FR-017).": "  /** A read position for the user the path names (FR-017).",
"  /** The profile, read and written (chapter 3.15, FR-023, FR-024).": "  /** The profile, read and written (FR-023, FR-024).",
"  /** Up to 100 users in one call (chapter 3.15, FR-025).": "  /** Up to 100 users in one call (FR-025).",
"  /** Delete a user, keeping their row and their messages (chapter 3.15, FR-027). */": "  /** Delete a user, keeping their row and their messages (FR-027). */",
"  /** The ban pair (chapter 3.15, FR-031).": "  /** The ban pair (FR-031).",
"   * reconnecting reads membership at the door (chapter 3.2), and the backstop's": "   * reconnecting reads membership at the door, and the backstop's",
"// THE LISTING, END TO END (chapter 3.15, FR-013 to FR-015, FR-022, SC-007).": "// THE LISTING, END TO END (FR-013 to FR-015, FR-022, SC-007).",
"  // \u2550\u2550 THE UNREAD COUNT (chapter 3.15, FR-016 to FR-018, SC-008) \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550": "  // \u2550\u2550 THE UNREAD COUNT (FR-016 to FR-018, SC-008) \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550",
"  // \u2550\u2550 THE PROFILE (chapter 3.15, FR-023, FR-024, SC-011) \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550": "  // \u2550\u2550 THE PROFILE (FR-023, FR-024, SC-011) \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550",
"    // (chapter 3.17, T021a). `toEqual` on a whole body is the only assertion that": "    // (T021a). `toEqual` on a whole body is the only assertion that",
"  // \u2550\u2550 A BOT IS A USER (chapter 3.17, US3, FR-004) \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550": "  // \u2550\u2550 A BOT IS A USER (US3, FR-004) \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550",
"    // (chapter 3.15). What matters here is that a second add of a bot behaves exactly": "    //. What matters here is that a second add of a bot behaves exactly",
"  // \u2550\u2550 THE BOT USER (chapter 3.17, FR-USR-07) \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550": "  // \u2550\u2550 THE BOT USER (FR-USR-07) \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550",
"  // \u2550\u2550 THE BAN (chapter 3.15, FR-031, FR-032, SC-013) \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550": "  // \u2550\u2550 THE BAN (FR-031, FR-032, SC-013) \u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550\u2550",
"// The channels module's shape, for the channels module's reasons (chapter 3.15).": "// The channels module's shape, for the channels module's reasons.",
"// THE USER SURFACE'S BODIES AND QUERIES (chapter 3.15, FR-013, FR-016, FR-017).": "// THE USER SURFACE'S BODIES AND QUERIES (FR-013, FR-016, FR-017).",
" * (chapter 3.15, FR-024). Two bounds, half an order of magnitude apart, and the SRS chose": " * (FR-024). Two bounds, half an order of magnitude apart, and the SRS chose",
"/** The profile body (chapter 3.15, FR-023, FR-024).": "/** The profile body (FR-023, FR-024).",
"  /** A bot's description, editable here (chapter 3.17, FR-004).": "  /** A bot's description, editable here (FR-004).",
"/** An entry in the bulk upsert (chapter 3.15, FR-025, FR-026).": "/** An entry in the bulk upsert (FR-025, FR-026).",
"    /** What kind of thing this user is (chapter 3.17, FR-USR-07).": "    /** What kind of thing this user is (FR-USR-07).",
"/** The read-position body (chapter 3.15, FR-017).": "/** The read-position body (FR-017).",
"/** The user surface (chapter 3.15, FR-013 and the clauses after it).": "/** The user surface (FR-013 and the clauses after it).",
"      // DOCUMENTED (chapter 3.17). A client that had to infer personhood from a null": "      // DOCUMENTED. A client that had to infer personhood from a null",
"  /** Record a read position for the user the path names (chapter 3.15, FR-017, FR-018).": "  /** Record a read position for the user the path names (FR-017, FR-018).",
"  /** Up to 100 users in one call, reported per entry (chapter 3.15, FR-025, FR-026).": "  /** Up to 100 users in one call, reported per entry (FR-025, FR-026).",
"      /** A FOURTH STATUS, IN A 200 (chapter 3.17, FR-002a). `kind_conflict` says the": "      /** A FOURTH STATUS, IN A 200 (FR-002a). `kind_conflict` says the",
"  /** Delete a user (chapter 3.15, FR-027 to FR-029).": "  /** Delete a user (FR-027 to FR-029).",
"  /** Ban and unban, tenant-wide (chapter 3.15, FR-031, FR-032).": "  /** Ban and unban, tenant-wide (FR-031, FR-032).",
"// The attempt record's SHAPE and its SILENCE (chapter 3.6).": "// The attempt record's SHAPE and its SILENCE.",
"// The attempt record, against a real broker and a real api (chapter 3.6).": "// The attempt record, against a real broker and a real api.",
"// The delivery schedule (chapter 3.5). Invariant 8 lives here; 9, 10 and 12": "// The delivery schedule. Invariant 8 lives here; 9, 10 and 12",
"// The failure run and automatic disablement (chapter 3.6, FR-006\u2026FR-012).": "// The failure run and automatic disablement (FR-006\u2026FR-012).",
"// The sweep as the RELAY runs it (chapter 3.6, T033's reason for existing).": "// The sweep as the RELAY runs it (T033's reason for existing).",
"// What the LOCK protects, as opposed to what the predicate protects (chapter 3.6).": "// What the LOCK protects, as opposed to what the predicate protects.",
"// That the LOOP calls the sweep (chapter 3.6, T026).": "// That the LOOP calls the sweep (T026).",
"// The second relay (chapter 3.5, research R13).": "// The second relay (research R13).",
"  /** The auto-disable sweep, riding this loop (chapter 3.6, research R1).": "  /** The auto-disable sweep, riding this loop (research R1).",
"// The decision to switch off a paying customer's endpoint (chapter 3.6).": "// The decision to switch off a paying customer's endpoint.",
"// When to stop trying (chapter 3.6, FR-007).": "// When to stop trying (FR-007).",
"// The retry schedule (chapter 3.5, FR-WHK-03).": "// The retry schedule (FR-WHK-03).",
"// DECISION (chapter 3.5, taken by the author on 2026-08-10): FR-WHK-03 is": "// DECISION (taken by the author on 2026-08-10): FR-WHK-03 is",
"// Webhook signing secrets at rest (chapter 3.5).": "// Webhook signing secrets at rest.",
"// DECISION (chapter 3.5): envelope encryption, not the salted hash chapter 3.2": "// DECISION: envelope encryption, not the salted hash the credentials chapter",
"// Proving an endpoint works again (chapter 3.6, FR-WHK-09, research R8).": "// Proving an endpoint works again (FR-WHK-09, research R8).",
"// The webhook management surface (chapter 3.5, FR-WHK-01 and FR-WHK-08).": "// The webhook management surface (FR-WHK-01 and FR-WHK-08).",
"   * (chapter 3.6, FR-013, FR-016).": "   * (FR-013, FR-016).",
"// (chapter 3.5). Invariants 1, 2, 3 and 6 of contracts/dispatcher.md live here.": "//. Invariants 1, 2, 3 and 6 of contracts/dispatcher.md live here.",
"/** The api's SECOND relay (chapter 3.5, research R13), started with the service": "/** The api's SECOND relay (research R13), started with the service",
"// The management surface's rules (chapter 3.5). Two of them are worth stating": "// The management surface's rules. Two of them are worth stating",
"/** DECISION (chapter 3.5, research R9): no requirement mandates this check, and": "/** DECISION (research R9): no requirement mandates this check, and",
"   * (chapter 3.6, FR-013\u2026FR-016, research R8).": "   * (FR-013\u2026FR-016, research R8).",
"// Posting to a machine the platform does not own (chapter 3.5).": "// Posting to a machine the platform does not own.",
"// (chapter 3.5). Invariant 7 lives here; 11, 13, 15 and 16 join it.": "//. Invariant 7 lives here; 11, 13, 15 and 16 join it.",
"      // Chapter 3.8: nor the notification relay, for the same reason.": "      // The rate-limit chapter: nor the notification relay, for the same reason.",
"    // A RANDOM HIGH PORT (chapter 3.13, T077). This bound a fixed 4131, which is": "    // A RANDOM HIGH PORT (T077). This bound a fixed 4131, which is",
"    // reach a conclusion that was available on the first pass (chapter 3.4).": "    // reach a conclusion that was available on the first pass.",
"// Turning an event into deliveries (chapter 3.5).": "// Turning an event into deliveries.",
"// The dispatcher (chapter 3.5) \u2014 the first service in this platform that exists": "// The dispatcher \u2014 the first service in this platform that exists",
"// The signature a customer verifies (chapter 3.5, FR-WHK-08).": "// The signature a customer verifies (FR-WHK-08).",
"// The one call the gateway makes for itself (chapter 3.11).": "// The one call the gateway makes for itself.",
"  /** The api's own error code and message, when it sent an envelope (chapter 3.15).": "  /** The api's own error code and message, when it sent an envelope.",
"  /** Chapter 3.2: the token the client presented at connect, carried so the": "  /** The token the client presented at connect, carried so the",
"  /** Chapter 3.2: present the token, be told who it belongs to and what it may": "  /** Present the token, be told who it belongs to and what it may",
"  /** Chapter 3.11: the one call the gateway makes FOR ITSELF.": "  /** The one call the gateway makes FOR ITSELF.",
"  /** Chapter 3.11. Absent by default and absent in every test that does not": "  /** Absent by default and absent in every test that does not",
"      // Chapter 3.11. So is 402, and it is a DIFFERENT answer: the credential is": "      // The connection-metering chapter, So is 402, and it is a DIFFERENT answer: the credential is",
"      /** Chapter 3.8. The environment's two socket allowances, read from": "      /** The environment's two socket allowances, read from",
"  /** Chapter 3.11. The api answered, and the answer was \"this environment has": "  /** The api answered, and the answer was \"this environment has",
"  it(\"accepts five and refuses the sixth with 4004 (FR-001 (3.22), FR-003, SC-002)\", async () => {": "  it(\"accepts five and refuses the sixth with 4004 (FR-001, FR-003, SC-002)\", async () => {",
"  it(\"frees a slot on close, reusable with NO waiting period (FR-010 (3.22), SC-003)\", async () => {": "  it(\"frees a slot on close, reusable with NO waiting period (FR-010, SC-003)\", async () => {",
"  it(\"logs the refusal with the user, the environment and the count, and no credential (FR-015 (3.22), SC-008)\", async () => {": "  it(\"logs the refusal with the user, the environment and the count, and no credential (FR-015, SC-008)\", async () => {",
"  it(\"delivers a message to both of one user's connections, each exactly once (FR-014 (3.22), SC-001)\", async () => {": "  it(\"delivers a message to both of one user's connections, each exactly once (FR-014, SC-001)\", async () => {",
"  it(\"delivers a membership change to both of one user's connections (FR-014 (3.22))\", async () => {": "  it(\"delivers a membership change to both of one user's connections (FR-014)\", async () => {",
"  it(\"keeps delivering to a live connection when an EARLIER one is gone (FR-014 (3.22))\", async () => {": "  it(\"keeps delivering to a live connection when an EARLIER one is gone (FR-014)\", async () => {",
"  it(\"counts five across two instances and refuses the sixth on either (FR-006 (3.22), SC-004)\", async () => {": "  it(\"counts five across two instances and refuses the sixth on either (FR-006, SC-004)\", async () => {",
"  it(\"frees a dead instance's slots after the bound (FR-007 (3.22), SC-005)\", async () => {": "  it(\"frees a dead instance's slots after the bound (FR-007, SC-005)\", async () => {",
"  it(\"still refuses BEFORE the bound elapses (FR-007 (3.22), SC-005)\", async () => {": "  it(\"still refuses BEFORE the bound elapses (FR-007, SC-005)\", async () => {",
"  it(\"frees the slots IMMEDIATELY on a clean shutdown (FR-011a (3.22), SC-013)\", async () => {": "  it(\"frees the slots IMMEDIATELY on a clean shutdown (FR-011a, SC-013)\", async () => {",
"  it(\"keeps a heartbeating connection's slot across three bounds (FR-008 (3.22), SC-006)\", async () => {": "  it(\"keeps a heartbeating connection's slot across three bounds (FR-008, SC-006)\", async () => {",
"  it(\"counts each environment separately for one user (FR-012 (3.22))\", async () => {": "  it(\"counts each environment separately for one user (FR-012)\", async () => {",
"  it(\"does not resurrect an expired slot on renewal (FR-011 (3.22))\", async () => {": "  it(\"does not resurrect an expired slot on renewal (FR-011)\", async () => {",
"  it(\"refuses to renew a slot another connection took, rather than overwriting it (FR-011 (3.22))\", async () => {": "  it(\"refuses to renew a slot another connection took, rather than overwriting it (FR-011)\", async () => {",
"  it(\"keeps the connection working on a NEW slot after a re-claim (FR-011b (3.22), SC-014)\", async () => {": "  it(\"keeps the connection working on a NEW slot after a re-claim (FR-011b, SC-014)\", async () => {",
"  it(\"never admits a sixth under many simultaneous claims (FR-013 (3.22))\", async () => {": "  it(\"never admits a sixth under many simultaneous claims (FR-013)\", async () => {",
"  it(\"closes the connection when the cap is genuinely full at renewal (FR-011b (3.22), SC-014)\", async () => {": "  it(\"closes the connection when the cap is genuinely full at renewal (FR-011b, SC-014)\", async () => {",
"  it(\"logs that the cap was not enforced when the registry is unreachable (FR-016 (3.22), SC-011)\", async () => {": "  it(\"logs that the cap was not enforced when the registry is unreachable (FR-016, SC-011)\", async () => {",
"  it(\"tells 'not enforced' apart from 'enforced and under the limit' (FR-016a (3.22), SC-014)\", async () => {": "  it(\"tells 'not enforced' apart from 'enforced and under the limit' (FR-016a, SC-014)\", async () => {",
"  it(\"does NOT fall back to counting this instance's own connections (FR-016b (3.22))\", async () => {": "  it(\"does NOT fall back to counting this instance's own connections (FR-016b)\", async () => {",
"  it(\"refuses when every slot is held, and says five (FR-001 (3.22))\", async () => {": "  it(\"refuses when every slot is held, and says five (FR-001)\", async () => {",
"  it(\"counts each environment separately for one user identifier (FR-012 (3.22))\", async () => {": "  it(\"counts each environment separately for one user identifier (FR-012)\", async () => {",
"  it(\"renews a slot it still holds (FR-008 (3.22))\", async () => {": "  it(\"renews a slot it still holds (FR-008)\", async () => {",
"  it(\"re-claims when its slot is GONE and nothing else took it (FR-011b (3.22))\", async () => {": "  it(\"re-claims when its slot is GONE and nothing else took it (FR-011b)\", async () => {",
"  it(\"refuses to renew a slot ANOTHER connection now holds (FR-011 (3.22))\", async () => {": "  it(\"refuses to renew a slot ANOTHER connection now holds (FR-011)\", async () => {",
"  it(\"frees a slot it holds, and the slot is reusable at once (FR-010 (3.22))\", async () => {": "  it(\"frees a slot it holds, and the slot is reusable at once (FR-010)\", async () => {",
"  it(\"claims a slot whose tombstone has NOT expired (FR-010 (3.22))\", async () => {": "  it(\"claims a slot whose tombstone has NOT expired (FR-010)\", async () => {",
"  it(\"accepts a claim immediately after releaseAll frees all five (FR-011a (3.22))\", async () => {": "  it(\"accepts a claim immediately after releaseAll frees all five (FR-011a)\", async () => {",
"  it(\"does NOT free a slot another connection now holds (FR-010 (3.22))\", async () => {": "  it(\"does NOT free a slot another connection now holds (FR-010)\", async () => {",
"  it(\"releases every slot this instance holds (FR-011a (3.22))\", async () => {": "  it(\"releases every slot this instance holds (FR-011a)\", async () => {",
"  it(\"returns unenforced rather than zero when Redis is unreachable (FR-016 (3.22))\", async () => {": "  it(\"returns unenforced rather than zero when Redis is unreachable (FR-016)\", async () => {",
"  it(\"keeps the heartbeat strictly inside the bound, three to one (FR-009 (3.22))\", async () => {": "  it(\"keeps the heartbeat strictly inside the bound, three to one (FR-009)\", async () => {",
"  it(\"states the maximum in exactly one place (FR-002 (3.22))\", async () => {": "  it(\"states the maximum in exactly one place (FR-002)\", async () => {",
"/** The revision fabric's equivalent (chapter 3.23). Separate queue, separate deadline,": "/** The revision fabric's equivalent. Separate queue, separate deadline,",
"    // CHAPTER 3.23. TWO SUBJECTS ON ONE SUBSCRIBER, told apart by the prefix rather than": "    // TWO SUBJECTS ON ONE SUBSCRIBER, told apart by the prefix rather than",
" * (chapter 3.12, FR-007).": " * (FR-007).",
"    /** Chapter 3.17. The gateway declares its own narrow view of the repository \u2014": "    /** The gateway declares its own narrow view of the repository \u2014",
"   * member of (chapter 3.15). */": "   * member of. */",
"   * (chapter 3.17, T040b).": "   * (T040b).",
"   * already in it (chapter 3.15, T144).": "   * already in it (T144).",
"    // Chapter 3.15: a PRIVATE channel in the same tenant, and this user is NOT a": "    // A PRIVATE channel in the same tenant, and this user is NOT a",
"      /** Turn this tenant's own user into a bot (chapter 3.17, T040b). Exposed rather": "      /** Turn this tenant's own user into a bot (T040b). Exposed rather",
"  /** The close code, once it arrives (chapter 3.15, T151). Added because a refusal at": "  /** The close code, once it arrives (T151). Added because a refusal at",
"    // Chapter 3.24: WELL-FORMED IS THE POINT. `messageSchema` requires": "    // WELL-FORMED IS THE POINT. `messageSchema` requires",
"// The claim no single-process test can make (chapter 3.8): that the api and the": "// The claim no single-process test can make: that the api and the",
"      /** Chapter 3.17. A key send names a bot, so this file needs one. The gateway": "      /** A key send names a bot, so this file needs one. The gateway",
"  // it names a bot (chapter 3.17). Seeded here rather than per send: this suite counts": "  // it names a bot. Seeded here rather than per send: this suite counts",
"  // (chapter 3.13, FR-041). It bound a fixed 4124, which is the fault": "  // (FR-041). It bound a fixed 4124, which is the fault",
"describe(\"one counter, two services (chapter 3.8)\", () => {": "describe(\"one counter, two services\", () => {",
"// The gateway's share of the arithmetic (chapter 3.8). Pure, so a window": "// The gateway's share of the arithmetic. Pure, so a window",
"// The gateway's counter (chapter 3.8, research R12, R20).": "// The gateway's counter (research R12, R20).",
"  it(\"closes every module it builds (chapter 3.22)\", () => {": "  it(\"closes every module it builds\", () => {",
"  // Chapter 3.8. A SECOND Redis client, not fanout's \u2014 one of fanout's two is a": "  // A SECOND Redis client, not fanout's \u2014 one of fanout's two is a",
"  // Chapter 3.19. The FOURTH and FIFTH Redis clients, and the reason is chapter": "  // The FOURTH and FIFTH Redis clients, and the reason is",
"  // Chapter 3.20. The SIXTH Redis client, and only one where presence needed two:": "  // The SIXTH Redis client, and only one where presence needed two:",
"  // Chapter 3.21: the SEVENTH and EIGHTH Redis clients. Chapter 3.20 closed at": "  // The SEVENTH and EIGHTH Redis clients. The membership-revocation chapter closed at",
"  // Chapter 3.11. THE FIRST SECRET THIS SERVICE HAS EVER HELD, and it is not a": "  // THE FIRST SECRET THIS SERVICE HAS EVER HELD, and it is not a",
"    // reconnecting reads membership at the door (chapter 3.2). The other is the": "    // reconnecting reads membership at the door. The other is the",
"// Membership's gateway half (chapter 3.20, FR-RTM-10).": "// Membership's gateway half (FR-RTM-10).",
"// What a signal does to a bill (chapter 3.11, US2).": "// What a signal does to a bill (US2).",
"  /** CHAPTER 3.21. **`.resume()` DRAINS A STREAM AND KEEPS NOTHING**, which is a": "  /** **`.resume()` DRAINS A STREAM AND KEEPS NOTHING**, which is a",
"// Connection-minutes, from the service that cannot write them (chapter 3.11).": "// Connection-minutes, from the service that cannot write them.",
"/** The meter (chapter 3.11).": "/** The meter.",
"// Presence (chapter 3.19, ADR-19): who is online, and who is allowed to know.": "// Presence (ADR-19): who is online, and who is allowed to know.",
"    // AND ON RESUME IT NOW ARRIVES \u2014 WHICH IS HALF OF THE GAP CLOSING (chapter 3.17).": "    // AND ON RESUME IT NOW ARRIVES \u2014 WHICH IS HALF OF THE GAP CLOSING.",
"  /** Chapter 3.7. Per channel, the highest sequence this connection's backfill": "  /** Per channel, the highest sequence this connection's backfill",
"  /** Chapter 3.8. The environment's send allowance, as it stood when this socket": "  /** The environment's send allowance, as it stood when this socket",
"  /** Chapter 3.11. When this socket opened, and which environment owes for it.": "  /** When this socket opened, and which environment owes for it.",
"  /** Chapter 3.19. Every local connection this user holds \u2014 the question presence": "  /** Every local connection this user holds \u2014 the question presence",
"/** Chapter 3.2: the gateway holds no signing secret, so a test token is an": "/** The gateway holds no signing secret, so a test token is an",
"        // Chapter 3.15: the api now reports whether the user is banned, and a stub": "        // The api now reports whether the user is banned, and a stub",
"        // Chapter 3.8: the limits ride the session response now. Generous, and": "        // The limits ride the session response now. Generous, and",
"      // Chapter 3.20. Agrees with `session` above: this file is about the resume,": "      // Agrees with `session` above: this file is about the resume,",
"describe(\"two instances on one fabric (chapter 3.18)\", () => {": "describe(\"two instances on one fabric\", () => {",
"    // Chapter 3.20. The same list `session` answers with, so the backstop confirms": "    // The same list `session` answers with, so the backstop confirms",
"// Chapter 3.7. `flushable` decides what a resuming connection may hand over from": "// `flushable` decides what a resuming connection may hand over from",
"// The socket's credential cases (chapter 3.2), against a REAL api.": "// The socket's credential cases, against a REAL api.",
"    /** Chapter 3.18. An application credential may send only as a bot user": "    /** An application credential may send only as a bot user",
"     * (chapter 3.17), so a REST send needs one to exist \u2014 and `createUser`": "     *, so a REST send needs one to exist \u2014 and `createUser`",
"      // CHAPTER 3.21. **The child has already said why and nobody was": "      // **The child has already said why and nobody was",
"  // Chapter 3.18: the sender a REST send names. ADDITIVE to this fixture \u2014 the": "  // The sender a REST send names. ADDITIVE to this fixture \u2014 the",
"  // CHAPTER 3.23: two PEOPLE in the channel, because FR-005's property is \"every": "  // Two PEOPLE in the channel, because FR-005's property is \"every",
"    // Chapter 3.3: no outbox relay in this child. This suite is about the": "    // No outbox relay in this child. This suite is about the",
"      // Chapter 3.11: its own failed-authentication keyspace. Chapter 3.8's auth": "      // Its own failed-authentication keyspace. The rate-limit chapter's auth",
"describe(\"the socket's credentials (chapter 3.2)\", () => {": "describe(\"the socket's credentials\", () => {",
"  // T022c and T020a (chapter 3.24). THE SOCKET DOOR, WHICH IS THE ONE THAT DROPS THINGS.": "  // T022c and T020a. THE SOCKET DOOR, WHICH IS THE ONE THAT DROPS THINGS.",
"  it(\"refuses eleven at the GATEWAY, naming the field (FR-005 (3.24))\", async () => {": "  it(\"refuses eleven at the GATEWAY, naming the field (FR-005)\", async () => {",
"  it(\"refuses a media_id and SAYS hosted media is unavailable (FR-003a (3.24))\", async () => {": "  it(\"refuses a media_id and SAYS hosted media is unavailable (FR-003a)\", async () => {",
"  it(\"commits TWO attachments sent over the socket, in order (FR-001 (3.24), FR-006 (3.24))\", async () => {": "  it(\"commits TWO attachments sent over the socket, in order (FR-001, FR-006)\", async () => {",
"  it(\"accepts an attachments-only message and refuses one with neither (FR-019 (3.24), FR-019b (3.24))\", async () => {": "  it(\"accepts an attachments-only message and refuses one with neither (FR-019, FR-019b)\", async () => {",
"describe(\"the cap at the door (chapter 3.11, US3)\", () => {": "describe(\"the cap at the door (US3)\", () => {",
"describe(\"the socket's delivery, with a fan-out attached (chapter 3.18)\", () => {": "describe(\"the socket's delivery, with a fan-out attached\", () => {",
"  /** Chapter 3.20. **Its dependency injection is three hundred lines above the test": "  /** **Its dependency injection is three hundred lines above the test",
"  // T028b, T030 and T029a (chapter 3.24). THE TWO DELIVERY DOORS AND THE ACK.": "  // T028b, T030 and T029a. THE TWO DELIVERY DOORS AND THE ACK.",
"  it(\"delivers attachments SOCKET to SOCKET, in order (FR-008 (3.24), SC-001 (3.24))\", async () => {": "  it(\"delivers attachments SOCKET to SOCKET, in order (FR-008, SC-001)\", async () => {",
"  it(\"delivers a deletion with NO attachment field at all (FR-013 (3.24))\", async () => {": "  it(\"delivers a deletion with NO attachment field at all (FR-013)\", async () => {",
"  it(\"carries only `seq` on the sender's ack (FR-008 (3.24))\", async () => {": "  it(\"carries only `seq` on the sender's ack (FR-008)\", async () => {",
"    // speak only as software (chapter 3.17). `idempotency_key` must be a UUID on": "    // speak only as software. `idempotency_key` must be a UUID on",
"          // T030 (chapter 3.24). TWO, because one cannot show an order.": "          // T030. TWO, because one cannot show an order.",
"      // Chapter 3.24: WELL-FORMED IS THE POINT. `messageSchema` requires": "      // WELL-FORMED IS THE POINT. `messageSchema` requires",
"describe(\"the connection cap at the door (chapter 3.22, US1)\", () => {": "describe(\"the connection cap at the door (US1)\", () => {",
"  it(\"refuses a sixth connection for one user (FR-RTM-09 (3.22))\", async () => {": "  it(\"refuses a sixth connection for one user (FR-RTM-09)\", async () => {",
"    // Chapter 3.2: the api verifies tokens, so the stub is what decides which": "    // The api verifies tokens, so the stub is what decides which",
"            // Chapter 3.15: the api now reports whether the user is banned, and a stub": "            // The api now reports whether the user is banned, and a stub",
"            // Chapter 3.8. The limits ride the session response because the": "            // The limits ride the session response because the",
"    // Chapter 3.11. Null is what a gateway with no metering credential gets, and": "    // Null is what a gateway with no metering credential gets, and",
"  /** Chapter 3.23. The same injection for a revision: an edit or a deletion arriving from": "  /** The same injection for a revision: an edit or a deletion arriving from",
"  // CHAPTER 3.23. The stub gained these because the interface did, and the typecheck is": "  // The stub gained these because the interface did, and the typecheck is",
"/** A counter with no Redis in it (chapter 3.8). The arithmetic is unit-tested": "/** A counter with no Redis in it. The arithmetic is unit-tested",
"// Chapter 3.8. The socket's two limits \u2014 one at the door, one on every frame \u2014": "// The socket's two limits \u2014 one at the door, one on every frame \u2014",
"describe(\"the socket's limits (chapter 3.8)\", () => {": "describe(\"the socket's limits\", () => {",
"          // Chapter 3.15: the api now reports whether the user is banned, and a stub": "          // The api now reports whether the user is banned, and a stub",
"  it(\"emits 4008 for a quota, and 4009 from nowhere (chapter 3.11)\", async () => {": "  it(\"emits 4008 for a quota, and 4009 from nowhere\", async () => {",
"  /** T049a, SC-008 (chapter 3.23) \u2014 FR-RTM-05's SIX KINDS ALL HAVE A PRODUCER.": "  /** T049a, SC-008 \u2014 FR-RTM-05's SIX KINDS ALL HAVE A PRODUCER.",
"describe(\"INBOUND_FRAME_TYPES (chapter 3.21)\", () => {": "describe(\"INBOUND_FRAME_TYPES\", () => {",
"/** THE RENEWAL INTERVAL (chapter 3.21, FR-011).": "/** THE RENEWAL INTERVAL (FR-011).",
"/** THE FRAME TYPES A CLIENT MAY SEND (chapter 3.21, FR-003).": "/** THE FRAME TYPES A CLIENT MAY SEND (FR-003).",
"/** The handshake refusal (chapter 3.8, FR-RTL-03). Written onto the raw upgrade": "/** The handshake refusal (FR-RTL-03). Written onto the raw upgrade",
"  /** WHICH FIELD, on the socket door (chapter 3.24, FR-005).": "  /** WHICH FIELD, on the socket door (FR-005).",
"  /** The shared counter (chapter 3.8). Optional for the same reason `fanout`": "  /** The shared counter. Optional for the same reason `fanout`",
"  /** Chapter 3.11. Optional for the reason `limits` and `fanout` are: 2.5's": "  /** Optional for the reason `limits` and `fanout` are: 2.5's",
"  /** Chapter 3.19. Optional for the same reason `fanout`, `limits` and the meter": "  /** Optional for the same reason `fanout`, `limits` and the meter",
"  /** Chapter 3.20. Optional for the same four reasons, and one more that is this": "  /** Optional for the same four reasons, and one more that is this",
"  /** Chapter 3.21. Optional for the same reasons as the four above, and its own:": "  /** Optional for the same reasons as the four above, and its own:",
"  /** Chapter 3.21. Injectable for the reason `meterIntervalMs` above and chapter": "  /** Injectable for the reason `meterIntervalMs` above and",
"  // Chapter 3.11. A second timer beside the heartbeat, not a second job for it.": "  // A second timer beside the heartbeat, not a second job for it.",
"      // Chapter 3.7. A live connection is not necessarily a connection with": "      // A live connection is not necessarily a connection with",
"  /** An edit or a deletion arriving from the revision fabric (chapter 3.23, ADR-24).": "  /** An edit or a deletion arriving from the revision fabric (ADR-24).",
"  /** A typing signal arriving from its own fabric (chapter 3.21, T043).": "  /** A typing signal arriving from its own fabric (T043).",
"  /** The backstop (chapter 3.20, FR-018, constitution IV).": "  /** The backstop (FR-018, constitution IV).",
"      // Chapter 3.2: the api verifies, and answers with the identity AND the": "      // The api verifies, and answers with the identity AND the",
"      // Chapter 3.8. THE ESTABLISHMENT LIMIT IS SPENT HERE, before": "      // THE ESTABLISHMENT LIMIT IS SPENT HERE, before",
"    /** Chapter 3.22. The id the cap claimed a place with, so the connection and": "    /** The id the cap claimed a place with, so the connection and",
"      // Chapter 3.2: memberships arrived with the identity, from the session": "      // Memberships arrived with the identity, from the session",
"      // Chapter 3.11. Stamped BEFORE the resume and before the ack, because the": "      // Stamped BEFORE the resume and before the ack, because the",
"        // Chapter 3.19. Presence has its own subject per channel, so a channel now": "        // Presence has its own subject per channel, so a channel now",
"          // Chapter 3.21. Like the membership module, `typing.ts` swallows and": "          // Like the membership module, `typing.ts` swallows and",
"      // Chapter 3.21: the debounce entry for this whole connection. One of the": "      // The debounce entry for this whole connection. One of the",
"  /** THE DEBOUNCE (chapter 3.21, T054): the last publish time per (connection,": "  /** THE DEBOUNCE (T054): the last publish time per (connection,",
"    // Chapter 3.21. THE SECOND INBOUND FRAME, and it leaves before the send": "    // THE SECOND INBOUND FRAME, and it leaves before the send",
"    // Chapter 3.8. THE SEND LIMIT IS SPENT ON THE FRAME, not on the api call": "    // THE SEND LIMIT IS SPENT ON THE FRAME, not on the api call",
"    // A NAMED DESTRUCTURE, AND THAT IS THE POINT (chapter 3.24, FR-001). Widening": "    // A NAMED DESTRUCTURE, AND THAT IS THE POINT (FR-001). Widening",
"      // Chapter 3.2. A 401 here means the token this connection was opened": "      // A 401 here means the token this connection was opened",
"describe(\"a typing signal on its way out (chapter 3.21)\", () => {": "describe(\"a typing signal on its way out\", () => {",
"              // Chapter 3.24: this payload goes onto the fabric as JSON and the gateway": "              // This payload goes onto the fabric as JSON and the gateway",
"        // Chapter 3.24: this payload goes onto the fabric as JSON and the gateway": "        // This payload goes onto the fabric as JSON and the gateway",
"describe(\"createTyping's own arms (chapter 3.21)\", () => {": "describe(\"createTyping's own arms\", () => {",
"// Typing's gateway half (chapter 3.21, FR-RTM-05, FR-RTM-08).": "// Typing's gateway half (FR-RTM-05, FR-RTM-08).",
"// Chapter 3.24's close-out battery failed 10 times in 20, every one of them a suite": "// The attachments chapter's close-out battery failed 10 times in 20, every one of them a suite",
"    // it passed on every red run of chapter 3.24's battery too.": "    // it passed on every red run of the attachments chapter's battery too.",
" * REASSESSED IN CHAPTER 3.12 (T063a), and two of the three now have an API.": " * REASSESSED IN the isolation gauntlet (T063a), and two of the three now have an API.",
" * this file at all. The migration is chapter 3.15's, with the rest of FR-CHN. */": " * this file at all. The migration is the channel-control chapter's, with the rest of FR-CHN. */",
"   * random one from a band, which `session.itest.ts:133` draws and chapter 3.23": "   * random one from a band, which `session.itest.ts:133` draws and the revisions chapter",
"      // Chapter 3.8's other half: where the notification relay posts its SMTP.": "      // The rate-limit chapter's other half: where the notification relay posts its SMTP.",
"    // 3.3's own suite asserts on that same table is a race between two test": "    // The outbox chapter's own suite asserts on that same table is a race between two test",
"    // 3.8's own suite asserts on that column is a race between test files.": "    // The rate-limit chapter's own suite asserts on that column is a race between test files.",
"    // background consumer writing to a table 3.4's suite asserts on is a race": "    // background consumer writing to a table the broker chapter's suite asserts on is a race",
"    // the general form of 3.3's finding 4 rather than a coincidence.": "    // the general form of the outbox chapter's finding 4 rather than a coincidence.",
"      // `ECONNREFUSED`. **Ten of chapter 3.24's twenty-run battery failed exactly": "      // `ECONNREFUSED`. **Ten of the attachments chapter's twenty-run battery failed exactly",
"// Chapter 3.8's limiter never sees `/internal/messages`: `operationsFor` returns": "// The rate-limit chapter's limiter never sees `/internal/messages`: `operationsFor` returns",
"    // (FR-TEN-05). Chapter 3.2 changed what proves the tenant here \u2014 a key": "    // (FR-TEN-05). The credentials chapter changed what proves the tenant here \u2014 a key",
"  it(\"creates a PRIVATE channel, which the route began accepting in chapter 3.15\", async () => {": "  it(\"creates a PRIVATE channel, which the route began accepting in the channel-control chapter\", async () => {",
"    // The gap this exercise recorded had TWO causes. Chapter 3.17 removed the first:": "    // The gap this exercise recorded had TWO causes. The sender chapter removed the first:",
"    // resume. Chapter 3.18 removes the second, which was the whole of what remained": "    // resume. The fan-out chapter removes the second, which was the whole of what remained",
"    // `user: \"outside-bot\"` is not optional and not decoration. Chapter 3.17 made an": "    // `user: \"outside-bot\"` is not optional and not decoration. The sender chapter made an",
"   * credential a customer's server holds. Chapter 3.23's plan scheduled a title audit": "   * credential a customer's server holds. The revisions chapter's plan scheduled a title audit",
"   * \"sent over REST\" because chapter 3.18 corrected it. This file is the only": "   * \"sent over REST\" because the fan-out chapter corrected it. This file is the only",
"    // Chapter 3.21 built a module, awaited its `close()` so lint saw a used": "    // The typing chapter built a module, awaited its `close()` so lint saw a used",
"   * records what that bought: chapter 3.21 built a module, awaited its `close()`, never": "   * records what that bought: the typing chapter built a module, awaited its `close()`, never",
" * null, so chapter 3.23's tombstone predicate \u2014 `text === null` \u2014 is untouched.": " * null, so the revisions chapter's tombstone predicate \u2014 `text === null` \u2014 is untouched.",
"    // Sixteen until chapter 3.17 added `sender_not_permitted`, and **this assertion": "    // Sixteen until the sender chapter added `sender_not_permitted`, and **this assertion",
"    // accident. Chapter 3.11's close-code set did the same for 4003.": "    // accident. The connection-metering chapter's close-code set did the same for 4003.",
"    // Eighteen until chapter 3.23 added `not_message_author`, the fourth time, and then": "    // Eighteen until the revisions chapter added `not_message_author`, the fourth time, and then",
"    // its plan did not expect. **One pinned place, not the four chapter 3.22's close code": "    // its plan did not expect. **One pinned place, not the four the connection-cap chapter's close code",
"    // Chapter 3.24's one addition, and the three assertions a new code earns: it exists,": "    // The attachments chapter's one addition, and the three assertions a new code earns: it exists,",
"    // and it is here rather than at that call site for the reason chapter 3.2 gave": "    // and it is here rather than at that call site for the reason the credentials chapter gave",
"  //         new one succeeds and connecting fails again, which is chapter 3.15's": "  //         new one succeeds and connecting fails again, which is the channel-control chapter's",
"  // why chapter 3.2 put `wrong_credential_type` in it instead of inventing it at": "  // why the credentials chapter put `wrong_credential_type` in it instead of inventing it at",
"  // NOT `forbidden`. Chapter 3.2 made this argument when it added": "  // NOT `forbidden`. The credentials chapter made this argument when it added",
"   * (FR-016) adds a refusal that did not exist to be counted. Chapter 3.24's plan": "   * (FR-016) adds a refusal that did not exist to be counted. The attachments chapter's plan",
"  // Chapter 3.11's. A connection belongs to one environment for its lifetime, and": "  // The connection-metering chapter's. A connection belongs to one environment for its lifetime, and",
" * Chapter 3.11 declined to add a third instance and named the debt; a chapter whose": " * The connection-metering chapter declined to add a third instance and named the debt; a chapter whose",
" * MOVED HERE IN CHAPTER 3.18, and the reason is the same one chapter 3.4 gave": " * MOVED HERE IN the fan-out chapter, and the reason is the same one the broker chapter gave",
" * The spine's name is chapter 3.4's and is published; this one is new, so this": " * The spine's name is the broker chapter's and is published; this one is new, so this",
"    // is what makes every construction site name it. Chapter 3.24's inverse case is the": "    // is what makes every construction site name it. The attachments chapter's inverse case is the",
"    // Chapter 3.24 gave `messageSchema` a required attachments array, and the obvious": "    // The attachments chapter gave `messageSchema` a required attachments array, and the obvious",
"    // stored. Chapter 3.24's `outboxEventSchema` defect is the argument: a reader of": "    // stored. The attachments chapter's `outboxEventSchema` defect is the argument: a reader of",
" * A CONSTANT IS SAFE TO SHARE WHERE A SCHEMA WAS NOT. Chapter 3.24 found": " * A CONSTANT IS SAFE TO SHARE WHERE A SCHEMA WAS NOT. The attachments chapter found",
" * one: they arrived in chapter 3.23 as their own frames, `message.updated`": " * one: they arrived in the revisions chapter as their own frames, `message.updated`",
"/** THE ONE FRAME THAT DOES NOT CARRY A MESSAGE, and chapter 3.23 is where that became": "/** THE ONE FRAME THAT DOES NOT CARRY A MESSAGE, and the revisions chapter is where that became",
" * `messageDeletedSchema.shape.payload`. Chapter 3.23's fifth subject grammar carries this": " * `messageDeletedSchema.shape.payload`. The revisions chapter's fifth subject grammar carries this",
" * `request_id` ARRIVED IN CHAPTER 3.8, not in Part 2. The comment here promised": " * `request_id` ARRIVED IN the rate-limit chapter, not in Part 2. The comment here promised",
"// (chapter 2.5), and \u2014 from chapter 3.18 \u2014 the live fan-out's subject grammar,": "// (chapter 2.5), and \u2014 from the fan-out chapter \u2014 the live fan-out's subject grammar,",
"    /** `.min(1)` REMOVED in chapter 3.24 (FR-019), not relaxed by accident.": "    /** `.min(1)` REMOVED in the attachments chapter (FR-019), not relaxed by accident.",
"   * The same move chapter 3.2 made on this call, whose comment records it: the": "   * The same move the credentials chapter made on this call, whose comment records it: the",
" * Here rather than in either service, for the reason chapter 3.4 moved the event": " * Here rather than in either service, for the reason the broker chapter moved the event",
"// `analytics.{domain}.{action}.{environment_id}` extends chapter 3.4's": "// `analytics.{domain}.{action}.{environment_id}` extends the broker chapter's",
" * A CLAIMED write: the api reuses chapter 3.4's deduplication ledger, so an event": " * A CLAIMED write: the api reuses the broker chapter's deduplication ledger, so an event",
"  // this is what proves the topology holds. Chapter 3.19 asserted the same thing": "  // this is what proves the topology holds. The presence chapter asserted the same thing",
" * spine and chapter 3.19 followed it for presence. A new file is a whole-file fence": " * spine and the presence chapter followed it for presence. A new file is a whole-file fence",
" * NOT `subjectFor`, WHICH `internal.ts` ALREADY EXPORTS. Chapter 3.18 paid for that": " * NOT `subjectFor`, WHICH `internal.ts` ALREADY EXPORTS. The fan-out chapter paid for that",
" * being dropped. Chapter 3.19 chose the same strictness for the same reason.": " * being dropped. The presence chapter chose the same strictness for the same reason.",
" * practical payoff is that `fanout.ts` gains no hunk, and chapter 3.18's fences": " * practical payoff is that `fanout.ts` gains no hunk, and the fan-out chapter's fences",
" * and `fanout.ts` the second, and chapter 3.18 paid for that collision once:": " * and `fanout.ts` the second, and the fan-out chapter paid for that collision once:",
" * the event spine, chapter 3.19 followed it for presence and 3.20 for": " * the event spine, the presence chapter followed it for presence and the membership-revocation chapter for",
" * NOT `subjectFor`, WHICH `internal.ts` ALREADY EXPORTS. Chapter 3.18 paid for": " * NOT `subjectFor`, WHICH `internal.ts` ALREADY EXPORTS. The fan-out chapter paid for",
" * WHY ONE SHAPE AND NOT TWO. Chapter 3.20 needed a second, principal-addressed": " * WHY ONE SHAPE AND NOT TWO. The membership-revocation chapter needed a second, principal-addressed",
" * `environment` IS ON THE FABRIC AND NOT ON THE WIRE, as chapter 3.20's is and": " * `environment` IS ON THE FABRIC AND NOT ON THE WIRE, as the membership-revocation chapter's is and",
"// files \u2014 the same shape chapter 3.8 used to prove close code 4008 was emitted by": "// files \u2014 the same shape the rate-limit chapter used to prove close code 4008 was emitted by",
"// source. (Chapter 3.11 inverted that test: 4008 is emitted now, and the pattern": "// source. (the connection-metering chapter inverted that test: 4008 is emitted now, and the pattern",
"   * NINE GUARDED TABLES AS OF CHAPTER 3.12, not five: `sentinel.sql` gained": "   * NINE GUARDED TABLES AS OF the isolation gauntlet, not five: `sentinel.sql` gained",
"// drives all four rather than asserting the array's length. Chapter 3.10's SC-008": "// drives all four rather than asserting the array's length. The quota chapter's SC-008",
"    // `to_jsonb(OLD)::text` \u2014 the expression chapter 3.13 installed for the three": "    // `to_jsonb(OLD)::text` \u2014 the expression the channel-endpoints chapter installed for the three",
"    // against text that must match. Chapter 3.8's 4008 test is the precedent.": "    // against text that must match. The rate-limit chapter's 4008 test is the precedent.",
"// webhook deliveries left pending by runs that ended. Chapter 3.24's close-out needed": "// webhook deliveries left pending by runs that ended. The attachments chapter's close-out needed",
"  /** Chapter 3.12's usage-table bait. One row each, and the ids are derived like": "  /** The isolation gauntlet's usage-table bait. One row each, and the ids are derived like",
"  /** Chapter 3.16's read-position bait. `read_positions` references both a channel": "  /** The user-surface chapter's read-position bait. `read_positions` references both a channel",
"  // Chapter 3.12 added these four to the trigger array, and being in the array is": "  // The isolation gauntlet added these four to the trigger array, and being in the array is",
"    // Chapter 3.2 put authentication HERE rather than in a guard because Nest": "    // The credentials chapter put authentication HERE rather than in a guard because Nest",
" * ONE SECRET PER SERVICE, AND CHAPTER 3.11 IS WHY. Until this chapter there was": " * ONE SECRET PER SERVICE, AND THE CONNECTION-METERING CHAPTER IS WHY. Until this chapter there was",
" * header, trusted only for logging. Chapter 3.2 spent itself removing exactly": " * header, trusted only for logging. The credentials chapter spent itself removing exactly",
" * that must now decide about it stops compiling \u2014 which is chapter 3.11's lesson": " * that must now decide about it stops compiling \u2014 which is the connection-metering chapter's lesson",
"/** What a route accepts (research R6, narrowed by chapter 3.12's FR-044).": "/** What a route accepts (research R6, narrowed by the isolation gauntlet's FR-044).",
" * dispatcher does not. Chapter 3.11 gave each its own secret and stopped there, so": " * dispatcher does not. The connection-metering chapter gave each its own secret and stopped there, so",
" * `AcceptSpec` broke this and nothing in an earlier draft of chapter 3.12 fixed it:": " * `AcceptSpec` broke this and nothing in an earlier draft of the isolation gauntlet fixed it:",
"    // answered \u2014 a fact chapter 3.11 recorded as being \"for logs\", which is where": "    // answered \u2014 a fact the connection-metering chapter recorded as being \"for logs\", which is where",
"// Chapter 3.8 added `request_id` to every error body (constitution V's fourth": "// The rate-limit chapter added `request_id` to every error body (constitution V's fourth",
"    // tests. Chapter 3.6's `RELAY_DISABLE_SWEEP` states the rule: a flag whose": "    // tests. The retry-and-disable chapter's `RELAY_DISABLE_SWEEP` states the rule: a flag whose",
"    // Latent since chapter 3.1 and found by chapter 3.11's twenty-run battery on": "    // Latent since the tenancy chapter and found by the connection-metering chapter's twenty-run battery on",
"  // nowhere \u2014 here the unknown case SUCCEEDS, because chapter 3.16 made the mint create": "  // nowhere \u2014 here the unknown case SUCCEEDS, because the user-surface chapter made the mint create",
"    // Found by chapter 3.6's baseline, which ran the lane three times.": "    // Found by the retry-and-disable chapter's baseline, which ran the lane three times.",
"    // unreachable \u2014 but chapter 3.5 added a third principal kind that carries no": "    // unreachable \u2014 but the webhook dispatcher chapter added a third principal kind that carries no",
"    // **200 with a token**, because chapter 3.16 made the mint create the row. So there": "    // **200 with a token**, because the user-surface chapter made the mint create the row. So there",
"  /** For last_used_at, and for 3.6's quota accounting. */": "  /** For last_used_at, and for the retry-and-disable chapter's quota accounting. */",
"// this chapter is not making \u2014 chapter 3.15 owns the user-facing surface.": "// this chapter is not making \u2014 the channel-control chapter owns the user-facing surface.",
" * Declared rather than imported, which is chapter 3.4's decision in": " * Declared rather than imported, which is the broker chapter's decision in",
"   * 3.12's FR-044 hole exactly: a credential mismatch that passed for a whole": "   * the isolation gauntlet's FR-044 hole exactly: a credential mismatch that passed for a whole",
"    // CHAPTER 3.12 ASSERTED THE OPPOSITE HERE, and it was right at the time.": "    // The isolation gauntlet ASSERTED THE OPPOSITE HERE, and it was right at the time.",
"      // T047. The ceiling is chapter 3.13's and it is READ here, not reimplemented:": "      // T047. The ceiling is the channel-endpoints chapter's and it is READ here, not reimplemented:",
"      // Without it this would be a 403 for every USER instead \u2014 chapter 3.12's": "      // Without it this would be a 403 for every USER instead \u2014 the isolation gauntlet's",
"  // per user \u2014 chapter 3.13's add shape in both halves. The contract specified a": "  // per user \u2014 the channel-endpoints chapter's add shape in both halves. The contract specified a",
"  // `(channel_id, user_id, joined_at)` the whole time. Chapter 3.12's traceability": "  // `(channel_id, user_id, joined_at)` the whole time. The isolation gauntlet's traceability",
"  // Chapter 3.12 pinned this enum to `public` alone with the sharpest edit in that": "  // The isolation gauntlet pinned this enum to `public` alone with the sharpest edit in that",
"  // the guarantee before keeping it \u2014 which is the mistake chapter 3.12's fifth": "  // the guarantee before keeping it \u2014 which is the mistake the isolation gauntlet's fifth",
" * A UNION RATHER THAN A NEW SHAPE, because chapter 3.13 shipped": " * A UNION RATHER THAN A NEW SHAPE, because the channel-endpoints chapter shipped",
"   * not exist \u2014 SC-002, over the same oracle chapter 3.12 built for cross-tenant": "   * not exist \u2014 SC-002, over the same oracle the isolation gauntlet built for cross-tenant",
"   * THE CEILING IS READ, NOT REIMPLEMENTED. Chapter 3.13 counts members from": "   * THE CEILING IS READ, NOT REIMPLEMENTED. The channel-endpoints chapter counts members from",
" * Found by chapter 3.6's baseline.": " * Found by the retry-and-disable chapter's baseline.",
"    // filter, which is instance 3 exactly \u2014 the fault chapter 3.7 fixed forty lines": "    // filter, which is instance 3 exactly \u2014 the fault the deduplication chapter fixed forty lines",
"    // Found on run 11 of chapter 3.7's twenty post-fix lane runs: `expected": "    // Found on run 11 of the deduplication chapter's twenty post-fix lane runs: `expected",
"// So it sits here, exactly as chapter 3.3's outbox relay does under ADR-06's": "// So it sits here, exactly as the outbox chapter's outbox relay does under ADR-06's",
"// deployment\". What that costs is named rather than discovered: chapter 3.5's": "// deployment\". What that costs is named rather than discovered: the webhook dispatcher chapter's",
"/** On by default: an event spine nobody reads is what chapter 3.3 left behind.": "/** On by default: an event spine nobody reads is what the outbox chapter left behind.",
"// stealing 3.5's subject or inventing product nobody asked for, and Principle": "// stealing the webhook dispatcher chapter's subject or inventing product nobody asked for, and Principle",
"// RETIREMENT: chapter 3.5 replaces this with the webhook dispatcher, which is a": "// RETIREMENT: the webhook dispatcher chapter replaces this with the webhook dispatcher, which is a",
"    // same consumer that crashed after committing, which is chapter 3.4's whole": "    // same consumer that crashed after committing, which is the broker chapter's whole",
"    // one, and chapter 3.4's recorder puts it in the log line.": "    // one, and the broker chapter's recorder puts it in the log line.",
" * is FR-WHK-04's, in chapter 3.5. */": " * is FR-WHK-04's, in the webhook dispatcher chapter. */",
"   * what the recorder needs; a narrower filter is how chapter 3.5's dispatcher": "   * what the recorder needs; a narrower filter is how the webhook dispatcher chapter's dispatcher",
"// seven behind**, measured when they were deleted. Chapter 3.23 recorded them as six": "// seven behind**, measured when they were deleted. The revisions chapter recorded them as six",
"// behind and chapter 3.24 as seven. A generator whose snapshot is seven migrations stale": "// behind and the attachments chapter as seven. A generator whose snapshot is seven migrations stale",
"    // what chapter 3.13 changed here: the endpoint over this call has to be": "    // what the channel-endpoints chapter changed here: the endpoint over this call has to be",
"    // THE OBSERVATION CHANGED IN CHAPTER 3.12 AND THE PROPERTY DID NOT. This": "    // THE OBSERVATION CHANGED IN THE ISOLATION GAUNTLET AND THE PROPERTY DID NOT. This",
"// on it. Chapter 3.12's fifth analysis pass caught `POST /v1/channels` about to": "// on it. The isolation gauntlet's fifth analysis pass caught `POST /v1/channels` about to",
"    // which is what chapter 3.12's fourteen passing tests turned out to be": "    // which is what the isolation gauntlet's fourteen passing tests turned out to be",
"// necessity. These tests were written in chapter 3.15 against a state the platform could": "// necessity. These tests were written in the channel-control chapter against a state the platform could",
"// a written one are two different subjects, and 3.23's own `deleteMessage` tests assert": "// a written one are two different subjects, and the revisions chapter's own `deleteMessage` tests assert",
"  // maps the rows through unmodified. So the repair path chapter 3.23's resume": "  // maps the rows through unmodified. So the repair path the revisions chapter's resume",
"  // Chapter 3.15 wrote the same test for the channel LISTING and said why: *\"so the": "  // The channel-control chapter wrote the same test for the channel LISTING and said why: *\"so the",
"    // passes with half its subject applied**, which is chapter 3.17's T047c in a": "    // passes with half its subject applied**, which is the sender chapter's T047c in a",
"    // covered. Chapter 3.12 found three instruments that had never produced output for": "    // covered. The isolation gauntlet found three instruments that had never produced output for",
"    // is inserted directly, the way chapter 3.12's read-position clamp is planted a few": "    // is inserted directly, the way the isolation gauntlet's read-position clamp is planted a few",
"    // Chapter 3.16 wrote this arm for a state the public route produced on every": "    // The user-surface chapter wrote this arm for a state the public route produced on every",
"    // PLATFORM writes. Chapter 3.15's suite plants one by hand a few describes above,": "    // PLATFORM writes. The channel-control chapter's suite plants one by hand a few describes above,",
"    // question \u2014 chapter 3.23's `gaps.md` item 2 draws that line.": "    // question \u2014 the revisions chapter's `gaps.md` item 2 draws that line.",
"    // Chapter 3.15 decided this against a planted tombstone and stated the": "    // The channel-control chapter decided this against a planted tombstone and stated the",
"// a null, so chapter 3.23's tombstone predicate is untouched. That decision rests on a": "// a null, so the revisions chapter's tombstone predicate is untouched. That decision rests on a",
"// Chapter 3.23 ran the equivalent and it paid twice: it proved the read path was already": "// The revisions chapter ran the equivalent and it paid twice: it proved the read path was already",
"    // The listing reads `channels.last_sequence`, not the messages table (chapter 3.15's": "    // The listing reads `channels.last_sequence`, not the messages table (the channel-control chapter's",
"    // make chapter 3.23's tombstone predicate fire on a message somebody just sent.": "    // make the revisions chapter's tombstone predicate fire on a message somebody just sent.",
"// Chapter 3.23 left four sentences that had stopped being true because nothing compared": "// The revisions chapter left four sentences that had stopped being true because nothing compared",
"// `gaps.md` 3.23-3 has carried this since chapter 3.23 built both writes. Neither takes": "// `gaps.md` the revisions chapter-3 has carried this since the revisions chapter built both writes. Neither takes",
"// DO NOT START FROM `Promise.all` ON ONE CLIENT. Chapter 3.22 spent a phase learning": "// DO NOT START FROM `Promise.all` ON ONE CLIENT. The connection-cap chapter spent a phase learning",
"//   tenant-scoped. As of chapter 3.1 they build the whole container stack:": "//   tenant-scoped. As of the tenancy chapter they build the whole container stack:",
"  // Chapter 3.1 added the organisation above the application, and this": "  // The tenancy chapter added the organisation above the application, and this",
" * private methods on `Repository` stay as one-line delegations, so chapter 3.10's": " * private methods on `Repository` stay as one-line delegations, so the quota chapter's",
" * request rather than a heavier version of the first\", because chapter 3.10's H2": " * request rather than a heavier version of the first\", because the quota chapter's H2",
" * one chapter 3.10's T033 already recorded \u2014 an extra round trip holds a pooled": " * one the quota chapter's T033 already recorded \u2014 an extra round trip holds a pooled",
" * THE LOCK CHAPTER 3.10 WANTED AND COULD NOT HAVE. Crediting is read-then-write,": " * THE LOCK THE QUOTA CHAPTER WANTED AND COULD NOT HAVE. Crediting is read-then-write,",
" * chapter 3.8's nullable limit columns encode, and the reason `capsFor` returns": " * The rate-limit chapter's nullable limit columns encode, and the reason `capsFor` returns",
"      // Chapter 3.11's figure, read from the ROLL-UP and never summed over": "      // The connection-metering chapter's figure, read from the ROLL-UP and never summed over",
"      // connections for the month, which is chapter 3.10's R1 argument in a new": "      // connections for the month, which is the quota chapter's R1 argument in a new",
" * deliveries and 3.9's disablement emails. Same claim predicate, same": " * deliveries and the mail-transport chapter's disablement emails. Same claim predicate, same",
" * sit on 3.9's version is not repeated here: it discarded a row's failure with no": " * sit on the mail-transport chapter's version is not repeated here: it discarded a row's failure with no",
"// THIRD TIME \u2014 after chapter 3.3's events and chapter 3.5's deliveries \u2014 and": "// THIRD TIME \u2014 after the outbox chapter's events and the webhook dispatcher chapter's deliveries \u2014 and",
"// this one needed no migration at all: chapter 3.6 gave the table a": "// this one needed no migration at all: the retry-and-disable chapter gave the table a",
" * AN EXPLICIT LIMIT, no default. Chapter 3.7's baseline found four suites broken": " * AN EXPLICIT LIMIT, no default. The deduplication chapter's baseline found four suites broken",
" * Resolved from the row's `organisation_id`, which chapter 3.6 denormalised onto": " * Resolved from the row's `organisation_id`, which the retry-and-disable chapter denormalised onto",
" * run it (chapter 3.4's data model). */": " * run it (the broker chapter's data model). */",
" * The claim is chapter 3.4's, unchanged, and it is doing more work here than it": " * The claim is the broker chapter's, unchanged, and it is doing more work here than it",
"     * every attempt since chapter 3.5 and discarded until 3.6 wanted it (research": "     * every attempt since the webhook dispatcher chapter and discarded until the retry-and-disable chapter wanted it (research",
" * this is not all of them. Against chapter 3.5's tier table, one failing delivery": " * this is not all of them. Against the webhook dispatcher chapter's tier table, one failing delivery",
"    // `FOR UPDATE OF e SKIP LOCKED` is chapter 3.3's pattern and here it does two": "    // `FOR UPDATE OF e SKIP LOCKED` is the outbox chapter's pattern and here it does two",
"/** Claim the deliveries that are due and hand each to `publish` \u2014 chapter 3.3's": "/** Claim the deliveries that are due and hand each to `publish` \u2014 the outbox chapter's",
"      // In the `finally` for 3.3's reason: whatever went wrong with row N+1,": "      // In the `finally` for the outbox chapter's reason: whatever went wrong with row N+1,",
" * This is the shape chapter 3.3 used for the outbox row and the message it": " * This is the shape the outbox chapter used for the outbox row and the message it",
" * idempotency keys, 3.1's on signup).": " * idempotency keys, the tenancy chapter's on signup).",
" * **The limit of this, stated because chapter 3.5 will meet it**: the effect has": " * **The limit of this, stated because the webhook dispatcher chapter will meet it**: the effect has",
"   * chapter 2.1, and chapter 3.15 gave it its first DECISION. Before that it was": "   * chapter 2.1, and the channel-control chapter gave it its first DECISION. Before that it was",
"   * convenience is exactly what made chapter 3.24's `internalSendResponseSchema` a break": "   * convenience is exactly what made the attachments chapter's `internalSendResponseSchema` a break",
"   * first. Chapter 3.3's finding 3, in its third chapter. */": "   * first. The outbox chapter's finding 3, in its third chapter. */",
"   * Added in chapter 3.6 for the test event, which needs an UNSCOPED operation \u2014": "   * Added in the retry-and-disable chapter for the test event, which needs an UNSCOPED operation \u2014",
"   * `environment_id` where 3.3's outbox and 3.4's ledger did not. */": "   * `environment_id` where the outbox chapter's outbox and the broker chapter's ledger did not. */",
"          // never hands back null \u2014 and chapter 3.12 removed `addMember`'s": "          // never hands back null \u2014 and the isolation gauntlet removed `addMember`'s",
"   * Chapter 3.18's Redis publish is legal because `sendMessage` already wrote the": "   * The fan-out chapter's Redis publish is legal because `sendMessage` already wrote the",
"   * 3.13's `addMembers` shape in both halves. `contracts/membership.md` specified a": "   * the channel-endpoints chapter's `addMembers` shape in both halves. `contracts/membership.md` specified a",
"   * read \"the shape chapter 3.13 chose\" as *named outcomes* and dropped *bulk*.": "   * read \"the shape the channel-endpoints chapter chose\" as *named outcomes* and dropped *bulk*.",
"  /** How many deliveries an endpoint holds, scoped. Added for chapter 3.12's": "  /** How many deliveries an endpoint holds, scoped. Added for the isolation gauntlet's",
"   * `status` REPORTS WHICH HAPPENED, per entry, in the shape chapter 3.13 chose for": "   * `status` REPORTS WHICH HAPPENED, per entry, in the shape the channel-endpoints chapter chose for",
"    // that class from chapter 3.12 and its lines ratchet sat at 99; a third took the file": "    // that class from the isolation gauntlet and its lines ratchet sat at 99; a third took the file",
"    // rather than covering it (3.12's `addMember`, 3.16's `upsertUser`).": "    // rather than covering it (the isolation gauntlet's `addMember`, the user-surface chapter's `upsertUser`).",
"      // stamps the marker. It has **no production caller**: chapter 3.16 added it so the": "      // stamps the marker. It has **no production caller**: the user-surface chapter added it so the",
"      // every `users` row since chapter 3.15 and this check has never run for a bot": "      // every `users` row since the channel-control chapter and this check has never run for a bot",
"      // And that gate is only honest because chapter 3.15 made the public route": "      // And that gate is only honest because the channel-control chapter made the public route",
"      // back to `EITHER` and a user token was accepted there. Chapter 3.17 declared it;": "      // back to `EITHER` and a user token was accepted there. The sender chapter declared it;",
"      // exactly what chapter 3.12's indistinguishability oracle was built to": "      // exactly what the isolation gauntlet's indistinguishability oracle was built to",
"      // user, so it skipped the membership check entirely. That is chapter 3.15's": "      // user, so it skipped the membership check entirely. That is the channel-control chapter's",
"      // by design cannot say why. A capability chapter 3.15 delivered would have": "      // by design cannot say why. A capability the channel-control chapter delivered would have",
"      // refusing, which is the defect chapter 3.12's fifth analysis pass caught one": "      // refusing, which is the defect the isolation gauntlet's fifth analysis pass caught one",
"      // purpose: `users.banned_at` has no reader until chapter 3.16 gives it one at": "      // purpose: `users.banned_at` has no reader until the user-surface chapter gives it one at",
"      // Here rather than in middleware, because chapter 3.8's limiter never sees": "      // Here rather than in middleware, because the rate-limit chapter's limiter never sees",
"      // per-minute counter store chapter 3.8 built \u2014 a flush there costs one": "      // per-minute counter store the rate-limit chapter built \u2014 a flush there costs one",
"      // carried no `userId`, which chapter 3.3 decided and FR-MSG-15 reverses.": "      // carried no `userId`, which the outbox chapter decided and FR-MSG-15 reverses.",
"   * feature with a retention policy rather than a line in this method. Chapter 3.23's": "   * feature with a retention policy rather than a line in this method. The revisions chapter's",
"      // read path already branches on. Chapter 3.15's planted tombstone sets both.": "      // read path already branches on. The channel-control chapter's planted tombstone sets both.",
"       * the platform as many answers as it has callers \u2014 and chapter 3.23 shipped a": "       * the platform as many answers as it has callers \u2014 and the revisions chapter shipped a",
"    // and look correct in review. Chapter 3.15's research found that before writing it;": "    // and look correct in review. The channel-control chapter's research found that before writing it;",
"    // silently. Chapter 3.7's rule \u2014 cite what a thing is, never where it will": "    // silently. The deduplication chapter's rule \u2014 cite what a thing is, never where it will",
"    // Every reader built since chapter 3.15 reads `users`; a `bots` table would have": "    // Every reader built since the channel-control chapter reads `users`; a `bots` table would have",
"    // rows are not rewritten \u2014 chapter 3.16 measured that for `last_activity_at`.": "    // rows are not rewritten \u2014 the user-surface chapter measured that for `last_activity_at`.",
"    // THE CONSTRAINED TEXT COLUMNS IN THIS SCHEMA NAME EACH OTHER (chapter 3.15's": "    // THE CONSTRAINED TEXT COLUMNS IN THIS SCHEMA NAME EACH OTHER (THE CHANNEL-CONTROL CHAPTER'S",
"    // The default is what lets chapter 3.13's `addMember` keep working unchanged and": "    // The default is what lets the channel-endpoints chapter's `addMember` keep working unchanged and",
"// key, and chapter 3.13 installed": "// key, and the channel-endpoints chapter installed",
"    // than an oversight (chapter 3.16's `gaps.md` \u00a75).": "    // than an oversight (the user-surface chapter's `gaps.md` \u00a75).",
"// tenant's behalf. Same family of exception as 3.2's unscoped key lookup, and": "// tenant's behalf. Same family of exception as the credentials chapter's unscoped key lookup, and",
"// own bookkeeping rather than tenant data (constitution I, 3.3's data model).": "// own bookkeeping rather than tenant data (constitution I, the outbox chapter's data model).",
"    // An owner can pause an endpoint. Chapter 3.6 is the follow-on chapter 3.5": "    // An owner can pause an endpoint. The retry-and-disable chapter is the follow-on the webhook dispatcher chapter",
"    // DELETION IS SOFT, and this column is why. Chapter 3.2 made the same": "    // DELETION IS SOFT, and this column is why. The credentials chapter made the same",
"    // 3.3's outbox index and 3.5's delivery index are partial \u2014 a healthy": "    // The outbox chapter's outbox index and the webhook dispatcher chapter's delivery index are partial \u2014 a healthy",
"// The retry schedule \u2014 and it is chapter 3.3's outbox with one more column.": "// The retry schedule \u2014 and it is the outbox chapter's outbox with one more column.",
"    // Chapter 3.3's envelope id \u2014 the customer's deduplication key, stable": "    // The outbox chapter's envelope id \u2014 the customer's deduplication key, stable",
"    // Chapter 3.5 recorded an attempt by MOVING the delivery \u2014 state, attempt,": "    // The webhook dispatcher chapter recorded an attempt by MOVING the delivery \u2014 state, attempt,",
"    // for the reason 3.3's outbox index is partial \u2014 it covers only what the": "    // for the reason the outbox chapter's outbox index is partial \u2014 it covers only what the",
"    // Chapter 3.11's third figure, same type for the same reason. Ten thousand": "    // The connection-metering chapter's third figure, same type for the same reason. Ten thousand",
"// One row per connection per period, and the whole of chapter 3.11's": "// One row per connection per period, and the whole of the connection-metering chapter's",
"// THE OUTBOX, A FOURTH TIME \u2014 after 3.3's events, 3.5's deliveries and 3.9's": "// THE OUTBOX, A FOURTH TIME \u2014 after the outbox chapter's events, the webhook dispatcher chapter's deliveries and the mail-transport chapter's",
"    // both halves of the pair and the comparison would see nothing (3.17's T044).": "    // both halves of the pair and the comparison would see nothing (the sender chapter's T044).",
" * the first draft of chapter 3.18's contract copied the options without it. */": " * the first draft of the fan-out chapter's contract copied the options without it. */",
" *     chapter 3.23 and they do get `message.deleted` \u2014 **and resume does NOT carry": " *     The revisions chapter and they do get `message.deleted` \u2014 **and resume does NOT carry",
"// workaround, it is the reason chapter 3.4's claim-and-effect-in-one-transaction": "// workaround, it is the reason the broker chapter's claim-and-effect-in-one-transaction",
"  // Chapter 3.2 replaced `GET /internal/memberships` with": "  // The credentials chapter replaced `GET /internal/memberships` with",
"  // `repository.sendMessage` covers both doors. Chapter 3.12 recorded this route": "  // `repository.sendMessage` covers both doors. The isolation gauntlet recorded this route",
"// Chapter 3.5 adds the dispatch controller, which needs an UNSCOPED connection": "// The webhook dispatcher chapter adds the dispatch controller, which needs an UNSCOPED connection",
"    // 3.10's third analysis pass found exactly that.": "    // the quota chapter's third analysis pass found exactly that.",
"    // nobody registers is a route that does not exist, and chapter 3.10's third": "    // nobody registers is a route that does not exist, and the quota chapter's third",
"// `packages/protocol/src/internal.ts` since chapter 3.2 and parsed by nothing. The": "// `packages/protocol/src/internal.ts` since the credentials chapter and parsed by nothing. The",
"    // rather than a heavier `environmentLimits`, because chapter 3.10's H2 had": "    // rather than a heavier `environmentLimits`, because the quota chapter's H2 had",
"    // refusal would reach the gateway as `internal_error` \u2014 chapter 3.10's H3.": "    // refusal would reach the gateway as `internal_error` \u2014 the quota chapter's H3.",
"// COMMITTED RATHER THAN AD-HOC, because chapter 3.10's T033 measured with a": "// COMMITTED RATHER THAN AD-HOC, because the quota chapter's T033 measured with a",
"    // scan, which is the correction chapter 3.10's first analysis pass had to": "    // scan, which is the correction the quota chapter's first analysis pass had to",
"// environment. That is what a platform credential is; chapter 3.5 argued it, and": "// environment. That is what a platform credential is; the webhook dispatcher chapter argued it, and",
"// Chapter 3.5 added the first platform-credentialled routes and left no": "// The webhook dispatcher chapter added the first platform-credentialled routes and left no",
"// THE GATEWAY'S CREDENTIAL, and chapter 3.12 is why the variable changed.": "// THE GATEWAY'S CREDENTIAL, and the isolation gauntlet is why the variable changed.",
" * during chapter 3.21's close-out, in a file that chapter never touched. The": " * during the typing chapter's close-out, in a file that chapter never touched. The",
"  // never have had. Chapter 3.11 argued for two secrets on exactly this ground": "  // never have had. The connection-metering chapter argued for two secrets on exactly this ground",
"      // as `internal_error` \u2014 chapter 3.10's H3.": "      // as `internal_error` \u2014 the quota chapter's H3.",
" * driven with made-up answers instead (chapter 3.12's Phase 7 argument, one layer": " * driven with made-up answers instead (the isolation gauntlet's Phase 7 argument, one layer",
" * as an absent one, chapter 3.8 added `request_id` to every error body and forced": " * as an absent one, the rate-limit chapter added `request_id` to every error body and forced",
" * Chapter 3.8 added `request_id` to every error body (constitution V's fourth": " * The rate-limit chapter added `request_id` to every error body (constitution V's fourth",
"    // module: it has never needed to, and the two shapes chapter 3.15 adds are the": "    // module: it has never needed to, and the two shapes the channel-control chapter adds are the",
"      // THE FIFTH VERB (SC-001a, chapter 3.15's T121a). Its route is built in the": "      // THE FIFTH VERB (SC-001a, the channel-control chapter's T121a). Its route is built in the",
"        // and says nothing on its own \u2014 chapter 3.12's oracle exists because of it.": "        // and says nothing on its own \u2014 the isolation gauntlet's oracle exists because of it.",
"      // Chapter 3.12's fourteen green tests compared two refusals and meant nothing,": "      // The isolation gauntlet's fourteen green tests compared two refusals and meant nothing,",
"  // Chapter 3.12 closed at **24** derived targets, recorded in": "  // The isolation gauntlet closed at **24** derived targets, recorded in",
"  // chapter 3.20 revives `GET /internal/memberships` for the membership backstop": "  // The membership-revocation chapter revives `GET /internal/memberships` for the membership backstop",
"  // **THIS TEST DID ITS JOB AGAIN.** Chapter 3.20 added its route, ran this file,": "  // **THIS TEST DID ITS JOB AGAIN.** The membership-revocation chapter added its route, ran this file,",
"  it(\"has grown from chapter 3.12's 24 by exactly the routes since\", () => {": "  it(\"has grown from the isolation gauntlet's 24 by exactly the routes since\", () => {",
"/** And `\"either\"`, added by chapter 3.15 for the first route that genuinely takes both": "/** And `\"either\"`, added by the channel-control chapter for the first route that genuinely takes both",
"      \"the OAuth return. Its authority is a state cookie bound to the browser that began the flow, and chapter 3.1's suite already attacks that binding directly.\",": "      \"the OAuth return. Its authority is a state cookie bound to the browser that began the flow, and the tenancy chapter's suite already attacks that binding directly.\",",
"  // CHAPTER 3.23's EDIT HISTORY (T033h, FR-023, FR-023a). `accepts: \"application\"`": "  // The revisions chapter's EDIT HISTORY (T033h, FR-023, FR-023a). `accepts: \"application\"`",
"  // are the same authorisation fact written twice; chapter 3.23's `gaps.md` item 4 owns": "  // are the same authorisation fact written twice; the revisions chapter's `gaps.md` item 4 owns",
"  // CHAPTER 3.23's EDIT (T030a, FR-001, FR-013a). `accepts: \"user\"` because the method": "  // The revisions chapter's EDIT (T030a, FR-001, FR-013a). `accepts: \"user\"` because the method",
"  // CHAPTER 3.23's DELETION (T041a, FR-006, FR-012, FR-013). `accepts: \"either\"` \u2014 an": "  // The revisions chapter's DELETION (T041a, FR-006, FR-012, FR-013). `accepts: \"either\"` \u2014 an",
"  // Chapter 3.12's two new routes, and the order they were added in is the point.": "  // The isolation gauntlet's two new routes, and the order they were added in is the point.",
"  // Chapter 3.15's two, and the derivation found them the same way it found the": "  // The channel-control chapter's two, and the derivation found them the same way it found the",
"    // CHAPTER 3.23 MADE THE REACH TRANSITIVE and this assertion is why the": "    // The revisions chapter MADE THE REACH TRANSITIVE and this assertion is why the",
"// That matters more than it sounds \u2014 chapter 3.7 spent a baseline on four suites": "// That matters more than it sounds \u2014 the deduplication chapter spent a baseline on four suites",
"// a header the caller can forge \u2014 the exact pattern chapter 3.2 removed when it": "// a header the caller can forge \u2014 the exact pattern the credentials chapter removed when it",
"    // whatever the database happens to be at. Chapter 3.8's `0008` adds the": "    // whatever the database happens to be at. The rate-limit chapter's `0008` adds the",
"    // chapter 3.5's retry schedule was built to avoid.": "    // The webhook dispatcher chapter's retry schedule was built to avoid.",
"    // chapter 3.2 removed exactly that pattern.": "    // The credentials chapter removed exactly that pattern.",
" * THE DEFAULT ENFORCES. Chapter 3.6's `RELAY_DISABLE_SWEEP` states the rule: a": " * THE DEFAULT ENFORCES. The retry-and-disable chapter's `RELAY_DISABLE_SWEEP` states the rule: a",
"// MIDDLEWARE, NOT A GUARD, for two reasons. Chapter 3.2's: Nest constructs": "// MIDDLEWARE, NOT A GUARD, for two reasons. The credentials chapter's: Nest constructs",
"// cannot key off the principal, and CHAPTER 3.11 STRENGTHENED THAT RATHER THAN": "// cannot key off the principal, and the connection-metering chapter STRENGTHENED THAT RATHER THAN",
"// one: chapter 3.11 gave it `RELAY_INTERNAL_CREDENTIAL_GATEWAY`, and the": "// one: the connection-metering chapter gave it `RELAY_INTERNAL_CREDENTIAL_GATEWAY`, and the",
"// accumulates. Chapter 3.7's baseline and this chapter's own both found suites": "// accumulates. The deduplication chapter's baseline and this chapter's own both found suites",
"  // publishing deliveries that have become due. Started here for 3.3's reason \u2014": "  // publishing deliveries that have become due. Started here for the outbox chapter's reason \u2014",
"  // object. Chapter 3.19's first version pushed the raw line, so every `msg` was": "  // object. The presence chapter's first version pushed the raw line, so every `msg` was",
"    // Chapter 3.18's test by name. The listener's stated reason is NFR-OBS-01 \u2014": "    // The fan-out chapter's test by name. The listener's stated reason is NFR-OBS-01 \u2014",
"    // this arm reads zero without one. Chapter 3.19's identical case measured [15, 0]": "    // this arm reads zero without one. The presence chapter's identical case measured [15, 0]",
"// SHAPED ON `fanout/publisher.ts` AND NOT ON `createFanout`. Chapter 3.18 built that": "// SHAPED ON `fanout/publisher.ts` AND NOT ON `createFanout`. The fan-out chapter built that",
"    // Chapter 3.18's three, for its reasons: a queued command rejects as soon as the": "    // The fan-out chapter's three, for its reasons: a queued command rejects as soon as the",
"  // missing listener kills the api; chapter 3.18 measured that against ioredis 6.0.0": "  // missing listener kills the api; the fan-out chapter measured that against ioredis 6.0.0",
"        // AND THE LOG LINE IS THE REQUIREMENT'S EVIDENCE (FR-015). Chapter 3.18's": "        // AND THE LOG LINE IS THE REQUIREMENT'S EVIDENCE (FR-015). The fan-out chapter's",
"    // THE CASE CHAPTER 3.18 GUARDED FOR TEXT, NOW WITH AN ATTACHMENT LIST. A message is": "    // THE CASE THE FAN-OUT CHAPTER GUARDED FOR TEXT, NOW WITH AN ATTACHMENT LIST. A message is",
"// Chapter 3.2 swapped the guard. `EnvironmentContextGuard` resolved a tenant": "// The credentials chapter swapped the guard. `EnvironmentContextGuard` resolved a tenant",
"// FR-AUT-10 does not reserve these routes) \u2014 and chapter 3.17 made that a DECLARATION": "// FR-AUT-10 does not reserve these routes) \u2014 and the sender chapter made that a DECLARATION",
"// `POST /internal/dispatch/replay` in chapter 3.12.": "// `POST /internal/dispatch/replay` in the isolation gauntlet.",
"// `POST /internal/dispatch/replay` in chapter 3.12. Both classes are genuinely accepted": "// `POST /internal/dispatch/replay` in the isolation gauntlet. Both classes are genuinely accepted",
"   * is the thing `credential.guard.ts:31` argues about and chapter 3.12 paid for. So": "   * is the thing `credential.guard.ts:31` argues about and the isolation gauntlet paid for. So",
"// The comparison this suite invented, now shared. Chapter 3.12 moved it into": "// The comparison this suite invented, now shared. The isolation gauntlet moved it into",
"    // Chapter 3.15's fixtures: a private channel, one member, one stranger of the": "    // The channel-control chapter's fixtures: a private channel, one member, one stranger of the",
"    // A BOT OF THIS TENANT, and a person, for chapter 3.17's four outcomes. Added": "    // A BOT OF THIS TENANT, and a person, for the sender chapter's four outcomes. Added",
"      // chapter 3.23 made for not comparing message texts to decide whether an edit": "      // The revisions chapter made for not comparing message texts to decide whether an edit",
"      // 3.23's falsification proved this shape of test stays green when the predicate is": "      // The revisions chapter's falsification proved this shape of test stays green when the predicate is",
"      // the only case chapter 3.15's `channelVisibleTo` alone answers, per chapter 3.23's": "      // the only case the channel-control chapter's `channelVisibleTo` alone answers, per the revisions chapter's",
"      // BYTE-IDENTICAL BUT FOR `request_id`, the same strip chapter 3.12's oracle uses": "      // BYTE-IDENTICAL BUT FOR `request_id`, the same strip the isolation gauntlet's oracle uses",
"  // outright \u2014 which is the change that breaks chapter 3.15's refusal. The pair is the": "  // outright \u2014 which is the change that breaks the channel-control chapter's refusal. The pair is the",
"      // why chapter 3.12's oracle drops exactly this one and nothing else.": "      // why the isolation gauntlet's oracle drops exactly this one and nothing else.",
"      // before chapter 3.15 this send SUCCEEDED, unattributed \u2014 and an unattributed": "      // before the channel-control chapter this send SUCCEEDED, unattributed \u2014 and an unattributed",
"    // importing `drizzle-orm` outside `db/` \u2014 constitution I, and chapter 3.23's T069a": "    // importing `drizzle-orm` outside `db/` \u2014 constitution I, and the revisions chapter's T069a",
"    // path exposes (chapter 3.23's `gaps.md` item 2).": "    // path exposes (the revisions chapter's `gaps.md` item 2).",
"      // `message_edits` HAS THREE COLUMNS AND THE SAD PUBLISHES THREE. Chapter 3.23 built": "      // `message_edits` HAS THREE COLUMNS AND THE SAD PUBLISHES THREE. The revisions chapter built",
"        // Chapter 3.2 changed WHERE the environment comes from and nothing": "        // The credentials chapter changed WHERE the environment comes from and nothing",
"    /** `.min(1)` REMOVED in chapter 3.24 (FR-019), and the floor moved to the": "    /** `.min(1)` REMOVED in the attachments chapter (FR-019), and the floor moved to the",
"     * chapter 3.23's tombstone predicate \u2014 `text === null` \u2014 is untouched. */": "     * The revisions chapter's tombstone predicate \u2014 `text === null` \u2014 is untouched. */",
" * THE FLOORS DIVERGED IN CHAPTER 3.24 AND MUST STAY DIVERGED. This paragraph used to say": " * THE FLOORS DIVERGED IN THE ATTACHMENTS CHAPTER AND MUST STAY DIVERGED. This paragraph used to say",
" * became a 200 and chapter 3.23's own test caught it.": " * became a 200 and the revisions chapter's own test caught it.",
"   * Chapter 3.24 found this field defined as `sendMessageBodySchema.shape.text`, so": "   * The attachments chapter found this field defined as `sendMessageBodySchema.shape.text`, so",
"     * longer true**: chapter 3.17 made every message carry a sender (FR-MSG-15), and a key": "     * longer true**: the sender chapter made every message carry a sender (FR-MSG-15), and a key",
"     * IT IS NO LONGER OPTIONAL FOR A USER TOKEN. Chapter 3.15 made the public": "     * IT IS NO LONGER OPTIONAL FOR A USER TOKEN. The channel-control chapter made the public",
"      // chapter 3.12 named `wrong_credential_service` for the same reason. The filter": "      // The isolation gauntlet named `wrong_credential_service` for the same reason. The filter",
"        // `402`, NOT `429`. Chapter 3.8 owns `429`, and a client that sleeps for": "        // `402`, NOT `429`. The rate-limit chapter owns `429`, and a client that sleeps for",
"        // lie chapter 2.2 fixed for 400 and chapter 3.2 for 403, and 3.2's": "        // lie chapter 2.2 fixed for 400 and the credentials chapter for 403, and the credentials chapter's",
"   * is the predicate chapter 3.15 built after finding `channelExists` answering only half": "   * is the predicate the channel-control chapter built after finding `channelExists` answering only half",
"// THE OUTBOX A THIRD TIME, and deliberately the same shape as chapter 3.3's:": "// THE OUTBOX A THIRD TIME, and deliberately the same shape as the outbox chapter's:",
"// NOT ON THE REQUEST PATH, for chapter 3.3's reason. Disabling an endpoint": "// NOT ON THE REQUEST PATH, for the outbox chapter's reason. Disabling an endpoint",
"    // owner. Chapter 3.6 denormalised that column so this lookup could not": "    // owner. The retry-and-disable chapter denormalised that column so this lookup could not",
"// The transport chapter 3.6 deferred, against a real mail server (chapter 3.8,": "// The transport the retry-and-disable chapter deferred, against a real mail server (chapter 3.8,",
"   * 3.7's baseline found in four suites, and which this file walked into on its": "   * the deduplication chapter's baseline found in four suites, and which this file walked into on its",
"    // oldest-first \u2014 chapter 3.7's baseline found four suites broken by": "    // oldest-first \u2014 the deduplication chapter's baseline found four suites broken by",
"  it(\"drains chapter 3.6's backlog with NO SPECIAL HANDLING (FR-WHK-07)\", async () => {": "  it(\"drains the retry-and-disable chapter's backlog with NO SPECIAL HANDLING (FR-WHK-07)\", async () => {",
"  // ASSERTED AS A SET AND AS A COUNT, which is chapter 3.19's `codes.test.ts`": "  // ASSERTED AS A SET AND AS A COUNT, which is the presence chapter's `codes.test.ts`",
"    // chapter 3.24's plan expected one new error code and shipped two, and an exact-count": "    // The attachments chapter's plan expected one new error code and shipped two, and an exact-count",
"    // A LOOKUP RATHER THAN A TERNARY, because chapter 3.23 made the shapes three: a": "    // A LOOKUP RATHER THAN A TERNARY, because the revisions chapter made the shapes three: a",
"  // CHAPTER 3.23's TWO, spelled as FR-WHK-02 spells them because a customer's": "  // The revisions chapter's TWO, spelled as FR-WHK-02 spells them because a customer's",
" * chapter 3.19's `codes.test.ts` earned its keep by asserting an exact set and an exact": " * The presence chapter's `codes.test.ts` earned its keep by asserting an exact set and an exact",
"// The subject grammar moved to @relay/protocol in chapter 3.4, because a": "// The subject grammar moved to @relay/protocol in the broker chapter, because a",
"// below and re-exported so 3.3's callers keep working.": "// below and re-exported so the outbox chapter's callers keep working.",
"/** A DISCRIMINATED UNION, AND IT WAS A LITERAL UNTIL CHAPTER 3.20 RAN IT.": "/** A DISCRIMINATED UNION, AND IT WAS A LITERAL UNTIL THE MEMBERSHIP-REVOCATION CHAPTER RAN IT.",
"      // (chapter 3.17's senderless rows predate FR-MSG-15); a membership change": "      // (the sender chapter's senderless rows predate FR-MSG-15); a membership change",
" * Chapter 3.3 created this with a name, its subjects and file storage, and left": " * The outbox chapter created this with a name, its subjects and file storage, and left",
" * had chosen. Chapter 3.4 makes every setting a decision (research R2).": " * had chosen. The broker chapter makes every setting a decision (research R2).",
" * inversion chapter 3.3's outbox exists to prevent. */": " * inversion the outbox chapter's outbox exists to prevent. */",
" * Reuses the `ensure` parameter chapter 3.5 added rather than introducing a": " * Reuses the `ensure` parameter the webhook dispatcher chapter added rather than introducing a",
"   * Defaults to the EVENTS stream this chapter created. Chapter 3.5 passes its": "   * Defaults to the EVENTS stream this chapter created. The webhook dispatcher chapter passes its",
"    // The key-authenticated public send is unattributed (3.2's recorded bound),": "    // The key-authenticated public send is unattributed (the credentials chapter's recorded bound),",
"    // Chapter 3.2 widened this twice. A thrower may now NAME its code, because": "    // The credentials chapter widened this twice. A thrower may now NAME its code, because",
"    // FOUR FIELDS AS OF CHAPTER 3.8, and constitution V has asked for four since": "    // FOUR FIELDS AS OF the rate-limit chapter, and constitution V has asked for four since",
" * Chapter 3.2 introduced the convention that a thrower may name its code, because": " * The credentials chapter introduced the convention that a thrower may name its code, because",
"// applied. Chapter 3.10 called the parser \"the second gate rather than the only": "// applied. The quota chapter called the parser \"the second gate rather than the only",
"  it(\"still refuses everything chapter 3.10's clauses refused\", async () => {": "  it(\"still refuses everything the quota chapter's clauses refused\", async () => {",
"    // drops. Both of 3.10's dimensions are re-checked here for that reason.": "    // drops. Both of the quota chapter's dimensions are re-checked here for that reason.",
" * chapters. Chapter 3.8 was offered it for rate-limit policy and refused in": " * chapters. The rate-limit chapter was offered it for rate-limit policy and refused in",
" * WHY A PARSER AT ALL. Chapter 3.8's limits are typed columns and need no": " * WHY A PARSER AT ALL. The rate-limit chapter's limits are typed columns and need no",
"    // The property that separates a quota from chapter 3.8's limiter. Connection": "    // The property that separates a quota from the rate-limit chapter's limiter. Connection",
"// filters by a per-test recipient. Chapter 3.10's suite established that shape": "// filters by a per-test recipient. The quota chapter's suite established that shape",
"// name the function (chapter 3.11 added it) and could not see the call anyway,": "// name the function (the connection-metering chapter added it) and could not see the call anyway,",
"    // 80% of 4 is 3.2, which chapter 3.10 got wrong twice before writing it down.": "    // 80% of 4 is the credentials chapter, which the quota chapter got wrong twice before writing it down.",
" * database round trip to test anywhere else. Chapter 3.6 separated `disable.ts`": " * database round trip to test anywhere else. The retry-and-disable chapter separated `disable.ts`",
" * for the same reason and chapter 3.8 separated `bucket.ts`.": " * for the same reason and the rate-limit chapter separated `bucket.ts`.",
" * NO SECRET, NO KEY, NO MESSAGE TEXT. Chapter 3.9 established that this is": " * NO SECRET, NO KEY, NO MESSAGE TEXT. The mail-transport chapter established that this is",
"// deliveries and 3.9's disablement emails. Same shape on purpose: a table whose": "// deliveries and the mail-transport chapter's disablement emails. Same shape on purpose: a table whose",
"      // The same real branch chapter 3.9 met: `humans.email` is nullable, so an": "      // The same real branch the mail-transport chapter met: `humans.email` is nullable, so an",
"  it(\"keeps the other two dimensions exactly as chapter 3.10 shipped them\", () => {": "  it(\"keeps the other two dimensions exactly as the quota chapter shipped them\", () => {",
" * TABLES RATHER THAN A TERNARY, because chapter 3.11 is where the ternary broke.": " * TABLES RATHER THAN A TERNARY, because the connection-metering chapter is where the ternary broke.",
"// must not. Chapter 3.8's limiter keeps its counters in Redis, where a flush costs": "// must not. The rate-limit chapter's limiter keeps its counters in Redis, where a flush costs",
"    // The whole store, not this environment's keys. Chapter 3.8's counters and": "    // The whole store, not this environment's keys. The rate-limit chapter's counters and",
"    // Chapter 3.8's header bug was found by printing a response and not by any": "    // The rate-limit chapter's header bug was found by printing a response and not by any",
"// chapter 3.9 established: only a received message can prove an email carries no": "// The mail-transport chapter established: only a received message can prove an email carries no",
"    // says so out loud. Chapter 3.9 met the same hazard from the other side,": "    // says so out loud. The mail-transport chapter met the same hazard from the other side,",
"// codebase that no test enters \u2014 chapter 3.3's, 3.5's and 3.9's all report the": "// codebase that no test enters \u2014 the outbox chapter's, the webhook dispatcher chapter's and the mail-transport chapter's all report the",
"    // Chapter 3.18 needed this: the fan-out publish logs its failure from inside": "    // The fan-out chapter needed this: the fan-out publish logs its failure from inside",
"// credentials are 3.2's subject, and the dashboard that would consume one is": "// credentials are the credentials chapter's subject, and the dashboard that would consume one is",
"    // Chapter 3.8 limited account creation per source address (FR-AUT-12), and this": "    // The rate-limit chapter limited account creation per source address (FR-AUT-12), and this",
"    // REVISED by chapter 3.3: this used to count ALL organisations before and": "    // REVISED by the outbox chapter: this used to count ALL organisations before and",
"    // chapters and then failed with \"expected 884 to be 883\" the day 3.3's": "    // chapters and then failed with \"expected 884 to be 883\" the day the outbox chapter's",
"    // tenant during one `fetch`. Chapter 3.7's lane runs caught it at 9,917": "    // tenant during one `fetch`. The deduplication chapter's lane runs caught it at 9,917",
" * Declared rather than defaulted, because chapter 3.15 found the cost of leaving it": " * Declared rather than defaulted, because the channel-control chapter found the cost of leaving it",
"    // chapter 3.12 built and this is the first route to rely on it.": "    // The isolation gauntlet built and this is the first route to rely on it.",
"    // Chapter 3.16 made the same trade for `last_message`.": "    // The user-surface chapter made the same trade for `last_message`.",
"    // where chapter 3.15 put it (read back rather than echoed).": "    // where the channel-control chapter put it (read back rather than echoed).",
"    // ago \u2014 chapter 3.12's fourteen green tests are why this line exists.": "    // ago \u2014 the isolation gauntlet's fourteen green tests are why this line exists.",
" * `attachments.ts` already said this in chapter 3.24 \u2014 *\"A URL validator that accepts": " * `attachments.ts` already said this in the attachments chapter \u2014 *\"A URL validator that accepts",
"    // Chapter 3.5's bug, in the one place it could recur. The delivery id is": "    // The webhook dispatcher chapter's bug, in the one place it could recur. The delivery id is",
"//     the outcome (3.3's outbox), and then a stalled analytics consumer backs up": "//     the outcome (the outbox chapter's outbox), and then a stalled analytics consumer backs up",
" * `createJetStreamPublisher` ensures exactly one stream, and chapter 3.5 already": " * `createJetStreamPublisher` ensures exactly one stream, and the webhook dispatcher chapter already",
"      // reason chapter 3.5 learned the hard way: the delivery id alone is stable": "      // reason the webhook dispatcher chapter learned the hard way: the delivery id alone is stable",
"// counting somebody else's work (chapter 3.3's finding 4, again).": "// counting somebody else's work (the outbox chapter's finding 4, again).",
" * chapter 3.6's own baseline is the reason this is a per-SUITE prefix rather than": " * The retry-and-disable chapter's own baseline is the reason this is a per-SUITE prefix rather than",
"   * chapter 3.3's outbox broker-agnostic \u2014 so claiming a delivery without putting": "   * The outbox chapter's outbox broker-agnostic \u2014 so claiming a delivery without putting",
"   * something the platform never does. Chapter 3.5's suite learned this and said": "   * something the platform never does. The webhook dispatcher chapter's suite learned this and said",
"    // stream, as chapter 3.5 established for DELIVERIES. So the first outcome is": "    // stream, as the webhook dispatcher chapter established for DELIVERIES. So the first outcome is",
"      // The durable goes with the run. Chapter 3.5's dispatcher suite leaked two": "      // The durable goes with the run. The webhook dispatcher chapter's dispatcher suite leaked two",
"      // consumers per run onto a shared broker and 3.6's baseline found ninety of": "      // consumers per run onto a shared broker and the retry-and-disable chapter's baseline found ninety of",
"// running beside it. Chapter 3.3's finding 4 is the reason that sentence exists.": "// running beside it. The outbox chapter's finding 4 is the reason that sentence exists.",
"    // And the claim is chapter 3.4's ledger, unchanged.": "    // And the claim is the broker chapter's ledger, unchanged.",
"   * suite got there first. Chapter 3.3's finding 3 met this twice; this is the": "   * suite got there first. The outbox chapter's finding 3 met this twice; this is the",
"   * Chapter 3.6's baseline drew the rule twice already: clean up what you created,": "   * The retry-and-disable chapter's baseline drew the rule twice already: clean up what you created,",
"    // Found at chapter 3.7's baseline, after 781 endpoints had accumulated an open": "    // Found at the deduplication chapter's baseline, after 781 endpoints had accumulated an open",
"// \u2026 easy to exercise in a way the instrument cannot see\". Chapter 3.5 ignored the": "// \u2026 easy to exercise in a way the instrument cannot see\". The webhook dispatcher chapter ignored the",
"// PLACE in the loop was not, which is the same shape as chapter 3.5's vacuous": "// PLACE in the loop was not, which is the same shape as the webhook dispatcher chapter's vacuous",
"// This is the moment chapter 3.3's outbox stops being a thing that moves EVENTS": "// This is the moment the outbox chapter's outbox stops being a thing that moves EVENTS",
"// The one difference from 3.3's relay is a predicate \u2014 `next_attempt_at <=": "// The one difference from the outbox chapter's relay is a predicate \u2014 `next_attempt_at <=",
" * The subject grammar lives in `@relay/protocol` \u2014 3.4's lesson, applied again:": " * The subject grammar lives in `@relay/protocol` \u2014 the broker chapter's lesson, applied again:",
"/** Small, for 3.3's reason: a batch is held inside one transaction, and a long": "/** Small, for the outbox chapter's reason: a batch is held inside one transaction, and a long",
" * rather than on an insert, so unlike 3.3's relay there is no \"wake me on write\"": " * rather than on an insert, so unlike the outbox chapter's relay there is no \"wake me on write\"",
"      // The same three-field port chapter 3.3 defined, unchanged. That the": "      // The same three-field port the outbox chapter defined, unchanged. That the",
"      // second relay needed no new seam is the evidence that 3.3's abstraction": "      // second relay needed no new seam is the evidence that the outbox chapter's abstraction",
"        // buffering 3.3's relay promises for events.": "        // buffering the outbox chapter's relay promises for events.",
"    // The window, doing its job. Chapter 3.5's first five attempts all land": "    // The window, doing its job. The webhook dispatcher chapter's first five attempts all land",
" * The hour alone is not enough. Chapter 3.5's schedule reaches a two-hour gap": " * The hour alone is not enough. The webhook dispatcher chapter's schedule reaches a two-hour gap",
"// Why a webhook signing secret is NOT stored the way chapter 3.2 stored an API": "// Why a webhook signing secret is NOT stored the way the credentials chapter stored an API",
"  it(\"cannot be recovered from chapter 3.2's treatment of the other credential\", () => {": "  it(\"cannot be recovered from the credentials chapter's treatment of the other credential\", () => {",
"   * public route refuses loopback addresses \u2014 chapter 3.5's SSRF check, which is": "   * public route refuses loopback addresses \u2014 the webhook dispatcher chapter's SSRF check, which is",
"    // Chapter 3.2's lesson about error messages that name the mistake, applied": "    // The credentials chapter's lesson about error messages that name the mistake, applied",
"    // five bare 422s survived from chapter 3.5. The sixth code is asserted here rather": "    // five bare 422s survived from the webhook dispatcher chapter. The sixth code is asserted here rather",
"    // review and chapter 3.23's gaps ledger both recommend \u2014 would refuse every one of": "    // review and the revisions chapter's gaps ledger both recommend \u2014 would refuse every one of",
" * exactly as 3.3's is. An event spine that only runs when someone remembers is": " * exactly as the outbox chapter's is. An event spine that only runs when someone remembers is",
" * a background loop marked them dispatched mid-assertion. Chapter 3.3's finding": " * a background loop marked them dispatched mid-assertion. The outbox chapter's finding",
"          // Its OWN stream, not the events one. Chapter 3.3's publisher": "          // Its OWN stream, not the events one. The outbox chapter's publisher",
"/** FR-WHK-01, and the error must say the number. Chapter 3.2's lesson about": "/** FR-WHK-01, and the error must say the number. The credentials chapter's lesson about",
"// Chapter 3.4's consumer claimed the event and ran its effect in ONE": "// The broker chapter's consumer claimed the event and ran its effect in ONE",
"// duplicate the recipient was handed the means to absorb \u2014 and chapter 3.3 spent": "// duplicate the recipient was handed the means to absorb \u2014 and the outbox chapter spent",
"      // Chapter 3.3's finding 4, for the third time: this suite drives the relay": "      // The outbox chapter's finding 4, for the third time: this suite drives the relay",
"  /** Publish a real event onto the EVENTS stream, exactly as chapter 3.3's": "  /** Publish a real event onto the EVENTS stream, exactly as the outbox chapter's",
"   * so the two cannot race (chapter 3.3's finding 4, third occurrence). */": "   * so the two cannot race (the outbox chapter's finding 4, third occurrence). */",
"      // Found at chapter 3.8's baseline. Chapter 3.7 fixed the same global drain": "      // Found at the rate-limit chapter's baseline. The deduplication chapter fixed the same global drain",
"    // `itest-deliver-<8 hex>` \u2014 and deleted neither. Chapter 3.24's close-out found": "    // `itest-deliver-<8 hex>` \u2014 and deleted neither. The attachments chapter's close-out found",
"    // `services/api/src/consumer/consumer.itest.ts` has done this since chapter 3.4 and": "    // `services/api/src/consumer/consumer.itest.ts` has done this since the broker chapter and",
"  it(\"invariant 16: the delivered body is 3.3's envelope and carries the event id\", async () => {": "  it(\"invariant 16: the delivered body is the outbox chapter's envelope and carries the event id\", async () => {",
"// inside chapter 3.4's claim transaction. What looks like an inconvenience is": "// inside the broker chapter's claim transaction. What looks like an inconvenience is",
"/** The envelope as chapter 3.3 publishes it. Parsed here rather than trusted,": "/** The envelope as the outbox chapter publishes it. Parsed here rather than trusted,",
" * that was available on the first. Chapter 3.4 made the same call for the same": " * that was available on the first. The broker chapter made the same call for the same",
" * receiving everything (chapter 3.4's research R8).": " * receiving everything (the broker chapter's research R8).",
" * Overridable, and chapter 3.4's suites explain why: a test that shares the": " * Overridable, and the broker chapter's suites explain why: a test that shares the",
"      // attempt budget ran out. Chapter 3.4 settled this: the same bytes fail the": "      // attempt budget ran out. The broker chapter settled this: the same bytes fail the",
"/** A failed internal call, carrying the status. Chapter 3.2 needs the": "/** A failed internal call, carrying the status. The credentials chapter needs the",
"  /** Chapter 3.20's backstop: what this connection may hear, now.": "  /** The membership-revocation chapter's backstop: what this connection may hear, now.",
"  // Chapter 3.2 retired two headers here. The gateway used to send": "  // The credentials chapter retired two headers here. The gateway used to send",
"      // Chapter 3.20's backstop. A GET, unlike every other method here: it presents": "      // The membership-revocation chapter's backstop. A GET, unlike every other method here: it presents",
"          // speak for, which is exactly the assertion chapter 3.2 removed.": "          // speak for, which is exactly the assertion the credentials chapter removed.",
"/** FOUR outcomes, and chapter 3.11 added the fourth. A refused token, an": "/** FOUR outcomes, and the connection-metering chapter added the fourth. A refused token, an",
" * whole point of T014**: chapter 3.18 already asserts both of a user's sockets": " * whole point of T014**: the fan-out chapter already asserts both of a user's sockets",
" * six repeats to find that out \u2014 chapter 3.21's rule one level down: a check that": " * six repeats to find that out \u2014 the typing chapter's rule one level down: a check that",
" * SUBSCRIBE has necessarily landed, which cost chapter 3.21 a flake at 315 ms \u2014": " * SUBSCRIBE has necessarily landed, which cost the typing chapter a flake at 315 ms \u2014",
"    // a teardown that closed servers first cost chapter 3.20 seven tests and": "    // a teardown that closed servers first cost the membership-revocation chapter seven tests and",
"    // the code that publishes is the shape chapter 3.18 warned about anyway.": "    // the code that publishes is the shape the fan-out chapter warned about anyway.",
"    // chapter 3.20's lesson in its own words \u2014 a claim about an observable": "    // The membership-revocation chapter's lesson in its own words \u2014 a claim about an observable",
"    // CHAPTER 3.19 SHIPPED A PRESENCE BUG BY ARMING A CHECK AT EXACTLY ITS OWN": "    // The presence chapter SHIPPED A PRESENCE BUG BY ARMING A CHECK AT EXACTLY ITS OWN",
"// sees says whether five was checked. Chapter 3.18 found the general case \u2014 the": "// sees says whether five was checked. The fan-out chapter found the general case \u2014 the",
"// another connection's place** \u2014 all three of which chapter 3.22's analysis found and": "// another connection's place** \u2014 all three of which the connection-cap chapter's analysis found and",
"// CHAPTER 3.22's slot registry \u2014 THE HALF THAT NEEDS NO BROKER (feature 043: FR-006,": "// The connection-cap chapter's slot registry \u2014 THE HALF THAT NEEDS NO BROKER (feature 043: FR-006,",
"    // 3.20's rule: a claim about an observable difference needs falsifying before": "    // The membership-revocation chapter's rule: a claim about an observable difference needs falsifying before",
"    // chapter 3.18 found in the fan-out \u2014 \"the send returned 201 while Redis was": "    // The fan-out chapter found in the fan-out \u2014 \"the send returned 201 while Redis was",
"    // And it is NOT the protocol keepalive, which chapter 3.19 paid for conflating.": "    // And it is NOT the protocol keepalive, which the presence chapter paid for conflating.",
" * `session.ts:48` sets the protocol keepalive to 30_000, and chapter 3.19 paid for": " * `session.ts:48` sets the protocol keepalive to 30_000, and the presence chapter paid for",
"  // into an unhandled rejection that takes the process down. Chapter 3.18's R10": "  // into an unhandled rejection that takes the process down. The fan-out chapter's R10",
"  // THE SUBJECT GRAMMAR'S TEST MOVED IN CHAPTER 3.18, to": "  // THE SUBJECT GRAMMAR'S TEST MOVED IN the fan-out chapter, to",
"// WHO PUBLISHES CHANGED IN CHAPTER 3.18. This comment used to say \"the": "// WHO PUBLISHES CHANGED IN the fan-out chapter. This comment used to say \"the",
"        // which is what chapter 3.23's resume decision rests on.": "        // which is what the revisions chapter's resume decision rests on.",
" * Chapter 3.12 narrows that seam for channels and members \u2014 those get public": " * The isolation gauntlet narrows that seam for channels and members \u2014 those get public",
"  // child, and chapter 3.8's per-IP failed-auth limiter is the one bucket that": "  // child, and the rate-limit chapter's per-IP failed-auth limiter is the one bucket that",
"// The arrangement is chapter 3.2's and 3.11 kept it for the same reason: the": "// The arrangement is the credentials chapter's and the connection-metering chapter kept it for the same reason: the",
"/** 120 SECONDS, NOT 30, and the number is measured (chapter 3.15's Phase 1).": "/** 120 SECONDS, NOT 30, and the number is measured (the channel-control chapter's Phase 1).",
" * Chapter 3.12's battery blew this same 30 s at run 11 for an unrelated reason \u2014 two": " * The isolation gauntlet's battery blew this same 30 s at run 11 for an unrelated reason \u2014 two",
"      // Its own failed-authentication keyspace. Chapter 3.8's auth limiter counts": "      // Its own failed-authentication keyspace. The rate-limit chapter's auth limiter counts",
"  // That is not a compromise invented here \u2014 it is the shape chapter 3.2 already chose": "  // That is not a compromise invented here \u2014 it is the shape the credentials chapter already chose",
"  // Chapter 3.15 gives `users.display_name`, `users.avatar_url` and `users.metadata` a": "  // The channel-control chapter gives `users.display_name`, `users.avatar_url` and `users.metadata` a",
"  // THE REASON CHANGED IN CHAPTER 3.18 AND THE FACT DID NOT. This comment used to say": "  // THE REASON CHANGED IN THE FAN-OUT CHAPTER AND THE FACT DID NOT. This comment used to say",
"  // mechanisms. Chapter 3.17 removed one and chapter 3.18 the other, so the api does": "  // mechanisms. The sender chapter removed one and the fan-out chapter the other, so the api does",
"      // `message.deleted` note below, which is chapter 3.23 making the same repair.": "      // `message.deleted` note below, which is the revisions chapter making the same repair.",
"    // CHAPTER 3.23 SPLIT THIS CASE OFF. `message.deleted` shared the `Message` above": "    // The revisions chapter SPLIT THIS CASE OFF. `message.deleted` shared the `Message` above",
"    // ELEVEN with chapter 3.21's `typing.send`. **The title carries the number": "    // ELEVEN with the typing chapter's `typing.send`. **The title carries the number",
"// `other side closed` and naming no port. And chapter 3.24's record carries an": "// `other side closed` and naming no port. And the attachments chapter's record carries an",
"// THE MAP WAS NOT 78% COMPLETE, IT WAS WRONG. Chapter 3.21's `gaps.md` item 4 put": "// THE MAP WAS NOT 78% COMPLETE, IT WAS WRONG. The typing chapter's `gaps.md` item 4 put",
"// Chapter 3.20 eliminated \"a port collision\" as a cause of that battery's failures": "// The membership-revocation chapter eliminated \"a port collision\" as a cause of that battery's failures",
"    // ADR-05: the gateway never touches Postgres. Chapter 3.8 needed the": "    // ADR-05: the gateway never touches Postgres. The rate-limit chapter needed the",
"      // ELEVEN from chapter 3.21's `typing.send`. The `toEqual` above is derived": "      // ELEVEN from the typing chapter's `typing.send`. The `toEqual` above is derived",
"  // 3.8's verbatim: a connection in subscribe mode cannot run `SET` or `EXISTS`,": "  // the rate-limit chapter's verbatim: a connection in subscribe mode cannot run `SET` or `EXISTS`,",
"  // signing secret: chapter 3.2's claim that \"the gateway holds no signing": "  // signing secret: the credentials chapter's claim that \"the gateway holds no signing",
"    // CHAPTER 3.22, T042. **THIS LINE IS THE ONE CHAPTER 3.21 FORGOT.** That": "    // T042. **THIS LINE IS THE ONE the typing chapter FORGOT.** That",
"  // chapter 3.8 refused by name.": "  // The rate-limit chapter refused by name.",
"// **THE ARMS THAT `session.ts` CANNOT REACH, WRITTEN NOW.** Chapter 3.19 met its": "// **THE ARMS THAT `session.ts` CANNOT REACH, WRITTEN NOW.** The presence chapter met its",
" * top level rather than nested. Chapter 3.19's first version pushed the raw line, so": " * top level rather than nested. The presence chapter's first version pushed the raw line, so",
"// and it makes chapter 3.19's `gaps.md` item 17 worse rather than better \u2014 six files": "// and it makes the presence chapter's `gaps.md` item 17 worse rather than better \u2014 six files",
"  /** THE SENDER MUST BE A BOT, and `createUser` cannot make one \u2014 chapter 3.17 made": "  /** THE SENDER MUST BE A BOT, and `createUser` cannot make one \u2014 the sender chapter made",
"  // 4222 \u2014 NATS. That second one is the likeliest reading of chapter 3.24's eleventh": "  // 4222 \u2014 NATS. That second one is the likeliest reading of the attachments chapter's eleventh",
" * empty frame list \u2014 chapter 3.19's first run failed exactly that way, with test 1": " * empty frame list \u2014 the presence chapter's first run failed exactly that way, with test 1",
"   * one, the way chapter 3.19's grace-period tests inject a hundred-millisecond": "   * one, the way the presence chapter's grace-period tests inject a hundred-millisecond",
"    // `counts.get(key) ?? 0` and the early return under it. Chapter 3.19's presence": "    // `counts.get(key) ?? 0` and the early return under it. The presence chapter's presence",
"    // **AND THE TITLE NAMES WHAT THE ASSERTION CHECKS.** Chapter 3.19 shipped": "    // **AND THE TITLE NAMES WHAT THE ASSERTION CHECKS.** The presence chapter shipped",
"    // after. A timer surviving `close()` is how chapter 3.19 leaked one suite's": "    // after. A timer surviving `close()` is how the presence chapter leaked one suite's",
"    // Chapter 3.19's identical case measured `[15, 0]` and needed a test written at": "    // The presence chapter's identical case measured `[15, 0]` and needed a test written at",
"    // stated reason is NFR-OBS-01 rather than process death: chapter 3.18 measured": "    // stated reason is NFR-OBS-01 rather than process death: the fan-out chapter measured",
"    // 3.19's `collect()` carries this warning in its own comment, T043 repeats it as": "    // The presence chapter's `collect()` carries this warning in its own comment, T043 repeats it as",
"    // makes is chapter 3.19's defect exactly, and T130 exists to catch it.": "    // makes is the presence chapter's defect exactly, and T130 exists to catch it.",
"    // log line is what carries the requirement, which is chapter 3.18's trap against": "    // log line is what carries the requirement, which is the fan-out chapter's trap against",
"// ONE CLIENT, AND THE TASK LIST SAID TWO. Chapter 3.8's rule is that a connection in": "// ONE CLIENT, AND THE TASK LIST SAID TWO. The rate-limit chapter's rule is that a connection in",
"  // listener kills the gateway; chapter 3.18 measured that against ioredis 6.0.0 and": "  // listener kills the gateway; the fan-out chapter measured that against ioredis 6.0.0 and",
"    // `?? 0` and NOT `?? 1`. Chapter 3.19's presence module used the latter and the": "    // `?? 0` and NOT `?? 1`. The presence chapter's presence module used the latter and the",
"      // file. Chapter 3.19 recorded that exact failure.": "      // file. The presence chapter recorded that exact failure.",
"        // what found this. Chapter 3.8's auth limiter counts failures per SOURCE": "        // what found this. The rate-limit chapter's auth limiter counts failures per SOURCE",
"        // answering 429 to EVERYBODY, including chapter 3.2's expired-token test": "        // answering 429 to EVERYBODY, including the credentials chapter's expired-token test",
"   * third variant of chapter 3.20's `gaps.md` item 19a and the most deceptive of": "   * third variant of the membership-revocation chapter's `gaps.md` item 19a and the most deceptive of",
"   * **This file's test is the one that failed run 8 of chapter 3.21's battery**": "   * **This file's test is the one that failed run 8 of the typing chapter's battery**",
"// input\" is to read what the source can do. Chapter 3.8's 4008 test is the": "// input\" is to read what the source can do. The rate-limit chapter's 4008 test is the",
"// `state: \"away\"`, and chapter 3.12's gauntlet proves a client cannot forge one.": "// `state: \"away\"`, and the isolation gauntlet's gauntlet proves a client cannot forge one.",
"// Nothing has ever produced one \u2014 chapter 3.17's `gaps.md` item 2 recorded it as": "// Nothing has ever produced one \u2014 the sender chapter's `gaps.md` item 2 recorded it as",
"  // implementation did the channels only. Chapter 3.18's `isolation-fixtures.ts`": "  // implementation did the channels only. The fan-out chapter's `isolation-fixtures.ts`",
"   * still opened\" exactly as well as a working one, which is chapter 3.18's trap": "   * still opened\" exactly as well as a working one, which is the fan-out chapter's trap",
"  // CHAPTER 3.22 BUILT THE CAP AND THE SENTENCE ABOVE IS STILL TRUE HERE, which is a": "  // The connection-cap chapter BUILT THE CAP AND THE SENTENCE ABOVE IS STILL TRUE HERE, which is a",
"// TWO CLIENTS, for the reason chapter 3.8 gave for the limiter's: a connection in": "// TWO CLIENTS, for the reason the rate-limit chapter gave for the limiter's: a connection in",
"  // documents is never taken. Chapter 3.18 measured the same thing about its": "  // documents is never taken. The fan-out chapter measured the same thing about its",
"      // well as a working one does, which is chapter 3.18's trap against its own": "      // well as a working one does, which is the fan-out chapter's trap against its own",
"// Chapter 3.12 built the first two endpoints for exactly this path, and this is": "// The isolation gauntlet built the first two endpoints for exactly this path, and this is",
"// distinction is worth a line because chapter 3.23 read it the other way. A task in": "// distinction is worth a line because the revisions chapter read it the other way. A task in",
"  // separately because the gap needed both. Chapter 3.17 amended FR-MSG-13 \u2014 \"on behalf": "  // separately because the gap needed both. The sender chapter amended FR-MSG-13 \u2014 \"on behalf",
"  // sender and `toFrame` stopped dropping the row from a resume. Chapter 3.18 gave the api": "  // sender and `toFrame` stopped dropping the row from a resume. The fan-out chapter gave the api",
"  // a publisher, so the same row now reaches a LIVE socket too. Chapter 3.12's `gaps.md`": "  // a publisher, so the same row now reaches a LIVE socket too. The isolation gauntlet's `gaps.md`",
"    // chapter 3.12's `gaps.md` G1 listed TWO independent mechanisms for \"a REST-sent": "    // The isolation gauntlet's `gaps.md` G1 listed TWO independent mechanisms for \"a REST-sent",
"    // FR-MSG-15 removed the second and chapter 3.18's publisher removed the first, so": "    // FR-MSG-15 removed the second and the fan-out chapter's publisher removed the first, so",
"/** Chapter 3.11 widened `ApiClient` with `reportUsage`, and every stub in this": "/** The connection-metering chapter widened `ApiClient` with `reportUsage`, and every stub in this",
"// nothing new (3.17's T040b, the fifth such incident in two features).": "// nothing new (the sender chapter's T040b, the fifth such incident in two features).",
"      // which is the entire reason chapter 3.20's `gaps.md` item 19a has four": "      // which is the entire reason the membership-revocation chapter's `gaps.md` item 19a has four",
"/** A FRESH PORT PER CALL, and chapter 3.11 had to learn why twice.": "/** A FRESH PORT PER CALL, and the connection-metering chapter had to learn why twice.",
"  // connected member\" and one socket cannot show it. ADDITIVE, on chapter 3.18's": "  // connected member\" and one socket cannot show it. ADDITIVE, on the fan-out chapter's",
"    // 3.3's suite is asserting on turns two unrelated test files into a race.": "    // the outbox chapter's suite is asserting on turns two unrelated test files into a race.",
"  // an undrained pipe fills (chapter 3.20 measured 4,000 requests before it": "  // an undrained pipe fills (the membership-revocation chapter measured 4,000 requests before it",
"// boots to prove nothing new. Chapter 3.17's T040b took five tests down doing": "// boots to prove nothing new. The sender chapter's T040b took five tests down doing",
"    // INVERTED IN CHAPTER 3.20, AND THE TITLE WITH IT. This test read \"keeps": "    // INVERTED IN THE MEMBERSHIP-REVOCATION CHAPTER, AND THE TITLE WITH IT. This test read \"keeps",
"    // delivering\" and asserted the violation on purpose from chapter 3.18 until": "    // delivering\" and asserted the violation on purpose from the fan-out chapter until",
"    // AND THE TITLE IS PART OF THE CHANGE. Chapter 3.19 shipped a test whose title": "    // AND THE TITLE IS PART OF THE CHANGE. The presence chapter shipped a test whose title",
"    // FR-CHN-05's fourth door. The read paths got three in chapter 3.15 \u2014 list,": "    // FR-CHN-05's fourth door. The read paths got three in the channel-control chapter \u2014 list,",
"          // check this test is about. Chapter 3.23 hit the identical fault with": "          // check this test is about. The revisions chapter hit the identical fault with",
"      // CHAPTER 3.23 SPLIT THIS CASE OFF, and the failure that forced it is the point": "      // The revisions chapter SPLIT THIS CASE OFF, and the failure that forced it is the point",
"  // module into chapter 3.11's \"cap at the door\" describe, whose five quota tests": "  // module into the connection-metering chapter's \"cap at the door\" describe, whose five quota tests",
"  // the other two door refusals: chapter 3.8's rate limit and chapter 3.11's": "  // the other two door refusals: the rate-limit chapter's rate limit and the connection-metering chapter's",
"    // Chapter 3.20's backstop reads this. The default answers what the session": "    // The membership-revocation chapter's backstop reads this. The default answers what the session",
" * gateway and this file knew. After chapter 3.2 neither of them holds a secret \u2014": " * gateway and this file knew. After the credentials chapter neither of them holds a secret \u2014",
"  // the compile-time half of chapter 3.21's lesson about a module built and never passed.": "  // the compile-time half of the typing chapter's lesson about a module built and never passed.",
"    // New in chapter 3.2, and the reason `authenticate` has three outcomes": "    // New in the credentials chapter, and the reason `authenticate` has three outcomes",
"    // and after chapter 3.2 the TOKEN travels too, because the internal hop": "    // and after the credentials chapter the TOKEN travels too, because the internal hop",
"    // CHAPTER 3.8 WROTE THIS TEST INVERTED, and said why:": "    // The rate-limit chapter WROTE THIS TEST INVERTED, and said why:",
"    // 4009 IS STILL EMITTED BY NOTHING. Chapter 3.11 gave the gateway its first": "    // 4009 IS STILL EMITTED BY NOTHING. The connection-metering chapter gave the gateway its first",
" * Chapter 3.19 armed a grace check at exactly its own grace period and stranded a": " * The presence chapter armed a grace check at exactly its own grace period and stranded a",
" * `request_id` ARRIVED IN CHAPTER 3.8, and the gateway had none to give \u2014 it": " * `request_id` ARRIVED IN the rate-limit chapter, and the gateway had none to give \u2014 it",
"   * phase, which makes the phase uncommittable \u2014 chapter 3.20 paid for that exact": "   * phase, which makes the phase uncommittable \u2014 the membership-revocation chapter paid for that exact",
"   * 3.20's `rereadIntervalMs` are: **a test that waits out two real seconds pays": "   * the membership-revocation chapter's `rereadIntervalMs` are: **a test that waits out two real seconds pays",
"   * means NO CAP. Optional is also what let chapter 3.21's module go unpassed": "   * means NO CAP. Optional is also what let the typing chapter's module go unpassed",
"   * A MAP IN THE CLOSURE RATHER THAN A FIELD ON `Connection`. Chapter 3.21 put": "   * A MAP IN THE CLOSURE RATHER THAN A FIELD ON `Connection`. The typing chapter put",
"   * their own presence transition \u2014 chapter 3.20 confirmed it from the other": "   * their own presence transition \u2014 the membership-revocation chapter confirmed it from the other",
"          // 3.18's lesson: the assertion that carries the requirement is the log": "          // The fan-out chapter's lesson: the assertion that carries the requirement is the log",
"          // NOT `refuseUpgrade`. That writes chapter 3.8's raw 429 and its whole": "          // NOT `refuseUpgrade`. That writes the rate-limit chapter's raw 429 and its whole",
"   * whether a channel exists, which is the probe chapter 3.15 closed on the REST": "   * whether a channel exists, which is the probe the channel-control chapter closed on the REST",
"   * current by chapter 3.20's two branches, so the check is a set lookup rather": "   * current by the membership-revocation chapter's two branches, so the check is a set lookup rather",
"// Chapter 3.21's fabric, against a REAL Redis.": "// The typing chapter's fabric, against a REAL Redis.",
"// failures across chapter 3.20's forty battery runs were one of those fixtures": "// failures across the membership-revocation chapter's forty battery runs were one of those fixtures",
"// api. Chapter 3.20 reached for a fixed range instead and collided twice \u2014 once": "// api. The membership-revocation chapter reached for a fixed range instead and collided twice \u2014 once",
"  /** Chapter 3.21 phase 7. **The `ApiClient` is the seam that widens the resume": "  /** The typing chapter phase 7. **The `ApiClient` is the seam that widens the resume",
"   * window**, and chapter 3.20 recorded why nothing else does: slowing the FABRIC": "   * window**, and the membership-revocation chapter recorded why nothing else does: slowing the FABRIC",
" * publishes is the shape chapter 3.18 warned about \u2014 a publisher that does": " * publishes is the shape the fan-out chapter warned about \u2014 a publisher that does",
"   * which is the probe chapter 3.15 closed on the REST surface. */": "   * which is the probe the channel-control chapter closed on the REST surface. */",
"   * Chapter 3.20 counted `presence.changed` frames by type alone and read two": "   * The membership-revocation chapter counted `presence.changed` frames by type alone and read two",
"   * about a fabric that is asleep. Chapter 3.19's presence collector was": "   * about a fabric that is asleep. The presence chapter's presence collector was",
"    // The handshake spends `connect`, which is chapter 3.11's and not this": "    // The handshake spends `connect`, which is the connection-metering chapter's and not this",
"   * against an implementation that never touches chapter 3.20's `added`": "   * against an implementation that never touches the membership-revocation chapter's `added`",
"    // The api's half of chapter 3.20's fabric, published directly: what is under": "    // The api's half of the membership-revocation chapter's fabric, published directly: what is under",
"   * that publishes is the shape chapter 3.18 warned about \u2014 a publisher that does": "   * that publishes is the shape the fan-out chapter warned about \u2014 a publisher that does",
"   * is the proof form that has now failed twice** \u2014 chapter 3.20 ran it on two": "   * is the proof form that has now failed twice** \u2014 the membership-revocation chapter ran it on two",
"   * headroom; chapter 3.20 tests a sixty-second backstop at 40 ms for the same": "   * headroom; the membership-revocation chapter tests a sixty-second backstop at 40 ms for the same",
"   * **CHAPTER 3.20's EQUIVALENT PASSED TWICE WITH ITS SUBJECT DELETED**, and its": "   * **The membership-revocation chapter's EQUIVALENT PASSED TWICE WITH ITS SUBJECT DELETED**, and its",
"   * quoting that clause was a good test under a false name \u2014 chapter 3.19's": "   * quoting that clause was a good test under a false name \u2014 the presence chapter's",
"    // queues satisfies \"the socket stayed open\" the way chapter 3.18's fan-out": "    // queues satisfies \"the socket stayed open\" the way the fan-out chapter's fan-out",
"   * **Not as what a grep finds.** Chapter 3.20's FR-032 declared three names while": "   * **Not as what a grep finds.** The membership-revocation chapter's FR-032 declared three names while",
"// TWO CLIENTS, AND THE TASK LIST SAID ONE. It carried chapter 3.20's finding": "// TWO CLIENTS, AND THE TASK LIST SAID ONE. It carried the membership-revocation chapter's finding",
"  // The subscriber keeps ioredis's default retry, as chapter 3.20's does and for": "  // The subscriber keeps ioredis's default retry, as the membership-revocation chapter's does and for",
"  // listener kills the gateway; chapter 3.18 measured that against ioredis 6.0.0": "  // listener kills the gateway; the fan-out chapter measured that against ioredis 6.0.0",
"  // Chapter 3.15, FR-031. A FIFTH CODE, AND NOT A REUSE OF 4001.": "  // FR-031. A FIFTH CODE, AND NOT A REUSE OF 4001.",
"  // CHAPTER 3.22, FR-RTM-09. A SIXTH CODE, AND EVERY REUSE FAILS THIS FILE'S OWN TEST.": "  // FR-RTM-09. A SIXTH CODE, AND EVERY REUSE FAILS THIS FILE'S OWN TEST.",
"  // Chapter 3.22, FR-RTM-09, and the socket's half of the connection cap: an error": "  // FR-RTM-09, and the socket's half of the connection cap: an error",
"  /** Chapter 3.15, FR-031. Whether this user is banned in this environment.": "  /** FR-031. Whether this user is banned in this environment.",
"  /** Chapter 3.15, FR-023. Both columns have existed since chapter 2.1 and **no route": "  /** FR-023. Both columns have existed since chapter 2.1 and **no route",
"  /** Chapter 3.15, FR-031. Read on the send path and at connect. Like `deleted_at`, it": "  /** FR-031. Read on the send path and at connect. Like `deleted_at`, it",
"  /** Chapter 3.15, FR-017. A deleted user KEEPS THEIR ROW: `ON DELETE SET NULL` on": "  /** FR-017. A deleted user KEEPS THEIR ROW: `ON DELETE SET NULL` on",
"  /** Chapter 3.17, FR-USR-07. What kind of thing this user is \u2014 `'person'` or `'bot'`.": "  /** FR-USR-07. What kind of thing this user is \u2014 `'person'` or `'bot'`.",
"  /** Chapter 3.6, FR-009. `enabled: false` with `disabled_at: null` means the": "  /** FR-009. `enabled: false` with `disabled_at: null` means the",
"    /** Chapter 3.15, FR-011b. Absent means the column's own default \u2014 `member` \u2014": "    /** FR-011b. Absent means the column's own default \u2014 `member` \u2014",
"      /** Chapter 3.17, FR-004. `string | undefined` and NOT `| null`, unlike its three": "      /** FR-004. `string | undefined` and NOT `| null`, unlike its three",
"  /** Chapter 3.23, ADR-24. Publish an edit or a deletion to its channel's revision": "  /** ADR-24. Publish an edit or a deletion to its channel's revision",
"      // Chapter 3.15, FR-031. THE ROW IS ALREADY IN HAND \u2014 `getUserByExternalId` above": "      // FR-031. THE ROW IS ALREADY IN HAND \u2014 `getUserByExternalId` above",
" * Chapter 3.15, FR-034. `seedTwoTenants` above gives every attack a victim in": " * FR-034. `seedTwoTenants` above gives every attack a victim in",
" * Chapter 3.15, FR-034a. `seedTenant` above label-prefixes every identifier \u2014": " * FR-034a. `seedTenant` above label-prefixes every identifier \u2014",
"      // Chapter 3.2, FR-AUT-02: the environment's first key, and the ONLY time": "      // FR-AUT-02: the environment's first key, and the ONLY time",
"  /** Chapter 3.15, FR-031. The api answered, the token is perfectly good, and the user": "  /** FR-031. The api answered, the token is perfectly good, and the user",
"    // Chapter 3.23, ADR-24. The `updated` arm's payload is a `Message`, which is exactly": "    // ADR-24. The `updated` arm's payload is a `Message`, which is exactly",
"  /** Chapter 3.23, ADR-24. Register the revision callback \u2014 an edit or a deletion of a": "  /** ADR-24. Register the revision callback \u2014 an edit or a deletion of a",
"  // Chapter 3.15, FR-001. Every attack above crosses a tenant boundary; this one": "  // FR-001. Every attack above crosses a tenant boundary; this one",
"  /** CHAPTER 3.23, FR-016 \u2014 THE GATEWAY'S HALF, AND ONLY THE GATEWAY'S HALF.": "  /** FR-016 \u2014 THE GATEWAY'S HALF, AND ONLY THE GATEWAY'S HALF.",
"  /** Chapter 3.22, FR-RTM-09's five-connection cap.": "  /** FR-RTM-09's five-connection cap.",
"  /** CHAPTER 3.22, FR-011a. What this instance holds, so a shutdown can free it": "  /** FR-011a. What this instance holds, so a shutdown can free it",
"          // Chapter 3.15, FR-031. THE SHAPE OF THE QUOTA REFUSAL, for the same reason:": "          // FR-031. THE SHAPE OF THE QUOTA REFUSAL, for the same reason:",
"      // CHAPTER 3.22, FR-011a. Before `wss.close()`, because that call does not": "      // FR-011a. Before `wss.close()`, because that call does not",
"    // 3.8 added a fifth container and the gap is how it nearly went unnoticed.": "    // The rate-limit chapter added a fifth container and the gap is how it nearly went unnoticed.",
"   * IT READS THE BUFFER `capture` ALREADY FILLS. `gaps.md` 3.22-6 counts eleven files": "   * IT READS THE BUFFER `capture` ALREADY FILLS. `gaps.md` the connection-cap chapter-6 counts eleven files",
"// Chapter 3.10 \u2014 the cap through the OTHER door, and what it leaves alone.": "// The cap through the OTHER door, and what it leaves alone.",
"    // create route took `public` only. Chapter 3.15 (`43899e3`, \"the private type decides": "    // create route took `public` only. The channel-control chapter (`43899e3`, \"the private type decides",
"    // chapters. That is chapter 3.14's unmet half showing itself: a sealed suite proves": "    // chapters. That is the outsider milestone's unmet half showing itself: a sealed suite proves",
"  /** CHAPTER 3.21, T100a \u2014 **the first `socket.send` in this file's history.**": "  /** T100a \u2014 **the first `socket.send` in this file's history.**",
"  /** CHAPTER 3.21, T100b \u2014 the refusal, from outside.": "  /** T100b \u2014 the refusal, from outside.",
"    // CHAPTER 3.22, T048. **THE ONLY INSTRUMENT THAT BOOTS THE SHIPPED BINARY**,": "    // T048. **THE ONLY INSTRUMENT THAT BOOTS THE SHIPPED BINARY**,",
"  /** CHAPTER 3.23 \u2014 an edit, over the shipped binary, seen on somebody else's socket.": "  /** An edit, over the shipped binary, seen on somebody else's socket.",
"   * here for the same reason one level out: 3.23 adds a second Redis subject, a second": "   * here for the same reason one level out: the revisions chapter adds a second Redis subject, a second",
" * since chapter 3.16. A second number for the same kind of value would be two limits a": " * since the user-surface chapter. A second number for the same kind of value would be two limits a",
"  // AND ONE MORE THAN FOUR, SINCE CHAPTER 3.15. `4003` is a ban, which is none of": "  // AND ONE MORE THAN FOUR, SINCE THE CHANNEL-CONTROL CHAPTER. `4003` is a ban, which is none of",
"// \u2500\u2500 THE PLATFORM HALF OF THE CLOSURE CHECK (chapter 3.14, FR-025, SC-011) \u2500\u2500\u2500\u2500\u2500": "// \u2500\u2500 THE PLATFORM HALF OF THE CLOSURE CHECK (FR-025, SC-011) \u2500\u2500\u2500\u2500\u2500",
"    // Thirteen until chapters 3.15 and 3.16 added `not_a_member`,": "    // Thirteen until the channel-control chapter and the user-surface chapter added `not_a_member`,",
"  // was none of them. Numbered here, in the space chapters 1.3 and 3.15 drew from.": "  // was none of them. Numbered here, in the space chapters 1.3 and the channel-control chapter drew from.",
"  // \u2500\u2500 CHAPTERS 3.15 AND 3.16 (FR-021, FR-031, research R11) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "  // \u2500\u2500 THE CHANNEL-CONTROL AND USER-SURFACE CHAPTERS (FR-021, FR-031, R11) \u2500\u2500\u2500\u2500\u2500",
"  // CHAPTER 3.23, AND **NOT** `forbidden` \u2014 the third time this file has made that": "  // THE REVISIONS CHAPTER, AND **NOT** `forbidden` \u2014 the third time this file has made that",
"  // CHAPTER 3.23, AND A SECOND NEW CODE IN ONE CHAPTER \u2014 which is one more than the": "  // THE REVISIONS CHAPTER, AND A SECOND NEW CODE IN ONE CHAPTER \u2014 which is one more than the",
"  //                        and 3.2 for 403.": "  //                        and the credentials chapter for 403.",
" * (3.10, 3.11) \u2014 shipped links to pages that did not exist even in principle.": " * (the quota chapter, the connection-metering chapter) \u2014 shipped links to pages that did not exist even in principle.",
"// chapter 3.18, and it exists before the old copy is deleted so the property is": "// The fan-out chapter, and it exists before the old copy is deleted so the property is",
" * grammar belongs where every party that uses it can agree on it. Until 3.18": " * grammar belongs where every party that uses it can agree on it. Until the fan-out chapter",
"  // Chapter 3.21, and the only INBOUND member besides `message.send`. One": "  // The typing chapter, and the only INBOUND member besides `message.send`. One",
"    // SIX UNTIL CHAPTER 3.24, and pinned here for the first time \u2014 the frame had no": "    // SIX UNTIL THE ATTACHMENTS CHAPTER, and pinned here for the first time \u2014 the frame had no",
"    // silent addition could reach. `codes.test.ts` has pinned its set since 3.2 for the": "    // silent addition could reach. `codes.test.ts` has pinned its set since the credentials chapter for the",
"// inside the api's outbox module in 3.3 because nothing else needed it. A": "// inside the api's outbox module in the outbox chapter because nothing else needed it. A",
"/** api \u2192 gateway, chapter 3.2: who the presented token belongs to, and what it": "/** api \u2192 gateway: who the presented token belongs to, and what it",
"   * records that must agree and are maintained separately is the defect `gaps.md` 3.23-4": "   * records that must agree and are maintained separately is the defect `gaps.md` the revisions chapter-4",
"  /** The envelope as it will be delivered, byte-identical to what 3.3 published.": "  /** The envelope as it will be delivered, byte-identical to what the outbox chapter published.",
" * of being dropped. Chapters 3.19 and 3.20 chose the same strictness for the same": " * of being dropped. the presence chapter and the membership-revocation chapter chose the same strictness for the same",
"   * with (chapter 3.14, FR-027).": "   * with (the error-registry chapter, FR-027).",
" * The docs_url is no longer a placeholder \u2014 chapter 3.14 made it a required option": " * The docs_url is no longer a placeholder \u2014 the error-registry chapter made it a required option",
"   * `usage_connections`, which chapters 3.10 and 3.11 added to the schema and to": "   * `usage_connections`, which the quota chapter and the connection-metering chapter added to the schema and to",
"// \u2500\u2500 THE FOUR USAGE TABLES (chapter 3.13, FR-038, SC-017) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "// \u2500\u2500 THE FOUR USAGE TABLES (FR-038, SC-017) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"// Chapters 3.10 and 3.11 added `usage_periods`, `usage_active_users`,": "// the quota chapter and the connection-metering chapter added `usage_periods`, `usage_active_users`,",
"describe(\"the four usage tables chapters 3.10 and 3.11 left unguarded\", () => {": "describe(\"the four usage tables the quota chapter and the connection-metering chapter left unguarded\", () => {",
"  // \u2500\u2500 THE GUARD'S TENTH TABLE (chapter 3.16, FR-017) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "  // \u2500\u2500 THE GUARD'S TENTH TABLE (FR-017) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"  // exists: chapters 3.10 and 3.11 put four tables in the array and the guard did": "  // exists: the quota chapter and the connection-metering chapter put four tables in the array and the guard did",
" * READ FROM THE NAMED CONST, not from a block's inline `ignores`. Chapter 3.12": " * READ FROM THE NAMED CONST, not from a block's inline `ignores`. The isolation gauntlet",
"      // Chapter 3.8 (FR-AUT-12). The failure is observable HERE \u2014 credential": "      // (FR-AUT-12). The failure is observable HERE \u2014 credential",
"    // Chapter 3.8 (FR-AUT-12, FR-RTL-02, research R18). The refusal for an": "    // (FR-AUT-12, FR-RTL-02, research R18). The refusal for an",
"  // \u2500\u2500 T042: the mint's three cases (chapter 3.17, FR-005, FR-005a, SC-006) \u2500\u2500": "  // \u2500\u2500 T042: the mint's three cases (FR-005, FR-005a, SC-006) \u2500\u2500",
"  // --- chapter 3.5: the third principal ----------------------------------": "  // --- the third principal ----------------------------------",
"  // --- chapter 3.11: one credential per service ---------------------------": "  // --- one credential per service ---------------------------",
"    // \u2500\u2500 THE USER ROW, CREATED IF ABSENT (chapter 3.15, FR-039a, FR-039b) \u2500\u2500\u2500\u2500": "    // \u2500\u2500 THE USER ROW, CREATED IF ABSENT (FR-039a, FR-039b) \u2500\u2500\u2500\u2500",
"    // CHAPTER 3.13'S IDEMPOTENT `createUser`, and that is the whole implementation.": "    // The channel-endpoints chapter'S IDEMPOTENT `createUser`, and that is the whole implementation.",
" * is scoped to exactly ONE environment by construction (3.1, 3.2), so a": " * is scoped to exactly ONE environment by construction (the tenancy chapter, the credentials chapter), so a",
"// WHY a dependency here, when 3.1 went out of its way to add none: hand-rolled": "// WHY a dependency here, when the tenancy chapter went out of its way to add none: hand-rolled",
"    // has carried `field` since chapter 1.3 and nothing set it until chapter 3.14.": "    // has carried `field` since chapter 1.3 and nothing set it until the error-registry chapter.",
"  // \u2500\u2500 THE PRIVATE TYPE, MADE TO MEAN SOMETHING (chapter 3.15) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "  // \u2500\u2500 THE PRIVATE TYPE, MADE TO MEAN SOMETHING \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"  // \u2500\u2500 REMOVAL, BULK, BECAUSE THE REQUIREMENT ALWAYS WAS (chapter 3.15) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "  // \u2500\u2500 REMOVAL, BULK, BECAUSE THE REQUIREMENT ALWAYS WAS \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"  // 3.13 chose\" as *named outcomes* and dropped *bulk*. Every pass compared": "  // the channel-endpoints chapter chose\" as *named outcomes* and dropped *bulk*. Every pass compared",
"  // \u2500\u2500 MEMBER ROLES (chapter 3.15, FR-CHN-04, FR-011) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "  // \u2500\u2500 MEMBER ROLES (FR-CHN-04, FR-011) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"      // Mixed forms in one request, because the entry is a union: chapter 3.13": "      // Mixed forms in one request, because the entry is a union: the channel-endpoints chapter",
"      // The field name is in the envelope only because chapter 3.14 stopped": "      // The field name is in the envelope only because the error-registry chapter stopped",
"  // \u2500\u2500 ARCHIVING (chapter 3.15, FR-020, FR-021, FR-021a) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "  // \u2500\u2500 ARCHIVING (FR-020, FR-021, FR-021a) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"// and adding a member is \"Part 3's tenancy work\". Part 3 ends at 3.12, and this": "// and adding a member is \"Part 3's tenancy work\". Part 3 ends at the isolation gauntlet, and this",
"// about Redis subjects and 3.3 about the outbox table: a shared store needs a": "// about Redis subjects and the outbox chapter about the outbox table: a shared store needs a",
"    // Immutable once created, and both already right because 3.3 chose them.": "    // Immutable once created, and both already right because the outbox chapter chose them.",
"    // the error path, and a crash does not (research R7, the shape 3.3 used).": "    // the error path, and a crash does not (research R7, the shape the outbox chapter used).",
" * 3.3 learned the hard way that a background loop mutating a table two other": " * The outbox chapter learned the hard way that a background loop mutating a table two other",
"// feature's preference \u2014 ADR-16 has said it since chapter 3.9: *\"migrations remain": "// feature's preference \u2014 ADR-16 has said it since the mail-transport chapter: *\"migrations remain",
"// \u2500\u2500 A PRIVATE CHANNEL IS PRIVATE (chapter 3.15, FR-001, FR-CHN-05) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "// \u2500\u2500 A PRIVATE CHANNEL IS PRIVATE (FR-001, FR-CHN-05) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"// Chapter 3.15, T068. `channels.itest.ts` proves the zod enum refuses `admin` at": "// T068. `channels.itest.ts` proves the zod enum refuses `admin` at",
"// \u2500\u2500 T113: THE TIE AT A PAGE BOUNDARY (chapter 3.15, FR-013) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "// \u2500\u2500 T113: THE TIE AT A PAGE BOUNDARY (FR-013) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"// \u2500\u2500 THE TOMBSTONE, AND THE CLAMP (chapter 3.15, FR-016, FR-019) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "// \u2500\u2500 THE TOMBSTONE, AND THE CLAMP (FR-016, FR-019) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"// 3.15 said was so that \"the day FR-MSG-08's chapter ships, the count and the preview": "// The channel-control chapter said was so that \"the day FR-MSG-08's chapter ships, the count and the preview",
"  // \u2500\u2500 T009 (chapter 3.23): THE READER, TESTED BEFORE THE WRITER EXISTS \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "  // \u2500\u2500 T009: THE READER, TESTED BEFORE THE WRITER EXISTS \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"// \u2500\u2500 THE ARMS THE ROUTES CANNOT REACH (chapter 3.15, T174a, T174b) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "// \u2500\u2500 THE ARMS THE ROUTES CANNOT REACH (T174a, T174b) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"// through the gateway's api CHILD PROCESS, whose coverage is not attributable. Chapter 3.5": "// through the gateway's api CHILD PROCESS, whose coverage is not attributable. The webhook dispatcher chapter",
"    // `userId`, by design since chapter 3.3. The listing's `last_message.user` is then": "    // `userId`, by design since the outbox chapter. The listing's `last_message.user` is then",
"    // 3.17 made `userId` required \u2014 and 121,250 of them exist in the lane, written": "    // the sender chapter made `userId` required \u2014 and 121,250 of them exist in the lane, written",
" * STANDALONE SINCE CHAPTER 3.11, and the reason is the same one `usageFor` and": " * STANDALONE SINCE THE CONNECTION-METERING CHAPTER, and the reason is the same one `usageFor` and",
" * so it takes `SELECT \u2026 FOR UPDATE` on the accounting row. 3.10 needed the same": " * so it takes `SELECT \u2026 FOR UPDATE` on the accounting row. The quota chapter needed the same",
"// The backlog 3.6 accumulated therefore drains on the first run with NO SPECIAL": "// The backlog the retry-and-disable chapter accumulated therefore drains on the first run with NO SPECIAL",
" * else \u2014 3.6 wrote the reason down and this is the first code to depend on it.": " * else \u2014 the retry-and-disable chapter wrote the reason down and this is the first code to depend on it.",
"  /** Chapter 3.2, research R8: the environment's FIRST key, present only when": "  /** Research R8: the environment's FIRST key, present only when",
"  /** Chapter 3.24 (FR-001, FR-007). **REQUIRED, unlike `edited_at` below**, and the": "  /** (FR-001, FR-007). **REQUIRED, unlike `edited_at` below**, and the",
"   * 3.12). This was a plain insert too, and the members endpoint creates a user": "   * (the isolation gauntlet). This was a plain insert too, and the members endpoint creates a user",
"   * This was a plain insert until chapter 3.13, which is fine for a fixture and": "   * This was a plain insert until the channel-endpoints chapter, which is fine for a fixture and",
"   * caller its own is the two-lists-that-must-agree defect `gaps.md` 3.23-4 records about": "   * caller its own is the two-lists-that-must-agree defect `gaps.md` the revisions chapter-4 records about",
"      /** Chapter 3.17 (FR-002b). ABSENT MEANS \"NO CHANGE\", NOT \"PERSON\" \u2014 the column": "      /** (FR-002b). ABSENT MEANS \"NO CHANGE\", NOT \"PERSON\" \u2014 the column",
"   * 3.15, FR-013, FR-CHN-08).": "   * (the channel-control chapter, FR-013, FR-CHN-08).",
"      /** REQUIRED SINCE CHAPTER 3.17 (FR-MSG-15, FR-006), and required is the whole": "      /** REQUIRED SINCE THE SENDER CHAPTER (FR-MSG-15, FR-006), and required is the whole",
"      // the third of three copies of this sentence, all corrected in 3.23. A check gated on a parameter": "      // the third of three copies of this sentence, all corrected in the revisions chapter. A check gated on a parameter",
"      // ARCHIVE, AFTER VISIBILITY AND NOT BEFORE (chapter 3.15, FR-020, FR-021,": "      // ARCHIVE, AFTER VISIBILITY AND NOT BEFORE (FR-020, FR-021,",
"          /** Chapter 3.24 (FR-015, FR-016). THIS READ CHANGES, AND T053's LIST OF FOUR": "          /** (FR-015, FR-016). THIS READ CHANGES, AND T053's LIST OF FOUR",
"      // `gaps.md` 3.23-3 recorded the opposite \u2014 *\"both interleavings end in a": "      // `gaps.md` the revisions chapter-3 recorded the opposite \u2014 *\"both interleavings end in a",
"    // A BOT IS EXEMPT FROM THE CEILING, AND THAT IS HALF OF IT (chapter 3.17, FR-018a,": "    // A BOT IS EXEMPT FROM THE CEILING, AND THAT IS HALF OF IT (FR-018a,",
"      /** Chapter 3.24 (FR-009). A CAST AND NOT A CHECK: `messages.attachments` is a bare": "      /** (FR-009). A CAST AND NOT A CHECK: `messages.attachments` is a bare",
"// 3.3 and is at the bottom of this file. `message_edits` ARRIVED IN 3.23 and": "// The outbox chapter and is at the bottom of this file. `message_edits` ARRIVED IN THE REVISIONS CHAPTER and",
"    // and SAD \u00a7338, read by nothing in seventeen chapters. Chapter 3.8": "    // and SAD \u00a7338, read by nothing in seventeen chapters. The rate-limit chapter",
"    // that may be lost and a quota that is money is the thing 3.8 is about.": "    // that may be lost and a quota that is money is the thing the rate-limit chapter is about.",
"    // (Deliberately not a chapter NUMBER: 3.7 renumbered quotas once already,": "    // (Deliberately not a chapter NUMBER: the deduplication chapter renumbered quotas once already,",
"// the way 2.1 recorded `members` and 3.1 recorded the tenancy containers.": "// the way 2.1 recorded `members` and the tenancy chapter recorded the tenancy containers.",
"    // Chapters 3.15 and 3.16 exist because five columns had no reader, so leaving a sixth": "    // the channel-control chapter and the user-surface chapter exist because five columns had no reader, so leaving a sixth",
"// derivation, recorded here the way 2.1 recorded `members`, 3.2 recorded": "// derivation, recorded here the way 2.1 recorded `members`, the credentials chapter recorded",
"// `api_keys` and 3.3 recorded the outbox's index.": "// `api_keys` and the outbox chapter recorded the outbox's index.",
"// 3.1 learned it again on signup.": "// The tenancy chapter learned it again on signup.",
"    // off the floor: it has crossed the internal seam on every attempt since 3.5": "    // off the floor: it has crossed the internal seam on every attempt since the webhook dispatcher chapter",
"// Chapter 3.10 \u2014 monthly usage quotas (FR-RTL-05 to FR-RTL-08).": "// Monthly usage quotas (FR-RTL-05 to FR-RTL-08).",
"// 3.8 was offered it for rate-limit policy and refused it in prose, on the": "// The rate-limit chapter was offered it for rate-limit policy and refused it in prose, on the",
"// \u2500\u2500 chapter 3.18: the failure path (Phase 6) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "// \u2500\u2500 the failure path (Phase 6) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"    // T018h, chapter 3.23. THE SUBJECT IS THE ASSERTION. A `publishRevision` that reached": "    // THE SUBJECT IS THE ASSERTION. A `publishRevision` that reached",
"// DECISION (chapter 2.5, narrowed by 3.2): these routes are still": "// DECISION (chapter 2.5, narrowed by the credentials chapter): these routes are still",
"  // \u2500\u2500 THE BACKSTOP'S ROUTE (chapter 3.20, FR-017) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "  // \u2500\u2500 THE BACKSTOP'S ROUTE (FR-017) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"  // \u2500\u2500 THE SOCKET'S ROUTE INHERITS THE CHECK (chapter 3.15, FR-001) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "  // \u2500\u2500 THE SOCKET'S ROUTE INHERITS THE CHECK (FR-001) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"// 3.11, constitution IV).": "// (the connection-metering chapter, constitution IV).",
"      // Four fields, as every refusal on this contract has since chapter 3.8.": "      // Four fields, as every refusal on this contract has since the rate-limit chapter.",
"  // \u2500\u2500 Chapter 3.12's own two routes, attacked on the build that added them \u2500\u2500\u2500\u2500": "  // \u2500\u2500 This suite's own two routes, attacked on the build that added them \u2500\u2500\u2500\u2500\u2500",
"  // \u2500\u2500 THE SAME-TENANT NON-MEMBER (chapter 3.15, FR-034, SC-015) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "  // \u2500\u2500 THE SAME-TENANT NON-MEMBER (FR-034, SC-015) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"  // \u2500\u2500 THE IDENTIFIER COLLISION (chapter 3.15, FR-034a) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "  // \u2500\u2500 THE IDENTIFIER COLLISION (FR-034a) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"  // feature. Chapters 3.15 and 3.16 add fourteen routes, taking it to 38, and": "  // feature. the channel-control chapter and the user-surface chapter add fourteen routes, taking it to 38, and",
"  // **AND AGAIN IN 3.23, IN BOTH DIRECTIONS AT ONCE.** That chapter declared its two": "  // **AND AGAIN IN THE REVISIONS CHAPTER, IN BOTH DIRECTIONS AT ONCE.** That chapter declared its two",
"  // \u2500\u2500 read, internal, end-user token (chapter 3.20) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "  // \u2500\u2500 read, internal, end-user token \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"    // unstructured, unbounded output \u2014 rather than process death, which 3.18": "    // unstructured, unbounded output \u2014 rather than process death, which the fan-out chapter",
" * since chapter 3.17, rather than inherited from `credential.guard.ts`'s `EITHER`": " * since the sender chapter, rather than inherited from `credential.guard.ts`'s `EITHER`",
" * THIS COMMENT SAID SOMETHING ELSE UNTIL CHAPTER 3.17, and what it said was the reading": " * THIS COMMENT SAID SOMETHING ELSE UNTIL THE SENDER CHAPTER, and what it said was the reading",
" * 3.23 added two more publish sites to this file, each with its own": " * The revisions chapter added two more publish sites to this file, each with its own",
"// 3.24 had to be told to carry across. What keeps them together is a shared": "// The attachments chapter had to be told to carry across. What keeps them together is a shared",
"    // PAST TENSE SINCE CHAPTER 3.17, and it took until 3.23 to say so. That chapter": "    // PAST TENSE SINCE THE SENDER CHAPTER, and it took until the revisions chapter to say so. That chapter",
"    // \u2500\u2500 the live fan-out (chapter 3.18, FR-004) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "    // \u2500\u2500 the live fan-out (FR-004) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"      /** Chapter 3.24 (FR-001), and SPELLED rather than spread for the reason this": "      /** (FR-001), and SPELLED rather than spread for the reason this",
"    // \u2500\u2500 the live fan-out (chapter 3.23, FR-005, ADR-24) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "    // \u2500\u2500 the live fan-out (FR-005, ADR-24) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"    // \u2500\u2500 the live fan-out (chapter 3.23, FR-007, FR-009, ADR-24) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "    // \u2500\u2500 the live fan-out (FR-007, FR-009, ADR-24) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"      // 3.23 shipped a control test that was green before its field existed for exactly": "      // the revisions chapter shipped a control test that was green before its field existed for exactly",
"      // work out which key it was about \u2014 chapter 3.14's whole subject, and the reason": "      // work out which key it was about \u2014 the error-registry chapter's whole subject, and the reason",
"  // \u2500\u2500 THE ROUTE A CUSTOMER'S CLIENT ACTUALLY CALLS (chapter 3.15, FR-001) \u2500\u2500\u2500\u2500\u2500\u2500\u2500": "  // \u2500\u2500 THE ROUTE A CUSTOMER'S CLIENT ACTUALLY CALLS (FR-001) \u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"  // so the guard fell back to `EITHER` and a user token was accepted here. Chapter 3.17": "  // so the guard fell back to `EITHER` and a user token was accepted here. The sender chapter",
"  // declared it; this sentence went on describing its absence until 3.23.": "  // declared it; this sentence went on describing its absence until the revisions chapter.",
"        // here. Middleware runs earlier still, which is why chapter 3.2": "        // here. Middleware runs earlier still, which is why the credentials chapter",
" * attachments field to justify empty text. 3.24 separated them into two literals; this": " * attachments field to justify empty text. The attachments chapter separated them into two literals; this",
" * This read `sendMessageBodySchema.shape.text` until chapter 3.24, which was correct": " * This read `sendMessageBodySchema.shape.text` until the attachments chapter, which was correct",
"        // Chapter 3.24 (FR-001). Spelled rather than spread, like every field above": "        // (FR-001). Spelled rather than spread, like every field above",
" * 3.2). It used to carry a headers bag, because the tenant arrived as an": " * The credentials chapter). It used to carry a headers bag, because the tenant arrived as an",
"      // WHICH FIELD, and chapter 3.14 is where that stopped being optional.": "      // WHICH FIELD, and the error-registry chapter is where that stopped being optional.",
"      // and first sent in 3.8: a field in the contract that the code never filled.": "      // and first sent in the rate-limit chapter: a field in the contract that the code never filled.",
"  // eight and FIVE exist since chapter 3.23; the other three arrive with the features": "  // eight and FIVE exist since the revisions chapter; the other three arrive with the features",
"    // AND WHICH ARE UNBUILT, by name. This is the half the review and `gaps.md` 3.23-1": "    // AND WHICH ARE UNBUILT, by name. This is the half the review and `gaps.md` the revisions chapter-1",
"    // ONE LIST, NOT TWO. `gaps.md` 3.23-4 records the two-lists-that-must-agree defect": "    // ONE LIST, NOT TWO. `gaps.md` the revisions chapter-4 records the two-lists-that-must-agree defect",
"    // SEVEN SINCE CHAPTER 3.24, and both events gained the field together because both": "    // SEVEN SINCE THE ATTACHMENTS CHAPTER, and both events gained the field together because both",
"  /** Chapter 3.24 (FR-015, FR-017). ON `message.created` AND `message.updated` AT ONCE,": "  /** (FR-015, FR-017). ON `message.created` AND `message.updated` AT ONCE,",
" * 3.23-4 records it about `targets.ts`, and `eslint.config.mjs`'s own comment says *MUST": " * The revisions chapter-4 records it about `targets.ts`, and `eslint.config.mjs`'s own comment says *MUST",
" * subscriptions name `channel.created`**. The review and `gaps.md` 3.23-1 both recommend": " * subscriptions name `channel.created`**. The review and `gaps.md` the revisions chapter-1 both recommend",
"      // Chapter 3.24 (FR-015). BOTH BRANCHES, and restated rather than shared for the": "      // (FR-015). BOTH BRANCHES, and restated rather than shared for the",
" * Had 3.3 taken memory storage as a convenience, applying this configuration": " * Had the outbox chapter taken memory storage as a convenience, applying this configuration",
" * `duplicate_window` is deliberately left where 3.3 found it. Raising it looks": " * `duplicate_window` is deliberately left where the outbox chapter found it. Raising it looks",
"  // Retention and storage are immutable on an existing stream \u2014 chapter 3.4": "  // Retention and storage are immutable on an existing stream \u2014 the broker chapter",
"    // TYPED AS `ErrorCode` (chapter 3.14, FR-025). The ladder emitted five codes": "    // TYPED AS `ErrorCode` (the error-registry chapter, FR-025). The ladder emitted five codes",
"/** An HTTP failure that NAMES ITS OWN CODE, typed (chapter 3.14, FR-025, FR-026).": "/** An HTTP failure that NAMES ITS OWN CODE, typed (the error-registry chapter, FR-025, FR-026).",
"    // be switched off deliberately (FR-RTL-06). This is the distinction chapter 3.8": "    // be switched off deliberately (FR-RTL-06). This is the distinction the rate-limit chapter",
"    // THIS TEST NAMED `connection_minutes` UNTIL CHAPTER 3.11, with the comment": "    // THIS TEST NAMED `connection_minutes` UNTIL THE CONNECTION-METERING CHAPTER, with the comment",
"/** The dimensions a quota is measured in. `connection_minutes` is chapter 3.11. */": "/** The dimensions a quota is measured in. `connection_minutes` is the connection-metering chapter's. */",
"// Chapter 3.10, User Story 1 \u2014 the month is counted, and the count survives.": "// User Story 1 \u2014 the month is counted, and the count survives.",
"   * fault this whole lane has been recording since chapter 3.3. */": "   * fault this whole lane has been recording since the outbox chapter. */",
"    // unattributed by design since chapter 3.3, which FR-MSG-15 reverses. There is no": "    // unattributed by design since the outbox chapter, which FR-MSG-15 reverses. There is no",
"// Chapter 3.10, User Story 2 \u2014 running out is predictable.": "// User Story 2 \u2014 running out is predictable.",
"// Chapter 3.10, User Story 3 \u2014 nobody is surprised.": "// User Story 3 \u2014 nobody is surprised.",
"    // fourth message clears 80% and 100% in one step, because 80% of 4 is 3.2": "    // fourth message clears 80% and 100% in one step, because 80% of 4 is the credentials chapter",
"    // `originalUrl` first, and this line was WRONG from chapter 2.2 until 3.8.": "    // `originalUrl` first, and this line was WRONG from chapter 2.2 until the rate-limit chapter.",
"// the seam it guards is untouched by this chapter (3.2 retires it).": "// the seam it guards is untouched by this chapter (the credentials chapter retires it).",
"// Part 5. The callback reports what it created \u2014 and, from 3.2, hands over the": "// Part 5. The callback reports what it created \u2014 and, from the credentials chapter, hands over the",
" * users. Users outnumber channels 3.4:1 here and the ratio only grows \u2014 a channel is a": " * users. Users outnumber channels the broker chapter:1 here and the ratio only grows \u2014 a channel is a",
" * (chapter 3.14 gave every validation error its field). Decoding it downstream would": " * (the error-registry chapter gave every validation error its field). Decoding it downstream would",
"// The attempt record, on its way to the analytical path (chapter 3.6, FR-001,": "// The attempt record, on its way to the analytical path (FR-001,",
"    // 3.5 recorded an attempt by moving the delivery and discarded the answer;": "    // The webhook dispatcher chapter recorded an attempt by moving the delivery and discarded the answer;",
"   * FOUND AT CHAPTER 3.7'S POST-FIX MEASUREMENT, on run 2 of 20: \"expected null": "   * FOUND AT THE DEDUPLICATION CHAPTER'S POST-FIX MEASUREMENT, on run 2 of 20: \"expected null",
"    // occurrence of the fault this lane has been recording since chapter 3.3, and": "    // occurrence of the fault this lane has been recording since the outbox chapter, and",
"    // Hashing is what 3.2 does to an API key, and it is one-way on purpose. The": "    // Hashing is what the credentials chapter does to an API key, and it is one-way on purpose. The",
"    // 256 bits, the same budget 3.2 gave an API key secret.": "    // 256 bits, the same budget the credentials chapter gave an API key secret.",
"const SECRET_BYTES = 32; // 256 bits, the budget 3.2 gave an API key secret": "const SECRET_BYTES = 32; // 256 bits, the budget the credentials chapter gave an API key secret",
"    // has been open since chapter 3.5.": "    // has been open since the webhook dispatcher chapter.",
"// The same wiring chapter 2.2 established and 3.2 re-pointed: the repository is": "// The same wiring chapter 2.2 established and the credentials chapter re-pointed: the repository is",
"   * THE REVIEW AND `gaps.md` 3.23-1 BOTH RECOMMEND `OUTBOX_EVENT_TYPES`, AND BOTH ARE": "   * THE REVIEW AND `gaps.md` the revisions chapter-1 BOTH RECOMMEND `OUTBOX_EVENT_TYPES`, AND BOTH ARE",
"// gateway's socket suite made in 3.2 and for the same reason. The dispatcher's": "// gateway's socket suite made in the credentials chapter and for the same reason. The dispatcher's",
"    // environments, 2.6 for subjects, 3.4 for its own durables.": "    // environments, 2.6 for subjects, the broker chapter for its own durables.",
"   * \"send failed\". Chapter 3.14 built thirteen codes and one registry precisely so a": "   * \"send failed\". The error-registry chapter built thirteen codes and one registry precisely so a",
"// The door (chapter 2.5, rebuilt by 3.2). Tokens are still checked BEFORE the": "// The door (chapter 2.5, rebuilt by the credentials chapter). Tokens are still checked BEFORE the",
"// CHAPTER 3.22 \u2014 FR-RTM-09's five-connection cap.": "// FR-RTM-09's five-connection cap.",
"  /** Chapter 3.22, T050a. **ONE MODULE PER INSTANCE, BUILT HERE**, the way": "  /** T050a. **ONE MODULE PER INSTANCE, BUILT HERE**, the way",
"   * needs the lines. Chapter 3.18: a publisher that does nothing satisfies \"the": "   * needs the lines. The fan-out chapter's finding: a publisher that does nothing satisfies \"the",
"// \"does this work without infrastructure\". `gaps.md` 3.23-9 carried that from the day it": "// \"does this work without infrastructure\". `gaps.md` the revisions chapter-9 carried that from the day it",
"// correct behaviour, and `gaps.md` 3.23-9 has carried that since it was found by": "// correct behaviour, and `gaps.md` the revisions chapter-9 has carried that since it was found by",
"   * would duplicate that counting and add two more Redis clients to a service chapter 3.21": "   * would duplicate that counting and add two more Redis clients to a service the typing chapter",
" * this lane has spawned a live api child since chapter 3.2 \u2014 so the gateway runs": " * this lane has spawned a live api child since the credentials chapter \u2014 so the gateway runs",
" * 2.8 opened and 3.2 widened, for the reason it gave: there is no admin API for": " * 2.8 opened and the credentials chapter widened, for the reason it gave: there is no admin API for",
"      // two unrelated suites into a race (chapters 3.3 and 3.8).": "      // two unrelated suites into a race (the outbox chapter and the rate-limit chapter).",
"  // until 09:00 tomorrow. The session route reads `banned_at` and, until chapter 3.17,": "  // until 09:00 tomorrow. The session route reads `banned_at` and, until the sender chapter,",
"  // \u2500\u2500 A REMOVED MEMBER'S RECONNECTION (chapter 3.15, SC-004) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "  // \u2500\u2500 A REMOVED MEMBER'S RECONNECTION (SC-004) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"  //      STILL TRUE OF THIS SUITE after chapter 3.18. That chapter gave the api a": "  //      STILL TRUE OF THIS SUITE after the fan-out chapter. That chapter gave the api a",
"  // \u2500\u2500 AN ARCHIVED CHANNEL AND THE SOCKET (chapter 3.16, FR-022a) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "  // \u2500\u2500 AN ARCHIVED CHANNEL AND THE SOCKET (FR-022a) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"  // \"the api publishes to no fan-out\", which was the platform-wide truth chapter 3.12": "  // \"the api publishes to no fan-out\", which was the platform-wide truth the isolation gauntlet",
"  // \u2500\u2500 THE SAME-TENANT NON-MEMBER, ON THE SOCKET (chapter 3.15, T087) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "  // \u2500\u2500 THE SAME-TENANT NON-MEMBER, ON THE SOCKET (T087) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"  // CHAPTER 3.21, and the second inbound frame in twenty chapters. It carries no": "  // The typing chapter, and the second inbound frame in twenty chapters. It carries no",
"  [\"typing.send\", \"inbound\", \"chapter 3.21: a client may say it is typing (session.ts)\"],": "  [\"typing.send\", \"inbound\", \"a client may say it is typing (session.ts)\"],",
"    // too**, and updating the assertion without the title is how chapter 3.19": "    // too**, and updating the assertion without the title is how the presence chapter",
"  // CHAPTER 3.22, T042b. THE SHUTDOWN SET, NAMED EXPLICITLY AND FAILING ON AN": "  // T042b. THE SHUTDOWN SET, NAMED EXPLICITLY AND FAILING ON AN",
"  // THIS IS CHAPTER 3.21'S DEFECT INVERTED. There, awaiting `close()` made lint": "  // THIS IS THE TYPING CHAPTER'S DEFECT INVERTED. There, awaiting `close()` made lint",
"    // crosses the boundary as data (chapter 3.14, FR-027, R9).": "    // crosses the boundary as data (the error-registry chapter, FR-027, R9).",
"    // CHAPTER 3.21, AND THIS LINE HAD NO OWNER UNTIL THE SEALED CLIENT ASKED FOR": "    // THE TYPING CHAPTER, AND THIS LINE HAD NO OWNER UNTIL THE SEALED CLIENT ASKED FOR",
"// Chapter 3.20, phase 3 \u2014 the fabric, and ONLY the fabric.": "// Phase 3 \u2014 the fabric, and ONLY the fabric.",
"    // Close code 4009 exists and this is not it, which is the refusal chapter 3.8": "    // Close code 4009 exists and this is not it, which is the refusal the rate-limit chapter",
"    // **CHAPTER 3.19'S `gaps.md` ITEM 2, CLOSING.** Presence subscriptions were": "    // **The presence chapter'S `gaps.md` ITEM 2, CLOSING.** Presence subscriptions were",
"// CHAPTER 3.19, PHASE 1 \u2014 THE FAILING STATE, OBSERVED.": "// PHASE 1 \u2014 THE FAILING STATE, OBSERVED.",
"// 3.18 recorded that trap against its own publisher: its `publish` swallows errors": "// the fan-out chapter recorded that trap against its own publisher: its `publish` swallows errors",
"  // 3.18 measured that against ioredis 6.0.0 by reproducing the exact client, and": "  // the fan-out chapter measured that against ioredis 6.0.0 by reproducing the exact client, and",
"  // here because chapter 3.14's exit criterion is that an outsider integrates on": "  // here because the outsider milestone's exit criterion is that an outsider integrates on",
"    // LIVE DELIVERY, WHICH THIS BLOCK ASSERTED WAS ABSENT UNTIL CHAPTER 3.18.": "    // LIVE DELIVERY, WHICH THIS BLOCK ASSERTED WAS ABSENT UNTIL THE FAN-OUT CHAPTER.",
"  it(\"chapter 3.23: replays an edited message as message.created with its current text, and no message.updated\", async () => {": "  it(\"replays an edited message as message.created with its current text, and no message.updated\", async () => {",
"// \u2500\u2500 chapter 3.18: two instances, one fabric (US2) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "// \u2500\u2500 two instances, one fabric (US2) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"// \u2500\u2500 chapter 3.18: the same harness, WITH a fan-out \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "// \u2500\u2500 the same harness, WITH a fan-out \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"      // until 3.23 gave the frame a payload of its own with no text and a": "      // until the revisions chapter gave the frame a payload of its own with no text and a",
"  /** CHAPTER 3.21, T034 \u2014 T009 INVERTED, and the same shape on purpose.": "  /** T034 \u2014 T009 INVERTED, and the same shape on purpose.",
"  // CHAPTER 3.22, T011. RED ON PURPOSE, and the phase commit says so.": "  // T011. RED ON PURPOSE, and the phase commit says so.",
"  it(\"chapter 3.7: a frame at the mark, arriving after the resume, is not delivered\", async () => {": "  it(\"a frame at the mark, arriving after the resume, is not delivered\", async () => {",
"  it(\"chapter 3.7: a frame above the mark, arriving after the resume, IS delivered\", async () => {": "  it(\"a frame above the mark, arriving after the resume, IS delivered\", async () => {",
"  it(\"chapter 3.7: a connection that never resumed suppresses nothing\", async () => {": "  it(\"a connection that never resumed suppresses nothing\", async () => {",
"  // \u2500\u2500 chapter 3.23: the revision fabric reaches a socket \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "  // \u2500\u2500 the revision fabric reaches a socket \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"  it(\"chapter 3.23: an edit on the fabric arrives as message.updated, not message.created\", async () => {": "  it(\"an edit on the fabric arrives as message.updated, not message.created\", async () => {",
"  it(\"chapter 3.23: a deletion arrives as message.deleted, with no text on it\", async () => {": "  it(\"a deletion arrives as message.deleted, with no text on it\", async () => {",
"  it(\"chapter 3.23: a buffering connection is sent no revision at all\", async () => {": "  it(\"a buffering connection is sent no revision at all\", async () => {",
"   * chapter 3.22 \u2014 it parses `main.ts` and asserts every module it builds is closed \u2014": "   * It parses `main.ts` and asserts every module it builds is closed \u2014",
"/** `ErrorCode`, not `string` (chapter 3.14, FR-025). Every code this function is": "/** `ErrorCode`, not `string` (the error-registry chapter, FR-025). Every code this function is",
"   * chapter 3.14, whose comment cites THIS schema while fixing only its own side.": "   * The error-registry chapter, whose comment cites THIS schema while fixing only its own side.",
"  /** A membership change arriving from its own fabric (chapter 3.20, FR-RTM-05,": "  /** A membership change arriving from its own fabric (FR-RTM-05,",
"          // Chapter 3.21 (T043a). **Without this a user added mid-connection": "          // (T043a). **Without this a user added mid-connection",
"        // Chapter 3.21 (T043b). A revoked channel leaves `channelIds`, and its": "        // (T043b). A revoked channel leaves `channelIds`, and its",
"      // CHAPTER 3.22, T037. THE CAP IS CHECKED HERE, after `authenticate` and": "      // T037. THE CAP IS CHECKED HERE, after `authenticate` and",
"          // Chapter 3.11, and this is the 4001 path's SHAPE for the 4001 path's": "          // The connection-metering chapter, and this is the 4001 path's SHAPE for the 4001 path's",
"        // Chapter 3.20, and the third. Without this line the membership fabric has": "        // The membership-revocation chapter, and the third. Without this line the membership fabric has",
"        // Chapter 3.21, and the fourth. A channel now carries four subscriptions": "        // The typing chapter, and the fourth. A channel now carries four subscriptions",
"    // CHAPTER 3.22, T040 and T040a. ONE RENEWAL TIMER PER CONNECTION, and it is": "    // T040 and T040a. ONE RENEWAL TIMER PER CONNECTION, and it is",
"      // Chapter 3.11, and the ORDER MATTERS. The meter is told first, because": "      // The connection-metering chapter, and the ORDER MATTERS. The meter is told first, because",
"      // CHAPTER 3.22, T041. The timer first, then the place. An interval that": "      // T041. The timer first, then the place. An interval that",
"      // Chapter 3.19, AND THIS HANDLER NOW CARRIES THREE ORDERING CONSTRAINTS, not": "      // THE PRESENCE CHAPTER, AND THIS HANDLER NOW CARRIES THREE ORDERING CONSTRAINTS, not",
"   * which chapters 3.7, 3.8, 3.11 and 3.19 all fence \u2014 a field there is four": "   * which the deduplication chapter, the rate-limit chapter, the connection-metering chapter and the presence chapter all fence \u2014 a field there is four",
"  /** CHAPTER 3.21 (T033): a client's typing signal, on its way out.": "  /** (T033): a client's typing signal, on its way out.",
"      // \u2500\u2500 THE API'S OWN REFUSAL, FORWARDED (chapter 3.15) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "      // \u2500\u2500 THE API'S OWN REFUSAL, FORWARDED \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"      // `codes.ts` does not define, which is the thing chapter 3.14's registry exists to": "      // `codes.ts` does not define, which is the thing the error-registry chapter's registry exists to",
"    // a describe-level teardown that closed servers first cost chapter 3.20": "    // a describe-level teardown that closed servers first cost the membership-revocation chapter",
"   * by two clocks, the shape that stranded a user online for ever in 3.19. */": "   * by two clocks, the shape that stranded a user online for ever in the presence chapter. */",
"      // MEMBERSHIP, FOR A PRIVATE CHANNEL, WHEN A USER IS SENDING (      // FR-001, FR-CHN-05).": "      // MEMBERSHIP, FOR A PRIVATE CHANNEL, WHEN A USER IS SENDING (FR-001, FR-CHN-05).",
"      // Nor the notification relay, for the same reason.": "      // The rate-limit chapter: nor the notification relay, for the same reason.",
"# (chapter 3.3's finding 4). `--profile services` runs the platform instead.": "# (the outbox chapter's finding 4). `--profile services` runs the platform instead.",
"    # Chapter 3.8. An SMTP server that accepts everything and delivers nothing,": "    # An SMTP server that accepts everything and delivers nothing,",
"      # Chapter 3.8. Container names, not localhost \u2014 the api's own default is": "      # Container names, not localhost \u2014 the api's own default is",
"      # Chapter 3.11: the gateway's first credential of its own. Every other": "      # The gateway's first credential of its own. Every other",
"        \"The counter store lives in services/api/src/limits and services/gateway/src/limits.ts only (constitution I, chapter 3.8). Its keys are per environment; an unrestricted client is a cross-tenant read.\",": "        \"The counter store lives in services/api/src/limits and services/gateway/src/limits.ts only (constitution I). Its keys are per environment; an unrestricted client is a cross-tenant read.\",",
"  // Chapter 3.17. THE SUBJECT IS A ROW NO REPOSITORY METHOD CAN WRITE ANY MORE, which": "  // THE SUBJECT IS A ROW NO REPOSITORY METHOD CAN WRITE ANY MORE, which",
"  // any deployment older than chapter 3.17 has them \u2014 can only be planted by hand. The": "  // any deployment older than the sender chapter has them \u2014 can only be planted by hand. The",
"  // Chapter 3.18. THE SAME ARGUMENT AS THE TWO LIMITS SUITES: its subject is what": "  // THE SAME ARGUMENT AS THE TWO LIMITS SUITES: its subject is what",
"  // Chapter 3.19, and it is 3.18's argument in the other direction. The presence": "  // The presence chapter, and it is the fan-out chapter's argument in the other direction. The presence",
"  // Chapter 3.20's, for that same reason and on THIS list rather than the `**/*.ts`": "  // The membership-revocation chapter's, for that same reason and on THIS list rather than the `**/*.ts`",
"  // Chapter 3.21, and the same case as the two above: the assertion is on Redis,": "  // The typing chapter, and the same case as the two above: the assertion is on Redis,",
"  // chapter 3.18's warning, in a new place.": "  // The fan-out chapter's warning, in a new place.",
"  // Chapter 3.22, and NOT for the reason the four above give. This file needs no": "  // The connection-cap chapter, and NOT for the reason the four above give. This file needs no",
"        // Chapter 3.11 added this one, and chapter 3.10 should have.": "        // The connection-metering chapter added this one, and the quota chapter should have.",
"    // Chapter 3.8 added the SECOND per-tenant store and the same argument": "    // The rate-limit chapter added the SECOND per-tenant store and the same argument",
"      // Chapter 3.18. THE RULE'S REASON DOES NOT APPLY HERE, and that is the": "      // THE RULE'S REASON DOES NOT APPLY HERE, and that is the",
"      // Chapter 3.20, AND IT IS THE ENTRY ABOVE'S CASE RATHER THAN THE LIMITER'S.": "      // THE MEMBERSHIP-REVOCATION CHAPTER, AND IT IS THE ENTRY ABOVE'S CASE RATHER THAN THE LIMITER'S.",
"      // Chapter 3.19. THIS IS `limits.ts`'s CASE, NOT `fanout.ts`'s, and the": "      // THIS IS `limits.ts`'s CASE, NOT `fanout.ts`'s, and the",
"      // Chapter 3.20, AND IT IS THE FAN-OUT'S CASE RATHER THAN PRESENCE'S \u2014 the": "      // THE MEMBERSHIP-REVOCATION CHAPTER, AND IT IS THE FAN-OUT'S CASE RATHER THAN PRESENCE'S \u2014 the",
"      // Chapter 3.21, AND IT IS THE FAN-OUT'S CASE \u2014 the cleanest of the four, and": "      // THE TYPING CHAPTER, AND IT IS THE FAN-OUT'S CASE \u2014 the cleanest of the four, and",
"      // THE `.itest.ts` FILE IS NOT LISTED HERE. Chapter 3.20 put an `.itest.ts`": "      // THE `.itest.ts` FILE IS NOT LISTED HERE. THE MEMBERSHIP-REVOCATION CHAPTER put an `.itest.ts`",
"      // Chapter 3.22's connection registry, and its keys are the strongest case on": "      // The connection-cap chapter's connection registry, and its keys are the strongest case on",
"    // naming the local infrastructure while every test still passed. Chapter": "    // naming the local infrastructure while every test still passed.",
"-- NINE, NOT FIVE, AS OF CHAPTER 3.12 (FR-036). The four usage tables were added": "-- NINE, NOT FIVE, AS OF THE ISOLATION GAUNTLET (FR-036). The four usage tables were added",
"-- TEN AS OF CHAPTER 3.16. `read_positions` carries `environment_id`, so it belongs": "-- TEN AS OF THE USER-SURFACE CHAPTER. `read_positions` carries `environment_id`, so it belongs",
"    -- Chapters 3.10 and 3.11's tables. Every one carries `environment_id`, which": "    -- Chapters 3.10 and the connection-metering chapter's tables. Every one carries `environment_id`, which",
"    -- Chapter 3.16's table. Per-user read positions, keyed": "    -- The user-surface chapter's table. Per-user read positions, keyed",
"// Chapter 3.16, T019 \u2014 and it is a SCRIPT and not part of migration 0011 for one": "// T019 \u2014 and it is a SCRIPT and not part of migration 0011 for one",
"// The chapter 3.4 walk: a redelivery, made to happen on purpose.": "// The the broker chapter walk: a redelivery, made to happen on purpose.",
"  // The claim and the effect commit together (chapter 3.4). After this line the": "  // The claim and the effect commit together. After this line the",
"// The chapter 3.2 walk, as a script (so the transcript in the chapter is": "// The the credentials chapter walk, as a script (so the transcript in the chapter is",
"// The chapter 3.3 walk: the dual-write problem, and the fix, run side by side.": "// The the outbox chapter walk: the dual-write problem, and the fix, run side by side.",
"// DECISION (chapter 3.3): the naive publish-after-commit path lives HERE and in": "// DECISION: the naive publish-after-commit path lives HERE and in",
"// A customer's webhook endpoint, behaving badly on purpose (chapter 3.5).": "// A customer's webhook endpoint, behaving badly on purpose.",
"// noticing \u2014 3.3's dual-write walk and 3.4's consumer walk made the same": "// noticing \u2014 the outbox chapter's dual-write walk and the broker chapter's consumer walk made the same",
"    // The line a parent process watches for. Chapter 3.4's walk established the": "    // The line a parent process watches for. The broker chapter's walk established the",
"// nothing could clear it. Chapter 3.24's close-out found the lane holding 56,193": "// nothing could clear it. The attachments chapter's close-out found the lane holding 56,193",
" *  rows are seconds old; the debris chapter 3.24 found went back nine days. */": " *  rows are seconds old; the debris the attachments chapter found went back nine days. */",
"// the guard refused an unnamed attempt during chapter 3.24's close-out because the": "// the guard refused an unnamed attempt during the attachments chapter's close-out because the",
"// deferring. Creating an organisation is the sign-up flow's job (chapter 3.4), and": "// deferring. Creating an organisation is the sign-up flow's job, and",
"// can complete. Minting a key is the dashboard's job, which chapter 3.2 deferred": "// can complete. Minting a key is the dashboard's job, which the credentials chapter deferred",
"// minted: the row keeps a hash, by design (chapter 3.2), so there is nothing to": "// minted: the row keeps a hash, by design, so there is nothing to",
"// Chapter 3.1's walk: sign up, and then sign up again.": "// The tenancy chapter's walk: sign up, and then sign up again.",
"    // Chapter 3.2: the first API key, shown exactly once. Truncated here \u2014": "    // The first API key, shown exactly once. Truncated here \u2014",
"// Chapter 3.2: nothing outside the api can sign a token, so this walk gets one": "// Nothing outside the api can sign a token, so this walk gets one",
"// (chapter 3.4). The chapter quotes the broker, not the config file \u2014 a": "//. The chapter quotes the broker, not the config file \u2014 a",
"// The stream is an ARGUMENT as of chapter 3.6, defaulting to the one stream that": "// The stream is an ARGUMENT as of the retry-and-disable chapter, defaulting to the one stream that",
"// existed when this was written. It had `\"EVENTS\"` in three places, and 3.6's": "// existed when this was written. It had `\"EVENTS\"` in three places, and the retry-and-disable chapter's",
"// The chapter 3.5 walk: one event, all the way to a customer's server.": "// The the webhook dispatcher chapter walk: one event, all the way to a customer's server.",
"-- Chapter 3.1 \u2014 the tenancy hierarchy (FR-TEN-01/02/03/04/07).": "-- The tenancy hierarchy (FR-TEN-01/02/03/04/07).",
"-- Chapter 3.2 \u2014 API keys (FR-AUT-01\u202605, NFR-SEC-02).": "-- API keys (FR-AUT-01\u202605, NFR-SEC-02).",
"-- Chapter 3.3 \u2014 the transactional outbox (ADR-06).": "-- The transactional outbox (ADR-06).",
"-- webhook delivery in chapter 3.5.": "-- webhook delivery in the webhook dispatcher chapter.",
"-- Chapter 3.4 \u2014 the consumer deduplication ledger (SAD risk R5).": "-- The consumer deduplication ledger (SAD risk R5).",
"-- Chapter 3.5 \u2014 webhook endpoints, the delivery schedule, and dead letters.": "-- Webhook endpoints, the delivery schedule, and dead letters.",
"--     environments. Unlike 3.3's outbox and 3.4's consumed_events, these are": "--     environments. Unlike the outbox chapter's outbox and the broker chapter's consumed_events, these are",
"--     rows cost nothing to keep \u2014 the same shape as 3.3's outbox index;": "--     rows cost nothing to keep \u2014 the same shape as the outbox chapter's outbox index;",
"--     Chapter 3.2 reached the same conclusion for api_keys.revoked_at.": "--     The credentials chapter reached the same conclusion for api_keys.revoked_at.",
"-- Chapter 3.6 \u2014 the failure run, automatic disablement, and its notification.": "-- The failure run, automatic disablement, and its notification.",
"--     same shape as 3.3's outbox index and 3.5's delivery index;": "--     same shape as the outbox chapter's outbox index and the webhook dispatcher chapter's delivery index;",
"-- Chapter 3.8 \u2014 per-environment rate limit policy (FR-RTL-04, FR-RTL-04).": "-- Per-environment rate limit policy (FR-RTL-04, FR-RTL-04).",
"-- Chapter 3.10 \u2014 monthly usage quotas (FR-RTL-05 to FR-RTL-08).": "-- Monthly usage quotas (FR-RTL-05 to FR-RTL-08).",
"-- Chapter 3.8 built the per-minute limiter. This is the other half of FR-RTL and": "-- The rate-limit chapter built the per-minute limiter. This is the other half of FR-RTL and",
"-- SRS \u00a76.1, and read by nothing for eighteen chapters. Chapter 3.8 was offered it": "-- SRS \u00a76.1, and read by nothing for eighteen chapters. The rate-limit chapter was offered it",
"-- WHAT THE JSONB BUYS: chapter 3.11 adds connection-minutes and FR-MED-12 later": "-- WHAT THE JSONB BUYS: THE CONNECTION-METERING CHAPTER adds connection-minutes and FR-MED-12 later",
"-- Chapter 3.3 published events, 3.5 dispatched webhook deliveries, 3.9 sent": "-- The outbox chapter published events, 3.5 dispatched webhook deliveries, 3.9 sent",
"-- The claim predicate the relay drains on, matching chapter 3.9's shape.": "-- The claim predicate the relay drains on, matching the mail-transport chapter's shape.",
"-- Chapter 3.11 \u2014 connection-minutes, the third dimension of FR-RTL-05.": "-- Connection-minutes, the third dimension of FR-RTL-05.",
"-- Chapter 3.10 metered messages sent and distinct active users and stopped": "-- The quota chapter metered messages sent and distinct active users and stopped",
"-- The policy: the one-line change chapter 3.10 promised, priced.": "-- The policy: the one-line change the quota chapter promised, priced.",
"--     chapter 3.11 adds connection-minutes and FR-MED-12 later adds media": "--     The connection-metering chapter adds connection-minutes and FR-MED-12 later adds media",
"-- distinct connections instead \u2014 chapter 3.10 made exactly this trade for": "-- distinct connections instead \u2014 the quota chapter made exactly this trade for",
"-- No fifth table. Chapter 3.10 said \"four concrete tables that look alike is a": "-- No fifth table. The quota chapter said \"four concrete tables that look alike is a",
"-- Chapter 3.16 \u2014 ordering a user's channels, and knowing what they have not read.": "-- Ordering a user's channels, and knowing what they have not read.",
"-- Chapter 3.15 \u2014 a member's role, and a deleted user who is still an author.": "-- A member's role, and a deleted user who is still an author.",
"-- `members` has been `(channel_id, user_id, joined_at)` the whole time. Chapter": "-- `members` has been `(channel_id, user_id, joined_at)` the whole time.",
"-- 3.12's traceability map recorded the clause as delivered, described it with a": "-- The isolation gauntlet's traceability map recorded the clause as delivered, described it with a",
"-- paraphrase belonging to FR-CHN-06, and was corrected while chapter 3.15 was": "-- paraphrase belonging to FR-CHN-06, and was corrected while the channel-control chapter was",
"-- DEFAULT 'member', which is what lets chapter 3.13's `addMember` keep working": "-- DEFAULT 'member', which is what lets the channel-endpoints chapter's `addMember` keep working",
"-- Chapter 3.17 \u2014 the sender a message never had.": "-- The sender a message never had.",
"-- \"on behalf of any user\", satisfied by naming nobody. Chapter 3.3 decided that when": "-- \"on behalf of any user\", satisfied by naming nobody. The outbox chapter decided that when",
"-- which chapter 3.16 measured for `last_activity_at` on the same table.": "-- which the user-surface chapter measured for `last_activity_at` on the same table.",
"-- member (chapter 3.15), so each of these constraints names its siblings.": "-- member, so each of these constraints names its siblings.",
"-- Chapter 3.23 \u2014 what a message used to say.": "-- What a message used to say.",
"-- an edit is. Chapter 3.13 installed": "-- an edit is. The channel-endpoints chapter installed",
"   * Without this decorator every user's join would be a 403, which is chapter": "   * Without this decorator every user's join would be a 403, which is",
"  // single-user `DELETE` for ten analysis passes, having read \"the shape chapter": "  // single-user `DELETE` for ten analysis passes, having read \"the shape",
"    // PLANTED WITH RAW SQL, because no write path can produce one any more \u2014 chapter": "    // PLANTED WITH RAW SQL, because no write path can produce one any more \u2014",
"  /** IDEMPOTENT, for `createChannel`'s reason and found the same way (chapter": "  /** IDEMPOTENT, for `createChannel`'s reason and found the same way",
"   * request\" and FR-007 says the result is reported per user \u2014 which is chapter": "   * request\" and FR-007 says the result is reported per user \u2014 which is",
"  /** A user's channels, most recently active first, keyset-paginated (chapter": "  /** A user's channels, most recently active first, keyset-paginated",
"    // gives: a controller nobody registers is a route that does not exist, and chapter": "    // gives: a controller nobody registers is a route that does not exist, and",
"// The gateway's only road to a number it can see and cannot write (chapter": "// The gateway's only road to a number it can see and cannot write",
" * ONE FUNCTION AND NOT THREE COPIES, and the coverage ratchet is what asked. Chapter": " * ONE FUNCTION AND NOT THREE COPIES, and the coverage ratchet is what asked.",
"      // `expect(read.attachments).toEqual([])` when the value is undefined \u2014 chapter": "      // `expect(read.attachments).toEqual([])` when the value is undefined \u2014",
"      // therefore no attachment whatever the read path does with the column. Chapter": "      // therefore no attachment whatever the read path does with the column.",
"   * which is precisely the \"local facts about a global operation\" fault chapter": "   * which is precisely the \"local facts about a global operation\" fault",
"    // The state the test event and the sweep both read (data-model.md). Chapter": "    // The state the test event and the sweep both read (data-model.md).",
"      // Chapter 3.10's relay, the fourth. Same reason as the other three.": "      // The quota chapter's relay, the fourth. Same reason as the other three.",
"// The dispatcher's integration lane (chapter 3.5). Same convention 2.1": "// The dispatcher's integration lane. Same convention 2.1",
"    // seen from here \u2014 and the ownership half of it is the test below. Chapter": "    // seen from here \u2014 and the ownership half of it is the test below.",
"    // their own channels with themselves, so `linh` sees `linh` arrive. Chapter": "    // their own channels with themselves, so `linh` sees `linh` arrive.",
"// opened\" is true of a working presence path and of an empty function, and chapter": "// opened\" is true of a working presence path and of an empty function, and",
"  // `limits.ts` says a missing listener means \"the gateway would die\"; chapter": "  // `limits.ts` says a missing listener means \"the gateway would die\";",
"    // socket's credentials; a background loop draining a table that chapter": "    // socket's credentials; a background loop draining a table that",
"          // the cap was checked and satisfied or not checked at all. Chapter": "          // the cap was checked and satisfied or not checked at all.",
"    // AND `packages/outsider` FOR A DIFFERENT REASON, added in chapter 3.15's Phase 1.": "    // AND `packages/outsider` FOR A DIFFERENT REASON, added in the channel-control chapter's Phase 1.",
"    // Chapter 3.12 split the lanes so `pnpm test:integration` is": "    // The isolation gauntlet split the lanes so `pnpm test:integration` is",
"    // not design for \u2014 3.3's outbox suite learned that the hard way.": "    // not design for \u2014 the outbox chapter's outbox suite learned that the hard way.",
"      // `json` joins the other two for chapter 3.13's FR-040, which asks for every": "      // `json` joins the other two for the channel-endpoints chapter's FR-040, which asks for every",
"        // CHAPTER 3.5 RAISED THESE, and only after earning it. The webhook work": "        // THE WEBHOOK DISPATCHER CHAPTER RAISED THESE, and only after earning it. The webhook work",
"        // CHAPTER 3.6 RAISED THEM AGAIN, and the ratchet earned its keep twice on": "        // THE RETRY-AND-DISABLE CHAPTER RAISED THEM AGAIN, and the ratchet earned its keep twice on",
"        // CHAPTER 3.12 DID NOT RAISE THIS, and the number says why. The chapter": "        // THE ISOLATION GAUNTLET DID NOT RAISE THIS, and the number says why. The chapter",
"        // CHAPTER 3.15 RAISED IT, which is the first time this file's branch ratchet has": "        // THE CHANNEL-CONTROL CHAPTER RAISED IT, which is the first time this file's branch ratchet has",
"        // **89.51% \u2192 92.11%**. Chapter 3.5's precedent was the opposite: six operations": "        // **89.51% \u2192 92.11%**. The webhook dispatcher chapter's precedent was the opposite: six operations",
"        // which is the thin margin chapter 3.12 declined to pin against; 91 locks in most": "        // which is the thin margin the isolation gauntlet declined to pin against; 91 locks in most",
"          // 91 -> 92 (chapter 3.17). Measured 92.59 after this feature, which is 0.59 of": "          // 91 -> 92. Measured 92.59 after this feature, which is 0.59 of",
"          // headroom \u2014 the same margin chapter 3.16 left when it raised 90 -> 91 at 92.11.": "          // headroom \u2014 the same margin the user-surface chapter left when it raised 90 -> 91 at 92.11.",
"        // The dispatcher's two decision-bearing files (chapter 3.5). `expand.ts`": "        // The dispatcher's two decision-bearing files. `expand.ts`",
"        // CHAPTER 3.7 RAISED THIS, 93 -> 95. The chapter added two pure functions": "        // THE DEDUPLICATION CHAPTER RAISED THIS, 93 -> 95. The chapter added two pure functions",
"        // Chapter 3.6's two new files, pinned at 100 on every metric because both": "        // The retry-and-disable chapter's two new files, pinned at 100 on every metric because both",
"        // by nothing but the aggregate 70 \u2014 chapter 3.11's T033c made the same": "        // by nothing but the aggregate 70 \u2014 the connection-metering chapter's T033c made the same",
"        // CHAPTER 3.15 RAISED THIS TOO, and T174d predicted the opposite. That task": "        // THE CHANNEL-CONTROL CHAPTER RAISED THIS TOO, and T174d predicted the opposite. That task",
"        // refusal and its rethrow (chapter 3.10) and the history cursor's decode (chapter": "        // refusal and its rethrow and the history cursor's decode (chapter",
"        // Pinned anyway, because an unpinned file is a figure that can slide (chapter": "        // Pinned anyway, because an unpinned file is a figure that can slide",
"        // 3.11's T033c) and this feature changed the file. A ratchet at 61 does not bless": "        // (the connection-metering chapter's T033c) and this feature changed the file. A ratchet at 61 does not bless",
"        // CHAPTER 3.23 MOVED THIS FILE IN BOTH DIRECTIONS, and the branch number is the": "        // THE REVISIONS CHAPTER MOVED THIS FILE IN BOTH DIRECTIONS, and the branch number is the",
"        // goes UP to 92 \u2014 0.85 of headroom, the same margin chapter 3.17 left on": "        // goes UP to 92 \u2014 0.85 of headroom, the same margin the sender chapter left on",
"        // CHAPTER 3.24. The attachment shape and the REST door's schemas, both at 100 on": "        // The attachment shape and the REST door's schemas, both at 100 on",
"        // The REST door, pinned for the first time because chapter 3.24 is the first to": "        // The REST door, pinned for the first time because the attachments chapter is the first to",
"        // CHAPTER 3.8's limiter. Pinned at what the work achieves, which for the": "        // The rate-limit chapter's limiter. Pinned at what the work achieves, which for the",
"        // CHAPTER 3.11's three, pinned at what they measure, with a reason each.": "        // The connection-metering chapter's three, pinned at what they measure, with a reason each.",
"        // CHAPTER 3.19's two, both at 100 on every metric, and the pin is": "        // The presence chapter's two, both at 100 on every metric, and the pin is",
"        // and drove each with a test in that phase. Chapter 3.19 met its equivalents": "        // and drove each with a test in that phase. The presence chapter met its equivalents",
"        // CHAPTER 3.21. `packages/protocol/src/typing.ts` reached 100 on the": "        // `packages/protocol/src/typing.ts` reached 100 on the",
"        // CHAPTER 3.22. `services/gateway/src/connections.ts` at 100 on all four,": "        // `services/gateway/src/connections.ts` at 100 on all four,",
"# Four stores, each here by a recorded decision (SAD \u00a79), and \u2014 since chapter": "# Four stores, each here by a recorded decision (SAD \u00a79), and \u2014 since the webhook",
"# 3.5 \u2014 the three services themselves. Host ports are env knobs with the": "# dispatcher chapter \u2014 the three services themselves. Host ports are env knobs with the",
"  # --- the services (chapter 3.5) -----------------------------------------": "  # --- the services -------------------------------------------------------",
"// an earlier block's setting for it rather than merging. That is the bug chapter": "// an earlier block's setting for it rather than merging. That is the bug the",
"// 3.12 found (R23, FR-043): a second block for `**/*.itest.ts` carrying feature": "// isolation gauntlet found (R23, FR-043): a second block for `**/*.itest.ts` carrying feature",
"        // environment, exactly as its three siblings above do, and 3.10": "        // environment, exactly as its three siblings above do, and the quota",
"        // listed it in neither this rule nor `exempt.ts` \u2014 whose comment": "        // chapter listed it in neither this rule nor `exempt.ts` \u2014 whose comment",
"    // CORRECTED IN 3.12 (T069c). This comment used to say it was \"the one TEST": "    // CORRECTED IN THE ISOLATION GAUNTLET (T069c). This comment used to say it was \"the one TEST",
"    // THE SEAL ON `packages/outsider` (chapter 3.14, FR-030, FR-034, R12).": "    // THE SEAL ON `packages/outsider` (FR-030, FR-034, R12).",
"// THE SEALED INTEGRATION (chapter 3.14, FR-030, FR-031).": "// THE SEALED INTEGRATION (FR-030, FR-031).",
"  -- `OLD.id` UNTIL CHAPTER 3.12, and that is why extending the array below was": "  -- `OLD.id` UNTIL THE ISOLATION GAUNTLET, and that is why extending the array below was",
"// A tenant an outsider can integrate against (chapter 3.14, FR-032).": "// A tenant an outsider can integrate against (FR-032).",
"//   node scripts/webhook-walk.mjs --fast-forward --watch-disable   # chapter 3.6": "//   node scripts/webhook-walk.mjs --fast-forward --watch-disable   # the retry-and-disable chapter",
"//   --watch-disable                    chapter 3.6: print the failure run as it": "//   --watch-disable                    print the failure run as it",
"// has ever held, which on a development machine is every test run since 3.3.)": "// has ever held, which on a development machine is every test run since the outbox chapter.)",
"--   * all three foreign keys are ON DELETE NO ACTION, the choice 3.5 made and": "--   * all three foreign keys are ON DELETE NO ACTION, the choice the webhook dispatcher chapter made and",
"-- alike, and the string `'0'` for zero. The distinction 3.8 needed nullable": "-- alike, and the string `'0'` for zero. The distinction the rate-limit chapter needed nullable",
"-- unattributed by design since chapter 3.3, and an unattributed send counts": "-- unattributed by design since the outbox chapter, and an unattributed send counts",
"-- read position is. The guard's refusal message interpolates a key, and chapter": "-- read position is. The guard's refusal message interpolates a key, and the",
"-- 3.13 installed `coalesce(to_jsonb(OLD) ->> 'id', to_jsonb(OLD)::text)` for": "-- channel-endpoints chapter installed `coalesce(to_jsonb(OLD) ->> 'id', to_jsonb(OLD)::text)` for",
"-- has carried `CHECK (role IN ('owner','admin','member'))` since chapter 3.1 \u2014": "-- has carried `CHECK (role IN ('owner','admin','member'))` since the tenancy chapter \u2014",
"-- TWO COLUMNS, NOT A SECOND TABLE. `users` is what every reader built since chapter": "-- TWO COLUMNS, NOT A SECOND TABLE. `users` is what every reader built since the",
"-- 3.15 already reads. A `bots` table would have meant teaching each of them a second": "-- channel-control chapter already reads. A `bots` table would have meant teaching each of them a second",
"        // (89.50% statements, 82.73% branches after chapter 3.8, up from 86.55%": "        // (89.50% statements, 82.73% branches after the rate-limit chapter, up from 86.55%",
"        // \u2500\u2500 CHAPTER 3.12'S NEW FILES, PINNED DELIBERATELY \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "        // \u2500\u2500 THE ISOLATION GAUNTLET'S NEW FILES, PINNED DELIBERATELY \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"        // \u2500\u2500 CHAPTER 3.15's USER SURFACE \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "        // \u2500\u2500 THE CHANNEL-CONTROL CHAPTER'S USER SURFACE \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500",
"        // send path had had that test since chapter 3.15; the two new routes and the": "        // send path had had that test since the channel-control chapter; the two new routes and the",
"        //     a senderless row (FR-018 of 3.23) before it can return, so the arm was": "        //     a senderless row (FR-018 of the revisions chapter) before it can return, so the arm was",
"        // \u2500\u2500 CHAPTER 3.20'S FOUR NEW PRODUCTION FILES \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500": "        // \u2500\u2500 THE MEMBERSHIP-REVOCATION CHAPTER'S FOUR NEW PRODUCTION FILES \u2500\u2500"
},
"runs": [
{
"old": [
"// chapter 3.13.",
""
],
"new": [
"// "
]
},
{
"old": [
"  // \u2500\u2500 THE FIVE THE PLATFORM HAS ALWAYS SENT AND NEVER REGISTERED (chapter 3.14,",
"  // FR-024) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500"
],
"new": [
"  // \u2500\u2500 THE FIVE THE PLATFORM HAS ALWAYS SENT AND NEVER REGISTERED (  // FR-024) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500"
]
},
{
"old": [
"/** Write a row for each threshold a usage increase crossed (chapter 3.10,",
" * FR-RTL-07), and the organisation to tell about it."
],
"new": [
"/** Write a row for each threshold a usage increase crossed ( * FR-RTL-07), and the organisation to tell about it."
]
},
{
"old": [
"/** Everything the connect path needs, in ONE round trip (chapter 3.11,",
" * FR-RTL-05, FR-RTL-06)."
],
"new": [
"/** Everything the connect path needs, in ONE round trip ( * FR-RTL-05, FR-RTL-06)."
]
},
{
"old": [
"    // THE CROSSINGS, IN THE SAME TRANSACTION AS THE CREDIT (chapter 3.11,",
"    // FR-RTL-07/FR-RTL-07). The report knows the figure before and after, so it knows"
],
"new": [
"    // THE CROSSINGS, IN THE SAME TRANSACTION AS THE CREDIT (    // FR-RTL-07/FR-RTL-07). The report knows the figure before and after, so it knows"
]
},
{
"old": [
"/** Disable every endpoint whose failure run has outrun the hour (chapter 3.6,",
" * research R1, contract invariant 12)."
],
"new": [
"/** Disable every endpoint whose failure run has outrun the hour ( * research R1, contract invariant 12)."
]
},
{
"old": [
"  /** Upsert a user by external id, updating the profile fields present (chapter 3.15,",
"   * FR-025, FR-026)."
],
"new": [
"  /** Upsert a user by external id, updating the profile fields present (   * FR-025, FR-026)."
]
},
{
"old": [
"      // MEMBERSHIP, FOR A PRIVATE CHANNEL, WHEN A USER IS SENDING (chapter 3.15,",
"      // FR-001, FR-CHN-05)."
],
"new": [
"      // MEMBERSHIP, FOR A PRIVATE CHANNEL, WHEN A USER IS SENDING (      // FR-001, FR-CHN-05)."
]
},
{
"old": [
"  /** Write a row for each threshold a usage increase crossed (chapter 3.10,",
"   * FR-RTL-07, FR-RTL-07)."
],
"new": [
"  /** Write a row for each threshold a usage increase crossed (   * FR-RTL-07, FR-RTL-07)."
]
},
{
"old": [
"// The transport the retry-and-disable chapter deferred, against a real mail server (chapter 3.8,",
"// FR-WHK-07 to FR-WHK-05)."
],
"new": [
"// The transport the retry-and-disable chapter deferred, against a real mail server (// FR-WHK-07 to FR-WHK-05)."
]
},
{
"old": [
"/** A deletion, built inside the transaction that wrote the tombstone (chapter 3.23,",
" * FR-019, FR-020)."
],
"new": [
"/** A deletion, built inside the transaction that wrote the tombstone ( * FR-019, FR-020)."
]
},
{
"old": [
"  /** Ban and unban this tenant's own user through the public route (chapter 3.15,",
"   * T153). */"
],
"new": [
"  /** Ban and unban this tenant's own user through the public route (   * T153). */"
]
},
{
"old": [
"    // WAS `every((m) => m.user === null)`, AND THAT IS THE CHAPTER (chapter 3.17,",
"    // T055's class). This assertion existed to prove the rows were senderless, which was"
],
"new": [
"    // WAS `every((m) => m.user === null)`, AND THAT IS THE CHAPTER (    // T055's class). This assertion existed to prove the rows were senderless, which was"
]
},
{
"old": [
"  // \u2500\u2500 THE FIVE THE PLATFORM HAS ALWAYS SENT AND NEVER REGISTERED (  // FR-024) \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500"
],
"new": [
"  // \u2500\u2500 THE FIVE THE PLATFORM HAS ALWAYS SENT AND NEVER REGISTERED (FR-024)",
"  // \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500"
]
},
{
"old": [
"    // THE CROSSINGS, IN THE SAME TRANSACTION AS THE CREDIT (    // FR-RTL-07/FR-RTL-07). The report knows the figure before and after, so it knows"
],
"new": [
"    // THE CROSSINGS, IN THE SAME TRANSACTION AS THE CREDIT (FR-RTL-07/FR-RTL-07).",
"    // The report knows the figure before and after, so it knows"
]
},
{
"old": [
"// The transport the retry-and-disable chapter deferred, against a real mail server (// FR-WHK-07 to FR-WHK-05)."
],
"new": [
"// The transport the retry-and-disable chapter deferred, against a real mail server",
"// (FR-WHK-07 to FR-WHK-05)."
]
},
{
"old": [
"    // WAS `every((m) => m.user === null)`, AND THAT IS THE CHAPTER (    // T055's class). This assertion existed to prove the rows were senderless, which was"
],
"new": [
"    // WAS `every((m) => m.user === null)`, AND THAT IS THE SENDER CHAPTER",
"    // (T055's class). This assertion existed to prove the rows were senderless, which was"
]
},
{
"old": [
"        // CHOSEN BEFORE THE FIRST COVERAGE REPORT, not read off it (chapter 3.18,",
"        // T011). The requirement is that the failure path be covered: this file's"
],
"new": [
"        // CHOSEN BEFORE THE FIRST COVERAGE REPORT, not read off it (        // T011). The requirement is that the failure path be covered: this file's"
]
},
{
"old": [
"-- by chapters 3.10 and 3.11 and neither added them here, so a cross-environment",
"-- UPDATE or DELETE on any of them passed for two chapters. Confirmed against a",
"-- running database rather than read off this file: `pg_trigger` held five"
],
"new": [
"-- by the quota and connection-metering chapters and neither added them here, so a",
"-- cross-environment UPDATE or DELETE on any of them passed for two chapters.",
"-- Confirmed against a running database rather than read off this file:",
"-- `pg_trigger` held five"
]
},
{
"old": [
"    -- Chapters 3.10 and the connection-metering chapter's tables. Every one carries `environment_id`, which",
"    -- is the only thing the WHEN clause below needs; what they do NOT all carry",
"    -- is `id`, which is what the message expression above had to change for."
],
"new": [
"    -- The quota and connection-metering chapters' tables. Every one carries",
"    -- `environment_id`, which is the only thing the WHEN clause below needs; what they",
"    -- do NOT all carry is `id`, which is what the message expression above had to",
"    -- change for."
]
},
{
"old": [
"-- The outbox chapter published events, 3.5 dispatched webhook deliveries, 3.9 sent",
"-- disablement emails. Each is a table whose claim predicate starts null, drained",
"-- by a relay, retried by falling due again. This is the fourth, and saying the"
],
"new": [
"-- The outbox chapter published events, the webhook dispatcher chapter dispatched",
"-- webhook deliveries, the mail-transport chapter sent disablement emails. Each is a",
"-- table whose claim predicate starts null, drained by a relay, retried by falling",
"-- due again. This is the fourth, and saying the"
]
}
]
}