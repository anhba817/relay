# Tasks — chapter 3.22, the five-connection cap

**Spec**: [spec.md](./spec.md) · **Plan**: [plan.md](./plan.md) · **Research**: [research.md](./research.md) · **Contract**: [contracts/refusal.md](./contracts/refusal.md)

**EVERY PHASE RUNS `pnpm lint`, `pnpm typecheck` AND `pnpm turbo run test` IN
`relay-platform` BEFORE ITS COMMIT.** Chapter 3.21 measured all three: 3.5 s, 3.4 s
and 5.9 s across eleven packages. Its analysis pass 8 found seven of eleven phases
committing with none of them, and pass 17 found a unit test red since Phase 2 and
invisible until Phase 11. **Run these from `relay-platform`, not from a package
subdirectory** — `pnpm turbo run test` from `services/gateway/src` ran 4 tasks of 11
and printed green, which is the one failure in that family that is silent.

**US3 RUNS SECOND, AND `plan.md` WAS EDITED TO AGREE RATHER THAN OVERRIDDEN.** The
plan's first draft put US3 — every connection a first-class recipient — at its Phase
6, after the cap exists. **US3 tests behaviour that already ships** (research R5:
delivery walks connections, not users), so writing its tests *before* any production
change makes them a regression guard for US1 and US2; written afterwards they prove
nothing about whether the cap broke delivery, because nobody would know they had ever
passed. The stories keep their spec numbering.

**Both documents now list the same eleven phases.** Chapter 3.21's tasks.md left its
plan saying something else and its header said so — which meant a reader had to hold
the correction in their head, and `sweep.py` exists because chapter 3.21 recorded a
phase order in `baseline.txt` and applied it only to `tasks.md`. **This paragraph was
itself stale for one edit**: it still said "the plan puts US3 at Phase 6" after the
plan had been changed, which is the same defect one level down.

**EVERY REQUIREMENT ID THIS CHAPTER WRITES INTO CODE NAMES ITS CHAPTER.** Write
`FR-011 (3.22)`, or spell the requirement. **`session.ts` already carries 25 bare
`FR-0NN` mentions and not one names its chapter**, and ten of this chapter's ids
already mean something else in that file: `FR-005` at line 319 is typing's
skip-the-signaller rule, `FR-008` at 389 is chapter 3.20's cut-then-send ordering,
`FR-015` at 513 and 1132 is swallow-and-log a lost typing frame. This chapter's
`FR-005` forbids disturbing the five already held, its `FR-008` is a renewing
connection still counting, its `FR-015` is logging a refusal. **The ids are per
feature and the file is shared** — `gaps.md`'s standing rule already says so about
its own items, and nothing had applied it to code comments. Not a defect this
chapter introduces; one it would widen by six.

**AND THE FALSIFICATION COMES BEFORE THE TEST, EVERY TIME.** Chapter 3.20 specified
two orderings as requirements and neither was observable. Chapter 3.21 wrote four
test titles that outran their assertions. Three tasks below say "break X, watch this
test go red, put X back" and they are not optional steps inside the task — they are
the task.

---

## Phase 1: Setup, the premises, and the failing state observed

- [X] T001 Bring the stack up and pin the lane environment in `specs/040-chapter-3-22/baseline.txt`: `RELAY_POSTGRES_PORT=15432 docker compose up -d --wait` in `relay-platform`, the nine variables from `specs/040-chapter-3-22/quickstart.md`, and `NO_COLOR=1 FORCE_COLOR=0`. **Five store containers, not four** — a lane without mailpit fails on `ECONNREFUSED :8025`. **Omitting the port variable takes Postgres down mid-recreate** with `bind: address already in use`.
- [X] T002 Record the starting lane in `specs/040-chapter-3-22/baseline.txt` with the colour codes stripped: `pnpm test:integration` in `relay-platform`, per-package files, tests and wall clock. Chapter 3.21 closed at 44 files, 727 tests, 228.63 s against a 240 s budget. **Pass `--force`** — chapter 3.21's T002 omitted it, turbo served a cached result and recorded 150 s against the previous chapter's 228.77 s.
- [X] T003 Verify quickstart P1 and record the output in `specs/040-chapter-3-22/baseline.txt`: `grep -rn "\.eval(\|defineCommand\|\.multi(" --include="*.ts" services packages | grep -v node_modules` from `relay-platform`. **Expect no output.** This is the entire basis for rejecting the SAD's sorted set (research R3) — if a Lua script or a MULTI has appeared, Constitution VII's "second language" argument weakens and ADR-23 must say so instead of asserting it.
- [X] T004 [P] Verify quickstart P2 and record it: `grep -n "4700 +" services/gateway/src/presence.itest.ts`, `grep -n "4710 +" services/gateway/src/meter.itest.ts`, `grep -nE "fileParallelism" services/gateway/vitest.integration.config.mts`. **Expect 4700–4899, 4710–4769, and no output from the third.** The unregistered range strictly contains a registered one and the files run concurrently. **If `fileParallelism: false` has appeared, the collision is impossible and research R7 is wrong** — say so rather than repeating it.
- [X] T005 [P] Verify quickstart P3 and record it: `grep -n "4001, 4002, 4003, 4008, 4009" packages/protocol/src/codes.test.ts`. **Expect TWO hits, lines 18 and 20** — the set is written in the assertion *and* in the test's own title. The first draft of that quickstart paragraph said one hit; running the command found two. Both get edited in Phase 3 or the test's name lists five codes while it checks six.
- [X] T006 [P] Verify quickstart P4 and record it: `grep -n "connect: 3_000" services/api/src/limits/policy.ts services/api/src/limits/policy.test.ts`. **One hit each.** `10_000 / 5 = 2_000` and the constant is 3,000; the comment above it claims each number names what it rests on. **Do not change either file in this chapter** — research R6 hands the arithmetic to `gaps.md`, and re-tuning another chapter's shipped limit inside this chapter's battery would make two changes indistinguishable.
- [X] T006a [P] Verify quickstart P5 and record the version and both replies in `specs/040-chapter-3-22/baseline.txt`: `docker compose exec -T redis redis-cli INFO server | grep redis_version`, then `SET p5 A PX 60000`, `SET p5 B IFEQ A PX 60000` (expect `OK`), `SET p5 C IFEQ WRONG PX 60000` (expect an empty reply and `p5` still `B`). **The renewal's whole correctness rests on this flag** and it is newer than anything else this platform asks of Redis; `compose.yaml:33` pins `redis:8-alpine` and the tag floats. Measured at 8.10.0 during analysis pass 1.
- [X] T006b [P] Verify that every gateway module is an **optional** parameter and record the consequence in `specs/040-chapter-3-22/baseline.txt`: `grep -nE "fanout\?:|limits\?:|presence\?:|membership\?:|typing\?:" relay-platform/services/gateway/src/session.ts` returns five lines (192, 206, 217, 222, 231), and `grep -c "attachSessions(" relay-platform/services/gateway/src/{presence,typing,resume}.itest.ts` returns one each. **This is the premise the whole test strategy rests on**: the cap is enforced only in fixtures that pass `connections`, so no existing integration file claims a slot. **Analysis pass 2 assumed the opposite and produced three tasks and two findings from it** — the collision it predicted between the new file and the two `env-1` fixtures cannot happen. If a module has become required, that reasoning inverts and R11b must be rewritten before Phase 4.
- [X] T007 [P] Verify the two SAD rows still disagree, and record both verbatim in `specs/040-chapter-3-22/baseline.txt`: `sed -n '167p' docs/05-sad.md` and `sed -n '574p' docs/05-sad.md`. Line 167 says instance ID, present tense; line 574 says set of instance IDs and "Not built". FR-017 closes both and Phase 9 owns the edit.
- [X] T008 Verify rather than repeat: `specs/040-chapter-3-22/check-refs.py` was copied and reset **during planning**, at analysis pass 4, because four passes had rewritten every artifact with no id or count instrument in the directory. Run it and read the one remaining complaint — `traceability.md is missing` — as the expected state until T090 writes it. **On its first run it found nine real problems**, including two of chapter 3.21's task ids written into this file's own prose, where a foreign id colliding with a local one passes silently, and three `gaps.md` item citations that named no chapter. **Keep chapter 3.21's close-out fix**: `REF` must accept the optional letter, `T(\d{3}[a-z]?)`, because `TASK` does, and the two disagreeing is what let a suffixed id be cited under a green "no undeclared task ids" in the previous chapter. Verify by adding a bogus suffixed citation to a scratch `.md`, watching it fail, and deleting the scratch file. **Do not write a predecessor's task id into this file**: `tasks.md`'s rule is that every T-id in it is a local task, and a foreign id that happens to collide with a local one passes silently — which is the whole reason the pair mechanism exists for the other artifacts.
- [X] T009 Verify rather than repeat: `specs/040-chapter-3-22/sweep.py` was copied and its phrase lists reset **during planning**, at analysis pass 4. **Two of its own checks were wrong on the first run and both are fixed in place**: its plan-phase extractor knew only `### Phase N —` and read this chapter's `**Phase N —**` as zero phases, reporting a count mismatch and an empty MVP marker against a healthy pair of documents; and its prose-count check had no exemption at all, so it read *"ran 4 tasks of 11"* — turbo's tasks — against this chapter's 111. Both now name their accepted set explicitly and fail on an unknown member. Also record and record in `specs/040-chapter-3-22/chapter-notes.md` that this is chapter 3.19's `gaps.md` item 8 answered the same way a third time — a copy with its declarations emptied, still with no owner. **Its STALE list starts with the three phrases this chapter supersedes**: "a Redis set with one TTL", "the correct version is a sorted set", and "FR-RTM-09's five is enforced NOWHERE".
- [X] T009a Verify `specs/040-chapter-3-22/check-prose.py`, copied and reset during planning at analysis pass 4: four claims, eight fragments, both locales. **It is RED on purpose and stays red until T073a**, because every fragment it names is still published — the two chapter-20 and chapter-21 sentences this chapter falsifies. Say so in the phase commit body: a red instrument nobody explained is indistinguishable from a red instrument nobody noticed. **Two of its own defects are fixed in place.** Its summary printed a hardcoded "5 claims" over a list of four — a checker lying about its own contents — and the count is computed now. And **one of its eight fragments matched nothing**: claim 4's Vietnamese text was chapter 20's wording (`*set*`) aimed at chapter 21, whose page reads `**set**`. Seven of eight matched and the eighth was invisible, so this file would have reported green for a page nobody corrected. **Run `python3 specs/040-chapter-3-22/check-prose.py --declared` before trusting the list**: that mode asserts the opposite of the normal run — every fragment must match *right now* — and is run once, when the list is written and before T073a corrects the pages. The two assertions cannot both be permanent, which is why it is a separate mode rather than a check.
- [X] T009b Write `specs/040-chapter-3-22/traceability.md` **now, during planning, both ways, before any test is written** — and keep T090 as the close-out re-derivation. The previous chapter's file opens with the reason: *"Chapter 3.18 ran the map the second way at close-out and found FR-007 — a MUST — with no test at all, after eight phases and nineteen analysis passes had each read `requirement → test` and believed it. Chapter 3.20 did the same and found a requirement whose planned test was not in the tree."* **Its own orphan table found three requirements with no task, "all three the shape a task list forgets: a requirement whose verification is a sentence rather than a test"** — which is exactly this chapter's FR-017, SC-009, SC-010 and FR-002. Use its schema: `SRS clause | Pri | Feature requirements | Verified by`, plus an orphan table. **This also clears `check-refs.py`'s standing complaint**, which has read "traceability.md is missing, so nothing is traced" for every analysis pass.
- [X] T010 Run `python3 specs/040-chapter-3-22/check-refs.py` and `python3 specs/040-chapter-3-22/sweep.py` from the repository root, capturing each exit code with `> /tmp/o 2>&1; echo $?`. **Do not pipe either into `tail`** — chapter 3.21's close-out committed over a red checker because `&&` read `tail`'s status.
- [X] T011 Write the failing test in `relay-platform/services/gateway/src/session.itest.ts`: one user opens six connections and all six are accepted today. **RED ON PURPOSE, and the phase commit body says so.** A red lane nobody explained is indistinguishable from a red lane nobody noticed. Chapter 3.21's `presence.itest.ts:728` already exploits this — it opens five connections *because* nothing stops it — so the comment there is edited in Phase 5, not deleted here.
- [X] T012 Commit phase 1, naming FR-017 as recorded-not-yet-closed. Run `git status --short` in all three repositories first, then `pnpm lint`, `pnpm typecheck` and `pnpm turbo run test --force` in `relay-platform`.

---

## Phase 2: User Story 3 — every one of the five is a first-class recipient (P2)

**Goal**: prove FR-014 against unchanged code, so the tests become a regression guard
for everything after them.

**Independent test**: two connections of one user in one channel; one publish; both
receive it exactly once; close one uncleanly and the other still receives the next.
No cap exists yet and none is needed.

- [ ] T013 [US3] Before writing anything, falsify the subject: in `relay-platform/services/gateway/src/session.ts`, temporarily replace `registry.subscribersOf(channelId)` at the message delivery site with a list de-duplicated by `identity.userExternalId`, and record in `specs/040-chapter-3-22/baseline.txt` which existing tests go red. **If none do, FR-014 is currently unverified and that is the finding** — say so, then put the line back.
- [ ] T014 [US3] Write the message-path test in `relay-platform/services/gateway/src/connections.itest.ts`: two connections of one user in one channel, one message published, **both receive it and each receives it once**. Assert the count, not just arrival — a test asserting only arrival passes against a delivery loop that sends twice to one socket.
- [ ] T015 [P] [US3] Write the same test for a membership change in `relay-platform/services/gateway/src/connections.itest.ts`, and note in the file that `deliverPresence` is deliberately unfiltered (`session.ts:310`) while typing's rule is the opposite (`session.ts:321`) — two adjacent functions with opposite self-delivery rules, so a test copied from one to the other asserts the wrong thing.
- [ ] T016 [P] [US3] Write the independence test in `relay-platform/services/gateway/src/connections.itest.ts`: with two connections of one user, close one uncleanly and assert the other receives the next event. **The falsification for this one is a send that throws** — make one socket's send reject and confirm the other still arrives, because "neither connection's delivery depends on the other's success" is FR-014's second half and arrival alone does not test it.
- [ ] T017 [US3] Re-apply T013's break in `relay-platform/services/gateway/src/session.ts` and confirm all three new tests in `relay-platform/services/gateway/src/connections.itest.ts` go red, then revert. **This is the task, not a check inside it.** A test that cannot be made to fail by breaking its subject proves nothing, and this chapter has three of them.
- [ ] T018 [US3] Commit phase 2 in `relay-platform`, naming FR-014, SC-001. Gates first. **The commit body says these tests were green on unchanged code** — a reader who finds them in the diff of a chapter that adds a cap will otherwise assume they were the cap's tests.

---

## Phase 3: The protocol — a sixth close code, and a title that lists it

- [ ] T019 Choose the close-code number and the error-code name in `relay-platform/packages/protocol/src/codes.ts`, **and name them in this task and nowhere else** so one decision has one home. Write both into `specs/040-chapter-3-22/contracts/refusal.md` in the same commit — every task downstream says "the new close code". Draw the number from the same unassigned 4xxx space chapters 1.3 and 3.15 used. **Prefer a name that says what the client must do**: the remedy is closing a connection it already holds, immediately, which is what makes this the only code in the set whose correct handling is not a retry.
- [ ] T020 Add the close code to `CLOSE_CODES` in `relay-platform/packages/protocol/src/codes.ts` with a comment stating why each of the four reuses fails, in the shape chapter 3.15 used for 4003: `4001` loops on re-auth against a valid token, `4008` tells the client to wait for a window that never helps, `4003` says the person is barred while four of their connections work, `4002` blames a client that did nothing wrong. Cite EIR-WS-06's four classes and say a cap is none of them.
- [ ] T021 Add the error code to `ERROR_CODES` in `relay-platform/packages/protocol/src/codes.ts`, registered rather than written at the call site — `codes.ts` already records why chapter 3.2 put `wrong_credential_type` in the registry, and `codes.test.ts` enforces uniqueness.
- [ ] T022 Update **both** places in `relay-platform/packages/protocol/src/codes.test.ts` that name the set: the assertion at line 20 and **the test's own title at line 18**. T005 measured two hits. A chapter that edits only the assertion ships a test called *"contains exactly 4001, 4002, 4003, 4008, 4009"* that checks six codes, which is the previous chapter's late pass found four test titles that outran their assertions, landing in the file that would carry it.
- [ ] T023 [P] **Change no schema.** The refusal reuses `errorFrameSchema` in `relay-platform/packages/protocol/src/frames.ts` exactly as it stands, and the limit and the count held go **inside `message`** — the precedent is `quota_exceeded`, whose registry entry reads *"the message names the dimension, the figures and the date it resumes"*. **The first version of this task offered a choice and one branch was impossible**: `errorFrameSchema.payload` is a `z.strictObject` of `code`, `message`, `docs_url`, `request_id` and an optional `field`, so the `limit` and `count` fields `contracts/refusal.md` used to promise would have been rejected. `field` is not used either — it names the payload key that failed validation, which is `invalid_frame`'s job.
- [ ] T023a [P] Confirm `relay-platform/services/gateway/src/main.test.ts` stays green and record why in `specs/040-chapter-3-22/baseline.txt`. Line 43 is `expect(body.protocol.frames).toHaveLength(11)` — a **hard-coded count of the frame union, which has exactly 11 members** — and chapter 3.21's comment on it says it is *"the second of the two hard-coded frame counts in the repository, and the only one no task owned until analysis pass 17. It failed here in the UNIT lane, which `test:integration` does not run."* **No task in this chapter owned it either until analysis pass 6.** T023 adds no union member, so 11 holds; adding one would turn this red in a lane no phase gate runs by default. The `close_codes` assertion two lines below is derived from `CLOSE_CODES` and needs nothing — do not "fix" it.
- [ ] T024 Run `pnpm build` in `relay-platform`, then `pnpm -s check:errors` from `relay-tutorial`, capturing the exit code. **That gate reads `packages/protocol/dist/codes.js`, the BUILT artifact**, so a stale `dist` makes it green for the wrong reason. This is the phase where that matters most.
- [ ] T025 Commit phase 3, naming FR-003. Gates first, and `pnpm build` before the gate.

---

## Phase 4: Foundational — the slot registry

- [ ] T026 List the branch arms before writing the module, in `specs/040-chapter-3-22/baseline.txt`: the `SET NX` miss on each of five slots, the walk finding no free slot, `SET IFEQ` refused because the key is gone, `SET IFEQ` refused because the key holds another connection's id, the `failable` catch when Redis is unreachable, a conditional release refused because the slot is no longer ours, a conditional release for a slot the connection never held, `releaseAll()` with nothing held, and **FR-011b's three re-claim branches — a slot was free, every slot held by another connection, the registry unreachable**. Those three were added by analysis pass 6: FR-011b arrived in pass 5 and nothing re-derived this list, which against a pin of 100/100/100/100 would have meant Phase 11 discovering them. Chapter 3.21 did this and three of four new files hit 100/100/100/100 on the first run; chapter 3.19 met the same requirement at close-out and paid seven tests, a deleted branch and a re-measured battery.
- [ ] T027 Create `relay-platform/services/gateway/src/connections.ts` with its own `ioredis` client and a narrow declared command interface, following `presence.ts` and `typing.ts`. **One `error` listener on the client** — chapter 3.18's R10 found `createFanout` without one while both rate limiters had one and explained why.
- [ ] T028 Implement the key builder in `relay-platform/services/gateway/src/connections.ts`: `conn:{environment_id}:{user_external_id}:{slot}` for slot 0–4, per `data-model.md`. **The value is the connection's `id`, not the instance id** — `docs/05-sad.md:167` says instance ID and that grain cannot tell two connections of one user on one instance which slot is theirs to refresh (FR-011).
- [ ] T029 Implement `claim` in `relay-platform/services/gateway/src/connections.ts`: walk slots 0–4 issuing `SET key id NX PX <bound>`, return the slot claimed or a refusal carrying the observed count. **`NX` is the atomicity** — two racers cannot win one slot and the loser walks on, which is FR-013 enforced by Redis rather than by application logic.
- [ ] T030 Implement `renew` in `relay-platform/services/gateway/src/connections.ts`: `SET key id IFEQ id PX <bound>`. **`IFEQ`, not `XX`, and this is a corrected decision rather than a preference.** `XX` tests existence and nothing more — measured on the lane's Redis 8.10.0, `SET k B XX PX 60000` against a key holding `A` returns `OK` and the value becomes `B`. So a connection whose slot expired during an outage longer than the bound would come back, find the slot re-claimed, and **silently take it**: six connections, five slots, FR-001 and FR-011 violated. `IFEQ` is refused both when the key holds another id and when the key is gone, and the second refusal is how a connection learns it has lost its place. Available on Redis 8.10.0 and present in ioredis 6.0.0's `RedisCommander.d.ts`, so no Lua and no cast.
- [ ] T030a Implement the refused-renewal branch in `relay-platform/services/gateway/src/connections.ts` (FR-011b): when `renew` is refused, **try once to claim a slot again** and return which of three outcomes happened — a new slot claimed, every slot held by another connection, or the registry unreachable. **Nothing said what a losing connection does for five analysis passes**: `data-model.md` named the state and deferred to the plan, the plan never decided, and the consequence is six open connections against a count of five with no test looking from the connection's side. The free-slot case is **the common one** after any brief outage, and getting it wrong costs a user their connection for Redis's downtime.
- [ ] T031 Implement `release` in `relay-platform/services/gateway/src/connections.ts` as `SET key - IFEQ id PX 1` — a one-millisecond tombstone written **only if the slot is still this connection's** — so a clean close frees it immediately (FR-010) rather than at the end of the bound. **Not `DEL`.** `DEL` has no ownership check, so a connection whose slot expired during an outage and was re-claimed would free the new owner's slot. Measured: `SET k x IFEQ OWNER PX 1` returns `OK` and the key is gone; `IFEQ WRONG` returns nil and leaves the value alone; `GETDEL` exists and is unconditional, so it is no use here. **The millisecond is the safe direction** — a claim inside it finds the key present, its `SET NX` fails, and it walks to the next slot: one slot briefly skipped, never an over-admit.
- [ ] T032 Define the two intervals in `relay-platform/services/gateway/src/connections.ts` as named exports with the derivation in a comment: bound 60_000 (the figure `docs/05-sad.md:574` already publishes), heartbeat 20_000 (three per bound, so two consecutive misses do not free a slot). **State that `PING_INTERVAL_MS` is 30_000 and is deliberately not reused** — chapter 3.19's three-30-second-numbers lesson, and tying a Redis TTL to a client-visible keepalive means changing the ping starts expiring slots.
- [ ] T032a [P] Assert FR-002's single-statement property in `relay-platform/services/gateway/src/connections.test.ts`: the maximum is exported once from `connections.ts` and no second literal five stands in for it. **The requirement is about drift, not about the value** — a second five is how two figures come apart, which is exactly what `policy.ts` did with its own derivation (research R6). T006 checks that derivation; nothing checked this. **Found by building `traceability.md` during planning**, where FR-002's only citation was a commit task.
- [ ] T033 Implement the unreachable-Redis path in `relay-platform/services/gateway/src/connections.ts` with a `failable(op, work)` wrapper returning `null`, copying the distinction `presence.ts:232` draws: **`null` means "could not ask" and `0` means "nothing held"**, and the caller must not conflate them (FR-016, FR-016b).
- [ ] T034 [P] Write `relay-platform/services/gateway/src/connections.test.ts` covering every arm from T026, against a real Redis rather than a stub. **`SET NX` and `SET IFEQ` semantics are the entire correctness argument** and a stubbed client would pass with a non-atomic implementation, and with an `XX` renewal or a `DEL` release that silently touches somebody else's slot — which is the same shape as a chapter 3.17 test that passed with half its subject applied, because its quota ceiling of 1 put the second person over the limit either way.
- [ ] T035 [P] Add a test in `relay-platform/services/gateway/src/connections.test.ts` that the heartbeat interval is strictly less than the bound by the stated margin (FR-009). **Assert the ratio, not the values** — a test pinning 20_000 and 60_000 goes red on a deliberate re-derivation and says nothing about the property.
- [ ] T036 Commit phase 4 in `relay-platform`, naming FR-002, FR-009, FR-010, FR-011, FR-011a, FR-011b, FR-016b. Gates first.

---

## Phase 5: User Story 1 — a sixth connection is refused while five keep working (P1) 🎯 MVP

**Goal**: the clause. Five accepted, the sixth refused with a reason a client can act
on, and the five undisturbed.

**Independent test**: open five, assert all five receive a message, open a sixth,
assert the documented refusal, then assert the original five each receive the next
message.

- [ ] T037 [US1] Wire the cap check into `relay-platform/services/gateway/src/session.ts`'s upgrade handler, **after `authenticate` and after `limits.spend`**. After authenticate because the environment and user are not known before it — `session.ts:658` already states that reasoning for the rate limiter. After the rate limiter because a client hammering the door should be refused by the cheaper check first.
- [ ] T038 [US1] Refuse inside `wss.handleUpgrade`'s callback in `relay-platform/services/gateway/src/session.ts`, in the shape `session.ts:730` established: `sendError(ws, "<new error code>", message)` then `ws.close(<new close code>, CLOSE_CODES[...])` then one `logger.log("info", "connection.rejected", { reason: ... })`. **Not `refuseUpgrade`.** Its whole justification was `Retry-After`, FR-004 forbids that header here, and **a browser cannot read the body of a failed upgrade** (`session.ts:729`) — so the HTTP shape is not merely unnecessary, it is unreadable in the primary client environment.
- [ ] T039 [US1] Claim the slot only for a connection that is going to be accepted, and hold the claimed slot number on the connection in `relay-platform/services/gateway/src/session.ts` so the heartbeat and the close know which key is theirs.
- [ ] T040 [US1] Start the heartbeat timer per connection in `relay-platform/services/gateway/src/session.ts` at the interval from T032, and **clear it on close** — an interval that outlives its connection refreshes a slot nobody holds, which is the `XX` guard's second job and a leak the guard cannot fix alone.
- [ ] T040a [US1] Act on T030a's three outcomes in `relay-platform/services/gateway/src/session.ts` (FR-011b): a new slot means update the slot held in memory and carry on; every slot held by another connection means close with the new close code and error frame, the same pair a refused sixth connection gets, because that is what it means; an unreachable registry means keep the connection and log the cap as unenforced (FR-016). **Name FR-005 in the comment and say why it does not conflict**: FR-005 governs a *refusal* — opening a sixth must not cost the five — and this connection has already lost its place to a competitor, so the choice is between closing it and running over the cap. Two rules, two events, and they sit adjacent.
- [ ] T041 [US1] Release the slot on every close path that exists in `relay-platform/services/gateway/src/session.ts`: a clean client close and a server-initiated close. **Do not add a drain path keyed on close code 4009.** The first draft of this task named one; `session.test.ts:965` says *"4009 IS STILL EMITTED BY NOTHING"* and `:982` asserts `expect(text).not.toMatch(/close\(\s*4009/)`, so adding one turns a passing test red for a reason unrelated to this chapter. The deploy case is T041a.
- [ ] T041a [US1] Implement `releaseAll()` in `relay-platform/services/gateway/src/connections.ts` and call it from the session module's `close()` in `relay-platform/services/gateway/src/session.ts` (FR-011a). **It uses the same conditional release as T031, per slot** — a `DEL` loop here would free slots this instance no longer owns, and a shutdown is exactly when a stale slot is most likely. **The deploy path holds slots for a full bound today and that breaks NFR-REL-03 on the ordinary path.** `shutdown()` awaits `sessions.close()`, whose body is `clearInterval`, `meter.stop()`, `meter.reportOnce` and `wss.close()` — and **`wss.close()` stops the server accepting connections without closing established sockets**, so whether each connection's own close handler runs is a race with process exit. Deleting the keys this instance holds is cheap, needs no new close code, and leaves `session.test.ts:982` alone.
- [ ] T042 [US1] Pass the module into `attachSessions` from `relay-platform/services/gateway/src/main.ts`. **This is the task chapter 3.21 got wrong and it cost that chapter its worst defect**: the module was built, its `close()` awaited so lint saw a use, and it was never passed — the feature was inert while 1,174 coverage tests and 174 gateway integration tests were green. `**/main.ts` is excluded from coverage, so no number could have shown it, and `main.ts:99` records the sequence in its own comment. **Do not treat a green lane as evidence this task is done**; T048 is the evidence.
- [ ] T042a [US1] Declare `connections` **optional** in `attachSessions`' parameter type in `relay-platform/services/gateway/src/session.ts`, matching `fanout?`, `limits?`, `presence?`, `membership?` and `typing?` — **and record the choice as a choice** in `specs/040-chapter-3-22/chapter-notes.md`. For a typing indicator "optional" means no typing; **for a cap it means no cap**, and optional is precisely what let chapter 3.21's module go unpassed and still compile. Required would make that failure impossible — the compiler would catch a fixture that forgot — but it breaks three fixtures, two of them fenced by earlier chapters, for a cap those fixtures do not test. **The bet is that T042, T042c and T048 are enough**, and the record says so plainly so that the next chapter to add a module can decide to make them all required and pay the fixture edits once.
- [ ] T042b [US1] Assert the shutdown sequence in `relay-platform/services/gateway/src/main.test.ts`: name the modules `shutdown()` must close as an explicit set and **fail on an unknown member**, so a module added later without a `close()` is a red test rather than a leak. **Nothing verified the registration before analysis pass 6** — `main.test.ts` calls `server.close()` and asserts nothing about which modules closed, so a missing registration leaks one Redis client per gateway, silently and for ever. This is chapter 3.21's defect inverted: there, awaiting `close()` made lint see a used variable and hid the missing wiring; here, not awaiting it is what nothing can see.
- [ ] T042c [US1] Register `connections.close()` in `shutdown()` in `relay-platform/services/gateway/src/main.ts`, beside the six modules already there. **Separated from T042 deliberately, because doing only this half is the exact shape of chapter 3.21's defect** — awaiting `close()` is what made lint see a used variable while the feature was unreachable. Without it every gateway leaks a Redis client; without T042 the cap does nothing. Neither task stands in for the other.
- [ ] T042d [US1] Pass the `connections` module into `attachSessions` in `session.itest.ts`'s **"the cap at the door"** describe, in `relay-platform/services/gateway/src/session.itest.ts`. **Without this, T011's red test can never go green** — that block calls `attachSessions({ server, api, logger })` with no modules, and every module is optional (T006b), so the cap is not enforced there. Phase 1 found this while writing the test. The block already holds the other two door refusals — chapter 3.8's rate limit and chapter 3.11's quota, the latter needing no module because the api decides it — and the connection cap is the third. **This makes two cap-enforcing fixtures, not one**, which research R11b now says.
- [ ] T043 [US1] Write the five-then-sixth test in `relay-platform/services/gateway/src/connections.itest.ts`: five accepted, the sixth refused, **and assert the close code and the error code**, not the fact of closing. A socket that closes for the wrong reason is identical from outside, which is the contract's whole subject.
- [ ] T044 [US1] Write the undisturbed test in `relay-platform/services/gateway/src/connections.itest.ts` (FR-005, SC-002, SC-012): with five open and a sixth refused, publish a message and assert **all five** receive it. The falsification: make the refusal path close the oldest connection instead, and confirm this test goes red.
- [ ] T045 [US1] Write the immediate-reconnect test in `relay-platform/services/gateway/src/connections.itest.ts` (FR-010, SC-003): with five open, close one, and assert a new connection is accepted **with no waiting period**. This is the observable difference between this refusal and a rate limit, and asserting it needs no clock.
- [ ] T046 [US1] Write the log test in `relay-platform/services/gateway/src/connections.itest.ts` (FR-015): a refusal logs the user, the environment and the observed count, and **carries no credential**. Chapter 3.2's rule — `NFR-SEC-06`, "the key rk_dev_abc… is invalid" is how a live secret reaches a support ticket.
- [ ] T047 [US1] Extend the comment at `relay-platform/services/gateway/src/presence.itest.ts:728` to say why its claim is **still true after this chapter**. It reads *"FR-RTM-09's five is enforced NOWHERE — `policy.ts:13` mentions it in a comment and nothing counts"* and then opens five connections for one user because nothing stops it. **That fixture passes no `connections` module, so nothing stops it there after this chapter either** (R11b). Say that, and say it is a decision rather than an oversight: modules are optional, this file tests presence, and the cap lives where a fixture asks for it. **Do not write "this test now sits exactly at the cap"** — the first version of this task said to, and it would have put a false claim into a test file, which is the defect the previous chapter found four times over in its own test titles. Do not change the test's five.
- [ ] T048 [US1] Extend `relay-platform/packages/outsider/src/integrate.itest.ts`: the sealed client holds five connections and is refused a sixth, reading the close code from the wire. **This is not a polish task.** It is the only instrument in the repository that boots the shipped binary, and it is what found chapter 3.21's inert feature after every other check was green.
- [ ] T049 [US1] Commit phase 5 in `relay-platform`, naming FR-001, FR-003, FR-004, FR-005, FR-010, FR-011a, FR-015, SC-002, SC-003, SC-007, SC-008, SC-012, SC-013. Gates first.

---

## Phase 6: User Story 2 — the count survives the gateway it was counted on (P1)

**Goal**: the half `docs/05-sad.md` calls "not built", and the half where the
published shape was wrong.

**Independent test**: two gateways in process against one Redis, three connections on
one and two on the other, the sixth refused on both; then kill one without closing
its sockets and assert a slot frees after the bound and not before.

- [ ] T050 [US2] Build the two-instance fixture in `relay-platform/services/gateway/src/connections.itest.ts`: two gateways booted in process on `server.listen(0)` with a stubbed `ApiClient`, as `resume.itest.ts` and chapter 3.21's `typing.itest.ts` do. **No api is spawned and no port range is claimed**, so the lane's spawn count stays at seven and this file does not join chapter 3.21's `gaps.md` item 9 list.
- [ ] T050a [US2] Create one `connections` module **per gateway instance** inside the fixture's `boot()` in `relay-platform/services/gateway/src/connections.itest.ts`, following `typing.itest.ts:101`, which calls `createTyping(...)` inside `boot()` for the same reason. **`releaseAll()` is what makes this a correctness question rather than a style one**: it releases the slots *this instance* holds, so a fixture sharing one module across two gateways would have the destroyed instance release the surviving instance's slots — and T052's crash test would pass for the wrong reason or fail confusingly. Two instances, two modules, two Redis client pairs, both closed in teardown.
- [ ] T050b [US2] Give the fixture in `relay-platform/services/gateway/src/connections.itest.ts` a **per-run environment id** — `env-${randomUUID()}` — and do NOT copy the constant those two files use. `typing.itest.ts:94` defaults to `"env-1"` and `resume.itest.ts` hardcodes `environment_id: "env-1"` in seven places; both lean on `user: "tuan"`; and the gateway's integration config sets no `fileParallelism`, so **all three files run at once**. **The reason is hygiene within this file, not a collision with those two** — pass 2 claimed a collision and pass 3 found the premise false: those fixtures pass no `connections` module and claim no slots (R11b). What remains is real anyway: this file is the only one that enforces the cap, it deliberately fills all five slots, and a constant identity would leak one test's slots into the next.
- [ ] T050c [US2] Delete this run's own slot keys in the fixture's teardown in `relay-platform/services/gateway/src/connections.itest.ts`, targeted at its own environment prefix — the pattern `limits.itest.ts:297` uses for its three rate-limit keys, and the only Redis cleanup anywhere in the gateway's integration files. **A slot's 60,000 ms TTL outlives the gateway package's ~45 s run**, so a slot leaked by a close handler that did not run survives into the next file and the next battery run, and this file deliberately fills all five.
- [ ] T051 [US2] Write the cross-instance refusal test in `relay-platform/services/gateway/src/connections.itest.ts` (FR-006, SC-004): three slots claimed on instance A, two on B, and the sixth refused **on either instance**. This is CON-02 as a test — connections must not require sticky routing for correctness.
- [ ] T052 [US2] Write the crash test in `relay-platform/services/gateway/src/connections.itest.ts` (FR-007, SC-005): five slots held by an instance that is destroyed **without closing its sockets**, and a new connection accepted after the bound. Use an injected clock as `presence.itest.ts` does; the wall-clock version belongs in `quickstart.md` and is what proves the injected clock is telling the truth.
- [ ] T053 [US2] Write the **before**-the-bound half in `relay-platform/services/gateway/src/connections.itest.ts`, as its own test: with those five slots held by a dead instance, a new connection is still refused before the bound elapses. **This is the half usually skipped**, and a test asserting only "the slot frees eventually" passes against an implementation with no bound at all.
- [ ] T053a [US2] Write the clean-shutdown test in `relay-platform/services/gateway/src/connections.itest.ts` (FR-011a, SC-013): five slots held by an instance, shut it down **cleanly** so `releaseAll()` runs, and assert a new connection is accepted **immediately** rather than after the bound. **T052 tests the opposite path** — an instance destroyed without closing its sockets, slots freeing at the bound — and the shutdown path frees them at once, which is the whole point of FR-011a against NFR-REL-03. **Found by building `traceability.md` during planning**: T041a implemented `releaseAll()`, T042b asserted it was *registered* in `main.ts`, and no task asserted it *released* anything.
- [ ] T054 [US2] Write the live-connection test in `relay-platform/services/gateway/src/connections.itest.ts` (FR-008, SC-006): a connection open and heartbeating across at least three consecutive bounds never loses its slot. Chapter 3.19 shipped a presence bug by arming a check at exactly its own grace period; this is the test that would have caught it.
- [ ] T055 [P] [US2] Write the per-environment test in `relay-platform/services/gateway/src/connections.itest.ts` (FR-012): the same user identifier holds five in each of two environments and neither count sees the other. Constitution I, and the key carries the environment.
- [ ] T056 [P] [US2] Write the resurrection test in `relay-platform/services/gateway/src/connections.itest.ts`: a renewal for a slot that has already expired **fails and does not re-create the key**. The falsification is direct — change `IFEQ` to a plain `SET` and confirm this test goes red.
- [ ] T056a [P] [US2] Write the **hijack** test in `relay-platform/services/gateway/src/connections.itest.ts`, which is a different state from T056's and the one FR-011's second sentence was added for: a slot expires, a second connection claims it, and the first connection's renewal **must be refused rather than overwrite it**. Set up by expiring the key and claiming it as another connection id. **The falsification is `IFEQ` → `XX`**, and this is the only test in the chapter that catches that substitution — T056 stays green under it, because `XX` also refuses a key that is absent.
- [ ] T056b [P] [US2] Write the re-claim test in `relay-platform/services/gateway/src/connections.itest.ts` (FR-011b, SC-014): expire a connection's slot with nothing else holding it, let the renewal be refused, and assert **the connection keeps working on a new slot** rather than closing. This is the branch a design that closes on any refused renewal gets wrong, and the one that happens after every brief outage.
- [ ] T056c [P] [US2] Write the cap-really-full test in `relay-platform/services/gateway/src/connections.itest.ts` (FR-011b, SC-014): expire a connection's slot, have another connection take it while the remaining four are held, and assert the loser is **closed with the cap's close code and error frame** — not left serving, which would be six connections against a count of five. **The falsification is direct**: make the branch keep the connection and confirm this test goes red.
- [ ] T057 [US2] Commit phase 6 in `relay-platform`, naming FR-006, FR-007, FR-008, FR-011, FR-011b, FR-012, SC-004, SC-005, SC-006, SC-014. Gates first.

---

## Phase 7: The race, and whether it can be observed at all

- [ ] T058 Falsify FR-013 before writing its test: change `claim`'s `SET ... NX` to a plain `SET` in `relay-platform/services/gateway/src/connections.ts`, run the Phase 6 suite, and record in `specs/040-chapter-3-22/baseline.txt` what goes red. **If nothing does, the race is not observable from the application** and T059 changes shape. Chapter 3.20 specified two orderings as requirements and neither was observable; the claim needs falsifying before the test is written.
- [ ] T059 Write the race test in `relay-platform/services/gateway/src/connections.itest.ts` per T058's result. **`Promise.all` of two connects does not force a race** — both reach Redis through one client on one socket and the commands serialise. Forcing it needs the two instances Phase 6 already built. If T058 found the ordering unobservable, this test asserts the invariant instead — never six held across many concurrent attempts — and the task record says the ordering itself could not be observed.
- [ ] T060 Record the outcome in `specs/040-chapter-3-22/baseline.txt` either way, including the case where the honest answer is *"`SET NX` makes the race unobservable and the invariant is what carries FR-013"*. Chapter 3.21 wrote that sentence twice and both times it was the right one.
- [ ] T061 Commit phase 7 in `relay-platform`, naming FR-013. Gates first.

---

## Phase 8: Failing open, where the log line is the only evidence

- [ ] T062 Wire the unreachable-registry path into `relay-platform/services/gateway/src/session.ts`: `claim` returning `null` accepts the connection (FR-016) and logs on **every occurrence** that the cap was not enforced (FR-016a).
- [ ] T063 Write the fail-open test in `relay-platform/services/gateway/src/connections.itest.ts` and **assert the log line, not the acceptance.** Chapter 3.18 found the general case: the fan-out's `publish` swallows its errors and resolves, so "the send returned 201 while Redis was down" is true of a publisher that does nothing at all — the assertion that carries the requirement is the log line. From outside, "accepted with the cap unenforced" and "accepted because the user was under it" are identical.
- [ ] T064 [P] Write the test distinguishing the two log states in `relay-platform/services/gateway/src/connections.itest.ts` (FR-016a): "not enforced" and "enforced and under the limit" must be told apart from the logs alone. A single line saying "accepted" satisfies neither.
- [ ] T065 [P] Write the no-local-fallback test in `relay-platform/services/gateway/src/connections.itest.ts` (FR-016b): with Redis unreachable, the system does **not** fall back to counting `registry.connectionsFor(user)`. The spec's Q2 rejected that explicitly — five per instance across four gateways is an effective cap of twenty wearing the label five, and a wrong number that looks right is worse than a stated absence.
- [ ] T066 Record in `specs/040-chapter-3-22/baseline.txt` that Constitution IV is passed non-vacuously here and why: the registry is ephemeral connection state, the api is the single writer of *persisted* state, and no row is written anywhere. Chapter 3.21 recorded that this principle can pass for the wrong reason.
- [ ] T067 Commit phase 8 in `relay-platform`, naming FR-016, FR-016a, FR-016b, SC-011. Gates first.

---

## Phase 9: The documents, including two that contradict each other

- [ ] T068 Declare `### ADR-23 — …` in `docs/05-sad.md`, following the ADR-17 through ADR-22 sections that end at line 1219. **An ADR has two homes and until analysis pass 13 this chapter tasked only one.** Counted: 22 `### ADR-NN` sections in the SAD and 22 `## ADR-NN` deep dives in `docs/06-adr-deep-dives.md` — ADR-22 sits at `05-sad.md:1219` and `06-adr-deep-dives.md:1377` under the same title, one the declaration and one the deep dive. **This is the half that matters most here**: the ADR contradicts a published row of this very file, and Constitution VII requires it. `sync-docs.sh` publishes both. ADR-23 is the right number — ADR-22 is the highest in both files.

  **Match the sibling format**, read from ADR-22 at `05-sad.md:1219`: a title line, then
  `**Status:** accepted (chapter 3.22) · **Drivers:** D<n>… · <clause it qualifies or
  supersedes>`, then the argument in roughly thirty lines. **The drivers are SAD driver
  codes, not prose** — ADR-22's are `D2, D8` — and Constitution VII's "drivers, rejected
  alternatives, reversal condition" is satisfied across the pair: the codes and the
  decision here, the rejected alternatives and the reversal condition in T068a's deep
  dive, which runs about twice as long.
- [ ] T068a Write the ADR-23 deep dive in `docs/06-adr-deep-dives.md`, the second half of T068 and not a substitute for it: five slot keys, claimed with `SET NX PX`, renewed with `SET IFEQ PX` and released with a conditional one-millisecond tombstone — **three commands, all conditional, and the TTL is the only unconditional way a slot is freed** — **contradicting `docs/05-sad.md:574`'s sorted set**. Constitution VII requires drivers, rejected alternatives and a reversal condition. The drivers: FR-013 needs atomic check-and-insert; a ZSET needs Lua for that; Constitution VII requires a superseding ADR *with profiling evidence* for a second language and **this lane cannot produce it** — largest fixture five channels, NFR-SCL-01 asks about ten thousand connections. Rejected: the ZSET with Lua, the ZSET with add-then-verify (4 held, two arrive, both refused — safe and wrong), a single counter (cannot expire per member, which is the original defect in a new shape). **Reversal condition**: a chapter needing a user's count without claiming a slot — an admin API, a dashboard — makes five reads the wrong shape, and the ZSET returns with the profiling evidence that would then exist. **And state the driver that was wrong**: the first draft argued that `SET XX` could not resurrect a released slot and cited `presence.ts:195`. `XX` tests existence, not ownership, and the analogy did not transfer because presence's value is the literal `"1"` and carries no identity. The choice survived its own broken driver; Constitution VII says disagreement attacks the driver, so an ADR is not allowed to ship with a false one.
- [ ] T069 Reconcile **both** `conn:{env}:{user}` rows in `docs/05-sad.md` (FR-017). Line 167's architecture overview describes registration in the present tense with a single instance ID as the value; line 574's table says a set of instance IDs and "Not built". **Both are wrong after this chapter and they were wrong differently before it** — one described something that did not exist, the other prescribed a shape this chapter rejects. Neither is a find-and-replace of the other.
- [ ] T070 [P] Update the heading in `docs/06-adr-deep-dives.md` that counts the records — chapter 3.21 moved it from "nineteen" to "twenty-two" and this chapter makes it twenty-three. **A count in a heading is a claim** and `sweep.py` compares it.
- [ ] T071 [P] Mark FR-RTM-09 as built in `docs/04-srs.md` and add the revision row, stating that **its second clause was already satisfied** by delivery walking connections and that this chapter added the tests rather than the behaviour. Do not claim the clause changed; it did not.
- [ ] T071a [P] Add the chapter 3.22 row to the published chapter table in `docs/07-tutorial-plan.md`, after 3.21's at line 170. **That table carries twenty-one rows, one per chapter from 3.1 to 3.21, and `sync-docs.sh` publishes the file** — so without this row the chapter ships with code, an ADR and a tutorial page while the published plan stops one chapter short. Name what the chapter closes: **FR-RTM-09**, both clauses — the cap, and *"each shall receive all events independently"* which existing code already satisfies and this chapter gives tests — and say that ADR-23 contradicts `docs/05-sad.md:574`. **No task owned this file until analysis pass 7**; its only other mention in this chapter is T073b citing it as evidence. Same class as chapter 3.19's fifth gaps item: nine translated chapters absent from the sitemap, nine one-line edits nobody made.
- [ ] T072 [P] Add **one** section to `docs/08-error-reference.md` — for the error code — and name the close code **inside its `**Status:**` line**, not in a heading of its own. `check-error-codes.mjs:59` filters every `## ` heading against `Object.keys(ERROR_CODES)` and fails on an orphan: *"these sections name no code in ERROR_CODES — remove them or the reference is lying"*, with no exemption. **A `## <close code>` section turns that gate red**, and the first version of this task said to add both. The convention is in the same file and chapter 3.21 wrote it — line 262: `**Status:** WebSocket \`error\` frame, then close 4002 · **Retryable:** no`. Copy that shape. **The client action is the point**: close one of the connections you already hold, then reconnect — immediately, with no waiting. It is the only code in the set whose correct handling is not a retry, and the gate requires both a cause and a client action.
- [ ] T073 Fix the port map in `relay-platform/services/gateway/src/limits.itest.ts` with the measured values, not the ones chapter 3.21's `gaps.md` item 4 states: add `4700-4900 presence.itest.ts api` and `5400-5600 membership.itest.ts api`, **and note that presence's range contains `meter.itest.ts`'s registered 4710-4770.** Item 4 says the map is 78% complete; it undercounts — one unregistered range strictly contains a registered one. **The fence cost this task used to claim does not exist.** It said chapters 3.08 and 3.13 both fence the file and that the edit costs two regenerated diffs — inherited from chapter 3.21's fourth gaps item and never re-run. `limits.itest.ts` is titled in **chapter-08 only, twice, and both are `(excerpt)`**, which `check-fence-chain.mjs:43` treats as its only skip. So the edit is free at the chain — **and free because it is unchecked**: an excerpt-only file is never verified against the repository at all, which is chapter 3.21's seventh gaps item.
- [ ] T073a Correct the two published tutorial pages this chapter falsifies, in **both locales**, and regenerate their fences. `relay-tutorial/app/(en)/part-3/chapter-20/the-membership-that-changed/page.mdx:2209` says *"FR-RTM-09's five-connection cap stays unbuilt… A sorted set scored by heartbeat and pruned on read is the correct version"* — **false twice over**: it will be built, and this chapter rejects the sorted set with an ADR. `chapter-21/the-frame-nobody-may-send/page.mdx:2911` says the chapter's *"first job is a decision"* and names a decision `docs/05-sad.md:574` had already published. **And it attributes the key to the wrong document**: it says *"The SRS describes `conn:{env}:{user}` as a Redis set"*, and `grep -c 'conn:{env}'` reads **0 in `docs/04-srs.md` and 2 in `docs/05-sad.md`**. Chapter 3.21 recorded this duty in its own words — *three published claims need correcting and two are chapter 3.20's* — and this chapter had no task for it until analysis pass 2.
- [ ] T073b Record EIR-WS-06 in `specs/040-chapter-3-22/gaps.md` with an owner, measured rather than asserted. The clause reads *"Close codes shall be documented and distinguish authentication failure, quota exhaustion, server shutdown, and protocol violation"*, verified by inspection. **Exactly one of the five close codes appears in `docs/08-error-reference.md`** — `close 4002`, at line 262, inside `unknown_frame_type`'s Status line. `4001`, `4003`, `4008` and `4009` appear nowhere a customer reads: one mention each in the SRS and the SAD, two in `docs/07-tutorial-plan.md` — which **is** published, so the argument is that a tutorial plan is not a close-code reference, not that it goes unread. **The first version of this task claimed `sync-docs.sh` does not publish it; it publishes all eight docs.** **This chapter takes it from one of five to one of six.** The gap predates the chapter and this chapter is the first to widen it — the same sentence chapter 3.21 wrote about EIR-WS-07, and the same remedy shape: an owner, not silence. **State the remedy that would close it**: `check-error-codes.mjs` reads `ERROR_CODES` and could read `CLOSE_CODES` too, requiring each close code to be named somewhere in the reference — five lines, and it goes red on the day it lands, which is why it is a decision and not a drive-by. **Do not add a `## <close code>` heading**: the orphan check would reject it (T072).
- [ ] T074 Record in `specs/040-chapter-3-22/gaps.md` that chapter 3.20's `gaps.md` item 19a eliminated "a port collision" on the grounds that *"the failing ports are in each file's own range"* — **a test that cannot detect this collision, because the colliding port is in each file's own range.** P = 1/200 per run against an observed 2.5–5% for those two files, so it is a contributor and not the cause. It is the first hypothesis in that item with a number attached.
- [ ] T075 Commit phase 9, naming FR-017, SC-009. Gates first, `pnpm build` before `check:errors`, and `pnpm -s check:docs` and `check:srs` from `relay-tutorial` with their exit codes captured.

---

## Phase 10: The chapter

- [ ] T076 Count what the chapter **teaches** and what it must **fence**, as two columns in `specs/040-chapter-3-22/chapter-notes.md`, and never ask either number to do the other's job. Chapters 3.15 and 3.16 revised one conflated count eight times; 3.17 kept two columns from T080 to T085 and neither was ever wrong.
- [ ] T077 Estimate the word count in `specs/040-chapter-3-22/chapter-notes.md` from the number of **arguments** the chapter makes, not the number of files it touches, and say which. 3.15 and 3.16 agreed on 153.5 and 154.3 words per taught file; 3.17 came in at 84.7, 45% below, because it taught sixteen files to make one argument. **This chapter's arguments are five**: where a refusal goes, why a sixth close code, why five keys instead of the published sorted set, why failing open is right for a cap when it is wrong for a lock, and **what a connection does when it loses its place** — the fifth added by analysis pass 5 with FR-011b, and the one carrying the FR-005 tension a reader will otherwise trip over. **The first version of this task said four**, written before that requirement existed and never re-derived; the word estimate comes from this count, and chapter 3.17 came in 45% below its own because prose tracks arguments rather than files.
- [ ] T078 Write the chapter page at `relay-tutorial/app/(en)/part-3/chapter-22/<slug>/page.mdx`. **MDX is not markdown** — an indented `400  {"code": …}` block is literal text in markdown and a JSX expression in MDX, which produced `Could not parse expression with acorn` at line 3134 of a 4,400-line page.
- [ ] T079 [P] Write `relay-tutorial/app/(en)/part-3/chapter-22/<slug>/figures.ts` with **four** diagrams: the slot walk, the two-instance count, the three refusals side by side with what each carries, and **the slot's lifecycle**. The fourth was added by analysis pass 7: the first three were listed before the conditional release, the re-claim branch and `releaseAll()` existed, and the lifecycle is now the chapter's most argued object — **five transitions out of `held`, four conditional on still owning the slot and one an unconditional TTL** — living only as ASCII in `specs/040-chapter-3-22/data-model.md`, where no reader of the chapter sees it. `check-figures.mjs` requires only that a diagram be passed as `code`, so a fourth costs nothing at the gate.
- [ ] T079a Record the fence exposure of every file this chapter touches in `specs/040-chapter-3-22/baseline.txt`, measured rather than assumed: `session.ts` **52 fences across 12 chapters** (24 of them excerpts), `main.ts` 22/10, `codes.ts` 16/8, `vitest.coverage.config.mts` 16/8, `codes.test.ts` 10/5, `integrate.itest.ts` 8/4, `main.test.ts` 4/2, `presence.itest.ts` 2/1, `limits.itest.ts` 2/1 (both excerpts). **This is not a cost table** — the chain is sequential and predecessors are untouched (T087). It is a **context warning for T080**: this chapter's `session.ts` fence sits at the end of the longest chain in the repository, and property 1 requires each hunk's pre-image to appear in the predecessor state **exactly once**. On a file with 52 prior fences, three lines of context is where *"more than one match"* starts to bite, which the chain's own comment calls context too thin to be a proof.
- [ ] T080 Generate the fences for every file the chapter must fence, with the predecessor `git rev-parse part3-ch21^{commit}` — **`0ecb21f`, a commit and not the tag**, because `part3-ch21` is annotated. The target is **HEAD with the appendix's edits undone**, not HEAD: a diff generated straight to HEAD performs the appendix's edit itself and the appendix's hunk then matches zero times.
- [ ] T081 Translate the page to `relay-tutorial/app/(vi)/vi/part-3/chapter-22/<slug>/page.mdx` **by splitting on the fence regex**, so the fences are byte-identical by construction rather than by review.
- [ ] T082 [P] Mirror `figures.ts` into the `(vi)` route.
- [ ] T083 Register the chapter in `relay-tutorial/lib/tutorial.ts`'s `series` with `status: "published"` and `translatedIn: ["vi"]`. **Not the sitemap** — `relay-tutorial/app/sitemap.ts:8` says *"publishing a chapter updates this with zero manual edits (FR-001/SC-001)"* and derives every entry from that registry, adding the Vietnamese alternate exactly when `translatedIn` includes `vi`. The first version of this task named the sitemap, which needs no edit. **`translatedIn` is the flag chapter 3.19's fifth gaps item was about** — nine translated chapters missing from the sitemap because nobody set it — and `lib/tutorial.ts` carries the comment saying it *"is the ONLY signal that a chapter BODY exists in a"* locale. The file is fenced by no chapter, so this costs nothing at the chain.
- [ ] T084 Run `pnpm -s check:fences` and `check:figures` from `relay-tutorial` with their exit codes captured, and expect the fenced-file count to rise from 234. **T073 edited a file two chapters fence**, so those two chapters' diffs are regenerated in this phase or the chain is red at the tag — which is exactly how chapter 3.20 tagged.
- [ ] T085 Commit phase 10 in `relay-tutorial` and `relay-platform`. Gates first, in both.

---

## Phase 11: Polish and close-out

**T084 RAN `check:fences` IN PHASE 10 AND THREE TASKS BELOW CAN EDIT A FENCED FILE
AFTER IT.** T086 may rewrite a test title in `connections.itest.ts`,
`connections.test.ts` or `codes.test.ts`; T087 pins a file eight chapters fence;
T088 may delete code from `connections.ts`. **Any of those forces THIS CHAPTER's diff for that file to be
regenerated before T097** — not any predecessor's, which describe earlier states and
must not be touched (T087) — and T097 is the only thing between a late edit and the
tag.
Chapter 3.20 ran its gates, changed a fenced file during its traceability
re-derivation, and tagged on a red chain.

- [ ] T086 Read every new test's title against its assertion, one at a time, in the same six files T090 traces: `relay-platform/services/gateway/src/connections.test.ts` and `relay-platform/services/gateway/src/connections.itest.ts` (new), and the tests this chapter adds to `relay-platform/services/gateway/src/session.itest.ts` (T011), `relay-platform/packages/protocol/src/codes.test.ts` (T022), `relay-platform/services/gateway/src/main.test.ts` (T042b) and `relay-platform/packages/outsider/src/integrate.itest.ts` (T048). **The first version named three and missed the same four T090 did.** The previous chapter's late pass found **four of its own** that outran their bodies: *"logs it once"* over a body proving five connection failures and zero publish ones, *"exactly the three names"* asserting two, an `expect(true).toBe(true)`, and one test with no `expect` at all. Grep the titles for a requirement id and read the assertion under each.
- [ ] T087 Run the coverage lane with the nine pinned variables and pin `services/gateway/src/connections.ts` at 100/100/100/100 in `relay-platform/vitest.coverage.config.mts`. **This file is the only one this chapter touches that the appendix amends, and that
— not its fence count — is the cost.** `relay-tutorial/fences/post-series.md` amends
seventeen files; intersecting them with everything this chapter edits leaves exactly
one, `vitest.coverage.config.mts`. Appendix hunks *"are applied AFTER the last
chapter"*, so their pre-image is the post-chapter state, which this task changes.
**Regenerate the appendix hunk in `relay-tutorial/fences/post-series.md`, and this
chapter's own fence for the file if it has one.** That was the only task of 121 whose
location was implied rather than named.

**DO NOT regenerate the eight predecessor chapters' diffs. An earlier version of this
task said to, and following it would have broken the chain in eight places.**
`check-fence-chain.mjs`'s header states the model: a path's fences form a chain
replayed by applying hunks, property 1 is *"every hunk's pre-image appears in the
predecessor state exactly once"*, and property 2 is *"the state after the last fence
equals the file on disk"*. Chapters 05–18's fences of this file describe **earlier**
states and are untouched by an edit made now; regenerating one against a later HEAD
moves its pre-image and fails property 1 for that chapter and every chapter after it.
The instruction was written from the shape of chapter 3.21's close-out note — *"four
files had drifted… plus a sixth appendix hunk"* — and only the appendix half of it
was right. **Read `coverage-summary.json`, not the text table** — the text reporter omits a file that is at 100% on all four metrics, so chapter 3.21's protocol module was simply absent from its table. A gap in a table is a gap in a table.
- [ ] T088 If any arm of `relay-platform/services/gateway/src/connections.ts` is uncovered, ask whether the code should be **deleted** before asking for a test. The ratchet has removed code three times rather than covered it; chapter 3.21 found all four of its uncovered arms reachable and drove two of them against the module directly, in a describe that builds it, because a gateway holds one connection per socket and cannot reach a reference count's second holder.
- [ ] T089 [P] Re-derive the files-changed count from `git diff --name-only` against `check:fences` and compare it with T076's prediction. **A first count is expected to be wrong**; the diff at the very end is what settles it.
- [ ] T090 [P] **Re-derive** `specs/040-chapter-3-22/traceability.md` against the shipped tree — T009b wrote it during planning, and this is the close-out check that what was planned is what exists. Both directions: every requirement to **how it is verified**, and every test this chapter adds to a named requirement, **wherever it lands**: `relay-platform/services/gateway/src/connections.test.ts` and `relay-platform/services/gateway/src/connections.itest.ts` (new), and the tests this chapter adds to `relay-platform/services/gateway/src/session.itest.ts` (T011), `relay-platform/packages/protocol/src/codes.test.ts` (T022), `relay-platform/services/gateway/src/main.test.ts` (T042b) and `relay-platform/packages/outsider/src/integrate.itest.ts` (T048). **The first version said "the three new files" and that missed four of the six** — the phrase was carried from chapter 3.21, which genuinely had three new files, and it counted `connections.ts`, which contains no tests. The omission included **T048's sealed-outsider test, the chapter's only proof the feature is not inert.** **And the forward column is a verification KIND, not a test title.** The previous chapter's file uses `SRS clause | Pri | Feature requirements | Verified by`, with values like "integration + arithmetic" — the "exact shipped test title" phrasing came from that chapter's close-out *measurement*, not its schema. Four items here cannot have a test title at all: **FR-017 and SC-009 are inspection** (no test reads `docs/05-sad.md`), **SC-010 is analysis** (the battery measures the lane mean), and **FR-002 is inspection or a checker**. Mark each with the constitution's own four methods — test, demonstration, inspection, analysis — which the SRS uses 139/22/21/25 times. Chapter 3.21's first re-derivation reported **28 gaps and every one was false** — it matched the first two long words of each proof phrase against test titles, and a proof phrase is a description while a title is a sentence. Write the mapping out requirement by requirement and check each title as an exact string.
- [ ] T091 Run the credential scan over the chapter's diff and record the patterns searched and every hit classified in `specs/040-chapter-3-22/baseline.txt`, never just "clean".
- [ ] T092 Complete `specs/040-chapter-3-22/gaps.md`, one item per gap with an owner, **each reference naming its chapter** because the numbers collide. Carry chapter 3.21's nine items with their status **re-checked against the tree rather than copied**. Its item 8 (coverage cannot see an omission) is answered by T042 and T048 and the answer belongs in writing. Its item 9 — five files still discarding their children's output — **is addressed to this chapter**: fix them or record the decision not to, and either way say which.
- [ ] T093 Decide `sweep.py`'s and `check-refs.py`'s ownership or record that they have none, for the fourth chapter running. Chapter 3.18's `sweep.py` died unowned and is chapter 3.19's item 8; 3.21 copied it again. **Two candidates and no third**: they graduate to `relay-tutorial`'s `check:*` scripts as the first instruments that compare a claim across two documents, or they stay per-chapter copies and the record says that is the decision rather than the default.
- [ ] T094 [P] Write `specs/040-chapter-3-22/chapter-notes.md`, and record that `attachSessions` now takes **nine** named parameters, up from eight. Constitution VII's "boring by design" governs services rather than parameter lists, so there is no violation to report — but this is the widest seam in the gateway and no document names a point at which the list becomes an options object. Name one, or record that nobody has. Then: what shipped, what the four arguments were, what went wrong, and the hand-off. **The ForwardRef says what the next chapter must DECIDE, not what it will conclude** — chapter 3.20 broke that rule and 3.21 corrected two published claims because of it, and then 3.21's own hand-off assigned this chapter a decision that had already been published at `docs/05-sad.md:574`. **Two consecutive chapters got the hand-off wrong in opposite directions.** 3.23 is editing and deleting a message (FR-MSG-07, FR-MSG-08, FR-MSG-10).
- [ ] T095 Stop the api, gateway and dispatcher containers with `RELAY_POSTGRES_PORT=15432 docker compose stop api gateway dispatcher`, then run the twenty-run battery of `pnpm test:integration` in `relay-platform`. **Nothing else runs on the machine, and that includes your own tooling** — chapter 3.20 counted 700 tests in run 1 and 701 in the rest because a test was written into a file mid-battery, and chapter 3.21 invalidated a coverage lane by running two forced turbo builds during it.
- [ ] T096 Record the battery in `specs/040-chapter-3-22/baseline.txt` with the mean, the stdev and every failure's file and message, and state whether the mean is inside the 240 s budget (SC-010). **Do not read a red run as this chapter's defect without reading the failure**, and do not read twenty green as proof: it rejects a per-run failure rate above 13.91% at 95% confidence and nothing finer, while chapter 3.20 measured 17.5% across forty runs.
- [ ] T097 **Run all gates LAST, after every record is written and every file is final**: `check:fences`, `check:docs`, `check:figures`, `check:srs`, `check:errors` from `relay-tutorial`, plus `check-refs.py` and `sweep.py`. Capture every exit code with a redirect; **do not pipe into `tail`**. A gate that runs before the last file change is a gate that ran. Chapter 3.20 ran its gates, then changed a fenced file during its traceability re-derivation, and tagged on a red chain.
- [ ] T098 Commit the close-out records in `specs/040-chapter-3-22/` **before anything is tagged**. A record of a commit hash inside the commit it names does not converge — chapter 3.21 proved it by quoting a hash that was overtaken twice within the hour, and the fix was to name the root tag by its role instead.
- [ ] T099 Tag `part3-ch22` in all three repositories with `git tag -a`, submodules first, and verify with `git ls-tree part3-ch22^{commit} relay-platform relay-tutorial` that the root's tree names exactly the two submodule tag commits. **Do not push** unless asked; all three repositories are already ahead of `origin/main`.
- [ ] T100 Trim `CLAUDE.md`. It reached 375 lines during this chapter's planning and it is loaded into every session; chapter 3.20's closed block is still 88 lines, three chapters back. **This file's own text names the hazard** — *"a proxy for a constraint outlives the constraint, and this file is where a proxy does the most damage"*. Decide what stays load-bearing rather than letting it grow by a chapter each time.
- [ ] T101 Hand `specs/036-chapter-3-18/reader-protocol.md` to a second person. **Named by chapters 3.14 through 3.21 and closed by none of them.** Forty-five minutes, six questions. No command in this repository discharges it: every check here compares bytes, and this chapter's three most expensive findings were a document contradicting itself, an arithmetic comment that does not produce its own constant, and an eliminated hypothesis eliminated by the wrong test.

---

## Dependencies and execution order

    Phase 1  setup, premises, the failing test        blocks everything
    Phase 2  US3, on UNCHANGED code                   depends on Phase 1 only
    Phase 3  the protocol                             blocks Phase 5's refusal
    Phase 4  the registry module                       blocks Phases 5, 6, 7, 8
    Phase 5  US1, the seam                     🎯 MVP  needs Phases 3 and 4
             T041 and T041a are two close paths; T042 and T042c are two
             main.ts edits and neither substitutes for the other; T042b is
             what makes the second one verifiable
    Phase 6  US2, cross-instance                      needs Phase 5's fixture work
    Phase 7  the race                                 needs Phase 6's two instances
    Phase 8  failing open                             needs Phase 4's `failable`
    Phase 9  the documents                            needs every decision made
             T068 BEFORE T070: T070 updates the heading that counts the ADRs to
             twenty-three, a number only true once T068 has written ADR-23
    Phase 10 the chapter                              needs Phase 9; T073 forces
                                                      two chapters' diffs to regenerate
    Phase 11 close-out                                last, and its gates are LAST

**Phase 2 is deliberately before Phase 3.** It touches no production file, so it can
also run in parallel with Phases 3 and 4 by anyone splitting the work — but it must
be *written* before Phase 5 changes the delivery path, or its tests stop being a
regression guard and become unfalsifiable.

## Parallel opportunities

    T004, T005, T006, T006a,    six independent premise checks and one
    T006b, T007                 measurement — all read-only, all different files
    T015, T016                  two US3 tests in one file, different subjects
    T023, T023a                 the payload decision and the unit-test check that
                                confirms it changed no count
    T034, T035                  the arms and the ratio, both unit-level
    T055, T056, T056a,          per-environment, resurrection, hijack, re-claim and
    T056b, T056c                cap-really-full — five states of one key, five
                                describes, three of them added by analysis
    T064, T065                  two fail-open properties
    T070, T071, T071a, T072     four documents — and T070's is SHARED with T068,
                                which writes the record its heading counts
    T079, T082                  the two locales' figures
    T089, T090                  the count and the traceability
    T094                        the chapter notes, beside the close-out measurements

**Every `[P]` in the phases above appears in this list and nothing else does**, checked
by comparing the two rather than by reading. The first version of this list omitted
T023 and T094 and named a task that does not exist in this chapter; pass 1 added
T056a and pass 2 added T006a and T006b.

**`[P]` means a different file or a different section of one.** T015 and T016 share
`connections.itest.ts` and are marked parallel because they add independent
describes; two tasks editing one function are not parallel however separate they
look. **Chapter 3.17 had a task that changed a shared helper and moved both halves
of a pair, so its oracle saw nothing** — that task's id is deliberately not written
here. This chapter has a task at the same number, and `check-refs.py` cannot tell a
foreign citation from a local one inside `tasks.md`: its own comment says a
predecessor's id "collides with a local one" and that pairs are what make the
collision impossible to hide — but the pair mechanism only guards the *other*
artifacts. Describing the task instead of naming it is the durable fix, which is what
that script's header asks for.

## Independent test criteria

| story | delivered when |
|---|---|
| US1 | five connections accepted, a sixth refused with the documented close code and error code, the five still receiving, and a slot freed by a close reusable immediately |
| US2 | the sixth refused whichever of two instances it reaches; a dead instance's slots free after the bound and not before; a live connection never loses its slot across three bounds |
| US3 | two connections of one user each receive every event exactly once, and one failing does not stop the other — provable on code this chapter did not change |

## Implementation strategy

**MVP is Phase 5**, which needs Phases 1, 3 and 4. That delivers FR-RTM-09's first
clause on one instance and every instance, because the slot design is cross-instance
from its first line — there is no local-only intermediate to ship and then replace.

**Phase 2 first is the deliberate inversion** and the reason is in the header: a
characterisation test written after the change proves nothing.

**Phase 6 is not optional to reach the requirement.** A cap that holds only where the
connection landed is not FR-RTM-09 and it violates CON-02. It is a separate phase
because its fixture is a separate piece of work, not because its behaviour is
separable.
