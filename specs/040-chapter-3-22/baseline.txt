CHAPTER 3.22 — BASELINE
================================================================

Every figure here was produced by a command and the command is named. A number
without one is a guess, and this chapter has had fifteen analysis passes' worth of
guesses corrected already.


T001 — THE LANE ENVIRONMENT
------------------------------------------------------------

    cd relay-platform
    RELAY_POSTGRES_PORT=15432 docker compose up -d --wait

    clickhouse running   mailpit running   nats running
    postgres running     redis running                      five, not four

**Five store containers.** A lane without mailpit fails on `ECONNREFUSED :8025`,
and omitting the port variable takes Postgres down mid-recreate with `bind:
address already in use`.

The nine pinned variables, prefixed on every lane command in this chapter:

    DATABASE_URL=postgres://relay:relay@localhost:15432/relay
    RELAY_REDIS_URL=redis://localhost:6379
    RELAY_NATS_URL=nats://localhost:4222
    RELAY_INTERNAL_CREDENTIAL=rk_svc_local_development_credential_0000
    RELAY_INTERNAL_CREDENTIAL_GATEWAY=rk_svc_local_development_gateway_00000
    RELAY_WEBHOOK_SECRET_KEY=BpDal75yBZp7Fc2GtGS3D1vh7qOKgCWJkF6/d0XWxBU=
    RELAY_OUTBOX_RELAY=off
    RELAY_EVENT_CONSUMER=off
    RELAY_DELIVERY_RELAY=off

plus `NO_COLOR=1 FORCE_COLOR=0`. **These apply to `pnpm coverage` as well as
`test:integration`** — chapter 3.21's close-out spent two full lane runs learning
that, and one of the two failures was predicted verbatim by its own `quickstart.md`.


T002 — THE STARTING LANE
------------------------------------------------------------

    pnpm test:integration --force        exit 0

    @relay/test-harness    1 file      17 tests
    @relay/api            29 files    510 tests
    @relay/gateway        10 files    174 tests
    @relay/dispatcher      1 file      16 tests
    @relay/e2e             3 files     10 tests
    ------------------------------------------------
    total                 44 files    727 tests

    wall clock  3m54.007s = 234.0 s      budget 240 s      6.0 s of headroom

**`--force` matters and this is why**: chapter 3.21's equivalent task omitted it,
turbo served a cached result, and the record read 150 s against the previous
chapter's 228.77 s.

**The count matches chapter 3.21's close-out exactly** — 44 files, 727 tests — so
nothing has drifted in the tree since it was tagged. **The wall clock is 5.4 s
slower** than that chapter's 228.63 s mean. Not investigated and not attributed: a
single run against a twenty-run mean is not a comparison, the same dev server from
another project was running then and now, and 6.0 s of headroom is the number that
matters for SC-010.


T003–T007, T006a, T006b — THE SEVEN PREMISES, ALL CONFIRMED
------------------------------------------------------------

Every one held. That is worth stating plainly because it is unusual: chapter 3.21
opened with three of its Phase 1 premises wrong, and this chapter's premises had
fifteen analysis passes to be wrong in.

T003 (P1) — no Lua, no MULTI, anywhere:

    grep -rn "\.eval(\|defineCommand\|\.multi(" --include="*.ts" services packages
      | grep -v node_modules
    -> 0 lines

The whole basis for rejecting the SAD's sorted set (research R3) and for the
Constitution VII argument. Still true.

T004 (P2) — one unregistered port range strictly contains a registered one:

    presence.itest.ts:95   const port = 4700 + Math.floor(Math.random() * 200)
    meter.itest.ts:65      const apiPort = 4710 + Math.floor(Math.random() * 60)
    gateway/vitest.integration.config.mts   fileParallelism: 0 lines

So 4700–4899 contains 4710–4769, and with no `fileParallelism` setting vitest's
default runs the two files at once. P = 1/200 per run. Chapter 3.20's `gaps.md`
item 19a eliminated "a port collision" because *"the failing ports are in each
file's own range"* — a test that cannot see this, because the colliding port is in
each file's own range.

T005 (P3) — the close-code set is written TWICE:

    codes.test.ts:18   it("contains exactly 4001, 4002, 4003, 4008, 4009", () => {
    codes.test.ts:20         4001, 4002, 4003, 4008, 4009,

Both get edited in Phase 3, or the test's name lists five codes while it checks six.
**The first version of this premise said one hit.** Analysis pass 6 ran the command.

T006 (P4) — `policy.ts` divides by five and ships a different number:

    policy.ts:43        connect: 3_000,
    policy.test.ts:54   expect(DEFAULT_LIMITS).toEqual({ ..., connect: 3_000 });

`10_000 / 5 = 2_000`. The limiter counts establishments, so the figure for its own
stated goal is 10,000. Three numbers. Not this chapter's to fix; the arithmetic goes
to `gaps.md`.

T006a (P5) — the renewal's flag exists on this server:

    redis_version:8.10.0
    SET p5 B IFEQ A     PX 60000  ->  OK          p5 = B
    SET p5 C IFEQ WRONG PX 60000  ->  (empty)     p5 = B, untouched

The design's correctness rests on this and it is newer than anything else this
platform asks of Redis. `compose.yaml:33` pins `redis:8-alpine` and that tag floats.

T006b — every gateway module is OPTIONAL, so the cap is opt-in per fixture:

    session.ts:192  fanout?: Fanout
    session.ts:206  limits?: GatewayLimits
    session.ts:217  presence?: Presence
    session.ts:222  membership?: Membership
    session.ts:231  typing?: Typing

    attachSessions( calls: presence 1, typing 1, resume 1   — all direct

No existing fixture will pass `connections`, so none claims a slot. **Analysis pass
2 assumed the opposite and built two findings and three tasks on it**; pass 3 found
the premise false. This is the check that carries the whole test strategy.

T007 — the two SAD rows still disagree:

    05-sad.md:167  (`conn:{env}:{user}` -> instance ID, TTL-refreshed), subscribes…
    05-sad.md:574  | `conn:{env}:{user}` -> set of instance IDs | … **Not built** …

Present tense and a single value in one; a set and "not built" in the other. FR-017
closes both.


T008–T010 — THE THREE INSTRUMENTS
------------------------------------------------------------

Copied and reset during planning at analysis pass 4, because four passes had
rewritten every artifact with no id or count checker in the directory.

    check-refs.py    exit 0   125 tasks, ids sequential, 28 declared foreign refs
    sweep.py         exit 0   125 tasks · 21 requirements · 14 criteria · 11 phases
    check-prose.py   exit 1   RED BY DESIGN — 8 fragments, 4 claims, all still
                              published until the two chapter pages are corrected
    check-prose --declared
                     exit 0   every fragment matches, so every claim is catchable

**check-refs red-tested here, not just run**: a scratch file citing `T077q` fired
`scratch-red.md:1 cites T077q`, and the file was deleted.

**And `check-refs` reached exit 0 for the first time in the chapter's life.** Its
standing complaint — *"traceability.md is missing, so nothing is traced"* — had been
reported as "expected" on all fifteen analysis passes without anyone asking why.
T009b is why.


T009b — TRACEABILITY, BUILT DURING PLANNING, AND IT FOUND TWO ORPHANS
------------------------------------------------------------

The previous chapter's file opens with the argument for this placement: chapter 3.18
ran the map at close-out and found FR-007 — a MUST — with no test at all, after
eight phases and nineteen analysis passes had each read `requirement -> test` and
believed it. **This chapter's re-derivation sat in the last phase until analysis pass
15 read that paragraph.**

Mapping 21 requirements and 14 criteria against the task list found two the list had
forgotten, both the shape the predecessor named — *a requirement whose verification
is a sentence rather than a test*:

    FR-011a / SC-013   the shutdown release was IMPLEMENTED AND UNTESTED. One task
                       writes releaseAll(), another asserts close() is REGISTERED in
                       main.ts — wiring, not release. The crash test covers the
                       opposite path, where slots free at the bound. Nothing
                       asserted a clean shutdown frees them IMMEDIATELY, which is
                       the point against NFR-REL-03.        -> new task T053a

    FR-002             "stated in exactly one place" was cited only by a commit
                       task. Nothing checked the single-statement property, which is
                       the whole requirement: a second literal five is how two
                       figures come apart, which is what policy.ts did.
                                                            -> new task T032a

And one requirement is now recorded as satisfied BY CONSTRUCTION rather than left
looking untested: FR-004's "no retry instruction" is guaranteed by
`errorFrameSchema` being a `z.strictObject` that cannot carry the field.

**The first draft of the file cited 56 task ids and `check-refs` rejected every
one.** Chapter 3.21's traceability carries zero: an id in another document goes
stale the moment tasks are renumbered, and what a reader needs is *how* a
requirement is verified rather than which line of the task list says so. Rewritten
to descriptions.


T011 — THE FAILING STATE, OBSERVED, AND A PREMISE THIS TASK GOT WRONG
------------------------------------------------------------

    × refuses a sixth connection for one user (FR-RTM-09 (3.22))   5094 ms
    Error: no close within 5s

**Red for exactly the stated reason.** The five acks succeeded — the loop asserts
each one's identity — and the sixth stayed open, which is the failing state this
phase exists to observe. Not `fetch failed`, not a timeout on the api, not a port
collision: the connection was accepted.

**AND WRITING IT FOUND A HOLE IN THE TASK.** T011 as planned put the test in
`session.itest.ts` and said nothing about which describe or which modules. But
T006b had just established that every gateway module is optional and that this file
calls `attachSessions` with none — **so a red test here could never go green.** A
red test that cannot pass is worse than no test: it trains a reader to ignore the
lane.

The resolution keeps the test where the task put it and adds what the task missed.
It lives in the describe named **"the cap at the door"**, which already holds the
other two door refusals — chapter 3.8's rate limit and chapter 3.11's quota, the
second needing no module because the api decides it — and **T042d passes the module
into that block in Phase 5.** The connection cap is the third refusal at that door
and belongs beside them.

**Research R11b is amended rather than left standing.** It said the new integration
file would be the only fixture enforcing the cap. Two will: the new file and this
block. That is a deliberate second, not a leak.


PHASE 2 — US3, AND THE FALSIFICATIONS FOUND MORE THAN THE TESTS DID
------------------------------------------------------------

T013 — THE BREAK, AND FR-014 IS NOT UNVERIFIED AFTER ALL

Replacing `registry.subscribersOf(channelId)` in `deliver` with a list
de-duplicated by `identity.userExternalId` turned three files red:

    session.itest.ts    × delivers to EVERY connection the same person holds (T021)
    presence.itest.ts   × does not stop messages reaching a connected member
    limits.itest.ts     FAIL (5/5 green clean and alone — collateral)

**The task anticipated the opposite.** T013 said "if none do, FR-014 is currently
unverified and that is the finding". One does: **chapter 3.18 already ships a test
for the message path.** So FR-014's first half was verified before this chapter
started, and the finding is the inverse of the one the task expected.

**But it stops short of Story 3 scenario 1.** That test waits for the FIRST
matching frame, so a DUPLICATE passes it unnoticed. The scenario says *"both
receive it, and each receives it once"* — and only a count can say the second half.
That is what T014 adds, and it is the whole reason a second test is not redundant.

T014, T015, T016 — GREEN ON UNCHANGED CODE, WHICH IS THE POINT

    src/connections.itest.ts    3 passed (3)

A per-run environment, `env-${randomUUID()}`, and not the `"env-1"` two neighbours
share. No api spawned, so the lane's spawn count stays at seven.

T017 — THE FALSIFICATION RUN, AND TWO OF THREE TESTS FAILED IT

Re-applying T013's break turned **one** of the three red, not three. That is a
finding about my own tests, not about the code:

    T014  message, exactly once     RED under the break            falsifiable
    T015  membership to both        green — the break was in the
                                    WRONG PATH; a second
                                    falsification, de-duplicating the
                                    membership audience by user
                                    instead of by connection id,
                                    turns it red                   falsifiable
    T016  a live connection after
          an earlier one is gone    NOT FALSIFIABLE — see below

**T016 could not be falsified and the test's claim was corrected rather than the
test kept.** FR-014's second half says one connection's delivery failure must not
prevent another's. `send` is a bare `socket.send(...)` with no try/catch, so the
falsification is to make it throw on a socket that is not OPEN — and **all three
tests stayed green**, because `registry.remove` runs from the terminated socket's
own close handler before any publish arrives. There is no failing send to survive:
a dead connection is *gone from the registry*, not *present and broken*.

So the property is real and **unobservable through this fixture**. Chapter 3.20's
lesson in its own words: a claim about an observable difference needs falsifying
before the test is written. The test stays, retitled to what it does assert — a
surviving connection keeps receiving after an earlier one is gone — and its comment
records why the stronger claim is not made.

**And the first draft of T016 terminated the WRONG socket.** `subscribersOf`
returns `[...byId.values()]` — a `Map`, so insertion order — so terminating the
second connection would leave a delivery loop that dies on its first failure
looking correct, having already reached the first. It now terminates the first.

ONE MORE THING THE PHASE NEEDED: AN ESLINT EXEMPTION, FOR A NEW REASON

`no-restricted-imports` forbids `ioredis` outside the two limits modules.
`connections.itest.ts` is now on `DRIVER_EXEMPT_TESTS`, and **not for the reason
the four gateway entries above it give**. Those four need a raw client as the
ORACLE — an assertion on Redis, read with neither service's code. This file needs
one as the STIMULUS: `Membership` exposes `onChange`, `subscribeChannel` and
`watch` and no `publish`, because the api publishes and the gateway only
subscribes. A fifth reason the rule cannot express, listed explicitly rather than
reached through a helper.

    lint       2 warnings, 0 errors      both pre-existing
    typecheck  13/13
    unit lane  11/11


PHASE 3 — THE PROTOCOL, AND A THIRD PINNED COUNT NO TASK OWNED
------------------------------------------------------------

T019 — THE TWO NAMES, DECIDED ONCE

    close code   4004  "connection limit reached"
    error code   connection_limit_reached

`4004` is the next free number after chapter 3.15's `4003`, from the space 1.3 and
3.15 drew from. **The error code's name was chosen against a collision rather than
for its sound**: `rate_limited` is one word away in the register and means the
opposite thing — it throttles a tenant's establishments per window and its own
message reads *"too many connections; retry after the window resets"*. Retrying is
what this code must not suggest, so it cannot borrow that phrasing.

T020, T021 — WHY NO REUSE WORKS, WRITTEN WHERE THE CODE IS

Every one of the five fails `codes.ts`'s standing test — *a client that cannot tell
them apart retries the wrong one for ever*:

    4001  sends the client to re-authenticate. The token is valid, so minting a new
          one succeeds and connecting fails again: 3.15's loop against a wall.
    4002  blames a client that did nothing wrong.
    4003  tells a person they are barred while four of their connections work.
    4008  tells a client to wait for a quota window. The slot may free a second
          later, and waiting never causes it.

T022 — AND A THIRD PINNED COUNT, WHICH T005 DID NOT MEASURE

T005 measured the close-code set's **two** places, at `codes.test.ts:18` and `:20`,
and both were updated. Running the file then failed on a third:

    × holds seventeen codes
    AssertionError: expected [ 'invalid_frame', …(17) ] to have a length of 17
                    but got 18

`ERROR_CODES`'s own count, pinned in a title and an assertion — 17 -> 18. **This
chapter's own `CLAUDE.md` records it** ("`codes.test.ts` asserts the exact close-code
set and the exact code count (16 -> 17)") and **no task named it**, which is the
fourth instance this chapter of a pinned number in a file it changes being owned by
nobody. Four places in one file, not two.

    codes.test.ts   10 passed (10)   after all four

T023, T023a — NO SCHEMA CHANGED, AND THE UNIT LANE PROVES IT

The refusal reuses `errorFrameSchema` exactly as it stands and the figures go in
`message`, per `quota_exceeded`'s precedent. So the frame union stays at eleven
members and `main.test.ts` stays green:

    main.test.ts    2 passed (2)

Line 43's `toHaveLength(11)` is untouched, and line 44's `close_codes` assertion is
derived from `CLOSE_CODES` and followed the new entry on its own. **Do not "fix"
it.**

T024 — check:errors IS RED, AND STAYS RED UNTIL PHASE 9

    pnpm build                exit 0   5 tasks
    pnpm -s check:errors      exit 1

    check-error-codes: these codes have no section in the reference, so their
    docs_url 404s:
      connection_limit_reached

**Expected, and now written into the task so nobody rediscovers it.** T072 adds the
section in Phase 9's documents phase; the gate is red from here until then. That is
the chapter's second deliberately-red instrument alongside `check-prose.py`, and the
reason is the same one chapter 3.21 gave: a red gate nobody explained is
indistinguishable from one nobody noticed.


PHASE 4 — T026, THE ELEVEN ARMS, LISTED BEFORE THE MODULE WAS WRITTEN
------------------------------------------------------------

Chapter 3.21 did this and three of four new files hit 100/100/100/100 on the first
run. Chapter 3.19 met the same requirement at close-out and paid seven tests, a
deleted branch and a re-measured battery.

     1  SET NX misses on a slot                       the walk moves to the next
     2  the walk finds no free slot                   five held, the cap bites
     3  SET IFEQ refused, key GONE                    the bound elapsed
     4  SET IFEQ refused, key holds ANOTHER id        somebody re-claimed it
     5  the failable catch, Redis unreachable         null, not zero
     6  a conditional release refused, slot not ours  do not free another's
     7  a conditional release for a slot never held   nothing to do
     8  releaseAll with nothing held                  the empty loop
     9  re-claim after a refused renewal: FREE SLOT   FR-011b, the common case
    10  re-claim: every slot held by another          the cap is genuinely full
    11  re-claim: the registry unreachable            FR-016 applies

**Eight of the eleven were listed in the task; three came from FR-011b**, which
analysis pass 5 added and which nothing re-derived this list against until analysis
pass 6 caught it.


T027–T035 — THE MODULE, AND TWO DEFECTS THE ARMS LIST DID NOT PREVENT
------------------------------------------------------------

    connections.test.ts    14 passed (14)    606 ms

**DEFECT ONE: `null` FROM A REPLY AND `null` FROM A THROW WERE THE SAME VALUE.**

`failable` returned `T | null` and read `null` as failure. But `SET … NX` returns
**nil on a miss**, which is the ordinary case when a slot is taken — so every claim
past the first reported `unenforced`:

    6 failed | 8 passed (14)
    AssertionError: expected { kind: 'unenforced' } to deeply equal
                    { kind: 'claimed', slot: 1, held: 1 }

**Arm 1 and arm 5 of T026's list produce the same wire value**, and listing them
separately did not stop me writing code that cannot tell them apart. The module's
own comment claimed the distinction — *"`null` means COULD NOT ASK; it is never
conflated with nothing held"* — while the code conflated them. Fixed with a
wrapper: `{ asked: true, reply } | { asked: false }`.

`presence.ts:232` gets away with `T | null` because its `SET` uses `NX`/`XX` where
nil and failure lead to the same branch. This module has to distinguish them, and
copying its signature was the mistake.

**DEFECT TWO: AN UNREACHABLE REDIS DID NOT FAIL, IT HUNG.**

    × returns unenforced rather than zero when Redis is unreachable   20003 ms

ioredis' defaults queue a command and retry, so `claim` waited out the test's own
timeout. **Against NFR-PRF-04's p95 < 1 s from handshake to `connection.ack`, a cap
that fails open must fail open QUICKLY** — otherwise FR-016's "accept the
connection" is indistinguishable from refusing it. `limits.ts:95` and
`presence.ts:133` both already use the three options that bound it:

    lazyConnect: true, maxRetriesPerRequest: 0, connectTimeout: 1_000

Now 606 ms for the whole file. **Neither defect was in the arms list, the plan, or
fifteen analysis passes** — both surfaced from running the tests against a real
Redis, which is the reason T034 insists on one.

AND A THIRD PINNED-NUMBER TEST, WHICH IS FR-002's

`connections.test.ts` reads `connections.ts` **from disk** and counts bare fives
outside comments. One, and it is `MAX_CONNECTIONS_PER_USER`. The requirement is
about drift rather than the value, and reasoning about it could not answer "how
many fives are in this file".

TWO ESLINT EXEMPTIONS, BOTH FOR STATED REASONS

`no-restricted-imports` forbids `ioredis` outside the two limits modules, in
production files as well as tests.

    services/gateway/src/connections.ts       the production block
    services/gateway/src/connections.itest.ts DRIVER_EXEMPT_TESTS (phase 2)

The production entry notes that this module's keys are the strongest case on that
list rather than the weakest: `conn:{env}:{user}:{slot}` puts the environment
first, so Principle I is structural in the key. And the `.itest.ts` file is
deliberately NOT in the production block — chapter 3.20 put one there and the later
`**/*.itest.ts` block silently overrode it.

    lint       2 warnings, 0 errors
    typecheck  13/13


PHASE 5 — THE SEAM, AND THE FIXTURE LEAKED INTO ITSELF
------------------------------------------------------------

T037–T041a — WHERE EACH PIECE WENT, AND WHY

    T037  the claim, after authenticate and after limits.spend. After
          authenticate because the environment is unknown before it — the rate
          limiter's own comment says the same. After the limiter because a client
          hammering the door should meet the cheaper check first: one INCR against
          a walk of up to five.
    T038  the refusal INSIDE handleUpgrade's callback: sendError then close 4004.
          A browser cannot read the body of a failed upgrade, so refusing at the
          seam gives it a bare connection failure with no code and no message.
    T039  the connection id is minted at the CLAIM, not at `open`, so the
          connection and its place agree — FR-011's "exactly one place for its
          lifetime". Only a connection that will be accepted claims one.
    T040  one renewal timer per connection, `unref`'d, cleared on close. An
          interval that outlives its connection renews a place nobody holds, and
          the `IFEQ` guard cannot fix that alone because the value would still be
          this connection's id.
    T040a FR-011b's three branches, with the FR-005 tension named in the code:
          FR-005 governs a REFUSAL, and here the connection has already lost its
          place to a competitor.
    T041  release on the close path that exists. **No 4009 drain path added** —
          `session.test.ts:965` says 4009 is emitted by nothing and `:982` asserts
          it stays that way.
    T041a `releaseAll` from `sessions.close()`, BEFORE `wss.close()`, because that
          call does not close established sockets and this is the last chance.

`heldPlaces` is a Map in the closure rather than a field on `Connection`: chapter
3.21 put one on the connection and corrected it to this, and the shared type in
`registry.ts` belongs to every chapter.

T042–T042d — THE FOUR WIRING TASKS, AND THE ONE THAT MADE ANOTHER VERIFIABLE

    T042   the `attachSessions` argument — the line chapter 3.21 forgot
    T042a  `connections?: Connections`, optional like the other five, recorded as
           a decision with what it costs
    T042b  main.test.ts asserts every module the file builds is also closed
    T042c  `await connections.close()` in `shutdown()`
    T042d  the module into `session.itest.ts`'s "cap at the door" describe

**T042b WENT RED ON ITS FIRST RUN AND THAT WAS THE CHECK WORKING.**

    × closes every module it builds (T042b (3.22))
    AssertionError: logger is built but never closed in shutdown()

`createLogger` returns a writer with nothing to release and `createServer` is the
file's own export. Both are now on a two-name `NOT_CLOSEABLE` list — **named rather
than pattern-excluded**, so a seventh module arriving without a `close()` fails
instead of being quietly skipped. Six closeable modules today.

    main.test.ts   3 passed (3)

**AND T011's RED TEST WENT GREEN.** The sixth connection closes 4004.

T043–T046 — AND A FIXTURE DEFECT OF MY OWN

First run: **2 failed | 4 passed**, both failures timing out at 5 s.

    × frees a slot on close, reusable with NO waiting period
    × logs the refusal with the user, the environment and the count

Both take five connections, and the test before them fills all five. **Every test
in the file shared one user**, so they shared five places — and the cap tests
exhaust them. Worse, each test did `await registry.close()` in its own body, which
quit the client BEFORE `afterEach` closed the sockets whose close handlers issue the
release. So five slots stayed held for the full sixty-second bound.

Two fixes, and the second is the one that makes the class impossible:

    a fresh user per test        `u-${randomUUID()}` in `beforeEach`, the way
                                `connections.test.ts` already does it
    registries closed in
    afterEach, after a 150 ms
    beat                        a release is fire-and-forget from a close handler

**The per-run environment reasoning was applied at file level and not at test
level**, which is the same mistake one scope down.

    connections.itest.ts   6 passed (6)

T044 — THE FALSIFICATION, WHICH IS THE TASK

Evicting the oldest instead of refusing the newest — the design the spec's Q1
rejected — turns the five-then-sixth test red:

    × accepts five and refuses the sixth with 4004    5332 ms

T047, T048 — AND T042d BROKE TWO OF CHAPTER 3.11's TESTS

**T048 PASSES AGAINST THE SHIPPED BINARIES.**

    packages/outsider    14 passed (14)

Five connections held and a sixth refused with 4004 and
`connection_limit_reached`, through rebuilt Docker images, a real api, a real
gateway and a browser `WebSocket`. **Nothing in that suite is stubbed**, which is
why it is the only instrument that could have caught chapter 3.21's inert feature —
and the reason T048 is a plan requirement rather than a polish item.

Running it needs the platform up, which the suite's own error message documents:
migrate, `--profile services up -d --build`, seed a tenant, export three
variables. **The images are built from Dockerfiles rather than bind-mounted**, so
they had to be rebuilt or the outsider would have tested the previous chapter's
code and passed for the wrong reason. `compose.yaml` sets
`RELAY_REDIS_URL: redis://redis:6379` for the gateway, checked before the run —
without it `createConnections` would default to `localhost` inside the container,
find nothing, and fail open silently.

**AND T042d WAS WRONG IN A WAY THE GATEWAY LANE FOUND.**

    × opens normally the moment the cap is raised
    × leaves a socket opened before the breach open and receiving

Both are chapter 3.11's quota tests. T042d wired the module into that chapter's
"cap at the door" describe, whose five tests share the user "tuan" — so the cap bit
them and exhausted the five places. **Wiring a new module into an existing describe
changes the behaviour of every test in it**, which is the same class as the leak
inside `connections.itest.ts` one file over, and I made it twice in one phase.

The fix keeps T011's test where Phase 1 put it and stops it borrowing somebody
else's fixture: a describe of its own, `the connection cap at the door (chapter
3.22, US1)`, with its own api, server and module. Chapter 3.11's block is back to
what it was.

    session.itest.ts        18 passed (18)
    gateway lane            11 files, 181 tests, all green

T047's comment records that `presence.itest.ts` still enforces nothing — it passes
no module — so its "FR-RTM-09's five is enforced NOWHERE" sentence stays true in
that file, and its five connections are now exactly the cap.

ONE FAILURE THAT WAS NOT MINE

    × typing.itest.ts > refuses a signal whose environment does not match

Passed alone: `1 passed | 22 skipped`. Parallel interference in the gateway
package, which is the class chapter 3.20's `gaps.md` item 19a is about. Recorded,
not attributed to this phase.
