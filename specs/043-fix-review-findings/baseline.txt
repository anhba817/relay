FEATURE 043 — FIX THE REVIEW'S FINDINGS
=======================================

THE LANE ENVIRONMENT, PINNED WHERE THE TASKS CAN SEE IT
-------------------------------------------------------

    DATABASE_URL=postgres://relay:relay@localhost:15432/relay
    RELAY_REDIS_URL=redis://localhost:6379
    RELAY_NATS_URL=nats://localhost:4222
    RELAY_INTERNAL_CREDENTIAL=rk_svc_local_development_credential_0000
    RELAY_INTERNAL_CREDENTIAL_GATEWAY=rk_svc_local_development_gateway_00000
    RELAY_WEBHOOK_SECRET_KEY=BpDal75yBZp7Fc2GtGS3D1vh7qOKgCWJkF6/d0XWxBU=
    RELAY_OUTBOX_RELAY=off  RELAY_EVENT_CONSUMER=off  RELAY_DELIVERY_RELAY=off

Local development values, pinned since chapter 3.24's `quickstart.md` and in the record
deliberately.


### THE ANALYSIS PASSES — SIX OF THEM, THIRTY-FOUR FINDINGS, AND TWO THAT WERE WRONG

Written during analysis rather than at close-out, because two of these entries are
findings ABOUT the analysis and would not survive to close-out otherwise.

    pass  asked                                          found  fixed
    ----  ---------------------------------------------  -----  -----
      1   do the artifacts agree with each other and
          with the tree?                                    11      9
      2   can the 69 tasks be executed in this order,
          and what do they break that nothing mentions?      6      6
      3   what did passes 1 and 2 themselves falsify?        6      6
      4   what happens if the premises are RUN?              4      4
      5   are the two read-only inferences from pass 2
          actually true?                                     3      3
      6   what does this look like to a reader who has
          seen none of these reports?                        4      4
      7   are the four stories the right four?               3      3
      8   is the review — the feature's INPUT — fully
          consumed?                                          3      3
      9   what do THIS REPOSITORY'S OWN INSTRUMENTS say
          about this feature?                                6      6
     10   what do the FOURTEEN GATES T005 asserts say,
          run together for the first time?                   3      3
     11   pass 10 derived a rule and did not apply it —
          what do the deferred complaints actually say?      3      3
    ----  ---------------------------------------------  -----  -----
                                                           52     50

**PASS 7 IS THE FIRST WHOSE MAIN RESULT IS PARTLY NEGATIVE.** Four decomposition hypotheses
went in and two died on reading the goal statements: FR-023 is not misfiled in Story 4,
whose goal names "the migration directory"; the close-code work is not misfiled in Story 3,
whose goal names looking up a close code. **A goal sentence earned its keep by refusing a
finding**, which is the only thing goal sentences are for.

What survived: FR-027 carried Story 1's label while three stories deliver it — **the same
defect pass 1 found in FR-024, reintroduced by pass 3**, because a requirement appended
during an edit gets filed wherever the edit was. And Story 1's independent test could not
see FR-007, one of its own twelve requirements; the story's goal and test were widened
rather than the requirement re-homed, which is the cheaper of the two repairs and is
recorded here as a choice.

**And the smallest useful increment was buried.** T010 alone — await child exit instead of
sleeping 200 ms — closes 10 of the last battery's 11 failures, in one file, with no
production code and no published prose falsified. It sat inside a twelve-requirement story
whose MVP claim was the story, not the line.

Two remain open by decision, both LOW: a near-duplicate pair (FR-006 stated from two
ends) and a missing "revert each probe" line in the gate-testing task.

**THE TWO FINDINGS THAT WERE WRONG ARE THE MOST USEFUL ENTRIES HERE**, and they share a
cause.

**Pass 2 said the `beforeEach` confounds the container-free classification.** It reasoned
from reading `connections.test.ts:41`: a hook that builds a registry against `REDIS` for
every test in the describe must break the tests that need no broker. Pass 3 wrote that
into `research.md` R2 and into two tasks. **Pass 4 ran it.** With
`RELAY_REDIS_URL=redis://127.0.0.1:6399`, the hook builds its registry, `afterAll` closes
it, and every no-broker test passes — `createConnections` does not connect eagerly. The
amendment stood in the research document for one full pass asserting something false
about the codebase.

**Pass 2 said the e2e lane's fixed ports actively collide with `limits.itest.ts`.** The
map at `limits.itest.ts:16` registers 4100-4300 to that file; the harness hard-codes
4100-4102 and appears nowhere in the map. Pass 2 added "turbo runs the two packages at
once" and derived `P = 3/200 per run`, and hinted it explained the battery's one
unexplained failure. **Pass 5 read one line of `package.json`.**

    "test:integration": "turbo run test:integration --concurrency=1 --filter=!@relay/outsider"

`--concurrency=1`. The two packages never run together. The overlap is real and LATENT;
the probability does not apply; the hint about run 18 is withdrawn and that failure still
has no supported cause. **`--concurrency=1` is written in `CLAUDE.md`** — it was read as a
fact about suites inside a package and never applied to packages. Within-package
parallelism is real (`presence.itest.ts` and `meter.itest.ts` collide for exactly that
reason); cross-package parallelism does not exist here.

**The failure mode both share: reading a file where one command would have answered it.**
Three passes of reading produced twenty-three findings; the first pass that executed
anything found that one of them was false.


### MEASUREMENTS TAKEN DURING ANALYSIS — RE-TAKE THEM BEFORE ACTING

Each was taken on a lane that has since run a twenty-run battery and three coverage runs.

    avatar_url schemes stored     (null) 387,091 · https 586 · anything else 0
                                  psql -Atc "select coalesce(split_part(avatar_url,':',1),
                                  '(null)'), count(*) from users group by 1"

    webhook event types           32,606 type-rows, four distinct, all DECLARED by FR-WHK-02
                                  message.created 31,757 · channel.created 741
                                  message.deleted 54 · message.updated 54
                                  refused by the chosen rule (declared eight):     0
                                  refused by the review's rule (emitted five):   741 = 2.3%

    connections.test.ts split     12 failed | 5 passed against a dead broker
                                  RELAY_REDIS_URL=redis://127.0.0.1:6399 vitest run
                                    src/connections.test.ts
                                  no broker: does not throw for a slot never held ·
                                  does not throw when it holds nothing · returns unenforced
                                  when Redis is unreachable · keeps the heartbeat inside the
                                  bound · states the maximum in exactly one place

    the lane's budget             240 s, slowest green run of the last battery 234.61 s,
                                  headroom 5.39 s; the e2e package costs 7.41 s across
                                  three suites and three boot/stop cycles

**Research predicted two container-free tests and there are five.** The heartbeat test was
filed under "each asserts registry behaviour" on the strength of its title; it asserts a
timing bound and never reaches the broker. **A title is not an inventory of what a test
touches** — the same defect as a task id in a test title, one category over.


### THE DECISION THAT IS A JUDGEMENT AND NOT A MEASUREMENT

`PORT=0` over the cheaper repair. With the overlap latent, keeping the fixed ports and
fixing only the teardown closes every failure the last battery produced, touches no
production file, and falsifies no published transcript.

What keeps `PORT=0` is the argument FR-008 makes about three literals: the port map is
maintained by hand and has been wrong twice — chapter 3.21's `gaps.md` item 4 found two
missing entries and it is missing `packages/e2e` today — and a range that is safe only
because of a flag in another package's script is a range somebody re-checks every time
that flag moves.

**Reverting costs one paragraph in `plan.md` and four task lines**: drop T007, T008, T009
and T018a, keep T010.


### WHAT NO GATE IN THIS REPOSITORY CAN CHECK

`check-fence-chain.mjs:77` collects a fence only if it matches

    /^```(\w+) title="([^"]+)"\s*$/

so a fence with no title is never compared to anything. `part-2/chapter-08`'s milestone
publishes its proof of a working run as a bare ```text transcript — `api up on 4100`,
`gateway 1 up on 4101`, `ws://127.0.0.1:4102` — and this feature makes every one of those
numbers ephemeral. **The milestone's own evidence is outside every gate here.** T018a is
the only thing that amends it, and T018a exists because pass 2 grepped for `4100` rather
than trusting the chain.


### PASS 8 — THE INPUT MAP NOBODY HAD DRAWN, AND A CLASS NOBODY HAD COUNTED

Seven passes checked this feature's internals. The eighth checked what it was built from.

    review group          items   covered by a requirement   deferred by a named assumption
    ------------------    -----   ------------------------   ------------------------------
    current findings         10                          9   1  audit log -> chapter 4.7
    roadmap rows              3                          0   3  Part 4 and later
    amendment concerns        8                          4   4
    ------------------    -----   ------------------------   ------------------------------
                             21                         13   8

**Nothing is silently dropped**, and that is the first clean result eight passes have
produced about anything structural.

**146 OF 904 PUBLISHED CODE FENCES ARE OUTSIDE EVERY GATE.** `check-fence-chain.mjs:77`
collects a fence only if it matches `title="…"`. Across the 41 English chapter pages: 758
titled and **146 untitled**, and an untitled fence is never collected and never compared to
anything. `gaps.md` 3.22-3 records excerpt-only chains, which are skipped *by* title; this is
one step further out and had never been counted.

Six passes discussed `part-2/chapter-08`'s milestone transcript as a one-off. It is 0.7% of a
class, and it was only ever noticed because pass 2 grepped for a port number rather than
trusting the chain. **T018a repairs one of the 146. T060 now opens the class as a gap.**

**And two counts of the review were wrong in the artifacts that count it.** `T060a` said "every
finding", which reaches ten of twenty-one rows — the review's second table calls its rows
*concerns*, and this feature closes four of them. The assumption block said "the five amendment
concerns not named in a requirement" and listed four. That second one is the defect chapter
3.24 recorded against its own C8 — *"it says 'a fourth instrument' and 'all four were
improved', then lists three"* — committed in the feature whose `baseline.txt` cites it.


### PASS 9 — NINE PASSES CHECKED THIS FEATURE WITH TOOLS POINTED AT THE PREDECESSOR

The six Python instruments were copied forward by a task that runs during implementation, so
every pass until the ninth ran `specs/042-chapter-3-24/check-refs.py` — which reads 042. **The
first time any of them read 043, five of the five that produce findings failed.**

    check-refs        61 problems      check-quickstart  14 problems
    sweep              structural      check-checklist   10 problems
    check-prose        clean

**`regen-traceability.py` had `CHAPTER = "3.24"` HARD-CODED** — the predecessor's number,
carried in by the copy and invisible to anyone reading the file. `check-refs.py` beside it
derives the same value from its own directory name and always has. Both now derive it, and both
fall back to matching a bare id when the directory names no chapter, because `(3.24)`
disambiguates ids ACROSS chapters and there is nothing to disambiguate inside one feature.

**`traceability.md` did not exist**, and `check-refs.py`'s first line says
`traceability.md is missing, so nothing is traced`. Nine passes never saw that line.

**AND "100% COVERAGE" WAS A READING, NOT A MEASUREMENT.** Every pass from the first reported it,
derived by mapping tasks to requirements by eye. The first mechanical count found **12 of 33
requirements and 3 of 13 criteria cited by id in a task line**. Fifty citations later it is 33
of 33 and 13 of 13, 0 untraced — and the gap between those two numbers is the difference between
a claim and a check.

**One more substring bug, mine, in the script that added the citations.** It skipped `FR-023`
because the substring was already present inside `FR-023a` — the same class `check-refs.py`'s own
comment records about `T(\d{3})` failing to see `T107a`.

**Three instruments still carry 042's configuration** and are named in T001: `check-quickstart`'s
OWED lists, `check-checklist`'s file enumeration, and `sweep`'s expectation of `### Phase N`
headings in a plan that has none.


### PASS 10 — THE BASELINE T005 ASSERTS, RUN FOR THE FIRST TIME

Nine passes reported gates green by running the three that were convenient. The fourteen T005
names had never been run together.

    platform  typecheck 0 · lint 0 · build 0
    tutorial  check:fences 0 · check:docs 0 · check:figures 0 · check:srs 0 · check:errors 0
    feature   check-refs 1 · sweep 1 · check-prose 0 · check-quickstart 1 · check-checklist 1
              regen-traceability 0

**Eleven repository gates green, three feature instruments red on the predecessor's
configuration** — and `check-quickstart` and `check-checklist` appeared nowhere in the task list
except inside T001, which described the work and assigned it to nobody. T001a now owns it.

**One of the three was not configuration at all.** `sweep` reported `phase count: plan 0, tasks
7`: the plan named no phases while the task list ran seven, so nothing could compare them. The
plan now names all seven. **An instrument complaining about its own configuration and an
instrument finding a gap look identical until you read the complaint.**

**`check-refs.py` could not see a `.py` path**, in a feature whose tasks reference six Python
instruments — the same blind spot as `.mjs` one pass earlier, found the same way, by arguing
with a false positive. `py` and `sh` are now in the pattern.

**AND THE FEATURE'S OWN GATE COULD NOT HAVE WORKED.** FR-021, as analysis pass 6 amended it,
required a gate in `relay-tutorial/scripts/` to read this feature's success criteria.
`relay-tutorial` is its own git repository, nothing in it reads `../specs`, and `sync-docs.sh`'s
precedent for `../docs` errors out when the parent is absent — and a feature directory is
transient, so a gate pointed at one dies when the feature closes. Split into FR-021 for the
published ledger and FR-021a for this feature's own criteria: **each gate in the repository that
owns the file it reads**, which is the doctrine "a chapter cannot do the appendix's work" one
level up.

**That is the second design defect introduced by an analysis pass** — pass 3 added a requirement
with no task, pass 6 added a gate with no reachable path. Both came from adding a rule where the
editing was already happening rather than where the rule belongs.


### PASS 11 — I WROTE THE RULE DOWN AND BROKE IT IN THE SAME PASS

Pass 10 read `sweep`'s complaint, found the plan named no phases against the task list's seven,
fixed it, and recorded the lesson here: **an instrument complaining about its own configuration
and an instrument finding a gap look identical until you read the complaint.** It then filed
twenty-four complaints from the other two instruments as the predecessor's configuration, from
their counts alone, and moved them into a task.

**Twenty-three of the twenty-four were real.**

    check-checklist    9 of 9    every file spec.md names must be enumerated with its class,
                                 and the count certified. Eight were missing.
    check-quickstart  13 of 14   an id counts only in a scenario's `**Expected**` line.
                                 Thirteen sat in scenario headings instead.
    check-quickstart   1 of 14   genuine: an exemption for a file this quickstart no
                                 longer mentions.

**`check-quickstart.py:68` says why the rule is what it is**, in its own comment: *"Only an
**Expected** line counts. A first version searched the whole file"* — it was deliberately
narrowed, because a mention anywhere is not proof. A heading says which criterion a scenario is
about; the Expected line says what would count as passing. Pass 9 added SC-012 and SC-013 to
headings and called them covered.

**One criterion needed an Expected line for something no tool can run.** SC-010 — every
statement in the review matches what it cites — is checked by a person reading two paragraphs.
It now has an expectation anyway: *a criterion nothing can automate still needs a stated
expectation, or the reader knows what to read and not what would count as wrong.*

**T001a shrank from a configuration task to a re-run.** It was created in pass 10 to own work
that turned out to be one stale exemption; the rest was this feature's own documents being
incomplete, reported accurately, on every run since pass 9.

**All fourteen gates are green for the first time.**
