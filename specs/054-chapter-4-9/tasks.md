# Tasks: chapter 4.9 — "Milestone: the meter agrees"

**Feature**: `specs/054-chapter-4-9` · **Plan**: [plan.md](./plan.md) ·
**Research**: [research.md](./research.md) — read it first, two of `docs/12` §2.3's four
premises are falsified there.

**Read before executing any task**: this feature's numbers were taken on 2026-09-17 and the
lane moves. A task that quotes a figure says where it came from; a task that asks for a
measurement says what would falsify it.

---

## Phase 1: Premises and openings

**Goal**: every figure this feature will be measured against, taken before anything changes.

- [X] T001 Run the quickstart's eight steps **as written**, and record where each one diverges from what it claims in `specs/054-chapter-4-9/baseline.txt`. Chapter 4.8's step 2 caught its step 1 the first time anybody ran it — `docker compose up -d` returns before ClickHouse will answer, and the positive control is what said so.
- [X] T002 [P] Enumerate every gate this feature must run at its close and record the command for each: `pnpm lint`, `pnpm typecheck`, `pnpm build` in both repositories, `pnpm test`, **the integration lanes, derived rather than counted from memory** — the packages carrying a `test:integration` script and at least one `.itest.ts` file, which is **six inside the gate** (api 33 suites, gateway 12, e2e 4, ingester 2, test-harness 2, dispatcher 1) plus `outsider`, excluded by `--filter=!`. An earlier draft of this task said *"the five integration lanes"* and another said *"the four lanes"*; neither is any of those numbers, and this is the task T064 runs *"every gate"* against — the same shape as the *"eleven gates"* figure `gaps.md` 053 found contradicted inside CLAUDE.md, in the task that cites it. The sealed suite is `pnpm test:outsider`, `package.json:16`, and not the six hand-run commands `gaps.md` 053-3 implies. `pnpm coverage`, and the tutorial's `check:docs`, `check:srs`, `check:figures`, `check:fences`, `check:errors`. **Derive the list with a command and record both the command and its output**, rather than counting from memory — `gaps.md` 053's own record had "eleven gates" contradicted inside CLAUDE.md, and the first draft of this very task said five lanes where six run.
  **And give the derivation a positive control.** The first attempt at this measurement used `src/*.itest.ts` and reported six packages **with the api — 33 suites, the largest — missing entirely**, because its suites live in `src/request-log/` and `src/metering/` rather than directly in `src/`. That is feature 049's glob trap inverted, and the tell was the same: *the package with the most suites was absent*, which is what a glob failure looks like rather than what a corpus looks like.
- [X] T003 [P] Record the fence-chain opening: total, APPLY and HEAD, and HEAD split into `differs at line` and `does not exist in relay-platform`. The close is reported as a delta against this, by kind and locale (FR-018). **Name the third bucket rather than calling it "elsewhere"**: measured, APPLY 74 is **30 `(en)` + 30 `(vi)` + 14**, and all 14 are `fences/post-series.md` — the appendix, which is where the gate's definition lives (R1a) and which T008b enumerates. Four chapters have reported that bucket without naming it, and it is the one file this feature edits twice.
  **AND CHECK THE INSTRUMENT BEFORE BELIEVING THE SPLIT.** Analysis pass 15's first attempt used `awk -F/ '$1 ~ /\(en\)/'` and returned **0 en, 0 vi, 74 elsewhere**, because the paths begin `app/(en)/…` so field 1 is `app`. It was caught only because 30/30/14 was already on record. **A split with no prior has no such tell**, so match against a known total and say which instrument produced each number.
- [X] T003a **Record that the workflow is red on every push, and by what.** `check-fence-chain.mjs` calls `process.exit(1)` whenever `problems.length > 0`; `check-fence-chain.sh` exits 0 only when `relay-platform` is absent; `ci.yml`'s tutorial job ends with `pnpm check:fences` and no `continue-on-error`. The standing count is **110** and every chapter since 4.1 has closed at *"110 → 110, delta 0"*. Record the count, the step, and the date the baseline was established — **this is the opening figure FR-002's scoping rests on.**
- [X] T003b [P] **Measure the prose words of all three published milestone chapters**, with `scripts/prose-words.mjs`, and record them beside `docs/07` §2's 2,000–4,000 bound. Expected: `milestone-the-tuan-test` 3,151, `milestone-the-isolation-gauntlet` 1,873, `errors-that-resolve-and-an-outsider` 1,683 — **two of the three below the floor this feature imposes on 4.9**. This is the opening figure FR-017a's decision rests on.
- [X] T003c [P] **Count the figures in all three published milestone chapters** — `grep -c "<Figure "` — and record them beside `docs/07` §2's *"2–4 captioned diagrams per chapter (≥1 per chapter half)"*. Expected: 0, 1, 2 against 4.7's and 4.8's 3 each. **The same two chapters that fail the word floor fail this one**, which makes it one decision rather than two (T059a). And **nothing enforces it**: `check:figures` verifies that every diagram is passed as `code=` and that every import resolves — a chapter with zero figures passes that gate.
- [X] T004 Record `pnpm test:integration`'s current behaviour exactly: tasks planned, tasks attempted, the printed summary line, and **each failing suite by name and by its assertion message**. Expected from research: 18 planned, 10 attempted, `Tasks: 8 successful, 10 total`, six failures.
  **AND DECOMPOSE THE 18 WITH `--dry=json`, because the summary counts tasks and four chapters have read it as lanes.** 18 is **9 builds + 9 test tasks**, and three of the nine — `@relay/config`, `@relay/protocol`, `@relay/service-kit` — have **no `test:integration` script** and complete as successful no-ops. Six packages hold real suites. Record both numbers and the difference between them.
  **And record the full command**, which carries `--filter=!@relay/outsider`: the sealed suite is excluded by name, not by the early stop, and `gaps.md` 053-3 says otherwise.
- [X] T004a For each of the six, run the suite **alone** and record whether it passes. A suite that passes alone and fails in the lane is a different defect from one that fails both ways, and chapter 4.8 found a gateway flake exactly that way — green once and red once, run alone twice.
- [X] T005 [P] Record both sides of the reconciler on the lane: `daily_usage_billing` rows and distinct environment ids, `usage_periods` rows and distinct environments, `max(messages_sent)` and the mean, and **how many environment ids appear in both**. Expected: 7 over 4, 2,317 over 2,212, max 1,017, and 0 in both.
- [X] T006 [P] Re-derive the smallest-expressible-drift table from the reconciler's own arithmetic rather than copying it from R9 or from chapter 4.7 — `difference = |a - o| / max(a, o)` and `<=`. Record the volumes 9, 100, 1,000, 10,000, 100,000 and **the lane's own largest tenant-period**.
- [X] T007 [P] Record `message_events`' row count and the TTL on it, from the server. The corpus load's window has to sit inside that TTL or the figure is about the TTL (R6, and chapter 4.2 measured the removal happening **at INSERT**).
- [X] T008 [P] Count the titled fences on every file the plan names, in both locales, and record which are already HEAD problems. The plan's counts were taken at plan time and `check:fences` is the instrument — **re-count rather than trust the plan**. Expected: `corpus.mjs` 0/0, `reconcile-usage.mjs` 1 en and **0 vi**, `compose.yaml` 8/6, `turbo.json` 10/10 clean, `package.json` 4/4 and already a HEAD problem at line 15.
- [X] T008a **Record the translation frontier rather than treating one file as special.** `scripts/reconcile-usage.mjs` at 1 en and 0 vi is not a shape needing handling: the Vietnamese Part 4 holds `chapter-01`, `chapter-02` and `chapter-03` and stops, so **every file chapters 4.4 through 4.8 fenced is en-only**. Record where the frontier is at this feature's opening, because it is the number `gaps.md` 050-3 is about and the reason no vi mirror is owed for this chapter.
- [X] T008b **Enumerate every file `relay-tutorial/fences/post-series.md` carries a fence for, at phase 1 rather than at phase 7.** The appendix applies **after every chapter**, so any file it edits cannot take a chapter hunk — chapter 4.8 discovered that at its close and spent a phase on it. Eighteen titles today, and **two are this feature's: `package.json` and `turbo.json`**. Cross it with T008's list and mark each file `chapter hunk` or `APPENDIX ONLY` before any code is written.
- [X] T008c **Read the appendix's `package.json` hunk and record what its `-`/`+` pair is.** It is the `test:integration` line — the only place in the series that line is published — plus `test:outsider` and `coverage`. **So the gate's definition lives in the appendix**, and a chapter hunk changing it would be anchored on a pre-appendix state and then **overwritten** by the appendix. 4.8's failure at least reported, because its edit and the appendix's did not overlap; an overlapping edit does not report, it disappears.
- [X] T009 [P] Quote constitution III's fourth bullet and SRS FR-ANL-06 as amended at 1.14 **side by side, from the files**, into `baseline.txt`. R7 is the argument; this is the evidence, and chapter 4.7 found two published documents citing a clause that does not exist by not doing this.
- [X] T010 [P] Read `docs/11-scalability-measurement-2026-09-06.md` **end to end** and record **every section heading**, not a summary of its shape. It is the precedent FR-010 binds this feature's measurement document to, and R8's first draft described it correctly in outline while missing four of its eight sections — which is what a description written from a summary looks like. Record its method line verbatim, and record **FR-ANL-06's verification letter beside NFR-SCL-01's**: `T` against `A`.
- [X] T011 Record what `corpus.mjs` writes today, by reading the file rather than by running it: every table it inserts into, every table it only counts. Expected: `usage_periods` appears once, as a row count in its closing report, and `usage_active_users` not at all.
- [X] T011a **Run `corpus.mjs`'s two validations red**, rather than reading them: `CORPUS_DAYS=90` and `CORPUS_ENVIRONMENTS=1` must each be refused with the message the file promises. Analysis pass 1 found both floors by reading, after three artifacts had already published a command that violates both — so the pass that runs them is the one that would have caught it first, and T001 is where that belongs.
- [X] T011b **Establish that nothing runs the reconciler, by searching rather than by asserting.** Grep `package.json`, `turbo.json`, every workflow, every shell script and any Makefile for `reconcile-usage`; record the hit count. Record `ci.yml`'s trigger list. Record the platform's existing recurring-work switches — `RELAY_OUTBOX_RELAY`, `RELAY_DELIVERY_RELAY`, `RELAY_QUOTA_RELAY`, `RELAY_NOTIFICATION_RELAY`, `RELAY_EVENT_CONSUMER` — because the pattern is what makes the schedule a decision rather than a gap. **A zero from a grep is a claim about the corpus only if the corpus is named**, so name the files searched.
- [X] T012 Commit phase 1.

**Checkpoint**: every number this feature will be judged against is written down, and the two
documents in conflict are quoted rather than paraphrased.

---

## Phase 2: The harness's arithmetic, with no store in sight

**Goal**: the half a unit test can drive, separated for the reason chapters 4.7 and 4.8
separated theirs — a contract with branches a test can drive is worth more than one that needs
Docker.

- [X] T013 [P] Add the smallest-expressible-drift function where the harness can use it and a test can drive it, with the volume as its only argument. It is what T006 derived, and **the function is what stops a figure being published without it** (FR-007, FR-014).
- [X] T014 [P] Unit-test that function at 9, 100, 1,000, 10,000 and 100,000, and **at the boundary in both directions** — the smallest drift that breaches over and the smallest that breaches under. `max(a, o)` is the denominator, so they are the same size, and a drift computed against the smaller side lands inside the bound (chapter 4.7).
- [X] T015 [P] ~~Add the counter-derivation as a pure function~~ — **PREMISE FALSE, and the finding is recorded in `baseline.txt` rather than worked around.** `corpus.mjs` writes messages with `INSERT … SELECT FROM generate_series`, and `created_at` and `user_id` are chosen by `random()` inside the server: **the rows this function was to be given never exist in Node.** A pure function over them would be a second implementation of a `GROUP BY` that nothing runs. The derivation is two statements against the rows just written (T020/T021), and the check it needs is an independent recount (T021a), not a unit test of a parallel implementation. **Null senders are still excluded from the active-user rows** — `usage_active_users.user_id` is NOT NULL and a foreign key to `users.id`, and the analytical side's `uniqState(user_id)` ignores NULL (chapter 4.2's pass 1), so the two agree only if this side excludes them too.
- [X] T016 [P] Add the argument parsing for `scripts/reconcile-usage.mjs`'s **one** new address, with **today's behaviour as the default** so the existing invocation is unchanged: `--database` falls back to `DATABASE_URL`. **There is no `--analytics-database`** — analysis pass 1 found `DB_ANALYTICS` is a constant at `reconcile.ts:110` rather than a parameter, and `analytics/apply.mjs` hardcodes the same name at line 18, so nothing in this repository can build a second analytical database for a flag to point at.
- [X] T017 [P] Discharge **FR-006c** and **SC-002a**: **validate BOTH values the reconciler interpolates into an analytical statement, before it is called, and test each refusal.** `reconcile.ts` builds `WHERE environment_id = toUUID('${environmentId}')` and `AND day >= toDate('${period}') AND day < toDate('${until}')`, and `arg()` produces the first two from `process.argv` with no checks. **`toUUID()` and `toDate()` are not guards** — an injection closes the quote before either function sees it. Measured, scoped to one tenant and one month: **208 rows honestly, 11,895 under `2026-09-01') OR 1=1 --`**, which is the whole table across every tenant.
  `--environment` against the canonical UUID pattern; `--period` against `^\d{4}-\d{2}-01$`. **`until` needs none and is safe by accident**: `nextPeriod` splits on `-`, maps to `Number` and rebuilds, so garbage becomes `NaN-NaN-01` and the server rejects it — record that rather than leaving it to look like a decision. The PostgreSQL URL needs no check because `pg` parses it rather than concatenating it, and the test says so rather than leaving the asymmetry unexplained.
  **Establish the count by reading the statements, not by naming the values already known.** The first draft of this task validated a database name that turned out not to exist; the second validated one of the two that do, and that miss survived nine analysis passes because the question asked was *"which value reaches SQL?"* rather than *"which values do?"*
- [X] T018 **Decide whether these files take coverage pins, and record the decision either way.** `scripts/` carries no per-file pin today. A harness whose arithmetic decides a published figure is not the same as a one-off script, and inheriting the absence is how a ratchet stops being one. If they are pinned, measure twice and run **both halves of the threshold probe** — an impossible pin on a real file must fire and a pin on a path that does not exist must be silent (045's trap).
- [X] T019 Run lint, typecheck and the unit lane; commit phase 2.

**Checkpoint**: the arithmetic that decides the published figure runs with no store, no database
and no broker.

---

## Phase 3: Both sides, for one tenant, at volume (US3)

**Goal**: FR-005 and FR-006 — a tenant-period the reconciler can return `pass` or `breach` for,
which no tenant in this platform has ever been.

- [X] T020 [US3] Extend `scripts/scale/corpus.mjs` to write `usage_periods` for every environment it created, one row per `(environment_id, period)`, with `messages_sent` from the rows it just wrote. **`corpus.mjs` carries 0 titled fences in either locale** — checked at T008, not assumed — so the edit costs the chain nothing.
- [X] T021 [US3] Discharge **FR-006b**: write `usage_active_users` the same way, one row per `(environment_id, period, user_id)`, excluding null senders (T015's rule). **This and T020 are one unit.** `reconcile.ts:177` is `op.messagesSent === null ? null : op.activeUsers`, which covers a tenant with no period row at all and not a tenant with a period row and no user rows: `usage_active_users` is a COUNT OF ROWS, so absent reads as **0** against an analytical ≈5,000 and the verdict is **`breach`** — the harness's, not the platform's. **`user_id` is a foreign key to `users.id`** (`schema.ts:1043`), so a null sender has no row to reference and the exclusion is the schema's rule rather than the harness's — which is also what makes a harness that got it wrong fail loudly instead of drifting.
- [X] T021a [US3] **Recount both counter tables with a statement that does not share an expression with the one that wrote them**, and refuse the run if they disagree. This is what T015 was supposed to buy and could not: the derivation is a `GROUP BY` in the server, so the only check available is a second question asked a different way — `sum(messages_sent)` from `usage_periods` against `count(*)` over `messages` joined to `channels` for the same environment, and `count(*)` from `usage_active_users` against `count(distinct user_id)`. **And check the period expression against `periodOf`**: the harness's `date_trunc('month', created_at at time zone 'UTC')::date` must produce the same string the reconciler asks for, and `quotas/period.ts` says in as many words what a missing zone costs — *"the row lands in a period nobody reads"*. A lane whose server runs on UTC cannot show that failure, so compare the two rather than assuming the lane is representative.
- [X] T022 [US3] **Write `connection_minutes` as 0 and say so in the report.** The corpus writes no connections, and a counter that invented them would make the connection-minutes comparison a measurement of the harness. The amended FR-ANL-06 already excludes that quantity; the report names the zero so a reader does not infer agreement from it.
- [X] T023 [US3] Extend the closing report with the counter rows written, the periods covered, — for the largest period — the volume and the smallest expressible drift at it (FR-007), **and the environment ids it created**. Without the ids there is no scoped cleanup, and the analytical side lands in the lane's own `relay_analytics` with no lane guard behind it (R6a).
- [X] T023a [US3] **Print, in the same report, that both sides derive from one source.** This is R10 as a sentence the harness itself says: the counters are computed from the same `messages` rows the analytical side is loaded from, so they agree by construction. A reader who runs the harness and reads its output should not have to find that in a chapter.
- [X] T024 [US3] Teach `scripts/reconcile-usage.mjs` to use T016's address and T017's validation. **It carries one English fence and no Vietnamese one** (T008/T008a), so this edit owes a `diff` hunk in phase 7 and the mirror question is already answered.
- [X] T025 [US3] Build a corpus at `CORPUS_MESSAGES=350000 CORPUS_ENVIRONMENTS=2 CORPUS_DAYS=91` and record the wall-clock time and the row counts. **`CORPUS_MESSAGES` is the SUBJECT environment's ninety-day window, not a total** — `planFor` gives the subject `ceil(messages × days / 90)` and each neighbour a tenth, so **a month is a third of it** and 350,000 puts ≈116,700 in one subject-month. Analysis pass 1 wrote 700,000 here with the sentence *"roughly 115,000 per environment-month"*, which was wrong twice: twice the volume needed, and split the wrong way. **Measure the subject.** **Both knobs are floors the script enforces and the first draft of this task violated both**: `corpus.mjs:88` refuses `CORPUS_DAYS` at or below 90 and `corpus.mjs:82` refuses `CORPUS_ENVIRONMENTS` below 2, so `CORPUS_ENVIRONMENTS=1 CORPUS_DAYS=31` would have failed on the first validation — chapter 4.6's recorded defect reproduced. Chapter 4.2 measured 1.6M messages in **92.4 s** and the analytical load in **1.2 s**, so a slow run is a finding rather than a cost to absorb.
- [X] T026 [US3] Load the analytical side **with the schema applied first**, so the materialised views fire on the insert, and record the order used. A view is a trigger on future inserts, not a query over history (chapter 4.6), and the wrong order leaves `daily_usage_billing` empty.
  **AND DO NOT USE `analytics/0013_backfill_billing.sql` AS THE RECOVERY.** `daily_usage_billing` is a `SummingMergeTree`, so a second `INSERT … SELECT` adds rows that **sum** with the first and doubles every count — while `apply.mjs`'s ledger has already recorded `0013`, so re-running the applier is a silent no-op. **The obvious retry doubles the answer and the obvious repair does nothing.** The backfill exists for data that was in the store before the views were created, which is not this case.
- [X] T026a [US3] **Assert the analytical side is non-empty before reconciling anything.** An empty rollup makes the reconciler answer `no-data`, which reads as an absence of evidence rather than as the missing step it is. This is the positive control the order in T026 needs.
- [X] T026b [US3] **Assert both counter row counts before the reconciler is asked anything** (FR-006b), the same positive control the analytical side gets at T026a. A verdict computed over a side nobody checked arrived is a verdict about the harness.
- [X] T027 [US3] Run the reconciler against the corpus and record the verdict per quantity, **with what each verdict is worth beside it** (FR-007a). **The requirement is `pass` or `breach` for messages — not `no-data` and not `not-comparable`** (SC-005), which are the two states every tenant in this platform has been in. **And expect three passes of which one is evidence**: `activeUsers` agrees by construction — `uniq` is exact below 65,536 and `CORPUS_USERS` defaults to 5,000 — and `connectionMinutes` is **0 against 0**, which `differencePct` returns 0 for because both-zero is agreement. `exitCodeFor` returns 0, correctly, and it is not the claim.
- [X] T028 [US3] Run the harness twice and confirm the second run does not depend on the first being cleaned up by hand (SC's independence, US3 scenario 3). `corpus.mjs` builds a fresh database per run, so the claim is about the counters and the analytical load rather than about the corpus.
- [X] T028a [US3] Discharge **FR-006a**: **add a scoped cleanup and verify it by count, because the corpus loads into the lane's own analytical store.** `load-analytics.mjs:14` writes `relay_analytics` — which is why the views fire (R6) and why a run leaves ≈389,000 `message_events` rows and their rollup rows beside four chapters' data, with no lane guard behind them (050-2). **The only cleanup this repository ships is `apply.mjs --drop-all`, which is `DROP DATABASE relay_analytics`** and takes every other chapter's data with it. Delete by the environment ids T023 prints, on `message_events` and `daily_usage_billing`, then **poll `system.mutations` AND assert the count is 0** — chapter 4.6's suite trusted a bare `ALTER … DELETE` and read three creations where it planted two, and 4.7 found a row from an earlier run still present.
- [X] T029 Run lint, typecheck and the unit lane; commit phase 3.

**Checkpoint**: one tenant-period exists with both sides at volume, and the reconciler has an
opinion about it.

---

## Phase 4: The gate that can go red 🎯 MVP (US1)

**Goal**: FR-001 through FR-004 — a planted drift fails the build, and the build's colour means
something.

- [X] T030 [US1] Make the integration run report **how many integration SUITES executed against how many exist** (FR-001, SC-002). **Not turbo's task count**: `Tasks: 8 successful, 10 total` includes 9 builds and 3 packages with no integration script, so it differs from the suite count by twelve and cannot be made to mean it. The number comes from the lanes' own vitest summaries or from the `.itest.ts` files in the tree. Today a green gate and a gate that stopped early are the same exit code, and the summary line does not separate them.
- [X] T030a [US1] **Decide the gate's shape and record it as an architecture decision, weighing the options as they actually cost.** `--continue` runs every task and reports all failures; raising `--concurrency` runs the lanes in parallel against one PostgreSQL, which feature 045 measured down to two workers for the api and four for the gateway. **And excluding suites is not among the cheap options** — `--filter` is package-granular and the suites in question share a package with the reconciler's (T031). Constitution VII: *"Every architecture decision is recorded as an ADR stating its drivers, rejected alternatives, and reversal condition."*
- [X] T030b [US1] **Make the gate change in the APPENDIX's hunk, not in a chapter's, and publish the cost.** The flags live in `package.json:15` and `turbo.json` holds only this task's `dependsOn` and `env`, so there is no choice of file. But **`fences/post-series.md` carries the `package.json` hunk whose `-`/`+` pair is that very line** (T008c), and the appendix applies after every chapter — so a 4.9 chapter hunk would be anchored on a pre-appendix state and then overwritten, silently. An earlier draft of this task called the edit *"invisible to `check:fences`"*, which is true of the **HEAD** comparison and false of the **APPLY** side: a file can be free on one half and expensive on the other. Regenerate the appendix's hunk from the checker's own replay, verify every pre-image is unique, and assert the result matches the tree before pasting.
- [X] T031 [US1] Resolve or record `request-log.itest.ts`'s five failures (FR-003, FR-004). The two shapes: give the environment that runs them an ingester — `compose.yaml` has none (`gaps.md` 050-8, open since chapter 4.5) — or separate them from this gate, in which case the separation is part of T030a's ADR and needs a reversal condition.
  **AND THE TWO ARE NOT THE SAME PRICE, WHICH AN EARLIER DRAFT OF THIS TASK GOT WRONG.** It said the second *"already has a precedent on this command line … same shape, same mechanism, one flag"*, citing `--filter=!@relay/outsider`. The shape has a precedent and the mechanism does not: **`--filter` selects packages**, `@relay/outsider` is its own, and these five suites sit inside `@relay/api` **beside `reconcile.itest.ts` — the suite this gate exists to reach**. No filter expression keeps one and drops the other.
  **Separating them costs a vitest `exclude` in `services/api/vitest.integration.config.mts`, a second config, or moving the file**, each of which changes what `pnpm --filter @relay/api test:integration` means for everyone who runs it. An ingester in `compose.yaml` is a service definition and 8/6 fences. Weigh those two, not a flag that cannot do the job.
- [X] T031a [US1] **If an ingester is added, measure what it drains.** Chapter 4.8 started one for a single measurement and it wrote 188 rows over 15 batches, the first alone clearing a 137-row backlog. A permanent one changes every analytical suite's opening state, which is the same class of lane-wide side effect as `SYSTEM STOP MERGES`.
- [X] T032 [US1] Resolve or record `limits.itest.ts`'s failure: *"the lane must configure a platform credential: expected undefined to be truthy"*. It is a missing environment variable, red since before Part 4, and it is the one of the six that is neither a product defect nor an absent process.
- [X] T032a [US1] Discharge **FR-004a** and **SC-003a**: **decide the schedule, and record it as an architecture decision with a reversal condition.** FR-ANL-06 asks for a *daily* job; T011b establishes that nothing runs the reconciler. The candidate is a **sixth** background relay beside the five the api already starts, and its cost is the one feature 030 measured for those five — another loop sweeping the database while other suites' fixtures sit in it, and another flag every analytical suite has to set. **Building it is optional; deciding and recording is not.**
- [X] T032b [US1] **Say which of the two schedules the gate satisfies.** `docs/12` §2.3 substituted *"the lane, every run"* for the clause's *"daily"* and did not name the substitution. A per-push check on a planted fixture runs more often than daily and **reads no real tenant**, so it is a different claim rather than a more frequent one. Whichever the milestone asserts, the record says which.
- [X] T032c [US1] Discharge **FR-002a**, **SC-001** and **SC-001a**: **decide what to do about the permanently-red workflow, and record it as an architecture decision with a reversal condition.** T003a establishes the fact. The options are the series' rather than this chapter's — a recorded baseline the checker compares against so a delta of 0 exits 0 and a regression exits 1, which is what every chapter already reports by hand; `continue-on-error: true` with the count published, which keeps the number and loses the enforcement; or its own job, which makes the other jobs' colours legible and leaves this one red on purpose. **Building the change is optional; deciding and recording is not**, and the milestone's claims are scoped to the step until it is.
  **And say plainly that this is the same defect the chapter is about.** Six reds mask the reconciler inside a job; `check:fences` masks every job's colour inside the workflow. A milestone about a gate that cannot fail for its own reason should not stand in one and not say so.
- [X] T033 [US1] Confirm the count of unexplained reds is **0** (SC-003), by listing each of the six with its outcome — green, or recorded with its cause and the chapter that owns it.
- [X] T034 [US1] Run the gate on a clean tree and confirm it is green **and that the reconciler's suite is among those that executed**, shown by naming the suite rather than by the absence of a failure (SC-002, US1 scenario 1).
- [X] T035 [US1] **Show the gate red on purpose**: change the reconciler's comparison from `<=` to `<`, run the gate, and confirm it fails and that the failure names the reconciler (SC-001, FR-002). Restore it. Chapter 4.7 used the same probe to establish that the boundary cases sit ON the bound — exactly one assertion should move.
- [X] T036 [US1] Confirm the gate's colour does not depend on a process the platform does not ship (US1 scenario 3): run it in an environment with no ingester and confirm the reconciler's verdict is still reached.
- [X] T037 Run **the gates T002 derived** and `pnpm coverage`; record failures in `baseline.txt`; commit phase 4. **Not "the four lanes"** — that phrase was in an earlier draft and matches nothing: six packages run inside the gate and a seventh is filtered out.

**Checkpoint**: a planted drift fails the build. This is the MVP and it is the half §2.3 calls
falsifiable.

---

## Phase 5: The measurement (US2)

**Goal**: FR-008 through FR-011 — one figure, published with everything a reader needs to check
it and everything it does not prove.

- [X] T038 [US2] Build the measurement corpus at **100,000 or more messages in one tenant-period**, or record the highest volume the lane could reach and the resolution that volume gives (SC-004). Below 10,000 the bound cannot be expressed at all. **Take the per-period volume from T023's report rather than computing it from the knobs** — the in-window split is exact by construction and the split across environments and channels is floor division with a remainder, so a derived figure is an estimate and FR-014 asks for the number.
- [X] T039 [US2] Run the reconciler over it and record the agreement figure with **the row counts on each side, the volume, and the smallest expressible drift at that volume** (SC-006, FR-007).
- [X] T039a [US2] Discharge **FR-007a** and **SC-006a**: publish the per-quantity table **with a standing column** — measurement, agreement by construction, zero-against-zero, or `not-comparable`. Without it the table reads *"three of four agree"*, and chapter 4.7 already wrote the general form: *a green 0.1% assertion at lane scale claims that nothing drifted at all.* Here it is again as 0 against 0.
- [X] T040 [US2] Plant a drift **at** the bound and one **one unit past** it, at the measurement's volume, and confirm the first passes and the second breaches (SC-007, FR-009). Not at the lane's volume — at 1,017 messages the smallest expressible drift is already 0.197%, twice the bound.
- [X] T040a [US2] **Confirm the boundary cases sit ON the bound** by changing `<=` to `<` and checking that exactly one assertion moves. Chapter 4.7's probe, re-run at a volume where the bound resolves.
- [X] T041 [US2] Record the window the measurement covers and **whether the TTL cut inside it** (FR-011). `message_events` carries 90 days and removes rows at INSERT, so a corpus spanning more than that loses its oldest end before anything queries it; the oldest day of any window disagrees for a reason that is not drift.
- [X] T042 [US2] Write `docs/13-metering-measurement-<date>.md` on `docs/11`'s model as T010 recorded it: its own document, dated in the filename, the harness named, and **the clause's verification method stated rather than borrowed** (FR-010). `docs/11` discharged NFR-SCL-01 because that clause's letter is `A` and a published analysis is the verification for an `A`. **FR-ANL-06's letter is `T`**, so the planted-drift suite is the discharge and this document is evidence — say that in the first paragraph rather than putting `Method: T` on prose.
- [X] T042a [US2] Discharge **FR-010a** and **SC-009a**: give the document a **what went wrong while measuring, and why the numbers still stand** section, and a **what this left in the lane** section. Both are in the precedent and neither is specific to it. The first is what this project's writing guide asks for — `docs/11`'s reads *"The first ladder measured the harness, not the gateway"* — and the second is where T045a's cleanup before-and-after goes.
- [X] T043 [US2] **Name the quantity — messages sent — and the three the amended clause excludes, each with its reason** (FR-008, SC-008): `uniq` exact to 65,536 distinct and 0.5676% at 65,537; a daily rollup's finest grain against a TTL that cuts at a timestamp, 0% at midnight rising to 1.0989% just before it; and connection-minutes counting a different population on each side.
- [X] T043a [US2] State in the document's first paragraph that the clause under verification is **FR-ANL-06 as amended at SRS 1.14**. Verifying the original would mean publishing a number the SRS says cannot exist for three quantities.
- [X] T044 [US2] **Write the section that says what the figure does not prove** (R10): both sides derive from one source, so it measures the reconciler's arithmetic at volume and the bound's resolution there, not the platform agreeing with itself. Name what does establish that — `sendMessage` writing both in one transaction, and chapter 4.7's 0 of 1,385 disagreeing for a non-fixture reason.
- [X] T044a [US2] Record the alternative that was rejected on cost: driving the corpus through the real send path, which at the lane's 20.5 ms p95 is over half an hour of wall clock for 100,000 messages and measures the harness rather than the meter.
- [X] T045 [US2] **Run the document's commands as written** and confirm they work (SC-009, FR-010). Chapter 4.2's quickstart carried `CORPUS_DAYS=60`, which `corpus.mjs` refuses, through five analysis passes because nobody ran it.
- [X] T045a [US2] Discharge **SC-005a**: **run the quickstart's cleanup and confirm the store is back where it started.** Record `message_events` and `daily_usage_billing` counts before the corpus and after the cleanup; they must match. A measurement that leaves 389,000 rows in a store three other chapters read is the measurement becoming the next chapter's opening figure.
- [ ] T046 Commit phase 5.

**Checkpoint**: the figure exists, with its volume, its scope, and its limits.

---

## Phase 6: The amendments

**Goal**: FR-019 — where a measurement falsifies a published clause or document, the clause is
amended rather than left to diverge.

- [ ] T047 **Write the constitution III amendment proposal**, with everything the governance section names: the motivation, the semantic version bump, and the migration impact on in-flight specs and plans. The bump is **MINOR** — materially expanded guidance, no principle removed or redefined.
- [ ] T047a Quote both documents in the proposal rather than summarising them: constitution III's *"Metered totals MUST reconcile against operational counts to within 0.1%, verified by a daily job that alerts on breach"* against FR-ANL-06 as amended at SRS 1.14.
- [ ] T047b State what does **not** change: the bound itself, for the quantity that can meet it.
- [ ] T047b1 **Decompose the sentence in the amendment, because two of its three requirements have no mechanism and only one was ever recorded.** *"…within 0.1%, verified by a **daily job** that **alerts on breach**"* is three requirements: the comparison exists (chapter 4.7); the **alert** has no path, recorded at SRS 1.14 — the job exits non-zero and the two mail paths share one transport defaulting to a local catcher; and the **daily job has no runner**, which no document had recorded before T011b measured it. Carry T032a's decision into the amendment.
- [ ] T047c **Decide whether the amendment ships in this feature or is proposed and left open**, and record the decision. The procedure specifies a PR modifying the file; this project has no separate PR for it. Constitution VII's ADR immutability had the same shape at chapter 4.5, and it was decided out loud rather than defaulted.
- [ ] T047d [P] **Amend `docs/07` §6's contradictory pair.** Line 725 says *"Defense 1 does **not** exist"* and line 732 says *"Defense 1 now exists"*, ten lines apart and both dated 2026-08-08. **Chapter 4.8 edited the second and left the first**, so a correction strengthened one side of a contradiction it had not read. Resolve it, and state what defense 1 actually is today rather than what §6 specified: a workflow against the tip, not per-tag checkpoint scripts (R7b).
- [ ] T048 [P] Amend `docs/12` §2.3 where this feature falsified it: *"`scripts/scale/` … has no analytical-volume mode and needs one"* is stale, and *"the planted drift is detected … the lane, every run"* has never been true. **Leave §3's table first column alone** — it keeps the pre-contraction ordinals on purpose.
- [ ] T048a [P] Amend `docs/12` §3's row 10 where this chapter falsifies its one-line description (FR-019), in the form rows 5 through 9 already use: what the line did not say, and what the chapter turned out to be.
- [ ] T049 [P] Record in the SRS whether FR-ANL-06's verification is now discharged, and by what — **naming which of the clause's three parts the answer covers** (T047b1), **and naming the test rather than the document as the thing that discharges it.** The letter is `T`, so the planted-drift suite is the verification and `docs/13` is evidence about the bound's resolution; `docs/11` could discharge NFR-SCL-01 only because that clause's letter is `A`. A `T` verdict on the comparison is not a verdict on the schedule or the alert, and the record says so rather than letting "discharged" cover the sentence.
- [ ] T050 **Re-check every clause this feature cites by opening the SRS**, not the artifacts. Chapter 4.7 found a citation pointing at nothing and 4.8 found its own artifacts agreeing with each other about a clause id.
- [ ] T050a **Diff `docs/` for bare-numbered ids this feature added** — `FR-0\d\d`, `SC-0\d\d` — and correct any that reached a published document. Chapter 4.8 reproduced 1.14's own defect three commits after reading the correction, by copying a task line into an ADR; the mechanism is that a task is feature-local and a published document is not. **Nothing runs this check** (`gaps.md` 052-7).
- [ ] T051 Bump the SRS revision if any clause moved, and **check the version header by looking at it** — `check-revision-order` reads the table and never the header.
- [ ] T052 Run `sync:docs`, then `check:docs` and `check:srs`; commit phase 6.

---

## Phase 7: The chapter (US4)

**Goal**: the milestone, and movement IV's close.

- [ ] T053 [US4] **Fix the chapter's title and derive its slug, once.** Three places must agree exactly: the directory, the manifest `path`, and the MDX `metadata.alternates`. Record all four strings in `baseline.txt`. The other two milestones are `milestone-the-tuan-test` and `milestone-the-isolation-gauntlet`, so the prefix is `milestone-`.
- [ ] T054 [US4] Draft the chapter at `relay-tutorial/app/(en)/part-4/chapter-09/<T053's slug>/page.mdx`. **At least one `<Trap>` and at least one `<Why>`** (FR-017). `docs/07` §4 Rule 3 governs every chapter: each `<Why>` links the code being written to its requirement id and ADR.
- [ ] T054a [US4] **Carry a `<Checkpoint>`, and say what runs it.** 4.5 and 4.6 each have one, 4.7 shipped with none, 4.8 reinstated it — counted, 1/1/0/1. **`docs/07` §6 specifies defense 1 as `tutorial/checkpoints/part2-chN.sh` scripts run against each chapter's git tag, and that directory exists in none of the three repositories while `ci.yml` contains zero occurrences of `tag` or `checkpoint`** (R7b). So the block is prose a reader may run by hand and nothing executes. Write it for the reader, and record that nothing else will — a milestone about a gate that cannot fail for its own reason should not imply an enforcement it does not have.
- [ ] T055 [US4] Discharge **FR-012** and **SC-010**: record what movement IV established, what it amended, and what it defined and did not build, each with the measurement behind it. The amendments are FR-ANL-06 at SRS 1.14 and FR-ANL-10, FR-DSH-03 and FR-ANL-08 at 1.15; the thing defined and not built is FR-ANL-10's producer.
- [ ] T055a [US4] Discharge **FR-020**: one sentence a reader can act on about whether the meter agrees, supported by a figure published in the same chapter (SC-015). **The sentence is supported by a test and informed by a document**, and not the other way round — FR-ANL-06's letter is `T`. Not "it depends" and not a percentage with no scope. **And it cannot claim more than the mechanisms support**: the comparison exists, the schedule's answer is T032a's, and the alert has none. A sentence that says "the meter agrees" without saying how often anybody checks is the sentence this chapter exists to avoid.
- [ ] T056 [US4] Discharge **FR-015**: do not re-derive chapters 4.5 through 4.8's mechanics. Cite them.
- [ ] T057 [P] [US4] Discharge **FR-017b** and **SC-012b**: **carry 2 to 4 figures with at least one in each half** (`docs/07` §2), put every mermaid source in `figures.ts`, never in `page.mdx`, and pass each as **`code=`**. **The count is not enforced by anything** — `check:figures` checks the `code=` prop and the import bindings, so a chapter with zero figures passes it, and `milestone-the-tuan-test` has zero.
- [ ] T058 [P] [US4] Take every number in a figure from `baseline.txt`. No checker reads prose.
- [ ] T059 [US4] Discharge **FR-017** and **SC-012**: measure prose words outside code fences against the 2,000–4,000 bound with `scripts/prose-words.mjs`, and count the `<Trap>` boxes in the same pass.
- [ ] T059a [US4] Discharge **FR-017a** and **SC-012a**: **decide, once, whether `docs/07` §2's chapter floors cover a milestone at all, and record the reason.** T003b has the prose counts and T003c the figure counts, and **the same two chapters fail both floors** — which is one question rather than two. §2's length row states one range for every chapter and its rationale is entirely about the upper bound: the floor has none, the only enforcement story is a split (3.8 at 4,700 words), and **nothing in the document contemplates a chapter being too short or too sparse**. A milestone is structurally shorter and draws fewer new concepts because it assembles rather than builds. Chapter 4.9 meets both either way; what is decided is whether the other two are a gap or a category the document never covered. **Do not exempt a chapter class on this evidence alone** — that is an amendment, and phase 6 is where amendments go.
- [ ] T060 [US4] Discharge **FR-016** and **SC-013**: register the chapter in `relay-tutorial/lib/tutorial.ts`. **Assert the anchor is unique before editing** — 4.4's edit matched two anchors, was refused, and `pnpm build` said `Error: Unknown chapter id: 4.4` at 112 of 112 with eight gates green.
- [ ] T061 [US4] Publish new files as whole bodies and changed ones as `diff` hunks against `part4-ch8`. **A titled fence is a whole-body claim** (051-6), and **a `diff` fence carries the `@@` hunks only** — 4.7 lost two attempts to the `--- a/` and `+++ b/` headers.
- [ ] T061a [US4] **Apply T008b's appendix list**, which phase 1 already produced, and **check which state each hunk is written against before blaming the hunk.** Chapter 4.8 wrote three correct hunks and one failed, because `fences/post-series.md` applies after every chapter and the chain's state for that file at the chapter's own position was 993 lines where the repository held 1,292. **Any file this feature edits that the appendix also edits takes an appendix hunk, not a chapter one.** Check the appendix's titles first.
- [ ] T062 [US4] Generate hunks from `git diff -U6 part4-ch8 -- <file>` or from the checker's own replay, and **verify they apply before pasting**. Regenerate wider when a pre-image matches twice; `-U8` can be worse than `-U10`, because widening merges adjacent hunks.
- [ ] T063 [US4] Discharge **FR-018** and **SC-014**: report the fence close as a delta against T003's opening, by kind and locale, with the two HEAD classes split **and the third APPLY bucket named** — it is `fences/post-series.md`, 14 hunks, not a scattering. **Name every file this feature edited that is already a HEAD problem**, because those edits are invisible to the gate and the delta reads as costing nothing.
- [ ] T064 [US4] Run **every gate T002 enumerated**, the sealed outsider suite included, and **refuse a skip as a pass** — capture each gate's output and treat `skipping` as red for the ones that exit 0 with an absent corpus. **Build before `check:errors`**, which reads `dist`.
- [ ] T064a [US4] Re-take T004, T005 and T006's measurements at the close and publish them beside the openings with any movement. Chapter 4.8's per-tenant median moved from 10 to 7 because the feature itself added 25 tenants — **a median over a population the feature changed is a measurement of the feature**.
- [ ] T065 Write `specs/054-chapter-4-9/gaps.md`, discharging **FR-013** and **SC-011**. **Re-measure every carried item**: 053's nine new ones and the survivors from 052, 051, 050 and 048. *Measure the carried ledger; do not copy it.* Chapter 4.8 found 048-2's own wording — *"0 occurrences under `services/`"* — false at 3.
- [ ] T065a In `gaps.md`, record what the milestone could not establish and why, in the same terms as what it could. A milestone that only lists successes is the document nobody trusts.
- [ ] T065b In `gaps.md`, give `053-3` its second reading, and **both of its halves are wrong in different ways**. *"No local command runs the sealed suite"* is false — `package.json:16` is `"test:outsider": "turbo run test:integration --filter=@relay/outsider"`, one command, checked against the tree. And *"`pnpm test:integration` does not reach it"* is true for a reason the item does not give: `--filter=!@relay/outsider` excludes it by name. **An item written after running something by hand, without looking for the script.** T065d carries the correction.
- [ ] T065b1 In `gaps.md`, **file defense 1 as the second gate in this milestone's own condition.** `docs/07` §6 specifies per-chapter `CHECKPOINT` scripts run against each chapter's tag; `tutorial/checkpoints/` does not exist and CI runs nothing per tag. What feature 024 closed it on is a workflow against the tip — useful, and not what §6 describes. **`gaps.md` 048-5 is the adjacent half** — *no gate checks that a chapter's tag matches the chapter* — and this is the reason for it. File them together.
- [ ] T065b2 In `gaps.md`, **file the permanently-red workflow**, with T003a's count and T032c's decision. `check:fences` at 110 is an honest number — 11 of those are fences whose titles name no file and can never be repaired by editing the platform (050-4) — and the arrangement is what makes every other gate's verdict illegible at the workflow level. File it beside 050-4 rather than as a complaint about the checker.
- [ ] T065b3 In `gaps.md`, **file the milestone chapters below `docs/07` §2's floors — both floors, one entry**, with their prose and figure counts and T059a's decision. `milestone-the-tuan-test` carries **zero figures** against a stated 2–4, and `milestone-the-isolation-gauntlet` is under both. A prose or figure addition costs the fence chain nothing — measured at chapter 4.8 — so extending them is cheap and is somebody's chapter to extend rather than this one's. **And file the gate's blind spot with them**: `check:figures` reports a series-wide count and enforces no per-chapter one, which is why a zero-figure chapter shipped green.
- [ ] T065c In `gaps.md`, record the state of the constitution III items: 051-2 and 052-6 on the cross-store read, and this feature's own on the 0.1% bound. Three items against one principle is a number worth stating.
- [ ] T065d In `gaps.md`, **correct `053-3` rather than only re-measuring it — two corrections, from two adjacent lines of one file.** Line 15 shows `--filter=!@relay/outsider`, so the sealed suite is excluded by name and not by turbo's early stop. Line 16 shows `test:outsider`, so a local command to run it has existed all along. The item was written at chapter 4.8's close after running that suite by hand in six commands, and this feature's own research repeated it through four analysis passes. **A carried item can be wrong about its mechanism while right about its symptom, and wrong about its symptom while right about its category** — two ways a ledger goes stale beyond going out of date, both in one entry.
- [ ] T066 Audit every test this feature added and confirm none asserts only that something ran. Record the count audited. **Read every title beside its body** — 4.7's audit found one claiming *"however much analytical data exists"* over a test that planted none.
- [ ] T067 Write `specs/054-chapter-4-9/traceability.md` mapping every FR and SC to tasks and artifacts. **Record the requirements nothing discharged**, and any discharged in a weaker form than their words suggest.
- [ ] T068 Rewrite `CLAUDE.md`'s `<!-- SPECKIT -->` block for the close, including every task premise this feature falsified by running it.
- [ ] T069 Commit phase 7, tag `part4-ch9` on `relay-platform`, and push all three repositories.

**Checkpoint**: movement IV is closed, and the milestone says in one sentence whether the meter
agrees.

---

## Dependencies

    Phase 1  ─────────────────────────────────────────► everything
    Phase 2  ─────► Phase 3 (the harness uses its arithmetic)
    Phase 3  ─────► Phase 5 (the measurement needs both sides)
    Phase 4  ──────────────────► independent of 3 and 5, and it is the MVP
    Phase 5  ─────► Phase 6 (the amendment cites the figure)
    Phase 6  ─────► Phase 7

**US1 (phase 4) does not depend on US3 (phase 3).** The gate's problem is six failures and a
summary line; the harness's problem is two missing counter tables. They are the same milestone
and not the same work, and either can ship first.

**US2 (phase 5) depends on US3.** There is no figure without both sides.

## Parallel opportunities

- **Phase 1**: T002, T003, T005, T006, T007, T008, T009, T010 are all reads of different things.
- **Phase 2**: T013, T015, T016, T017 touch different files.
- **Phase 6**: T048, T048a, T049 are three documents.
- **Phase 7**: T057 and T058 are the figures.

## Implementation strategy

**MVP is phase 4.** A planted drift fails the build — the half §2.3 calls falsifiable, and the
half that has never worked. It needs phases 1 and 2 and nothing else.

**Phase 5 may publish a smaller number than 100,000 and that is a complete outcome**, as long as
it publishes the resolution that volume gives. What would not be complete is a figure with no
volume beside it.

**Phase 6's amendment may end as a proposal rather than a change.** The governance section
specifies a PR modifying the constitution, and whether this project's practice satisfies that is
T047c's decision — made out loud, like chapter 4.5's ADR-immutability question, rather than
defaulted.
