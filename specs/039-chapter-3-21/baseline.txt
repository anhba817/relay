BASELINE — chapter 3.21, the typing indicator
=============================================

*Every measurement this feature takes, and every thing that went wrong taking it.
Written as it happens, not at close-out.*


WHAT PLANNING ALREADY MEASURED
------------------------------------------------------------

Four premises were checked by command before a requirement was written. Two of the
brief's were false, and both are recorded in `spec.md` rather than here because
they changed the scope rather than informed it.

    R1  the message path is still typed to messages, at four points
        fanout.ts:44  onDelivery(handler: (channelId, message: Message) => void)
        fanout.ts:47  publish(message: Message): Promise<void>
        fanout.ts:80  messageCreatedSchema.shape.payload.safeParse(parsed)
        session.ts:223 send(socket, { type: "message.created", payload: message })
        -> the fourth grammar is justified, not assumed

    R2  session.ts:948 refuses every inbound frame but `message.send`
        -> this chapter opens the second inbound frame in twenty chapters

    R3  frames.ts:96  typingSchema payload is { channel, user } — no `state`
        -> there is no frame for "stopped", so the expiry is the client's

    R5  limits.ts:83  operation: "connect" | "send" — a closed union
        -> a third member is a typed decision, checked at every call site

TASK LIST — WHAT THE CHECKER CAUGHT
------------------------------------------------------------

`check-refs.py`, carried forward from chapter 3.20 with `FOREIGN` reset, on its
first run against a finished `tasks.md`:

    27 problems — 26 tasks naming no file path, and a missing traceability.md

A quarter of the task list did not say where the work goes. Every one is an
integration test, and the reason they were vague is that the file they belong in
was an open question until R9's constraint was read properly.

R9 SAYS "NO TENTH INTEGRATION FILE" AND THAT IS NOT WHAT IT MEANS
    grep -c "spawn(" services/gateway/src/resume.itest.ts    0
    grep -c "spawn(" services/gateway/src/session.itest.ts   1

`resume.itest.ts` stubs the `ApiClient` and boots gateways in process — it spawns
no api at all. **The constraint that costs battery runs is the spawn count, not
the file count**: seven of nine files spawn an api and five of chapter 3.20's
seven failures across forty runs were a fixture failing to come up.

Typing writes nothing and reads nothing, so it needs no api either.
`typing.itest.ts` follows `resume.itest.ts`'s pattern and the spawn count stays
at seven.

THE PLAN'S PHASE ORDER WAS WRONG
------------------------------------------------------------

`plan.md` puts US1 (Mai sees Tuan typing) before US2 (the inbound seam). **US1
cannot be delivered without US2** — no frame lets a client say it is typing, so
the delivery path has no trigger. The stories keep their spec numbering and the
phases run US2 first.

Found while writing the task list rather than while executing it, which is the
cheap place to find it. Chapter 3.15/3.16 had five wrong premises and 3.17 had
five more; three of 3.17's surfaced only during implementation.

WHAT TRACEABILITY FOUND BEFORE ANY CODE
------------------------------------------------------------

Three requirements with no task, all the same shape: **a requirement satisfied by
prose is the kind a task list forgets**, because a task list is written by
thinking about what to build.

    FR-019  what to do if the SRS diff is NOT empty
    FR-020  state which of the six kinds have producers
    FR-021  state that the fourth grammar was taken rather than avoided

And six of twenty-one requirements are verified by inspection or a command rather
than by a test. Chapter 3.20 had five and its close-out found the one whose test
did not exist by reading that column the other way.

THE STARTING LANE
------------------------------------------------------------

Carried from chapter 3.20's close-out; to be re-measured at T002.

    43 files   701 tests   228.77 s against a 240 s budget
    gateway package 45.48 s — the pacesetter is presence.itest.ts
    two twenty-run batteries: 17/20 and 16/20 green

    7 failures in 40 runs = 17.5%, in two mechanisms, neither new code:
      the rate limiter's wall-clock-aligned fixed window          2 of 7
      a gateway api fixture failing to come up, in three files    5 of 7

**Twenty green would reject a rate above 13.91%**, so this lane's measured rate is
above what twenty runs can exclude. Expect failures in this chapter's battery;
a recurrence of either mechanism is expected rather than information.


ANALYSIS PASS 1 — TWO CRITICALS, AND BOTH WERE UNASKED QUESTIONS
------------------------------------------------------------

Nine findings: 2 CRITICAL, 3 HIGH, 3 MEDIUM, 1 LOW. All eight above LOW fixed.

**C1 — THE CLAUSE THIS CHAPTER EXISTS TO CLOSE HAD NO TASK.**
FR-009 says the indicator expires with no frame sent to end it. Twenty-one
requirements, 108 tasks, and `grep -niE "expire" tasks.md` returned one line —
T064, about reconnect replay, which is FR-018.

It slipped because "the client holds the timer" reads like an answer, and an
answer feels like coverage. **The server's half is testable and was untested**:
FR-009a now asserts that no frame of any kind follows a signal, on the watcher's
whole frame list rather than on the absence of one type — a server that sends
nothing ever would satisfy the weaker version.

**C2 — THE PLANNED RATE LIMIT COULD NOT WORK, AND THE FIX MADE THE DESIGN
SMALLER.**
Research R5 said a third `"typing"` operation on the existing limiter needs "no
schema change and no new Redis shape". True of the grammar. Two more lines of the
same file:

    limits.ts:89   const WINDOW_MS = 60_000
    limits.ts:115  `rl:${environmentId}:${operation}:${windowStartFor(now, WINDOW_MS)}`

    scope    per tenant   vs  per connection — 10,000 users would share one budget
                              and one chatty user would silence the rest
    window   60 s         vs  2 s — "N per minute" cannot express "one per 2 s"
    subject  no channel   vs  per channel

Three mismatches, each fatal alone. **The replacement is a `Map` on the
connection** — no Redis, no third operation, `limits.ts` not edited.

**And the per-environment ceiling was dropped rather than kept alongside.** It
would bound a rate the debounce already bounds: the gateway enforces the interval
itself, so a hostile client cannot exceed it by trying. What stays unbounded is the
number of connections, and that is FR-RTM-09's cap — chapter 3.22.

**The design got smaller because a premise was checked, not because somebody
wanted a smaller design.** Constitution VII passed the third bucket as a small
addition; it was an addition that could not do the job.

THE OTHER SIX
------------------------------------------------------------

    H1  FR-008 said `typingSchema` must not change and named `git diff` as its
        verification. No task ran it. The file IS edited to add a sibling schema,
        so "unchanged" is a claim about one REGION of a changed file and nothing
        in this repository checks a region -> T017a.

    H2  "Renewal interval" and "typing limit" were two mechanisms under one name —
        a per-connection debounce and a per-tenant ceiling. Chapter 3.19's three
        30-second numbers, recurring. Split into FR-011a and FR-013a.

    H3  FR-014 — typing must not spend the message send quota — was in US3 at P2.
        Stopping after the MVP would have shipped a cosmetic feature able to
        exhaust a customer's message budget. Moved to US1 as T048b.

    M1  T043 asked the session to log an environment mismatch and the contract's
        closed set had no name for it. Decided as `typing.failed` with
        `op: "environment_mismatch"` — chapter 3.20's shape — and the contract's
        description widened to say so, before the code emitted a fourth name.

    M2  SC-002 had no task; the same root as C1, fixed with it.

    M3  The inbound frame's name was deferred to T011 with no home but the code.
        T011 now writes it into the contract in the same commit.

    L1  Four subscriptions per channel after this chapter. Recorded, measured by
        T051, not a defect.

**Both CRITICALs are the same kind of thing: a document asserting a mechanism
exists without asking the repository.** C2's claim was written during planning by
the same author who then verified four other premises by command and skipped this
one — because it read like a detail rather than a decision.


ANALYSIS PASS 2 — TWO MORE CRITICALS, AND ONE OF THEM WAS INVISIBLE FROM HERE
------------------------------------------------------------

Pass 1 asked whether the tasks' premises held. Pass 2 asked two different
questions: **does pass 1's own fix hold up, and do the artifacts agree with each
other?** Five findings, all fixed.

**C3 — TWO TASKS IN ONE FILE ASSERTED OPPOSITE BEHAVIOURS.**

    T047  a second signal within the renewal window still delivers a frame
    T057  repeated signals inside the interval produce at most one publish

Both would have been written before anyone noticed. US1's scenario 2 was true
when there was no interval and became false when US3 added one; **pass 1 walked
past it while fixing the very requirement that contradicts it.** Pure document
drift, visible without opening a single source file.

**C4 — CHAPTER 3.20's TWO BRANCHES ARE INCOMPLETE AND NOTHING HERE COULD SEE IT.**

    session.ts:367   the `added` branch subscribes fanout, presence, membership
    session.ts:425   the revocation branch releases the same three

A fourth grammar makes both wrong. **A user added mid-connection would receive
messages and presence but no typing** — FR-004 silently false for exactly the case
chapter 3.20 built, and the obvious test (a member who was in the channel at
connect) passes against it.

**This is the finding that would have shipped.** Every artifact in this feature
was internally consistent; the defect lives in the interaction with the chapter
before it, and it is only visible by opening `session.ts` and noticing that the
previous chapter left two branches that enumerate subscriptions. Now FR-004a,
T043a, T043b and T048c.

**H4 — THE DEBOUNCE WAS ABOUT TO EDIT A TYPE FOUR CHAPTERS FENCE.**
T054 said "a `Map` on the connection". `Connection` lives in `registry.ts`, fenced
by chapters 3.7, 3.8, 3.11 and 3.19. A `Map` in `attachSessions`'s closure keyed
by connection id needs no fenced type and has a clearer lifetime. No task
mentioned `registry.ts` at all.

**M4** — nothing cleared a channel's debounce entry when a revocation dropped the
channel. Folded into T043b, which is the act that drops it.

**M5** — "renewal window" and "renewal interval" were one thing under two names,
in the file where C3's contradiction lived.

**AND A CLAUSE READ RATHER THAN ASSUMED, WHICH CONFIRMED PASS 1's FIX.**

    FR-RTL-01  "per-tenant rate limits on REST requests, message sends, and
                connection establishment"

Three operations, and typing is not one of them. So removing the typing bucket
does not merely avoid a mechanism that could not work — **it aligns with the
clause, and the third bucket would have added a counted operation the SRS does not
name.** The fix was more right than the argument given for it in pass 1.


ANALYSIS PASS 3 — A CUSTOMER-FACING DOCUMENT, AND WHY TWO PASSES MISSED IT
------------------------------------------------------------

Pass 3 asked one question: **what else in the repository enumerates the things
this chapter changes, and did R8 look everywhere it should have?**

**C5 — `docs/08-error-reference.md` TELLS A CUSTOMER THE WRONG THING.**

    :264  "`message.send` is the only inbound frame; every other member of the
           frame union is server-to-client"
    :272  "**What to do:** send `message.send`. Do not send events; receive them."

That is the `unknown_frame_type` entry — **the reference for the error code this
chapter's own seam produces.** After this chapter, a customer refused for a typing
frame would read documentation telling them the wrong thing, in the document
written to explain the refusal. The file is in `sync-docs.sh`'s published list, so
it mirrors to the site.

**No task touched it.** Phase 9 named `05-sad.md`, `04-srs.md` and
`07-tutorial-plan.md`. Now T077a corrects it and FR-019a requires it.

**`check:errors` CANNOT CATCH THIS, AND A GREEN RUN AFTER A FALSE ENTRY IS THE
CHECKER WORKING AS DESIGNED.** It asserts 17 codes, 17 sections, each with a cause
and a client action. Structure, not truth. T077b records that beside the gate's
output so nobody reads green as coverage.

**H5 — R8's SEARCH SPACE WAS THE REASON, AND IT IS THE FINDING WORTH KEEPING.**
R8 said "checked with `grep` against both locales" and meant
`relay-tutorial/app/(en)` and `(vi)`. It never looked at `docs/`.

    relay-tutorial/app/**   a chapter's NARRATIVE claims
    docs/*.md               the PRODUCT's claims — the ones a customer reads

**Chapter 3.20 found this exact class in ADR-07's deep dive, also in `docs/`, and
R8 still did not include the directory.** Widened to four claims and eight
fragments across both trees, and `check-prose.py`'s paths must span both.

**M6** folded into C5. **L2** is a confirmation: EIR-WS-06 still holds, because
4002 keeps its meaning and its entry.

TWO THINGS CAME BACK CLEAN
------------------------------------------------------------

    packages/outsider     only READS frames, sends none — a new inbound type does
                          not touch the sealed client
    the grammar count     no document asserts a total; ADR-19 and ADR-20 each name
                          their own, so a fourth breaks no published count

C4 AND C5 ARE ONE FAILURE IN TWO COSTUMES
------------------------------------------------------------

Both are true statements about the system that this chapter falsifies, and **both
live outside the feature directory** — one in the previous chapter's code, one in a
published reference. Every document inside `specs/039-chapter-3-21/` stayed
internally consistent through three passes while both sat there.

    pass 1   premises           2 CRITICAL   both inside the feature's documents
    pass 2   self-audit, code   2 CRITICAL   one document drift, one in session.ts
    pass 3   the wider tree     1 CRITICAL   in docs/

**The yield has not fallen and the direction has moved outward each time.** Chapter
3.16's pass 12 recommended stopping and its passes 13, 14 and 15 each found a
CRITICAL.


ANALYSIS PASS 4 — RAN THE COMMANDS THE TASKS TELL SOMEBODY TO RUN
------------------------------------------------------------

CLAUDE.md's third mechanism, and it found the previous chapter tagged on a red
tree.

**C6 — CHAPTER 3.20 WAS TAGGED ON A FENCE CHAIN THAT DOES NOT PASS.**
`pnpm check:fences` in `relay-tutorial`, before this chapter touched one line:

    [HEAD] fences/post-series.md:1517
           services/api/src/outbox/outbox.itest.ts differs at line 599
           chapters: it("writes nothing at all for a role change", …)
           repo:     it("writes no row when the membership write itself fails (FR-016)"

The FR-016 atomicity test that chapter 3.20's traceability re-derivation asked for
was added **after** its phase 11 had regenerated the fences. Its chapter fence for
that file was generated from a tree without the test, so the replay diverges.

**3.20's own task list predicted this and named the reason** — *"Deleting
production code changes files the chapter fences, and the gates last ran in
Phase 10 before any of this. Nothing between here and the tag would catch it."*
The gates ran. Then a later task changed a fenced file. Nothing caught it.

**And a green gate was reported to the user that had stopped being true.** All six
were green when they ran and the chain was red by the time the tag was cut.

Fixed: the chapter's `outbox.itest.ts` diff regenerated in both locales, hunks
checked for single-match uniqueness, all six gates green, and `part3-ch20` re-cut
in all three repositories — the third time that tag has moved.

    229 fenced files, 37 chapters, 37 translated
    root gitlinks verified against both submodule tags

**H6 — "THREE TYPED POINTS" IS WRONG IN ADR-19, AND "FOUR" WAS WRONG HERE.
THERE ARE SEVEN.**
T003's own grep, run as written:

    fanout.ts:44   onDelivery(handler: (channelId, message: Message) => void)
    fanout.ts:47   publish(message: Message): Promise<void>
    fanout.ts:62   let deliver: (channelId, message: Message) => void
    fanout.ts:80   messageCreatedSchema.shape.payload.safeParse(parsed)
    session.ts:223 send(socket, { type: "message.created", … })
    session.ts:896 the same literal, in the resume flush
    session.ts:912 the same literal again

Eight lines, seven places. ADR-19 counted three; research R1 counted four; the
task beside the command said "four hits". **Nobody re-derived it until a task said
to run the grep** — which is `grep -c sendMessage(` reading 100 for 27, one
chapter later.

**The argument gets stronger.** Seven places to widen is a worse case for sharing
`chan:` than the three ADR-19 argued from. Corrected in `research.md`, `spec.md`,
`plan.md`, the contract and T003.

**M7 — A GATE THAT RUNS BEFORE THE LAST FILE CHANGE IS A GATE THAT RAN.**
The generalisation from C6, and now T107a: the six gates run **last**, after every
record is written and every file is final. T103 also says to re-run them if the
traceability re-derivation produces a code change, because that is precisely what
happened in 3.20.

THREE CLAIMS VERIFIED AND CORRECT
------------------------------------------------------------

    check:errors        17 codes — the count this chapter must not move
    T006's grep         matches exactly as written
    check-refs.py       green at 118 / 27 / 10

FOUR PASSES, AND THE DIRECTION HAS MOVED OUTWARD EVERY TIME
------------------------------------------------------------

    pass 1   the feature's own premises        2 CRITICAL
    pass 2   self-audit, and 3.20's code       2 CRITICAL
    pass 3   the wider tree — docs/            1 CRITICAL
    pass 4   running the tasks' commands       1 CRITICAL — in the predecessor's tag

**Six CRITICALs, and only two of them were inside this feature's documents.** The
other four were in the previous chapter's code, a published customer-facing
reference, and the tag itself. Reading the three core artifacts against each other
would have found two of six.


ANALYSIS PASS 5 — WALKED THE PHASES AS AN IMPLEMENTER
------------------------------------------------------------

Pass 5 asked one question: **can each task actually be written as described?**

**C7 — T024 SPECIFIED ONE REDIS CLIENT AND THE MODULE CANNOT WORK WITH ONE.**

    fanout.ts:33  "a subscribed connection cannot issue ordinary commands, so
                   publisher and subscriber [are separate]"
    fanout.ts:63  const publisher = new Redis(url);
    fanout.ts:64  const subscriber = new Redis(url);

`PUBLISH` is an ordinary command. **This module publishes** — a client's signal
arrives at its own gateway, which puts it on the fabric — and the same instance is
subscribed because it holds members of that channel. Both roles coexist on every
instance, so it needs both clients. The task said one and gave a reason that is
false for this module: "there is no key to read" is about keys, and the constraint
is about commands.

**This is the sharpest of seven CRITICALs, because following it produces code that
cannot run.** Every other one was a missing test, a stale claim, or a wrong count.

**AND THE CAUSE IS A LESSON OVER-APPLIED.** Chapter 3.20 recorded *"written with
two by analogy and the second was created, listened to, closed and never used"* —
a real finding — and this chapter carried it to a module with a different shape.
**3.20's gateway module only ever RECEIVED; its api did the publishing.** Here
there is no api in the path.

    A lesson from the previous chapter is a claim about THAT chapter's code, and it
    transfers only when the shapes match. Checking the shape is the same discipline
    as checking a premise.

Corrected in T024, T028 (seventh and eighth clients, not seventh), `plan.md` and
`data-model.md`.

**H7 — NO TASK CREATED THE CHAPTER'S FIGURES.**
T084 wrote `page.mdx`; nothing wrote `figures.ts`. Every recent chapter in this
part has three or four:

    3.17  four    3.18  four    3.19  three    3.20  three

A page importing a file that does not exist fails the build; a page with no
`<Figure>` breaks a convention the whole part follows. `check:figures` would not
catch either — it validates that diagrams pass as `code`, not that a chapter has
any. Now T083b, with the three diagrams named.

**M8 — `<slug>` APPEARED THREE TIMES AND NO TASK DECIDED IT.** An unresolved
placeholder, which is a detection target the analysis brief names. Now T083a.

**M9 — ONE TASK LINE CARRIED BOTH THE CORRECTED COUNT AND THE STALE ONE.**
T086 said "Four claims, **eight** fragments" and then "watch all **six** fail".
Pass 3 widened R8 from three claims to four and updated the number it was thinking
about, not the one three lines later.

FIVE PASSES
------------------------------------------------------------

    pass 1   the feature's own premises        2 CRITICAL
    pass 2   self-audit, and 3.20's code       2 CRITICAL
    pass 3   the wider tree — docs/            1 CRITICAL
    pass 4   running the tasks' commands       1 CRITICAL — in the predecessor's tag
    pass 5   implementability                  1 CRITICAL — a module that cannot publish

**Seven CRITICALs. Two were inside this feature's documents.** The rest: the
previous chapter's code, a published customer-facing reference, the tag itself, and
an instruction that cannot be executed. **The yield has not fallen and no two
passes found the same class of thing.**


ANALYSIS PASS 6 — WHOSE OBLIGATION IS THE CLAUSE?
------------------------------------------------------------

**C8 — THE CLAUSE SAYS THE SYSTEM SHALL, AND THIS DESIGN CANNOT.**

    FR-RTM-08  "Typing indicators shall expire automatically after 5 seconds
                without renewal and shall not be persisted."

Second half: met absolutely, nothing is stored anywhere. **First half: the
platform cannot execute it.** `typingSchema` carries no state field, so no frame
exists with which a server could end an indicator, and there is **no SDK in this
repository** — `packages/` is config, e2e, outsider, protocol, service-kit,
test-harness. The timer belongs to the customer's own application.

The spec said US1 "discharged" the clause. It discharges half.

**Verdict recorded rather than asserted**: met on the platform's half, delegated on
the client's, with the delegation named as a boundary. Nothing else the platform
could build satisfies the first half without editing `frames.ts`, which this
chapter refuses for chapters 3.19 and 3.20's reason. Now FR-009c, T104a, and a
section in `plan.md`'s constitution check.

Chapter 3.20 recorded FR-RTM-10 as met on the happy path and bounded under fabric
loss, with the 55-second excess stated. Chapter 3.18 refused to narrow a clause
until the code passed. **This is the same act on a different clause.**

**WHY FIVE PASSES WENT PAST IT.** "The protocol has no stop frame, therefore the
client holds the timer" is a correct account of the mechanism and an elegant one.
It never asks whose obligation the clause states. **A derivation that satisfies the
reader is not the same as a requirement that is met** — and elegance is the
condition under which nobody re-reads the clause.

**H8 — THE ARM LIST WENT STALE WHEN PASS 5 CHANGED THE MODULE.**
It said "the error listener", singular, and the module now has two clients. It also
had no arm for a failed publish, which the module only gained in pass 5. **Chapter
3.20 recorded this exact drift in the opposite direction** — its list said "three
error listeners" and became two when its module dropped to one client.

    an arm list is a claim about a module's shape, and it goes stale when the
    shape moves

**M10** — `onSignal` in one place and `onChange` in another, the second carried
from 3.20's module.

TWO CLEAN CONFIRMATIONS
------------------------------------------------------------

    no SDK package        nothing needs the new frame but the protocol package
                          itself, which this chapter already edits
    session.ts unpinned   bounded only by the aggregate 70, so the handler,
                          debounce and delivery cannot break a ratchet

SIX PASSES
------------------------------------------------------------

    1  premises                    2 CRITICAL
    2  self-audit, 3.20's code      2 CRITICAL
    3  the wider tree — docs/       1 CRITICAL
    4  running the commands         1 CRITICAL — the predecessor's tag
    5  implementability             1 CRITICAL — a module that cannot publish
    6  whose obligation             1 CRITICAL — a clause the platform cannot execute

**Eight CRITICALs. Two were inside this feature's documents.** No two passes found
the same class. The yield has not fallen once.


ANALYSIS PASS 7 — DO THE DEFERRALS HOLD?
------------------------------------------------------------

Pass 7 asked whether the claims this chapter makes about its neighbours are true —
the clauses it defers to, and the chapters it defers to them in.

**C9 — A DEFERRAL TO A CLAUSE THAT CANNOT ACCEPT IT.**
FR-013a, R5 and the plan all said the residual risk is "FR-RTM-09's cap — chapter
3.22". Read the clause:

    FR-RTM-09  "A user shall be permitted up to 5 concurrent connections"

**Per USER.** A tenant with 3,000 users holds 15,000 connections and complies, so
chapter 3.22 does not bound the per-instance publish rate and never will.

    0.5 publishes/s per connection x 10,000 per instance = 5,000/s worst case

The real bound is NFR-SCL-01, and the SAD's own risk register calls it **"a budget,
not a measurement"** — R2, *"the SAD's single most urgent action item"*,
unvalidated since the architecture was written.

**The conclusion still stands: do not build the ceiling.** A per-tenant counter
bounds a tenant and the thing at risk is an instance. What was wrong was the
sentence explaining where the residual risk went — and **it read as rigour because
it cited an identifier.**

Pass 1 verified the removal three ways: the limiter's scope, its window, and
FR-RTL-01's three named operations. All correct. **Nobody read the clause the same
paragraph deferred to.** Enumerating an id answers *is this number free*; it never
answers *does this clause say what I need it to say.*

**H9 — A TASK THAT QUOTED THE LESSON IT BROKE.**
T077 said move the Part 3 header "from 20 to 24" for the four-chapter plan, while
the table has twenty rows and this chapter adds one. A header of 24 against 21
rows.

**Chapter 3.20 replaced that header's `(16 published, 3 planned)` form precisely
because a split, unre-derived count was the anomaly**, having found it stale by
three — and T077 quoted that finding in the sentence that instructed the defect.
Now `(21 chapters)`, matching the rows; 3.22–3.24's rows arrive with those
chapters.

**M11 — NEITHER OF THE TWO COUNTS EXISTED, SEVEN PASSES IN.**
T082 says keep them "from the start". Now in `plan.md`:

    9     taught      -> the word estimate
    ~20   fenced      -> the chain, and stated as a PREDICTION
    ?     changed     -> re-derived from `git diff` at the end
    ~3,200 words      -> from NINE arguments, not from a per-file rate

Chapter 3.20 made seven arguments and measured 2,999; 3.19 made five and measured
2,445. The rate per taught file is not an estimator — 3.17 came in 45% below the
figure 3.15 and 3.16 had agreed on to within 1%.

**The counts are the one item on this list no analysis pass would ever find.**
They were absent, not wrong, and absence contradicts nothing.

SEVEN PASSES
------------------------------------------------------------

    1  premises                 2 CRITICAL
    2  self-audit, 3.20's code   2 CRITICAL
    3  the wider tree — docs/    1 CRITICAL
    4  running the commands      1 CRITICAL — the predecessor's tag
    5  implementability          1 CRITICAL — a module that cannot publish
    6  whose obligation          1 CRITICAL — a clause the platform cannot execute
    7  the deferrals             1 CRITICAL — a clause that cannot accept one

**Nine CRITICALs. Two were inside this feature's documents.** No two passes found
the same class and the yield has not fallen once.


ANALYSIS PASS 8 — DOES EVERY PHASE COMMIT CLEANLY?
------------------------------------------------------------

**THE FIRST PASS WITH NO CRITICAL**, and the honest reading is that the question
was narrower than the seven before it. "Does each phase commit cleanly" has one
answer per phase and all eleven could be checked. "What does this chapter falsify"
had an unbounded search space and kept producing.

**H10 — SEVEN OF ELEVEN PHASES COMMITTED WITH NO LINT OR TYPECHECK.**
Only T029 (Phase 3) and T099 (the close-out) ran them. Phases 1, 2, 4, 5, 6, 7 and
8 each ended in a commit task with neither.

**Chapter 3.20 had the same thin coverage — three mentions across eleven phases —
and paid for it.** Its Phase 3 was uncommittable because a destructured binding had
no consumer, and that surfaced only because a gate happened to run in that phase.
In six of its others it would not have.

Now all eleven phases gate before committing. **A phase that commits without them
is a phase whose breakage surfaces in a later phase's gate, attributed to the wrong
work.**

**M12 — A TASK MANDATED DEAD CODE.**
T016 said to add a case to `isolation.itest.ts`'s sample builder for the new frame
type. That builder feeds the refusal loop, which iterates
`DIRECTIONS.filter(([, d]) => d === "outbound")`, and the new type is **inbound** —
so nothing ever asks for its sample. The task now says to omit the case and to say
why in the file, because the next reader adding an inbound type will wonder.

THREE CONFIRMATIONS, ONE OF WHICH I EXPECTED TO BE A DEFECT
------------------------------------------------------------

    T009's red test typechecks. It sends a frame type Phase 2 has not created yet,
    which looked like a compile failure — and `session.itest.ts` sends
    `JSON.stringify({ type: … })` on an object literal, so no union type is
    involved. Frames go over the wire as strings.

    One switch in the tree touches the frame union — isolation.itest.ts:746 — and
    T016 owns it in the same phase that breaks it.

    No `: never` exhaustiveness guards anywhere in the gateway or protocol source,
    so an eleventh union member cannot break a narrowing somewhere unseen.

EIGHT PASSES, AND WHY THE YIELD FELL
------------------------------------------------------------

    1  premises                 2 CRITICAL
    2  self-audit, 3.20's code   2 CRITICAL
    3  the wider tree — docs/    1 CRITICAL
    4  running the commands      1 CRITICAL — the predecessor's tag
    5  implementability          1 CRITICAL — a module that cannot publish
    6  whose obligation          1 CRITICAL — a clause the platform cannot execute
    7  the deferrals             1 CRITICAL — a clause that cannot accept one
    8  phase committability      0 CRITICAL

**Nine CRITICALs. Two were inside this feature's own documents.**

**Falling yield is not a stopping signal in this project** — chapter 3.16's pass 12
recommended stopping and its passes 13, 14 and 15 each found a CRITICAL. But the
REASON it fell here is diagnostic: every pass that produced a CRITICAL opened a new
search space, and pass 8 walked one already covered. **The stopping question is not
"has the yield fallen" but "is there a direction nobody has looked in yet."**

ANALYSIS PASS 9 — WHAT WAS PROMISED TO THE READER
------------------------------------------------------------

The direction nobody had looked in: not what the previous chapters CONCLUDED, but
what they PROMISED. R8 had been asking "which published sentence does this chapter
falsify" and answering it from narrative prose. A promise is also a claim, and this
one was inside a fence.

    C10  CRITICAL  chapter 3.19 :71-73, both locales — a fence titled
                   `chapter 2.6 (excerpt)` reading "And presence (FR-RTM-06) and
                   typing (FR-RTM-08) will reuse this exact pub/sub plumbing with
                   TTLs per ADR-10." 3.19 discharges the presence half in the prose
                   beneath and leaves typing's standing. This chapter falsifies it,
                   and R8 did not list it.

                   Chapter 3.20's R8 examined this exact sentence and cleared it:
                   "not contradicted by this chapter at all." True of 3.20. Carried
                   forward as "not a claim to worry about."

                   The correction cannot edit the fence — chapter 2.6's words, which
                   the fence chain compares byte for byte. The prose beneath retires
                   the typing half the way 3.19 retired presence's.

    H11  HIGH      R8's claim 2 was a DESCRIPTION, not a quotable string: "'typing'
                   named as a kind still without a producer". A fragment list is an
                   input to a program — a description compiles into a checker that
                   matches nothing and reports green. Chapter 3.20 hit the same
                   failure from the other end, quoting sentences already deleted.

                   And it was worse than the finding said: EVERY `vi` entry read
                   "the same sentence". Five of the ten fragments were descriptions,
                   not one.

    M13  MEDIUM    Four claims and eight fragments became five and ten. Restated in
                   research R8, T086 and FR-019a.

FRAGMENT VERIFICATION RUN IN THE PASS THAT WROTE IT
------------------------------------------------------------

    10 fragments, 0 missing        whitespace-collapsed, both trees

    1a 1b  docs/08-error-reference.md          one locale — docs/ has no mirror
    2en 2vi  chapter 3.19 :62                  translated, so two different strings
    3en 3vi  chapter 3.20 :2204 / :2208
    4en 4vi  chapter 3.20 :2217 / :2222
    5en 5vi  chapter 3.19 :71-73               byte-identical, verified not assumed

Two checks came back clean. `docs/01-product-vision.md:110` — "presence and typing
indicators" — is made MORE true by this chapter. And no document counts the subject
grammars, so a fourth breaks no published total.

NINE PASSES
------------------------------------------------------------

    1  premises                 2 CRITICAL
    2  self-audit, 3.20's code   2 CRITICAL
    3  the wider tree — docs/    1 CRITICAL
    4  running the commands      1 CRITICAL — the predecessor's tag
    5  implementability          1 CRITICAL — a module that cannot publish
    6  whose obligation          1 CRITICAL — a clause the platform cannot execute
    7  the deferrals             1 CRITICAL — a clause that cannot accept one
    8  phase committability      0 CRITICAL — a direction already walked
    9  what was PROMISED         1 CRITICAL — a fenced IOU

**Ten CRITICALs. Two were inside this feature's own documents.** The other eight were
in the previous chapter's code, a customer-facing reference, the tag itself, an
instruction that could not be executed, a clause the platform cannot satisfy, a
deferral to a clause that cannot accept it, and a promise made to the reader.

ANALYSIS PASS 10 — WHO ACTUALLY DRIVES THE NEW SURFACE
------------------------------------------------------------

The direction: not what the documents say the checks do, but what one check
actually does. One yes-or-no question to the repository.

    grep -c "\.send(" packages/outsider/src/integrate.itest.ts   ->  0

    C11  CRITICAL  The sealed outsider has never sent a frame. Eleven tests: ten
                   REST, one socket test that RECEIVES a message sent over REST —
                   and whose title says so in capitals because chapter 3.18
                   corrected it.

                   This chapter's argument is the inbound seam. FR-001, FR-002,
                   FR-003 widen the accepted set; SC-006 says everything outside it
                   is still refused. All of that is verified in-workspace with the
                   `ws` package. The one thing here shaped like a customer touches
                   none of it.

                   And two of this chapter's own artifacts said otherwise, in the
                   same sentence: "It is the only check that uses the public surface
                   as a customer does, and this chapter adds a frame to that
                   surface." First clause true, second does not follow, and it read
                   as though it did — which is why no task added outbound coverage
                   to the sealed client in the chapter about inbound frames.

                   T100a and T100b are the first `.send(` in that file's history.

    H12  HIGH      FR-005 says the signalling USER receives no frame of their own.
                   T045 tests one connection per user and passes whether the filter
                   is by identity or by socket. FR-011a REQUIRES the plural case to
                   exist — its interval is per connection, and its scenario 3 puts
                   two connections of one user in one channel. The spec built the
                   topology that breaks FR-005 and pointed no test at it. T059a uses
                   the fixture T059 already builds.

    M14  MEDIUM    data-model.md's lifetimes table said the debounce is "a `Map` on
                   the connection". The task that builds it says, in bold, NOT a
                   field on `Connection` — `registry.ts` is fenced by four chapters.
                   The row was wrong in its last column too: an outer key never ends
                   by an interval elapsing.

TEN PASSES
------------------------------------------------------------

     1  premises                 2 CRITICAL
     2  self-audit, 3.20's code   2 CRITICAL
     3  the wider tree — docs/    1 CRITICAL
     4  running the commands      1 CRITICAL — the predecessor's tag
     5  implementability          1 CRITICAL — a module that cannot publish
     6  whose obligation          1 CRITICAL — a clause the platform cannot execute
     7  the deferrals             1 CRITICAL — a clause that cannot accept one
     8  phase committability      0 CRITICAL — a direction already walked
     9  what was PROMISED         1 CRITICAL — a fenced IOU
    10  who drives the surface    1 CRITICAL — a client that has never sent

**Eleven CRITICALs. Three were inside this feature's own documents.**

WHY A TENTH PASS STILL FINDS ONE
------------------------------------------------------------

Not because the artifacts are getting worse. Every pass that found something
asked a question no previous pass had asked, and the supply of such questions is
not exhausted by re-reading. Passes 9 and 10 are the same shape one level apart:
a claim about something OUTSIDE these documents, believed because it was written
down rather than because it was checked. 3.20's R8 cleared a fenced promise;
T100 credited a test file with a capability it does not have. Both were checkable
in one command, and neither command had been run.

**The stopping rule is unchanged and it is not yield: stop when nobody can name a
direction that has not been walked.** Pass 8 walked a covered one and found
nothing, which is what that looks like.

ANALYSIS PASS 11 — A DECISION THAT CHANGED, AND EVERYWHERE IT DIDN'T
------------------------------------------------------------

Pass 10 found a claim believed because it was written down. This pass asked the
same question of a claim that was REVISED: does the revision reach every place
that carries it?

    C12  CRITICAL  R9's constraint was refined from "no tenth integration file" to
                   "no new api spawn" — recorded in this file above, and stated in
                   capitals in the Phase 5 header. FIVE other places still carried
                   the proxy, and the finding underestimated it at three:

                     tasks.md T052    "**No tenth file was added**, so the package
                                      clock should not move" — an instruction
                                      somebody executes, asserting a false fact
                                      into this file
                     plan.md :269     a Risk that reads as a constraint the
                                      chapter violates
                     research.md :336 "go in an existing file or share an api"
                     quickstart.md:99 the same, found only by grepping the claim
                                      after the first four were fixed
                     CLAUDE.md :47    loaded into every session in this project

                   **A proxy for a constraint outlives the constraint.** The file
                   count was always a stand-in for the api boot count, and the
                   stand-in kept being true-looking after the real rule was
                   measured. Four of the five were fixed from the finding; the
                   fifth came from `grep -rn "no tenth"` afterwards.

    H13  HIGH      The renewal interval was a hardcoded constant, against the seam
                   the same file already carries twice:

                     session.ts:150        meterIntervalMs?: number
                     membership.ts:67      rereadIntervalMs?: number
                     membership.itest.ts:882   build({ rereadIntervalMs: 40 })

                   Chapter 3.20 tests a SIXTY-second backstop in FORTY
                   milliseconds. This chapter's plan tested a two-second interval
                   by waiting two seconds, in the package that paces a lane with
                   11.2 s of headroom, in a battery already measured at a 17.5%
                   per-run failure rate. And T060's proof was edit-and-restore —
                   the form that produced no failure twice in chapter 3.20.
                   Now `renewalIntervalMs?`, defaulting to 2_000, and T060 is a
                   case built with 0 rather than a source edit.

    M15  MEDIUM    T052's prediction was wrong in both directions: a tenth suite
                   costs something, and what it does not cost is an api boot.
                   Predict against `resume.itest.ts`'s clock from the same run.

WHAT CAME BACK CLEAN
------------------------------------------------------------

    coverage location   protocol/typing.test.ts and gateway/typing.test.ts both
                        planned; 8 of 12 gateway modules have a unit test, and the
                        coverage config includes *.itest.ts on purpose. The
                        28.57% class does not apply.
    metering            meter.ts counts connection-minutes, not frames. Nobody is
                        billed for keystrokes.
    the failure path    T068 severs the fabric with a TCP proxy and requires ONE
                        structured event; T069 asserts the emitted SET. 3.18's
                        lesson applied — the log line is what carries FR-015.
    the gauntlet        T015 predicts its three failures, T016 owns the eleventh
                        member and says why it gets no sample case.

ELEVEN PASSES
------------------------------------------------------------

    11  a decision that changed   1 CRITICAL — a proxy that outlived its rule

**Twelve CRITICALs. Four were inside this feature's own documents.**

Passes 9, 10 and 11 are one shape at three distances: a fenced promise nobody
re-read, a test file credited with a capability it lacks, and a constraint stated
as its proxy in five places after the real one was measured. **All three were one
grep away, and in all three the sentence had been written down, which is what
stopped it looking like a question.**

ANALYSIS PASS 12 — DID THE CORRECTION REACH THE NORMATIVE TEXT?
------------------------------------------------------------

Pass 11's finding, asked one level in: if a refinement can reach one file and
miss five, which of the misses are NORMATIVE? Pass 11 closed by saying no
twelfth direction could be named. The direction was inside pass 11's own result.

    C13  CRITICAL  FR-021 required the chapter to cite "ADR-19's three typed
                   points" as the reason for the fourth grammar. **R1 measured
                   seven.** The grep it was told to run returned eight lines over
                   seven places: fanout.ts 44, 47, 62, 80 and session.ts 223, 896,
                   912. ADR-19's record counts three; R1's own first draft said
                   four.

                   The correction reached research.md, contracts/typing-fabric.md
                   and plan.md. It did not reach:

                     spec.md :289  FR-021 — a MUST
                     spec.md :340  the assumption note
                     checklists/requirements.md :44
                     CLAUDE.md :9  "three points, and all three are intact"

                   **FR-020 and FR-021 are verified by a sentence** — no checker
                   here reads prose — so a chapter satisfying FR-021 exactly as
                   written publishes the number its own research disproved, with
                   every gate green.

                   Second consecutive pass in which CLAUDE.md carried the
                   superseded version of a number this chapter measured.

    H14  HIGH      **No task wrote this chapter's ForwardRef.** grep -c
                   '<ForwardRef>' reads 1 for chapters 3.17, 3.18, 3.19 and 3.20.
                   This chapter retracts 3.20's ForwardRef and derives the rule —
                   describe what the next chapter must DECIDE, not what it will
                   conclude — and had no task applying that rule to itself.

                   The trap is specific: 3.22's shape is already known to be
                   unworkable, so "3.22 will replace it with a sorted set" is a
                   prediction of a conclusion, which is the exact sentence being
                   retracted. T085a says what 3.22 must decide instead.

    M16  MEDIUM    `deliverPresence` at session.ts:240 walks `subscribersOf` with
                   NO self-filter — a user sees their own presence transition, and
                   chapter 3.20 confirmed it counting two frames where a watcher
                   correctly sees their own arrival. FR-005 requires the opposite
                   for typing. **Two adjacent functions in one file with opposite
                   self-delivery rules**, and the natural implementation move —
                   copy the neighbour — gives the wrong one. Now warned at T043
                   and stated in the chapter at T085.

WHAT CAME BACK CLEAN
------------------------------------------------------------

    FR-RTL-01   names REST requests, message sends and connection establishment.
                A typing signal is none of the three, so FR-013a's refusal of a
                per-environment typing quota is what the clause permits rather
                than a gap in it.
    FR-RTM-09   "each shall receive all events independently" governs events
                addressed to a user, not events they generate. No conflict with
                FR-005.

TWELVE PASSES
------------------------------------------------------------

    12  did the correction reach the MUST   1 CRITICAL — it reached three files
                                            of seven, and missed the normative one

**Thirteen CRITICALs. Five were inside this feature's own documents.**

Passes 11 and 12 are one finding at two depths. A correction propagates through
the documents that argue and stalls before the documents that instruct — and the
instruction is the copy that gets executed. **Grep the claim, then ask which hit
is normative.**

ANALYSIS PASS 13 — WHERE THE CORRECTIONS WENT TO DIE
------------------------------------------------------------

Pass 12's filter, applied to every other correction this chapter made: which
artifact didn't get it? The answer was the same file five times, and the sixth
turned up inside the fix.

    C14  CRITICAL  **plan.md and tasks.md disagreed on the phase order and on
                   which phase is the MVP.**

                     plan.md   Phase 4 = US1 (delivery) MVP, Phase 5 = US2 (seam)
                     tasks.md  Phase 4 = US2, Phase 5 = US1 MVP, stated twice —
                               in the dependency graph and in "MVP is Phase
                               1 + 2 + 3 + 4 + 5"

                   The correction is recorded in THIS FILE, under the heading
                   "THE PLAN'S PHASE ORDER WAS WRONG", written while the task list
                   was being written: US1 cannot be delivered without US2, because
                   no frame lets a client say it is typing. **It reached tasks.md
                   and this record and never the file its own heading names.**

                   `/speckit-implement` runs once per phase, eleven times, and
                   loads the plan as design context. The first invocation would
                   have been told to build a delivery path with no trigger.

                   And the plan's Phase 6 note — "FR-014 is asserted in Phase 5" —
                   pointed at US2 in the plan's numbering and US1 in the task
                   list's, correct in exactly one of them.

    H15  HIGH      plan.md Phase 10 still said "R8's three published claims".
                   Pass 9 made it five claims and ten fragments and updated
                   research.md, tasks.md and spec.md. **Third artifact to carry a
                   superseded count** — after FR-021's three typed points last
                   pass.

    M17  MEDIUM    plan.md Phase 6 still said "A `Map` on the connection" — the
                   exact phrase pass 11 corrected in data-model.md, which T054
                   contradicts in bold. Pass 11 fixed two files and did not grep
                   for the third.

    M18  MEDIUM    plan.md Phase 1 said "the four premises R1–R4". Phase 1 verifies
                   R1, R2, R3 and **R5**. R4 is verified by no command anywhere.

    M19  MEDIUM    checklists/requirements.md still listed as open a question pass 1
                   had answered: "whether the typing limit reuses `rl:{env}:{bucket}`
                   or needs its own bucket". The answer is **neither** — three
                   mismatches, each fatal alone, and `limits.ts` is not edited.
                   **The question offered a binary that excluded its own answer.**
                   Second stale claim found in a 53-line file in two passes.

    AND A SIXTH, INSIDE C14's OWN FIX. The plan's Phase 4 body told the
    implementer to update "the gauntlet's DIRECTIONS table, its count, and its
    sample builder". T016 says explicitly NOT the sample builder — that loop
    filters for outbound and the new type is inbound, so a case there is dead
    code an earlier version of the task mandated. The task was corrected; the
    plan sentence that says the same thing was not.

WHY plan.md AND NOT THE OTHERS
------------------------------------------------------------

Every correction this chapter made reached three places: the artifact that argues
(research.md), the artifact that gets executed (tasks.md), and this record. The
plan is written once, read at the start of implementation, and compared against
nothing. `check-refs.py` reads it for task ids and requirement ids — both were
always fine — and **no checker compares a phase heading in one file to a phase
heading in another.**

    thirteen passes, and the artifact with the most stale facts is the one
    nobody re-reads and no instrument inspects

THIRTEEN PASSES
------------------------------------------------------------

    13  which artifact missed it   1 CRITICAL — the plan, five times over

**Fourteen CRITICALs. Six were inside this feature's own documents.**

ANALYSIS PASS 14 — THE SPEC'S OWN ASSUMPTIONS, AND A SWEEP
------------------------------------------------------------

    C15  CRITICAL  "**The gateway holds no typing state.** No Redis key, no timer
                   per indicator, nothing to refresh. This is what makes the cost
                   against 10,000 connections per instance a publish rate rather
                   than a key count."

                   It holds a last-publish timestamp per (connection, channel),
                   which is a key count, and FR-011a requires it to be
                   authoritative at the gateway. **Two assumptions twenty lines
                   apart in one list, contradicting each other on the property the
                   chapter is about** — and the chapter is required to state it, so
                   the wrong version would have shipped as prose.

                   The split that resolves it: no state about an INDICATOR — which
                   is why nothing can announce that one stopped — and state about
                   the gateway's own recent publishes, in memory, one interval
                   long. Conflating them also invites someone to delete the map
                   believing FR-010 forbids it. **FR-010 forbids persistence.**

    H16  HIGH      Nothing pruned an elapsed debounce entry. Removal happened on
                   close and on revocation, so the bound was channels a connection
                   had EVER typed in while connected. One line on read —
                   `if (now - last > interval) channels.delete(channelId)` — makes
                   it channels it is typing in.

    M20  MEDIUM    The disconnect edge case read "Nothing is published" in a
                   scenario whose premise is that a signal WAS published.

    M21  MEDIUM    No edge case covered a user's own second connection, which is a
                   requirement and a test after pass 10.

THE SWEEP, AND WHAT IT FOUND
------------------------------------------------------------

Every gate green: check:fences 229 files / 37 chapters, check:docs, check:figures
232, check:srs 245 rows, check:errors 17 codes, check-refs.

Then a cross-artifact sweep, which found two more:

    traceability.md :67   FR-019a still said "four claims, eight fragments".
                          **Pass 9's count reached research, tasks and spec and
                          missed this file.** The fourth instance of one
                          correction landing in some artifacts and not others.
    plan.md :203          a bare `FR-032` — chapter 3.20's id, in a chapter whose
                          own ids stop at FR-021.

So the sweep is now `sweep.py`, and it checks the class rather than the instances:
the phase order and MVP marker across plan and tasks, every superseded phrasing
this chapter has corrected, stated counts against measured ones, foreign ids, and
placeholders. **Tested red five ways.** Four fired; the fifth (renaming a traced
id) belongs to `check-refs.py` and it catches it.

**THE THIRD RED TEST FOUND A BLIND SPOT, WHICH IS WHY IT WAS RUN.** The
foreign-id rule sat behind the prose skip list — the list that lets a superseded
phrase through when a nearby line says it was superseded. The skip word "said
three" appears in the very sentence that cites chapter 3.20's FR-032, so a real
hit read GREEN. Whether an id is labelled is a byte comparison and no longer has
a heuristic in front of it. Chapter 3.17 recorded this class five times in one
feature: **a pattern matching the examples in front of you rather than the set
the rule names.**

`sweep.py` needs an owner or a recorded absence of one, and that is now part of
the gaps task. Chapter 3.18's `sweep.py` died unowned.

FOURTEEN PASSES
------------------------------------------------------------

    14  the spec's own assumptions   1 CRITICAL — the central claim, overstated
                                     + 2 from a sweep nobody had run

**Sixteen CRITICALs. Eight were inside this feature's own documents.**

ANALYSIS PASS 15 — THE CLAIMS ONLY ONE ARTIFACT MAKES
------------------------------------------------------------

`sweep.py` proves consistency and says so about itself: a sentence nobody has
contradicted passes every line of it. So the unchecked surface is every claim
only ONE artifact makes. Six of those, each asserting something about the
repository, were run as commands. All six true:

    sed -n '96,102p' frames.ts shows a two-field payload, no `state`   T005  exact
    operation: "connect" | "send" is a two-member union                T006  limits.ts:83
    the gauntlet asserts members.length).toBe(10)                      T015  isolation:777
    presence.itest.ts has the TCP proxy pattern                        T068  11 matches
    DRIVER_EXEMPT_TESTS is the list's name                             T029  eslint:57
    figures: 3.17 four, 3.18 four, 3.19 three, 3.20 three              T083b 4 4 3 3

Then three things nobody had asserted at all:

    C16  CRITICAL  **`typing.itest.ts` had no port strategy.** The right answer
                   was already in the tree and unstated: `resume.itest.ts:78` does
                   `server.listen(0)` and reads `server.address()`, so the OS
                   assigns the port — no range, no map entry, and two in-process
                   instances get two distinct ports for free. `fanout.itest.ts` is
                   the same. **The seven files that take ranges are the seven that
                   spawn an api.**

                   An implementer reaching for the nearest fabric itest finds
                   `membership.itest.ts:233`'s `5400 + Math.random() * 200` and
                   then has to pick a range. Chapter 3.20 did that and collided
                   twice. **A port collision does not fail loudly** — it fails one
                   run in N, in a battery at 17.5% with four failures unexplained,
                   and T101 would have had to explain it.

    H17  HIGH      **The lane's port map lists seven ranges and the lane has
                   nine.** Missing: presence.itest.ts 4700–4900 (chapter 3.19) and
                   membership.itest.ts 5400–5600 (chapter 3.20). Two consecutive
                   chapters took a range and neither registered it, while
                   isolation.itest.ts:73 and public-surface.itest.ts:61 both cite
                   that block as the authority.

                   **A 78%-complete registry is worse than none, because it is
                   consulted and believed** — and it is why 3.20's second collision
                   happened. Chapters 3.08 and 3.13 both fence that file, so two
                   comment lines cost two chapters' diffs. T083c makes it a
                   decision with the cost stated rather than a third silent pass.

    H18  HIGH      T057 required the publish count to be asserted "on a Redis
                   subscriber" and did not say which client. Raw `ioredis` is what
                   presence.itest.ts and membership.itest.ts both chose and both
                   took the exemption for; the alternative measures publishes
                   through the code that publishes, which is chapter 3.18's warning
                   in a new place. **No task added typing.itest.ts to
                   DRIVER_EXEMPT_TESTS**, so `pnpm lint` fails and every gate from
                   Phase 5 runs lint first — the failure lands on the Phase 5
                   commit and is attributed to the wrong work.

AND THE SWEEP NEEDED A SCOPE DECISION
------------------------------------------------------------

Running it after pass 14's own record was appended produced two hits inside
`baseline.txt`: pass 14's entry opens by quoting "the gateway holds no typing
state" and pass 13's names a bare `FR-032` as its finding. Both are history.

**The record is now exempt from the claim checks, and the docstring says so and
says what that accepts**: a genuinely stale statement written into the record
would not be caught. Placeholders are still checked everywhere. Re-tested red
after the change — the phase-order and foreign-id tests still fire on live
artifacts.

FIFTEEN PASSES
------------------------------------------------------------

    15  claims only one artifact makes   1 CRITICAL — a new itest with no ports

**Seventeen CRITICALs. Eight were inside this feature's own documents.**

ANALYSIS PASS 16 — THE PUBLISHED DOCUMENT SET
------------------------------------------------------------

R8 searched the published documents for sentences this chapter FALSIFIES. Nobody
asked what it is obliged to ADD. `sync-docs.sh` publishes seven files: the
vision, the personas, the journey map, the SRS, the SAD, the ADR deep dives, the
error reference.

    C17  CRITICAL  **The FR-RTM-08 verdict reached no published document.**
                   `docs/04-srs.md:475` says typing indicators "shall expire
                   automatically after 5 seconds" — a platform obligation — and
                   this chapter's finding is that the platform cannot keep it:
                   `typingSchema` has no `state`, no frame ends an indicator, the
                   disappearance is the receiving client's.

                   FR-009c requires an honest verdict and gets one, into
                   `chapter-notes.md` and the tutorial chapter. **A customer reads
                   neither.** FR-019 rightly forbids editing the clause. The SAD
                   is published, this phase already writes into it, and an ADR is
                   the instrument for a decision whose consequence a requirement's
                   plain reading does not convey.

                   So ADR-22, separate from ADR-21, for T074's own reason: ADRs are
                   immutable once accepted, and these are two decisions with
                   different drivers that will be superseded on different
                   schedules.

    H19  HIGH      **No published document is a protocol reference.** EIR-WS-07 —
                   "the protocol shall be fully documented, including reconnection,
                   ordering and backfill semantics", P2, verified by inspection —
                   appears twice in the repository, both times inside the SRS.
                   Nothing claims to satisfy it. The gap predates this chapter and
                   **this chapter is the first to widen it**, adding the first new
                   inbound frame in twenty chapters. Recorded in the gaps task with
                   an owner; ADR-22 carries the client's timer obligation and is
                   not a substitute for the frame's shape.

    C18  CRITICAL — AND IT CAME OUT OF C17's OWN FIX.
                   `docs/06-adr-deep-dives.md`'s Purpose says the long form exists
                   **"for each ADR"**. The SAD holds twenty ADRs; the deep dives
                   stop at ADR-19.

                     grep -c "^### ADR-" docs/05-sad.md          20
                     last dive in docs/06-adr-deep-dives.md      ADR-19

                   Chapter 3.20 wrote ADR-20 into the SAD, amended ADR-07's deep
                   dive in that same file, and wrote no dive for its own record.
                   Nineteen for nineteen, then a gap nobody recorded — not in its
                   `gaps.md`, not in its task list. **Adding ADR-21 and ADR-22
                   without dives would have taken it to three of twenty-two**, so
                   the fix for one CRITICAL was quietly making another worse.

                   Found by reading a document's own statement of scope rather than
                   grepping for a contradiction — the same mechanism as R8's
                   claims, pointed at a Purpose paragraph.

WHAT CAME BACK CLEAN
------------------------------------------------------------

    EIR-WS-02      "a `type` discriminator and a `payload`" — the new inbound
                   frame is { type, payload: { channel } }. Compliant.
    sad.md :119    the C4 box already reads "WS termination, fan-out, presence,
                   backfill, typing". Made true by this chapter.
    ADR-10         never promised typing a TTL. Its only typing sentence is "the
                   correct amount of durability for typing dots and green circles
                   is none", which this chapter corroborates exactly. **So chapter
                   2.6's fenced "with TTLs per ADR-10" was wrong ABOUT ADR-10**,
                   which makes R8's claim 5 easier to retire: the prose beneath the
                   fence can cite ADR-10 agreeing.
    EIR-WS-06      no new close code; check:errors holds at 17.

SIXTEEN PASSES
------------------------------------------------------------

    16  the published document set   2 CRITICAL — one of them exposed by fixing
                                     the other

**Nineteen CRITICALs. Eight were inside this feature's own documents.**

The last eight have been about the tree or the published product rather than these
files, and pass 16 is the first where **applying a fix surfaced the next finding.**
Worth remembering as a habit rather than an accident: after adding an artifact,
ask what counts it, what claims completeness over it, and what pattern it joins.

ANALYSIS PASS 17 — WHAT COUNTS THE ARTIFACT
------------------------------------------------------------

Pass 16 ended on a habit: after adding an artifact, ask what counts it, what
claims completeness over it, and what pattern it joins. Applied to everything
this chapter adds. The eleventh union member has two counters and the task list
owned one.

    grep -rn "toHaveLength(10)\|length).toBe(10)" services packages

      isolation.itest.ts:777   expect(members.length).toBe(10)               T015, T016
      main.test.ts:38          expect(body.protocol.frames).toHaveLength(10) nobody

    C19  CRITICAL  Three things follow and the third is the expensive one.

                   **It breaks in Phase 2 and is reported as clean.** T015
                   predicts the gauntlet's three failures precisely and T017 then
                   said "T009's test is still red; nothing else is." False:
                   `main.test.ts` is a UNIT test and `test:integration` never runs
                   it.

                   **No phase gate catches it.** Every phase commit runs `pnpm
                   lint` and `pnpm typecheck` — an earlier pass's decision, and the
                   right one — but not `test`. The first command that runs unit
                   tests is T099, "the gates in CI's order", in Phase 11. **Nine
                   phases, attributed to whatever was touched last** — the exact
                   failure mode eleven phase-gate tasks warn about, arriving
                   through the one gate they do not run.

                   **And the file is fenced by chapter 1.04.** So the edit needs
                   what T083 gives `eslint.config.mjs`. **T083 could not have
                   caught it**: it checks the files this chapter edits, and no task
                   edited this one. No task -> no fence check -> no fence -> a red
                   test and a broken chain. T014a runs the unit lane and expects
                   the failure, T014b fixes the count and treats the file as
                   fenced, T014c re-runs it and records which lane each red test
                   lived in.

    H20  HIGH      **The gateway's `/health` advertises the wire vocabulary.**

                     health: () => ({ uptime_s, protocol: { frames, close_codes } })

                   Derived from `@relay/protocol`, with its own comment saying the
                   advertisement "cannot drift from the contract" — a completeness
                   claim that holds, because it is derived, and the reason the
                   `toEqual` half of main.test.ts can never rot. After this chapter
                   it advertises eleven frames over HTTP, and no artifact here
                   mentioned it, in a chapter whose thesis is that the inbound seam
                   is where a protocol is attacked.

                   **This also corrects pass 16's H19.** I reported that nothing
                   satisfies EIR-WS-07 and that no protocol reference exists.
                   `/health`'s `protocol` object is the closest thing that does:
                   machine-readable, current by construction, named in none of the
                   seven published documents. It carries no payload shapes, no
                   semantics and nothing about reconnection, ordering or backfill,
                   so it does not discharge the clause — but the gaps item is now
                   "a derived advertisement exists and no document points at it".

WHAT CAME BACK CLEAN
------------------------------------------------------------

    no log-name registry in docs/ — the three names are asserted only by T069's
      emitted-set test, which is the right place
    the SAD needs no ADR index row — §9 is sequential headings
    main.test.ts's other derived list is Object.keys(CLOSE_CODES); no new code
    public-surface.itest.ts does not enumerate the union, and the outsider checks
      /healthz's status and not its payload
    nothing outside the gateway consumes `frameSchema`, so plan.md's "services NOT
      touched" holds

SEVENTEEN PASSES
------------------------------------------------------------

    17  what counts the artifact   1 CRITICAL — a second counter, in the lane no
                                   phase gate runs

**Twenty CRITICALs. Eight were inside this feature's own documents.**

Two passes running, the finding came from a habit rather than a new search space:
pass 16 from "what does my own fix make worse", pass 17 from "what counts this".
Both are cheap, both are repeatable, and neither needed a direction nobody had
walked.

ANALYSIS PASS 18 — THE GATE THAT COSTS LESS THAN THE TWO ALREADY THERE
------------------------------------------------------------

Pass 17 found one unit test broken by Phase 2, by accident. This pass ran that
search exhaustively and then measured the gap that let it hide.

MEASURED, on this machine, nothing else running:

    pnpm lint                    3.5 s   at every phase gate
    pnpm typecheck               3.4 s   at every phase gate
    pnpm turbo run test --force  5.9 s   at NO phase gate
    pnpm turbo run test          2.2 s   warm, 8 of 11 packages cached

    H21  HIGH   Eleven phase-commit tasks each say "a phase that commits without
                them is a phase whose breakage surfaces in a later phase's gate,
                attributed to the wrong work" — and stop one command short. The
                first thing that ran unit tests was T099, in Phase 11.

                What sits in that lane:

                    gateway unit lane   8 files, 100 tests
                      session.test.ts   29 tests, 3 of them chapter 3.7's
                      resume.test.ts    23 tests

                **Phases 4, 5, 6, 7 and 8 all edit `session.ts`.** T062 changes how
                a frame reaches a socket, on the same delivery path the suppression
                logic lives on, and the oracle for that logic is entirely in the
                unit lane: deleting `suppressed(connection.marks, message)` turns
                two `resume.test.ts` cases red in about ten seconds. That is this
                project's most expensive historical defect and its detector was
                100% in the lane no phase gate ran.

                Now in nine of ten commit tasks. **T030 had named none of the three
                gates** and it is the phase that creates two production modules.
                T080 is the documented exception and now says so: phase 9 edits
                prose in two trees and no TypeScript, so `check:docs` is its gate.

                **HIGH and not CRITICAL, deliberately.** The Quality Gates section
                and Principle VI both name what must be verified and neither
                enumerates commands — checked, not assumed — so no MUST was
                violated, and pass 17's instance is already owned by T014a-c. What
                remained was residual risk on five phases with a six-second remedy.

THE EXHAUSTIVE SWEEP FOUND NOTHING, WHICH IS THE OTHER RESULT
------------------------------------------------------------

    hard-coded frame counts, whole repo   exactly two, both now owned
    session.test.ts's 4002 test           sends `message.ack`, still server-only
    frames.test.ts                        no member count exists; T014 adds the
                                          protocol package's first
    subject-builder enumerations          none in an existing test
    protocol export-surface test          none exists
    main.test.ts's other derived list     Object.keys(CLOSE_CODES); no new code
    api package, 241 unit tests           the union change is additive

**Pass 17 found the only instance there was.** Worth stating: the value here was
not a second defect but knowing the first was not the tip of anything.

EIGHTEEN PASSES
------------------------------------------------------------

    18  the lane no gate ran   0 CRITICAL, 1 HIGH — measured rather than argued

**Twenty CRITICALs across eighteen passes. Eight were inside this feature's own
documents.**


================================================================
PHASE 1 — SETUP, AND THE FAILING STATE OBSERVED
================================================================

T001 — THE LANE ENVIRONMENT
------------------------------------------------------------

    RELAY_POSTGRES_PORT=15432 docker compose up -d --wait

    clickhouse   Up (healthy)
    mailpit      Up (healthy)
    nats         Up (healthy)
    postgres     Up (healthy)
    redis        Up (healthy)

Five containers, and the nine variables plus NO_COLOR=1 FORCE_COLOR=0 exported
for every recorded run. This machine's own PostgreSQL holds 5432.

T003 — R1 VERIFIED. EIGHT LINES, SEVEN TYPED POINTS, UNCHANGED
------------------------------------------------------------

    services/gateway/src/fanout.ts
      2:  messageCreatedSchema,                                    (the import)
      44: onDelivery(handler: (channelId: string, message: Message) => void): void;
      47: publish(message: Message): Promise<void>;
      62: let deliver: (channelId: string, message: Message) => void = () => {};
      80: const message = messageCreatedSchema.shape.payload.safeParse(parsed);

    services/gateway/src/session.ts
      223: send(connection.socket, { type: "message.created", payload: message });
      896:          type: "message.created",
      912: send(connection.socket, { type: "message.created", payload: message });

Seven typed points, the same seven research R1 lists. The fourth grammar's
justification stands.

**AND THE QUICKSTART SAID "Expect four hits".** Written before pass 4 re-derived
the count, corrected in three files and not in the command block a person would
actually run. `sweep.py` now carries the phrase, so it cannot come back.

T004 — R2 VERIFIED, AND IT IS TWO REFUSALS RATHER THAN ONE
------------------------------------------------------------

    session.ts:939   frameSchema.safeParse(parsed)  -> invalid_frame, NO close
    session.ts:947   type !== "message.send"        -> unknown_frame_type, 4002

The quickstart's R2 line says "Expect one refusal". There are two, and which one
a frame meets depends on whether its type is in the union. **That distinction is
the whole of T009's premise and T009 got it wrong** — see below.

T005 — R3 VERIFIED
------------------------------------------------------------

    typingSchema = { type: "typing", payload: { channel, user } }

Two fields, no `state`, no deadline. The expiry stays the client's.

T006 — R5 VERIFIED
------------------------------------------------------------

    limits.ts:83   operation: "connect" | "send",

Two members. A third would be a typed decision at every call site, and this
chapter adds none.

T007, T008, T008a — THE INSTRUMENTS
------------------------------------------------------------

    check-refs.py   136 tasks, 28 requirements, 10 success criteria, all traced
    sweep.py        no superseded claim, no placeholder, no count disagreement

`FOREIGN` is an empty set, as T007 requires.

T009 — THE RED TEST, AND ITS PREMISE WAS WRONG
------------------------------------------------------------

T009: *"a client uttering the typing signal today receives `unknown_frame_type`
and close 4002."* **It does not.** `unknown_frame_type` is the DIRECTION refusal
and only a frame that PARSED can reach it. `typing.send` is not in `frameSchema`
yet, so `safeParse` fails first: the answer is `invalid_frame` and the socket
stays open.

So the refusal this chapter narrows has THREE states:

    phase 1   not in the union         ->  invalid_frame, socket open
    phase 2   in the union, not send   ->  unknown_frame_type, close 4002
    phase 4   in the named inbound set ->  accepted

Two tests in `session.itest.ts` rather than one:

    answers typing.send with invalid_frame today, and keeps the socket open
        GREEN. Pins the starting state, which no test recorded.

    PHASE 2: refuses typing.send as a direction violation, not a parse failure
        RED ON PURPOSE, and red for ONE phase rather than four. Green when
        phase 2 adds the schema; inverted in phase 4.

**Both are needed and the task asked for one.** A test asserting today's
behaviour is green and documents the baseline; a test asserting the next state is
red and drives the change. T009 described the second and called for the first.

**THE FRAME TYPE IS `typing.send`, DECIDED HERE AND NOT IN T011.** T011 says
"name it in this task and nowhere else", one phase later than the test that has
to utter it — a premise failure of the same shape as chapter 3.20's phase 3,
where a task wanted a binding a phase before its consumer. The name matches the
one inbound verb the protocol already has, so the inbound set is
`{ message.send, typing.send }` and **the rule is legible: an inbound frame ends
in `.send`**. That is worth more than any single name, and FR-003 asks for a
named set rather than a list.

T002 — THE LANE, AND THREE ATTEMPTS TO MEASURE IT
------------------------------------------------------------

**T002 names `pnpm test:integration` and does not say `--force`, and turbo
caches.** The first run read

    Time: 2m30.034s      150.0 s      against chapter 3.20's 228.77 s

which looks like a 35% improvement and is two of four packages actually
executing — the rest were cache hits. A recorded baseline that is not comparable
to the one it is compared against is worse than no baseline.

    pnpm test:integration
      = turbo run test:integration --concurrency=1 --filter=!@relay/outsider

**Attempt two dropped two of those three flags** and died in 97 ms:
`--force` alone pulls in `@relay/outsider`, which needs a running api and gateway
and refuses without them. `Tasks: 0 successful, 4 total`. All four flags matter
and the task quotes one.

**Attempt three is the canonical command with `--force` appended.**

AND A NEXT.JS DEV SERVER IS RUNNING ON THIS MACHINE
------------------------------------------------------------

    384034 node /home/dong/work/3d_site/molten-chrome/.next/dev/...

Another of this machine's projects, not relay's, and **not killed** — it is
somebody's work in progress. Chapter 3.12's first battery attempt failed at run
11 to exactly this, "with no port held and no `EADDRINUSE`". For a single
baseline it is a contaminant worth naming; **for T101's twenty-run battery it is
disqualifying**, and the rule says so in the words "nothing else runs on the
machine". Check `pgrep -af node` before the battery rather than after run 11.

T002 — THE STARTING LANE, MEASURED
------------------------------------------------------------

    package        files  tests   duration
    test-harness       1     17     0.44 s
    api               29    510   102.15 s
    dispatcher         1     16    71.19 s
    gateway            9    150    45.22 s
    e2e                3     10     6.08 s
    ------------------------------------------
                      43    703

    turbo wall clock, four packages, uncached   225.68 s   (3m45.683s)
    e2e, measured separately                     10.50 s
    ------------------------------------------------------
    ~236 s against a 240 s budget

**43 files matches chapter 3.20's record exactly**, and 703 tests is its 701 plus
this phase's two. The arithmetic confirms the predecessor's number rather than
restating it.

**The wall clock is ~236 s, not 228.77 s, and the headroom is ~4 s rather than
11.2 s.** Three honest caveats, in order of size: a Next.js dev server from
another project was running (see above); one run is not a mean and chapter 3.20's
228.77 s was a twenty-run mean; and the e2e package's 10.5 s was measured in a
separate turbo invocation, so it carries its own startup rather than sharing the
first one's. **T101's battery is what produces a comparable figure.** Recording
one run as if it were the mean is how a budget gets believed.

    gateway package clock   45.22 s   against 3.20's 45.50 / 45.48

WHY e2e HAD TO BE MEASURED SEPARATELY, AND WHAT THAT FOUND
------------------------------------------------------------

**A red test in an earlier package hides every later one.** `--concurrency=1`
makes turbo abort remaining tasks on a failure, `@relay/gateway` runs before
`@relay/e2e`, and the run therefore reported

    40 files, 693 tests      Tasks: 8 successful, 9 total

against chapter 3.20's 43 and 701. `turbo run test:integration --dry=json`
schedules **16** tasks including `@relay/e2e#test:integration`, and that package
holds exactly 3 files and 10 tests. 40 + 3 = 43. 691 + 10 = 701.

**The hidden package is the one that matters most.**
`packages/e2e/src/tuan.itest.ts` — 8 tests — is the suite that guards chapter
3.6's duplicate delivery, the defect the twenty-run battery exists for. Phases 1
through 4 would have run with it silently unexecuted, because T017 keeps the lane
red until phase 4 inverts the test.

**So T009's red half is `it.fails` rather than a failing test.** It passes while
the assertion throws and **fails the moment the behaviour arrives**, so phase 2
does not have to remember to look. Proven rather than assumed: flipping the
expected code to what the code actually answers today makes the body succeed, and
vitest reports `× PHASE 2: refuses typing.send as a direction violation`.
Restored afterwards.

AND TWO INSTRUMENT FAILURES OF MY OWN, BOTH THE SAME CLASS
------------------------------------------------------------

A background waiter armed on `grep -qE "^ *Time:|ERR_PNPM|error"` fired
immediately: `error` matches the `"level":"error"` lines the api's own tests emit
on purpose. And a results filter matched nothing on a file that was complete,
because its pattern had been written against a different run's format.

**A pattern that matches something other than the set it was meant to name** —
chapter 3.17 recorded this five times in one feature, `sweep.py`'s skip list did
it to a real hit two passes ago, and these are the third and fourth instances in
this chapter. All four were mine.


================================================================
PHASE 2 — THE PROTOCOL, AND THE TWO COUNTERS
================================================================

T011, T012 — `typing.send`, AND THE DIFF SHAPE FR-008 ASKS FOR
------------------------------------------------------------

    packages/protocol/src/frames.ts | 26 ++++++++++++++++++++++++++
    1 file changed, 26 insertions(+)
    removals: 0

**T017a satisfied by command.** `typingSchema` is not touched; the new schema is
a sibling. "Unchanged" was a claim about one region of a file that changed, and
nothing else in this repository checks a region — so the check is the diff's
shape, and its shape is 26 additions and nothing else.

T014a, T014b, T014c — BOTH COUNTERS, IN THEIR OWN LANES
------------------------------------------------------------

    pnpm turbo run test --force

      × advertises exactly the vocabulary @relay/protocol exports    main.test.ts

**Pass 17's prediction, confirmed at the moment it said it would arrive.** The
test's `toEqual(expectedFrames)` assertion is derived on both sides and passed;
only `toHaveLength(10)` failed. Its title is accurate — the count is the brittle
half, not the claim.

This is the lane that had no phase gate until pass 18, and the failure surfaced
in phase 2 rather than at T099 in phase 11. Afterwards: 11 tasks, 11 successful,
5.3 s.

T015 — PREDICTED THREE FAILURES, GOT TWO, AND THE THIRD CAME LATER
------------------------------------------------------------

    × derives all ten members from the union itself
        AssertionError: expected 11 to be 10
    × classifies every member exactly once
        these frames are in frameSchema and classified nowhere: typing.send

    ✓ names no frame the union does not have        DID NOT FAIL

**The third prediction could not have failed.** That test filters DIRECTIONS for
names absent from the union — only a REMOVAL reaches it, and this chapter adds.
A task predicting an observable that cannot occur, which is chapter 3.20's
recorded lesson arriving in a new place: *a task claiming an observable
difference needs that claim falsified before the test is written.*

**And there is a fourth test T015 never mentioned**: *"agrees with the gateway:
exactly one member is inbound"*, whose own comment says a second inbound row
"would be a classification the code does not implement". It fails one edit later
— after T016 adds the row — and it is RIGHT to fail, because `session.ts` still
accepts only `message.send` until phase 4. So it is `it.fails` from here to phase
4, with the reason in the file.

    after T012 (union)        2 failures
    after T016 (DIRECTIONS)   those 2 green, the fourth red -> it.fails

**The test title carried the number too** — *"derives all ten members"* — and
updating the assertion without the title is how chapter 3.19 shipped a good test
under a false name. Both moved.

T017 — T009's `it.fails` FLIPPED ON SCHEDULE
------------------------------------------------------------

    × answers typing.send with invalid_frame today, and keeps the socket open
        expected { code: 'unknown_frame_type' } to match { code: 'invalid_frame' }
    × PHASE 2: refuses typing.send as a direction violation, not a parse failure
        (it.fails, and the body now succeeds)

Both phase-1 tests failed on the commit that changed the behaviour, which is what
they were built to do. **Phase 1 held two and phase 2 holds one**: the
`invalid_frame` assertion was true until this commit and keeping it would assert a
state the code has left. Its record is here, which is where such a state belongs.

The `it.fails` half is the part worth keeping as a technique. A red test would
have sat red for four phases and taken `@relay/e2e` down with it; `it.fails`
stayed green and **announced the transition the instant it happened**, with no
task needing to remember to look.

T018 — NO NEW CLOSE CODE
------------------------------------------------------------

    pnpm build                              5 tasks, 3 cached
    check-error-codes: 17 codes, 17 sections, each with a cause and a client action

Read from the BUILT `packages/protocol/dist/codes.js`, after the build, so the
green is not a stale artifact's.


================================================================
PHASE 3 — THE FABRIC
================================================================

T020, T021 — `packages/protocol/src/typing.ts`
------------------------------------------------------------

`subjectForTyping(channelId)` -> `typing:{channelId}`, and `typingFabricSchema`
as a strict object of `environment`, `channel`, `user`. **One subject shape, not
two**: chapter 3.20 needed a principal-addressed subject because an addition
cannot ride the channel it adds you to, and typing has no such case — a signal is
only interesting to people already in the channel.

Not named `subjectFor`, which `internal.ts` already exports and chapter 3.18 paid
for once with `TS2308`.

T022, T023 — FIVE GRAMMARS, PAIRWISE DISTINCT
------------------------------------------------------------

    protocol package   7 files, 85 tests

The pairwise test has a third arm the task did not ask for: **no other grammar
starts with `typing:`**. A `new Set` catches equal strings and cannot see a prefix
collision, which is the one a `psubscribe` or any future wildcard read would meet.

T024 — TWO CLIENTS, AND THE TASK LIST HAD SAID ONE
------------------------------------------------------------

    fanout.ts       2        publisher + subscriber
    limits.ts       1
    presence.ts     2
    membership.ts   1        the api publishes; this half only receives
    typing.ts       2        <- both roles on every instance
                   --
                    8        chapter 3.20 closed at six

Analysis pass 5 caught this before any code existed. The module is the first
fabric this service both publishes to and consumes from: a client's signal
arrives at its own gateway over the socket, that instance puts it on the fabric,
and the same instance is subscribed because it holds members of that channel.
One client would subscribe successfully and fail every publish.

T025 — THE TASK ASKED FOR PURE HELPERS AND THIS MODULE HAS NONE
------------------------------------------------------------

`presence.ts` exports `graceCheckDelay` and `wonTransition` because presence has
arithmetic — three thirty-second numbers that turned out to be three quantities.
**Typing has no arithmetic at all**: the five-second expiry is the receiving
client's and the two-second renewal interval lives in `session.ts`. What is left
is transport and a reference count, and every branch of both needs a client.

So `typing.test.ts` covers the one thing that is pure — `DEFAULT_REDIS_URL` — and
says in the file that the module's eight arms are `typing.itest.ts`'s. **Padding
it with a constructed `createTyping` would have opened two sockets to nothing and
leaked them past the file**, because ioredis connects on construction.

T026 — THE EXEMPTION, AND WHICH LIST
------------------------------------------------------------

`services/gateway/src/typing.ts` goes in the `files: ["**/*.ts"]` block's
`ignores`, beside `fanout.ts`, `presence.ts` and `membership.ts`. **It is the
fan-out's case and the cleanest of the four**: PUBLISH and SUBSCRIBE onto
`typing:{channel_id}`, a channel uuid, and no key of any kind — so it does not
need the argument presence and membership each had to make about an
environment-scoped id.

The `.itest.ts` file is NOT listed there. Chapter 3.20 put one in that block and
the later `**/*.itest.ts` block silently overrode it; the typing suite's exemption
is T057a's `DRIVER_EXEMPT_TESTS` entry instead.

T027 — THE OPTION WITHOUT THE DESTRUCTURING
------------------------------------------------------------

`typing?: Typing` on `SessionServerOptions` and the import, and **no destructuring
in `attachSessions`**. `no-unused-vars` rejects a binding whose first consumer is
a later phase, which makes the phase uncommittable — chapter 3.20 paid for that
exact task once. Phase 5 destructures it in the commit that calls it.

T028, T029, T030 — THE GATES
------------------------------------------------------------

    pnpm lint         green
    pnpm typecheck    green
    pnpm turbo run test --force   11 successful, 11 total, 4.8 s

AND TWO SELF-INFLICTED TRAPS, BOTH THE ONE T029 WARNS ABOUT
------------------------------------------------------------

**`pnpm lint` from `packages/protocol/src`** answered
`ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL Command "lint" not found` — loudly, which is
the good case. **`pnpm turbo run test` from `services/gateway/src`** did not: it
ran 4 tasks instead of 11 and printed a green summary. T029 says "pnpm in the
wrong repository exits without running anything and reads as green"; the sharper
version is that a subdirectory of the RIGHT repository filters the workspace and
reads green too. Both gates were re-run from the workspace root.


================================================================
PHASE 4 — THE INBOUND SEAM (US2)
================================================================

T031 — THE NAMED SET COST THE NARROWING, AND THE COMPILER SAID SO
------------------------------------------------------------

Replacing `frame.data.type !== "message.send"` with `INBOUND_FRAME_TYPES.has(…)`
compiled at the seam and broke the send path forty lines below:

    src/session.ts(1081,13): error TS2339: Property 'channel' does not exist
    src/session.ts(1081,22): error TS2339: Property 'text' does not exist
    src/session.ts(1081,28): error TS2339: Property 'idem_key' does not exist

**The single `!==` had been doing two jobs and only one of them was visible.** It
refused the other types AND narrowed the discriminated union for everything after
it. A `Set.has` does neither for the type system.

**The first fix did not work either.** A predicate over the type string —
`(type: string): type is InboundFrameType` — compiles, reads correctly, and still
leaves the send path broken: TypeScript cannot push a narrowing of `.type` back
onto the union it came from. The predicate has to take the FRAME:

    function isInboundFrame(frame: Frame): frame is Extract<Frame, { type: InboundFrameType }>

The rejected alternative was an unreachable `if (type !== "message.send") return;`
after the typing branch — dead code, in a chapter whose coverage pins are
100/100/100/100.

T033 — THREE THINGS THE CLIENT DOES NOT DECIDE
------------------------------------------------------------

The user, the environment and the audience, each one line. The payload has no
`user` field at all, so reading `connection.identity` is belt and braces — but a
reader checks the field, not the schema.

A signal for a channel the connection does not hold publishes nothing and says
nothing (FR-007). Not an error: an error tells a client whether a channel exists,
which is the probe chapter 3.15 closed on the REST surface.

T036 — THE TENTH FILE, WITH NO API AND NO PORT RANGE
------------------------------------------------------------

`services/gateway/src/typing.itest.ts`, on `resume.itest.ts`'s shape: stubbed
`ApiClient`, gateways in process, real Redis, `server.listen(0)`. **Spawn count
stays at seven.** The file appears in no port map because it needs no range.

Its publish assertions are on a raw `ioredis` subscriber, which is why it takes
the `DRIVER_EXEMPT_TESTS` entry T057a specifies — added a phase early, because
`pnpm lint` needs it the moment the file imports the driver.

T039 — THE FALSIFICATION, AND IT BIT IN FOUR PLACES
------------------------------------------------------------

Adding `message.ack` to `INBOUND_FRAME_TYPES` by hand:

    × refuses every non-inbound type with unknown_frame_type and 4002
        direction refusal for message.ack: expected { code: 'invalid_request' }
        to match object { code: 'unknown_frame_type' }
    × closes with 4002 when a client utters a server-only frame (EIR-WS-06)
        15022ms — chapter 2.5's test, waiting for a close that never came
    × has exactly two members
    × is exactly message.send and typing.send
    × holds no server-to-client type, checked against the ones that matter

**Both lanes, and one of them is a test written twenty chapters ago.** The
per-type label in T035's assertion earned its place: the failure names
`message.ack` rather than saying an object did not match.

And the widened seam did not go silent — `message.ack` passed the direction check
and fell through to the send path, which answered `invalid_request`. A refusal
moved rather than vanished, which is the harder case to notice and the reason the
assertion pins the CODE rather than the presence of an error.

AND THE STORE CONTAINERS HAD GONE DOWN BETWEEN PHASES
------------------------------------------------------------

    ECONNREFUSED 127.0.0.1:15432

Not the test's Postgres — this file stubs the api and touches no database. It is
the integration config's `globalSetup`, which migrates once per lane. `docker
compose ps` printed a header and no rows. Brought back with T001's command,
port variable included.


================================================================
PHASE 5 — DELIVERY, CROSS-INSTANCE (US1, MVP)
================================================================

T042, T043a, T043b — FOUR SUBSCRIPTION SITES, NOT ONE
------------------------------------------------------------

    open()                    subscribe    the ordinary path
    close()                   unsubscribe
    the `added` branch        subscribe    chapter 3.20's mid-connection join
    the revocation branch     unsubscribe

**The previous chapter's own note is what made the first one findable.** Its
equivalent had no task, because the revocation release and the user subject each
had one and the ordinary open path fell between them. A channel now carries four
subscriptions on one instance, all reference-counted.

T043 — THE NEIGHBOUR WITH THE OPPOSITE RULE
------------------------------------------------------------

`deliverTyping` sits directly above `deliverPresence`, and they disagree on
purpose: presence walks `subscribersOf` unfiltered, so a user sees their own
transition; typing skips every connection whose identity is the signaller. **The
natural move — copy the neighbour — gives the wrong one**, which is why the
warning is in the file rather than only in a task.

Skipped by IDENTITY, never by socket. The signal arrives from Redis with no
socket reference, on the publishing instance too, and a user may hold several
connections.

T045b — THE TEST T045 CANNOT BE
------------------------------------------------------------

    sends nothing to the signaller's OTHER connection, which a socket filter would

Three instances: the signaller, the signaller again, and a watcher. **T045 passes
with one connection per user whether the filter compares identities or sockets**,
and the wrong implementation shows a user their own indicator on their own second
device. Analysis pass 10 found the gap in the requirement; this is the test.

T048c — THE MID-CONNECTION JOIN, AND MY FIRST VERSION PUBLISHED ON THE WRONG
SUBJECT
------------------------------------------------------------

    expected [] to deeply equal [ { channel, user: 'tuan' } ]

Published on `member:{channel_id}` and delivered nothing. **That is the previous
chapter's central asymmetry, walked into from the outside**: an ADDITION cannot
ride the channel's subject, because the instance holding the new member is not
subscribed to that channel yet — which is exactly the case under test, since Mai
connects holding nothing. The event goes on `member:{env}:{user}`.

The failure was worth more than the test: three chapters of documents say this
and it still took a red assertion to make it mine.

T048a — FIVE AND A HALF SECONDS BOUGHT NOTHING
------------------------------------------------------------

The obvious FR-009a test waits past FR-RTM-08's five seconds to watch the expiry
instant pass unannounced. It cost 5.6 s, tripped vitest's 5 s default timeout,
and would have added its weight to the package that paces the lane — against
about four seconds of headroom in the whole budget.

**And it was waiting for nothing.** There is no server timer: no Redis key, no
`setTimeout`, no row. The gateway does not know an indicator exists, which is
exactly why it cannot end one. Any silence window proves the same property and
FR-009a names no duration, so the wait is 1.5 s with the argument written beside
it.

T051 — THE SUBSCRIPTION COST, MEASURED
------------------------------------------------------------

    CONFIG RESETSTAT, then the typing suite alone, then INFO commandstats

    cmdstat_subscribe   calls=24   usec_per_call=3.42
    cmdstat_publish     calls=15   usec_per_call=2.00
    cmdstat_hello       calls=43   usec_per_call=6.07
    cmdstat_info        calls=43   usec_per_call=35.30

**24 subscribes for 10 tests across 14 gateway instances**, which is the
reference counter working: an instance holding one channel subscribes once
whatever else it does. The publishes are one per signal, 15 of them, at 2 µs
each. The expensive command in the file is `INFO` — the harness's own health
check — at ten times a publish.

T052 — THE TENTH FILE COST THE PACKAGE NOTHING MEASURABLE
------------------------------------------------------------

    gateway package     45.22 s     10 files, 161 tests
      phase 1           45.22 s      9 files, 150 tests
      phase 4           45.67 s     10 files, 153 tests

    typing.itest.ts alone   5.84 s   10 tests

The package clock is `max(file)` and `presence.itest.ts` still paces it at ~45 s,
so a 5.84 s file that spawns no api runs inside that shadow. **The prediction was
"the cost of one api-less suite — not zero, and not an api boot", and the honest
reading is that it is not zero and is not visible either**: it is 5.84 s of
parallel work under a 45 s ceiling.

T047 IS DEFERRED TO PHASE 6, AND COULD NOT HAVE BEEN OTHERWISE
------------------------------------------------------------

T047 asks for "a signal after the renewal interval delivers a second frame, and a
signal inside it delivers nothing further", built with `renewalIntervalMs: 40` —
**an option T054 creates, in phase 6.** Both halves of the assertion need an
interval that does not exist yet. The task names its own dependency and sits in
the wrong phase; it runs beside T057.


================================================================
PHASE 6 — THE RENEWAL INTERVAL (US3)
================================================================

T054, T056 — TWO SECONDS, AND WHY IT IS NOT FIVE
------------------------------------------------------------

    DEFAULT_RENEWAL_INTERVAL_MS = 2_000     the gateway's minimum gap
    FR-RTM-08's expiry          = 5_000     the receiving client's timer

**2.5 renewals per expiry window**, so one dropped publish does not make an
indicator flicker. At 1.67 s the margin is thinner for no gain; at 4 s a single
loss blanks the indicator for a user who never stopped typing. **Five and two are
two quantities**, and chapter 3.19's grace check — armed at exactly its own grace
period, two deadlines on one instant reached by two clocks — is what the ratio
exists to avoid.

The map is `Map<connectionId, Map<channelId, number>>` in `attachSessions`'s
closure. Not a field on `Connection`: that type lives in `registry.ts`, which four
chapters fence, and a two-second value is not worth four diffs. **No Redis, no
third `operation`, `limits.ts` untouched** — R5's bucket cannot express this rule
on three counts, each fatal alone.

Three deletions, and the third is the one an obvious implementation misses:

    the close handler        the connection's whole entry
    a revocation             that channel's entry
    a read, once elapsed     `if (now - at > interval) channels.delete(channel)`

Without the third the bound is channels a connection has EVER typed in while
connected rather than channels it is typing in.

T060 — THE PROOF IS A CASE, NOT A SOURCE EDIT
------------------------------------------------------------

    publishes once for a burst inside the interval                 8 sends -> 1
    publishes every signal when the interval is zero               8 sends -> 8

Same burst, two instances, `renewalIntervalMs` 2000 and 0. **Edit-and-restore is
the proof form that has now failed twice** — chapter 3.20 ran it on two orderings
and got no failure either time — and a proof written as a case stays in the suite
rather than being something somebody did once and wrote down.

PHASE 6 BROKE A PHASE 5 TEST, CORRECTLY
------------------------------------------------------------

    × sends nothing at all after the signal, until another signal is sent
      AssertionError: expected [ { …(2) } ] to have a length of 2 but got 1

T048a ends with a liveness probe — a second signal, sent to show the fabric is
alive so the silence before it reads as a decision. It was written in phase 5 with
the default interval, which was nothing. **Phase 6 made the default a two-second
debounce and the probe landed inside it.**

A P2 story's mechanism silently changing a P1 story's test, caught in the phase
that caused it rather than at T099 — which is the gate analysis pass 18 added.
The test's subject is FR-009a's silence, not the interval, so it now builds with
`renewalIntervalMs: 40` and says why in the file.

T047 — RAN HERE, AND 120 ms AGAINST 40 rather than 40 AGAINST 40
------------------------------------------------------------

Deferred from phase 5 because both halves need the option this phase creates.
Built with 40 ms rather than waiting out two real seconds, and the wait is 120 ms
— **never exactly the interval**, which is the two-deadlines-one-instant shape
again.


================================================================
PHASE 7 — THE RESUME BUFFER, AND WHAT MUST NOT ENTER IT
================================================================

T062 — VERIFIED RATHER THAN BUILT
------------------------------------------------------------

`deliverTyping` was written in phase 5 consulting neither `connection.phase` nor
`connection.marks`, so this phase's implementation task was already satisfied.
Confirmed by command: zero matches for either identifier inside the function.

T063 — MY FIRST VERSION SLEPT THROUGH THE WINDOW IT WAS TESTING
------------------------------------------------------------

    AssertionError: expected [ { type: 'message.created' } ] to deeply equal []

The backfilled frame arrived inside a 300 ms check against an 800 ms delay.
**Because the test awaited the watcher's `connection.ack` first**, and on the
resume path the order is *"confirm, backfill, ack, emit, flush, live"* —
`session.ts` says so in its own comment. The ack goes out AFTER `api.backfill`
returns, so waiting for it burns the whole window.

**A wait for the wrong signal closes the window it was meant to hold open.** The
signaller is acked first, then the watcher is opened and NOT awaited.

Chapter 3.20's two traps, both handled: the connection is genuinely `buffering`
(a cursor is presented — `session.ts:766` makes `phase` depend on it) and the
cursor/sequence collision does not apply, because a typing frame has no sequence
at all. That absence is why it cannot be meaningfully buffered.

**The assertion is an ORDERING, not an arrival.** A buffered frame still arrives,
after the flush — so "it arrived" proves nothing. An immediate frame arrives
BEFORE the backfilled `message.created`.

T065 — AND IT BITES
------------------------------------------------------------

    if (connection.phase === "buffering") continue;   // spliced into deliverTyping

    × sends a typing frame during a resume, before the backfill it is racing
      AssertionError: expected [] to have a length of 1 but got +0

Restored. Chapter 3.20 ran this same proof on two orderings and got no failure
either time, so the result was worth having rather than assuming.

A FLAKE, CAUGHT AND FIXED RATHER THAN RE-RUN
------------------------------------------------------------

    × sends nothing at all after the signal, until another signal is sent  315ms

Passed on repeat. **A connection is acked before its Redis SUBSCRIBE has
necessarily landed** — the non-resume branch of `open()` acks without awaiting
`subscribing` — so a test that acks a watcher and then signals can miss the
frame, and a fixed `settle()` afterwards makes that unlikely rather than
impossible.

Every positive-arrival assertion in the file now polls with a deadline
(`untilTyping`) instead of sleeping. Negative assertions keep a fixed wait,
because there is nothing to poll for. Three consecutive runs: 17/17, 11.1 s,
11.0 s, 11.0 s.

**This is the shape the twenty-run battery exists to catch, and the shape that
gets waved away as "flaky" when a re-run is green.** The re-run was green.

AND I FAILED TO RESTORE A FILE FROM THE WRONG DIRECTORY
------------------------------------------------------------

    cp: cannot create regular file 'services/gateway/src/session.ts': No such
    file or directory

The `cd` was into `services/gateway/src` and the path was repository-relative, so
T065's temporary edit stayed in the tree until the next command. Third directory
mistake of this implementation, after `pnpm lint` from a package subdirectory and
`pnpm turbo run test` filtering to one package while reading green. **`git status`
is the check that caught it**, which is why every phase gate runs one.

AND THE FULL LANE FAILED ONCE, WITH THE SHAPE gaps.md ITEM 19a DESCRIBES
------------------------------------------------------------

    × stops delivering to a member who was REMOVED while connected (FR-RTM-10)  5562ms
    × delivers nothing from a PRIVATE channel to a non-member's socket             3ms
    × accepts typing.send and answers with nothing at all                          2ms
    × refuses every non-inbound type with unknown_frame_type and 4002              2ms
    × refuses a typing.send whose payload names a user                             1ms

    Test Files  1 failed | 9 passed (10)      Tests  5 failed | 163 passed (168)

**One root and four cascades.** The first is a 5.5-second timeout; the other four
fail in one to three milliseconds, which is a dead fixture rather than four
independent defects — `session.itest.ts` spawns an api and every test after the
one that killed it fails instantly.

Narrowed rather than re-run and forgotten:

    session.itest.ts alone                    17/17
    session.itest.ts + typing.itest.ts        34/34
    the full package, again                   168/168, 45.57 s

**So it is not this chapter's new file interfering with that one**, which was the
obvious hypothesis and the wrong one. It reproduces only under the whole
package's parallelism, and its shape — an api fixture that stops answering, in a
file that spawns one — is chapter 3.20's `gaps.md` item 19a: four failures across
forty runs with three hypotheses measured and eliminated, still open because
every spawning file either uses `stdio: "ignore"` or pipes and never reads.

**Recorded rather than explained.** One failure in three full-lane runs this phase
is consistent with the 17.5% per-run rate that chapter measured, and T101's
battery is the instrument for it. What this phase can say is that it is not new:
the same file, the same spawn, and a green run on either side.


================================================================
PHASE 8 — FAILURE, AND THE CLOSED VOCABULARY
================================================================

T068 — THE PUBLISH DOES NOT FAIL, AND FR-015 DID NOT EXPECT THAT
------------------------------------------------------------

Severing the fabric with a TCP proxy and reading the sink:

    AFTER CUT: [{"msg":"typing.failed","op":"connection"},
                {"msg":"typing.failed","op":"connection"},
                {"msg":"typing.failed","op":"connection"},
                {"msg":"typing.failed","op":"connection"},
                {"msg":"typing.failed","op":"connection"}]

**Five lines, all `op: "connection"`, and not one `op: "publish"`.**
`publisher.publish()` never rejects: ioredis's default offline queue accepts the
command and resolves it when the connection returns. So a severed fabric does not
DROP a typing signal — it DELAYS it, and the queued signal arrives on reconnect
alongside the next one.

FR-015's first two clauses hold and were asserted: the socket stays open and the
client is told nothing. **Its third does not.** "MUST be logged once with a stable
event name" — the name is stable and what is logged once per outage is *nothing*,
while what is logged per reconnect attempt is unbounded.

**A publisher that queues satisfies "the socket stayed open" the way chapter
3.18's fan-out satisfied "the send returned 201 while Redis was down":
trivially.** The test was written expecting one publish failure, measured five
connection failures and zero publish failures, and now asserts what happens.

**Not fixed here.** All four fabric modules share this error-listener shape —
`fanout.ts`, `presence.ts`, `membership.ts` and `typing.ts` — so bounding the
retry logging is a cross-module decision rather than this chapter's. It goes to
`gaps.md` with the measurement attached.

T069, T070 — THE SET, AND THE SILENCE
------------------------------------------------------------

    emitted, from one instance:   typing.invalid_payload, typing.published

Asserted as the set an instance actually emitted rather than as what a grep
finds. Chapter 3.20's FR-032 declared three names while the code emitted six and
the clause had to be amended afterwards; a set assertion is what catches that on
the day.

`typing.failed` is the third declared name and is reached by the severed-fabric
test above and by the environment-mismatch test earlier in the file — asserted
there rather than forced here, because **a name reached only by a test that
exists to reach it is a name nothing needs.**

And a debounced signal writes nothing at all: the sink length is unchanged across
a burst of five. One line per keystroke is the unbounded output NFR-OBS-01 exists
to prevent.

T072 — FOUR KINDS, ONE CHANNEL, ONE WATCHER
------------------------------------------------------------

    message.created     1
    presence.changed    1
    membership.changed  1
    typing              1
    total non-ack       4

**Five subject shapes share one Redis** and every gateway subscribes to a string.
`typing.test.ts`'s pairwise test proves the SUBJECTS cannot collide; this proves
the DELIVERY does not, which is a different claim — a builder can be distinct
while a handler is wired to the wrong one.

The first version wired fanout and presence and not membership, and the failure
named itself: `membership: expected [] to have a length of 1`.

AND A SECOND FIXTURE RACE, SAME SHAPE AS PHASE 7's
------------------------------------------------------------

    AssertionError: expected [] to have a length of 1 but got +0

An instance publishing through the TCP proxy opens a fresh connection through an
extra hop, and the first publish after a boot lands later than a fixed 300 ms
wait. Polled with a deadline instead (`untilSignals`), matching the socket-side
`untilTyping` phase 7 added. **Both are properties of the fixture rather than of
the code**, and both would have read as flakes in a battery.


================================================================
PHASE 9 — THE DOCUMENTS
================================================================

T074, T074a — TWO ADRs, NOT ONE
------------------------------------------------------------

    ADR-21   typing on a fourth subject grammar
    ADR-22   the typing expiry belongs to the receiving client

**ADR-22 exists because analysis pass 16 found the chapter's central finding
reaching no published document.** FR-009c's verdict was going to `chapter-notes.md`
and the tutorial, and a customer reads neither — while `docs/04-srs.md`'s
FR-RTM-08 goes on saying indicators "shall expire automatically", which reads as a
server promise. The clause is unchanged: a requirement is the customer's contract,
and a chapter does not rewrite one to match its code.

Three status lines now point at the correction rather than being edited, because
ADRs are immutable once accepted:

    ADR-07   extended by ADR-22 — the opposite case to ADR-20's; a dropped typing
             frame self-corrects, so ADR-07's loss argument holds for it exactly
    ADR-19   extended by ADR-21 — seven typed points where this record says three
    ADR-20   the count it borrows from ADR-19 is corrected in ADR-21

**And I mangled ADR-07's status line first**, appending to it twice so it read
"extended by ADR-20 … · extended by ADR-20, which". Repaired before the commit.
The first edit had also targeted the wrong record — ADR-07 rather than ADR-19 —
because both status lines matched the pattern I searched for.

T074b, T074c — THE DEEP DIVES, AND ADR-20's DEBT PAID HERE
------------------------------------------------------------

    grep -c "^### ADR-" docs/05-sad.md            22
    grep -c "^## ADR-"  docs/06-adr-deep-dives.md 22

**That file's Purpose says the long form exists "for each ADR" and it stopped
being true in chapter 3.20**, which wrote ADR-20 into the SAD, amended ADR-07's
deep dive in the same file, and wrote no dive for its own record. Analysis pass 16
found it while checking what ADR-22 would join.

T074c asked which chapter pays. **Paid here**: this phase already had the file
open, the alternative was leaving a published document's own scope claim false for
a third chapter, and the cost was one page. The closing section's heading moved
from "Reading the nineteen together" to twenty-two.

T077 — TWO COUNTS IN ONE DOCUMENT, AND ONLY ONE WAS MAINTAINED
------------------------------------------------------------

    ### Part 3 — Becoming a platform (20 chapters)   -> 21, matching its rows
    Part 3   Becoming a platform   10 chapters       -> 21
                                   54 chapters       -> 65

**The summary table at the top of the plan said Part 3 had ten chapters.** Every
other part's summary matches its rows exactly — 5, 4, 8, 8 — so this is not a
historical table with one stale row; it is a live table nobody updated while Part
3 grew from ten to twenty-one across chapters 3.11 to 3.21. The total was wrong by
eleven.

This is the project's own two-counts discipline failing in the document that
teaches it.

T077a, T077b — THE CUSTOMER-FACING CORRECTION
------------------------------------------------------------

`docs/08-error-reference.md`'s `unknown_frame_type` entry said *"`message.send` is
the only inbound frame"* and *"**What to do:** send `message.send`."* Both now
name the set, and the entry states the rule rather than the list: **an inbound
frame ends in `.send`.**

It also now says which refusal comes first — a type the union does not contain
fails schema validation and gets `invalid_frame` with the socket open, which is
the three-state finding from phase 1 reaching the document a customer reads.

`pnpm check:errors` is green at 17 codes and **could not have caught any of
this**: it asserts structure, not truth. A green run after a false entry is the
checker working as designed.

T077c — THE ADVERTISEMENT, AND THE ROUTE IS `/healthz`
------------------------------------------------------------

    curl -s localhost:4001/healthz | jq '.protocol.frames | length'   ->  11

    connection.ack, error, membership.changed, message.ack,
    message.created, message.deleted, message.send, message.updated,
    presence.changed, typing, typing.send

**T077c's command said `/health` and the gateway answered 404.** The route is
`/healthz`. Corrected in the task; the count is right and `typing.send` is on the
wire.

T076, T078, T079 — THE CHECKS
------------------------------------------------------------

    git diff docs/04-srs.md          empty, as R10 expected
    pnpm sync:docs                   05-sad, 06-adr-deep-dives, 08-error-reference
    pnpm check:docs                  all mirrored docs match their sources

    "only inbound frame"             0 hits remaining, both trees
    "declared words with nothing"    chapter 3.19 — phase 10's to correct
    "genuinely reuse"                chapter 3.20 — phase 10's to correct


================================================================
PHASE 10 — THE CHAPTER
================================================================

T081 — WHAT THE CHAIN DEMANDED, BEFORE A WORD WAS WRITTEN
------------------------------------------------------------

    9 problems, 9 files, in 5 pages plus the appendix

    eslint.config.mjs                        post-series.md
    packages/protocol/src/frames.ts          chapter 3.18
    packages/protocol/src/frames.test.ts     chapter 3.18
    packages/protocol/src/index.ts           chapter 3.08
    services/gateway/src/main.ts             chapter 3.20
    services/gateway/src/main.test.ts        **chapter 1.04**
    services/gateway/src/session.ts          chapter 3.20
    services/gateway/src/session.test.ts     chapter 3.20
    services/gateway/src/isolation.itest.ts  chapter 3.18

**`main.test.ts` is chapter 1.04's**, which is analysis pass 17's finding arriving
as a fence demand: a Part 1 chapter fences a file this chapter had to edit for the
frame count, and no task touched it until that pass.

    9 demanded + 5 created = 14 fenced      T082 predicted ~20
    9 taught                                -> the word estimate

T083 — THE ESLINT FENCE BELONGS TO THE APPENDIX, AND I WROTE IT TWICE
------------------------------------------------------------

The chapter's first draft fenced `eslint.config.mjs` itself and the chain refused:

    [HEAD] fences/post-series.md:2560
        eslint.config.mjs differs at line 112

**The appendix owns that file** — its exemption lists do not exist at the moment a
chapter could fence them, which is what T083 says and what chapters 3.19 and 3.20
both did. Removed from the chapter, added to two of the appendix's hunks: the
ioredis `ignores` block and `DRIVER_EXEMPT_TESTS`.

AND THREE FENCES THAT WERE NOT FENCES
------------------------------------------------------------

    services/gateway/src/session.ts (before this chapter)   does not exist
    the grep that changed the chapter                        does not exist
    packages/protocol/src/frames.ts (chapter 1.3, unchanged) does not exist

**Every titled fence is a file path unless the title contains `(excerpt)`** —
`check-fence-chain.mjs:43` is the whole rule. An illustrative snippet with a
descriptive title is read as a claim about a file that must exist. Three of mine
were, including one whose title was not a path at all.

    check-fence-chain: 234 fenced files across 38 chapters   (was 229 / 37)

T086, T086a — THE CHECKER FOUND ITS OWN BAD FRAGMENT
------------------------------------------------------------

Written before any correction and run red: **8 of 10 fragments present**, and the
two that passed were claim 1's, already corrected in phase 9.

**But claim 5's two fragments passed for the wrong reason.** I had guessed at
chapter 3.19's sentence — "Typing's half of this promise is still open." — and it
matched nothing, because the real sentence is 2,700 lines further down in that
chapter's ForwardRef:

    Typing may well reuse the fan-out's plumbing — that half of 2.6's sentence
    is still open, and this chapter closed only presence's half of it.

**A fragment that describes a claim rather than quoting it reports green**, which
is exactly what analysis pass 9 named and exactly what I then did. Running the
checker BEFORE correcting is what surfaced it: one green line among seven red ones
is visible in a way a green run is not.

T087 — FIVE CLAIMS, TEN FRAGMENTS, BOTH LOCALES
------------------------------------------------------------

    check-prose: none of the superseded sentences is still published

Claim 5's correction could not touch its own fence — those are chapter 2.6's words
and the chain compares them byte for byte — so the ForwardRef beneath answers the
question the fence left open, and says chapter 3.21 answered it *no*.

Claims 3 and 4 are chapter 3.20's, written one chapter ago. Its ForwardRef
predicted a conclusion; it now names a decision. **A ForwardRef should describe
what the next chapter must decide, not what it will conclude** — and this
chapter's own, about FR-RTM-09's per-member expiry, is written to that rule.

T088, T089, T091–T093 — THE MIRROR AND THE GATES
------------------------------------------------------------

    check-fence-chain   234 fenced files, 38 chapters, 38 translated  (was 37)
    check-figures       238 figures, every diagram passed as `code`   (was 232)
    check:srs           245 clause rows, 245 unique ids
    check:errors        17 codes, read from a fresh build
    check-prose         none of the superseded sentences is still published
    check-docs          all mirrored docs match their sources

    tutorial build      99 static pages  (was 97 — two per chapter)
                        /part-3/chapter-21/the-frame-nobody-may-send
                        /vi/part-3/chapter-21/the-frame-nobody-may-send

**The translation was produced by splitting the page on its fences and rewriting
only the prose**, so every fence is byte-identical by construction rather than by
inspection. Chapter 3.20 lost four fences from its mirror to whitespace drift
while their bytes were identical; splitting on the fence regex makes that
impossible rather than unlikely.

`lib/tutorial.ts` has all three Vietnamese fields including `translatedIn: ["vi"]`
— chapters 3.10 to 3.18 each shipped a translated body without it and are absent
from the sitemap.


================================================================
PHASE 11 — POLISH AND CLOSE-OUT
================================================================

T095 — THE SERVICES WERE RUNNING, AS T095 PREDICTED
------------------------------------------------------------

    docker compose stop api gateway dispatcher
      Container relay-gateway-1 Stopped
      Container relay-api-1 Stopped

Chapter 3.20 found them up at this exact step, and a live dispatcher moved a
branch pin on a byte-identical file by 7.7 points in chapter 3.18. They were up
again — from phase 9's `/healthz` check, which started a gateway to read the
protocol advertisement and is exactly the kind of thing this task exists to catch.

T100a, T100b — THE SEALED CLIENT SENDS, FOR THE FIRST TIME
------------------------------------------------------------

    grep -c "\.send(" packages/outsider/src/integrate.itest.ts
      before: 0        after: 3

Eleven tests, ten REST and one socket test whose title says "sent over REST"
because chapter 3.18 corrected it. **This file is the only check in the repository
that uses the public surface as a customer does** — Node's global `WebSocket`, no
workspace import — and it had never exercised the inbound seam at all.

Two tests now do:

    says it is typing, and a second member's socket hears it
    is refused with unknown_frame_type for a frame only the server may send

The second is the correction to `docs/08-error-reference.md` in bytes rather than
in prose. That document tells a customer *"send `message.send` … Do not send
events; receive them"* and **nothing had ever checked what happens when they do.**

T095 — COVERAGE IS RED, AND THE CAUSE IS THE CALENDAR
------------------------------------------------------------

    Test Files  2 failed | 91 passed (93)
    Tests       2 failed | 1164 passed | 5 skipped (1171)
    Duration    456.97 s

**Vitest prints no coverage report when tests fail**, so there are no per-file
numbers yet and T096's pins cannot be written until the lane is green.

    × services/api/src/internal/usage.itest.ts
        answers 402 with a named code when the cap is spent
        AssertionError: expected 200 to be 402
        carries NO Retry-After, which is the whole difference from a rate limit

**Neither file is this chapter's, and the cause is a hardcoded month.**

    services/api/src/internal/usage.itest.ts:56    const AUGUST = "2026-08-01";
    date -u +%Y-%m-%d                              2026-09-01

`oneReport` credits connection-minutes to the **August** period; the quota check
in `POST /internal/session` reads the **current** period, which became September
while this chapter was being implemented. The test credits 50 minutes to a month
nobody is asking about, the cap reads zero used, and the session is allowed.

**It was correct yesterday and wrong today, and nothing in this chapter touched
it.** Phase 1's baseline recorded the api lane at 29 files / 510 tests all green;
the same lane now reads 508 passed, 2 failed, on the same commit for that file —
whose last change was chapter 3.12's.

FIXED, AND ONLY ONE OF THE FIVE NEEDED IT
------------------------------------------------------------

Five api integration files hardcode a period and **the literal is not the
hazard** — the CROSSING is: a fixture naming a period while the subject derives
one. Checked individually rather than replaced in bulk:

    usage.itest.ts             THE BUG. `oneReport` writes a period;
                               `session.controller.ts:104` reads
                               `periodOf(new Date())`. Now `const PERIOD =
                               periodOf(new Date())`, so the two agree in every
                               month. 21/21 green.

    connections.itest.ts       passes the period EXPLICITLY —
                               `assertConnectionsWithinQuota(db, env, AUGUST)`.
                               Both sides agree by construction. Correct.

    period.itest.ts            tests `periodOf`'s arithmetic: a fixed input with
                               a fixed expected output, which is the right shape
                               for a pure function. Correct.

    config.itest.ts            the period is filler in a row that exists to test
                               a `dimension` CHECK constraint. Nothing reads it.

    session.perf.itest.ts      an `EXPLAIN ANALYZE` whose own comment says "THE
                               ASSERTION IS ABOUT THE PLAN, NOT THE CLOCK".

**The first version of this entry said five files had the problem**, written
before any of them was read. One did.

**A test whose fixture names a month while its subject reads the clock is a test
with an expiry date** — and the timing is worth keeping: a twenty-run battery
started on 31 August and finished on 1 September would have recorded this as a
flake at a 5% rate, and three hypotheses would have been measured before anyone
ran `date`.

    × services/gateway/src/limits.itest.ts — one counter, two services (3.8)

The second failure is chapter 3.20's `gaps.md` item 19, unchanged: the rate
limiter's fixed window is aligned to the wall clock, and two tests send three
requests expecting the third to be refused.

T096, T097 — 100/100/100/100 ON BOTH, AFTER FOUR ARMS
------------------------------------------------------------

    coverage, green         93 files, 1174 tests, 433.43 s
    every pin held          exit 0

    packages/protocol/src/typing.ts    2/2 statements, 0/0 branches, 1/1 functions
    services/gateway/src/typing.ts    45/45,          13/13,         13/13

**The protocol module reached 100 on the first run** — one function and no
branches, which is what a subject builder and a schema are. The gateway module did
not: **97.77 / 76.92 / 92.3 / 97.67**, with four arms unreached.

T097 asks whether unreachable code should be deleted before it asks for a test.
**All four were reachable and nothing had reached them:**

    url ?? DEFAULT_REDIS_URL   every test supplies a url, or the env var does
    the onSignal no-op         every test wires a handler through attachSessions
    counts.get(c) ?? 0         needs a SECOND subscribe for one channel
    next <= 0's else           needs one of TWO holders releasing

The last two are the reference count, and they decide whether a second member of a
channel silently loses typing when the first disconnects. **A gateway cannot reach
them** — the session layer holds one connection per socket — so they are driven
against `createTyping` directly, in a describe that builds the module itself.

AND THE PROTOCOL MODULE'S NUMBER CAME FROM THE JSON, NOT THE TABLE
------------------------------------------------------------

The text reporter omits a file that is at 100% on all four metrics, so
`packages/protocol/src/typing.ts` was simply **absent** from the table. Its
absence is not evidence: `coverage-final.json` is, and it was read rather than
inferred. A gap in a table is a gap in a table.

T098 — FOUR TITLES READ AGAINST THEIR BODIES, AND FOUR WERE WRONG
------------------------------------------------------------

Every new test's title, printed beside its `expect` calls and read as a pair.
Twenty-three integration tests, one gateway unit test, ten protocol unit tests.
**Four did not say what they check.**

    "survives a severed fabric, LOGS IT ONCE, and publishes again"

The body proves the opposite — five `op: "connection"` lines and zero
`op: "publish"`. **The title quoted FR-015's clause while the test refuted it**,
which is chapter 3.19's exact failure appearing in this chapter's own file.
Retitled to say which failure is logged.

    "emits exactly THE THREE NAMES it declares, and none for a debounced signal"

Asserts two: `typing.invalid_payload` and `typing.published`. The third is
deliberately reached by other tests and the body's comment says so. Retitled to
"the two names this run reaches".

    "drops a signal on the floor when no handler is wired"
        expect(true).toBe(true)

**A test with no assertion about its subject.** "No throw" is equally true of a
module that never received the signal. Now a second, WIRED module subscribes to
the same subject: its delivery proves the publish landed, so the unwired one's
silence is the no-op default running rather than an absent publish.

    "falls back to DEFAULT_REDIS_URL when neither a url nor the env var is given"
        (no expect at all)

It took the branch, moved the coverage number, and proved nothing about where the
client connected. Now it asserts a round trip through the fallback url.

**Two of the four moved a coverage number while asserting nothing.** That is the
combination worth naming: the ratchet is satisfied by execution, and only a
reader is satisfied by an assertion.
