# Chapter battery — measured on the published page (T045)
# relay-tutorial/app/(en)/part-3/chapter-02/keys-and-tokens/page.mdx
# Canonical words = prose only: fences stripped, plus lines starting
# `import `, `<`, `/` or `export const`.

chapter  words  WHY  TRAP  SKIP  FWD  FIG  CHK  verdict
3.2      3385   2    2     1     1    3    1    PASS

thresholds  2000-4000  >=2  >=1  ==1  >=1  2-4  ==1

SKIP AHEAD names `part3-ch2`  ✓
Figures: figTwoCredentials (flowchart), figConnect (sequence), figAuthOrder
(flowchart). All three render as SVG in the built site — verified in headless
chromium, 3 mermaid SVGs, zero "Syntax error" text, zero page errors.

## For comparison, the chapters before it

3.1   3190   2   1   1   2   3   1
2.8   2871   2   1   1   1   3   1
2.7   3402   3   2   1   2   4   1

## One number that is not in the table

41 fenced files, which is roughly three times any earlier chapter (2.7 carried
15). That is not padding: retiring two seams touched 29 files earlier chapters
had already shown, and the fence chain requires every one of them to be
amended in the chapter that changes them. The prose count is unaffected —
fences are stripped before counting — but the page is long, and the chapter
says why in "The blast radius, file by file".
