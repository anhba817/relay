# Chapter 3.11 — baseline
# Recorded 2026-08-22T01:44:10Z

## T001 — provenance

outer repo      5d3c3bc docs: chapter 3.11 analysis pass six — 6 of 7 applied, 1 LOW declined
relay-platform  b21a87c docs: this chapter's comments cite ids that resolve
                tag: part3-ch10
relay-tutorial  92e2440 docs: 3.10 published, mirrored, fences regenerated after the traceability pass

parent pins vs submodule HEADs:
  pinned  b21a87cde9682bf4f4ad93992b7a241d64c5bff9  relay-platform
  pinned  92e2440aa555d2a277163187cad7659599e1db0a  relay-tutorial
  head    b21a87cde9682bf4f4ad93992b7a241d64c5bff9  relay-platform
  head    92e2440aa555d2a277163187cad7659599e1db0a  relay-tutorial

Working trees: platform 0 modified,
               tutorial 0 modified.

## Environment

The host holds 127.0.0.1:5432 with a Postgres this project does not own, and an
unrelated `lendtrust-postgres` container sits on 5433. Neither matters: this
project's own default is **15432** (`db/client.ts:13`, `test-harness/db-url.ts:17`),
so the stack runs on its documented ports and nothing was moved to accommodate
the neighbours. Recorded because the previous chapter lost time to a collision
here.

  RELAY_POSTGRES_PORT=15432   redis 6379   nats 4222   mailpit 8025/1025   clickhouse 8123

## T002 — the platform lanes, before anything changes

Unit (`pnpm turbo run test --force`) — exit 0:

  test-harness   9      config          7      service-kit    3
  protocol      37      api           158      dispatcher     8
  gateway       64                                    TOTAL 286

Integration (`pnpm test:integration`) — exit 0:

  test-harness   8      api           201      gateway       21
  dispatcher    16      e2e            10              TOTAL 256

256 matches what chapter 3.10 closed on, exactly.

Coverage (`pnpm coverage`) — exit 0, 263s:

  All files   89.45 statements / 82.73 branches / 88.94 functions / 90.86 lines

Per-file ratchets, measured against what `vitest.coverage.config.mts` pins:

  file                          measured (st/br/fn/ln)     pinned         headroom
  repository.ts                 97.48 / 90.17 / 100 / 99.38   97/90/100/99   BRANCHES +0.17
  user-token.ts                 96 / 96 / 100 / 100           96/…           0
  auth-limiter.ts               100 / 87.5 / 100 / 100        …/87/…         +0.5
  store.ts                      100 / 91.66 / 100 / 100       …/91/…         +0.66
  deliver.ts                    100 / 90.9 / 100 / 100        …/90/…         +0.9
  expand.ts                     92.3 / 92.3 / 100 / 100       92/92/…        +0.3
  gateway limits.ts             100 / 90.9 / 100 / 100        …/90/…         +0.9
  resume.ts                     100 / 95 / 100 / 100          …/95/…         0

**`repository.ts` has 0.17 points of branch headroom.** R23 predicted this file
would go red; the margin is one uncovered branch. T033b is scheduled in Phase 4
for that reason and the number above is what it measures against.

## T003 — the site, before anything changes

  check:fences  exit 0   173 fenced files replay across 27 chapters
                         (27 translated, fences mirrored, 1 retired, plus post-series)
  check:docs    exit 0
  lint          exit 0
  build         exit 0

## T004 — three consecutive integration runs

  run 1  exit 0  190s  256 tests
  run 2  exit 0  189s  256 tests
  run 3  exit 0  188s  256 tests

Green three times, same count, 2s of spread. T005 had nothing to fix forward.

**A defect was found here, and not by any of the seven analysis passes.** The
quickstart's V12 battery extracts the test count from the lane's output, and the
command could not have worked: it anchored on `^\s*Tests `, and in a turbo log
every line carries a `@relay/<pkg>:test:integration:` prefix. Measured:
`grep -E '^\s*Tests ' itest-1.log` returns **0 lines**. It also took `tail -1`
where a run prints **five** summary lines, one per package — and `@relay/e2e` is
the package a `[a-z-]+` pattern silently drops, because of the digit. The first
version of the same command, which the fourth analysis pass replaced, was wrong
differently: it read the failure count as the total on red runs. Fixed against a
real log and recorded in the quickstart, both wrong versions included.

## T004a — the connect path, measured before it changes

`POST /internal/session`, 400 calls per level after 100 warm-up calls:

  | concurrency | n   | mean ms | p50    | p95    | p99    | req/s |
  |-------------|-----|---------|--------|--------|--------|-------|
  | 1           | 400 | 6.807   | 6.087  | 12.537 | 14.201 | 147   |
  | 8           | 400 | 5.490   | 4.734  | 9.558  | 17.777 | 1423  |
  | 32          | 416 | 15.001  | 13.007 | 35.939 | 53.808 | 2026  |

What it reads today, `EXPLAIN (ANALYZE, BUFFERS)`:

  environmentLimits     Index Scan environments_pkey                  3 buffers, 0.009 ms
  getUserByExternalId   Index Scan users_environment_id_external_id   3 buffers, 0.009 ms
  channelsForUser       Nested Loop over members_user_channel        19 buffers, 0.035 ms

**The database is not what the connect path costs.** Three index scans totalling
0.053 ms of execution against 6.807 ms of request. The rest is token verification
and HTTP. That is the headroom the cap read in Phase 6 spends, and SC-012 compares
against these numbers rather than against an impression.

## T006 — the fence surface, generated rather than copied

21 files, 95 fences in the English chapters, 5 further entries in `post-series.md`.
The generated table matches R16 exactly. R16 also under-listed the post-series
side: `repository.ts` has one too, so the count there is 5, not 4.

## Phase 2 — what the foundational work turned up

Two tripwires chapter 3.10 left for this chapter, and only one of them was in
any plan.

**Scheduled (T048b, still ahead):** `session.test.ts`'s "STILL emits close code
4008 from nowhere".

**NOT scheduled, and found by a red test:** `config.test.ts` carried

    it("rejects a dimension nobody implemented rather than ignoring it", …)
      // `connection_minutes` is chapter 3.11. Until then …
      expect(capsFor({ connection_minutes: { hard: 10 } }, "messages").error)
        .not.toBeNull();

Seven analysis passes did not find it, because none of them opened
`config.test.ts` — the fence table lists `config.ts`, not its test. The assertion
inverts onto `media_bytes` (FR-MED-12, Part 4, still unimplemented), so the
tripwire survives and only the dimension standing on it changes.

**And one wrong expectation of my own**, the same class chapter 3.10 hit twice
with Mailpit: the month-boundary test asserted 3 + 2 buckets for 23:58:10 →
00:01:40. It is 2 + 2 — 23:58, 23:59, 00:00, 00:01. Counting boundaries by eye is
what `bucketsFor` exists to stop anyone doing, and it caught its author first.

**Counts after Phase 2:** api unit 158 → 181, gateway unit 64 → 75, protocol
37 → 45, and a new integration file at 9 (`config.itest.ts`). Lint, build and the
harness's `no-trigger-in-migrations` gate all green.

## Phase 3 — the credential

Green: lint, build, typecheck, unit 333, integration **271** (from 256; +9
`config.itest.ts`, +6 `credentials.itest.ts`), 190s.

**`PLATFORM_CREDENTIAL_ENV` had no consumer outside its own file** — M16's grep,
run and recorded rather than guessed. The export survives as the dispatcher's
variable name and gains a sibling, `GATEWAY_CREDENTIAL_ENV`.

**The env read stays at call time**, which M16 flagged as the refactor's quiet
risk. `credentials.itest.ts` SETS both variables during the test and says so in a
comment; a module-level map of resolved secrets would have gone green against a
credential nobody configured.

**`ApiClient` grew a method and seven stubs stopped compiling** — six inline
literals in `resume.itest.ts` and one factory in `session.test.ts`. The six became
one edit: `boot(api: Omit<ApiClient, "reportUsage">)`, with the method supplied
inside. The `Omit` says which half of the client those tests speak to, which is
more honest than six copies of a no-op.

**Compose default is 38 characters**, checked, because `resolvePlatformCredential`
refuses anything under 32 and a short default would have made the gateway
unauthenticated in every local run while looking configured.

**One lint failure**: `internalUsageReportRequestSchema` imported and unused —
the request is validated by the api, not by the caller that builds it. Removed.

## Phase 4 — US1, and a prediction that was wrong

Green: lint, build, unit 348, integration **301** (from 271), 191s, coverage
exit 0 with three new ratchet entries.

**R23 predicted `repository.ts` would go red. It did not.**

  before  97.48 / 90.17 / 100 / 99.38
  after   97.60 / 90.57 / 100 / 99.41    ratchet 97 / 90 / 100 / 99

Branch coverage went UP by 0.40 points, against a prediction built on chapters
3.5 and 3.6 — where the same file lost 7.69 points and 1.20 points respectively
by gaining operations without tests. The difference is not luck and is worth
naming: 3.5's `deliveryMaterial` and `pendingDeliveryDepth` were called only from
a suite that spawns the api as a child, whose coverage is not attributable, so
the code arrived measured at zero. `creditConnectionMinutes` is called from
nineteen in-process tests in the same lane, which is what T033b scheduled and
T033d protected. The instrument was right about the risk and the mitigation was
the thing that changed the outcome.

**Two coverage gaps the plan did not anticipate**, both found by measuring:

  usage.controller.ts   88.88 / 50 / 100 / 88.88  →  100 / 100 / 100 / 100
  meter.ts              96.42 / 87.5 / 86.66 / 96.07  →  100 / 93.75 / 100 / 100

The controller's 50% was the RETHROW beside the 409 — the branch separating "this
connection moved tenants" from "something else broke". The 409 was tested and its
neighbour was not, which is how a broken caller becomes a conflict nobody
investigates. Closed with a report for an environment that does not exist.

`meter.ts` was missing its default clock and its timer callback, because every
other test drives `reportOnce` directly. Closed with the one test that proves the
meter is a background thing at all.

`meter.ts` is pinned at 93 branches and not 100. The remaining arm is the
retention cap's duplicate-key guard, which needs four thousand closed connections
with a repeat among them — a fixture longer to read than the branch is worth.

**Totals** 90.22 / 83.80 / 89.42 / 91.40, from 89.45 / 82.73 / 88.94 / 90.86.

## Phase 5 — the signals

Green: lint, build, integration **304** (from 301), 192s.

**C2 was real, and the fix is bigger than a handler.** `serve()` returned a bare
`node:http` Server, nothing called `server.close()`, and no signal handler
existed — so `server.on("close")` ran on no path. Fixing it meant three changes,
not one: `createServer` now hands back an awaitable `shutdown`, `sessions.close()`
became async, and the signal handler awaits it before `process.exit(0)`. A flush
that is fired rather than awaited is the same non-guarantee one line lower down.

**`meter.itest.ts` runs two children and spawns a third per test.** The api child
is shared; each test starts its own gateway, because each ends by killing one and
two signals must not share a victim. `RELAY_METER_INTERVAL_MS` exists so those
tests drive a 300ms interval rather than waiting a real minute per assertion —
registered in `turbo.json` too, because strict env mode drops what it is not told
about.

**And one assertion of mine broke the rule I wrote.** T042 asserted
`expect(await minutes()).toBe(0)` on an environment the two tests above it had
already credited: `expected 2 to be +0`. That is FR-032 — "any assertion about a
shared resource MUST be scoped to rows this test created" — failing against its
own author, twenty minutes after being written into the spec. Fixed by capturing
the figure before the test and comparing against it.

**4009 stays unemitted.** `CLOSE_CODES[4009]` is "server shutdown (drain)" and
this chapter builds the first shutdown path the gateway has ever had, so the code
is sitting right there. Draining is telling clients to reconnect elsewhere, which
is a feature with its own semantics; reaching for the code because a handler
arrived is the "declared, so use it" chapter 3.8 refused by name.

## Phase 6 — the cap at the door, and 4008 after nineteen chapters

Green: lint, build, integration **324** (from 304), 192s.

**The tripwire fired on schedule.** `session.test.ts`'s "STILL emits close code
4008 from nowhere (quickstart V7)" went red the moment `session.ts` emitted it —
the one thing in this chapter that seven analysis passes DID predict, scheduled as
T048b since the second. Inverted: 4008 is asserted present, 4009 asserted absent,
and the self-check that makes it honest survives on 4009. `bait-size.test.ts`'s
cross-reference to it was updated too.

**Three tests failed on my own mistakes before they passed**, all in the new
socket-door block and none of them about the code under test:

1. `startApi()` binds a fixed port (`RELAY_SESSION_ITEST_API_PORT ?? 4123`), so a
   second `describe` calling it silently failed to bind and every test after died
   on `fetch failed`. It now takes a port.
2. I set `url` to `ws://…/v1/ws` and then used a `connect` helper copied from the
   block above, which appends `/v1/ws` itself. `/v1/ws/v1/ws` hits
   `socket.destroy()` and the client sees `ECONNRESET` with no log line anywhere —
   the upgrade handler never runs, so nothing reports the path it refused.
3. `listMessages` and `sendMessage` have different signatures than I assumed.

The second cost the most time and is worth the sentence: a path the gateway
refuses at `server.on("upgrade")` produces **no log at all**, which makes a typo
in a test URL indistinguishable from a broken feature.

**T055 is committed rather than ad-hoc**, which is the whole point. Chapter
3.10's T033 measured with a script that lived nowhere, reported 273% to 411%
regressions, and sent two code changes chasing a warm-up artefact before the real
figure turned out to be 0.56 ms. This one asserts on the PLAN — index scans, no
`Seq Scan` — because a clock cannot show a scan, which is the correction 3.10's
first analysis pass had to make to its own criterion.

## Phase 7 — the seam, and FR-RTL-05 closed

Green: lint, build, integration **330** (from 324), 192s.

**T056a earned its place.** The extraction of `recordCrossings` and
`organisationOf` out of `Repository` is on `sendMessage`'s critical path, and the
lane after it read **324, identical, zero failures** — the same number as before,
which is what a pure refactor is supposed to look like and the only way to know
it was one before four more tasks landed on top.

**R22's lint entry is added and its limits are written into the rule itself.**
`drainQuotaNotifications` now sits beside its three siblings in
`eslint.config.mjs`, where chapter 3.10 should have put it. The comment says what
it does not buy: this chapter's own email tests reach the drain through
`createQuotaRelay`, and the rule's existing note is explicit that an indirect call
is what it cannot see. The half that protects the chapter is the scoping.

**So every assertion in the new email tests is by recipient address**, never on
what `drainOnce()` returned. Chapter 3.10's suite establishes the address-scoping
shape in a comment and then undercuts it one line later with
`expect(await relay().drainOnce()).toBeGreaterThan(0)` — true whether it drained
this test's row or a neighbour's.

**The 80%-of-4 arithmetic held this time.** A cap of 4 and four minutes in one
step crosses 50, 80 AND 100, because 80% of 4 is 3.2. Chapter 3.10 got that wrong
twice; the note it left is why this test was written expecting three.

**One lint failure:** `no-useless-escape` on backticks inside a template literal
in `quota-email.ts`. Extracted to a `DEFAULT_STOPPAGE` constant, which reads
better anyway.

FR-RTL-05 is now closed on all three dimensions.

## T064 — the guard's prediction, verified rather than assumed

Zero refusals across the full lane. And a zero proves nothing on its own, so:

  triggers installed          5  (webhook_endpoints, webhook_deliveries,
                                  webhook_disable_notifications, channels, users)
  sentinel environments      27
  baited rows reachable     251  (all in webhook_endpoints)
  self-check                 an UPDATE across every sentinel environment was
                             REFUSED — "global-operation guard: this statement
                             modified sentinel row public.webhook_endpoints"

  exempt.ts vs part3-ch10    no diff
  lint ignores list          no new file
  eslint.config.mjs          +13 lines, all of them a RESTRICTION being added
                             (drainQuotaNotifications), not an exemption

R5's no-sweep prediction holds for the second chapter running.

**What it does not prove**, and SC-014 now says so: the guard watches five tables
and none of them is a usage or notification table. Silence about
`usage_connections`, `usage_periods` or `quota_notifications` means nobody is
looking, not that nothing happened.

The first self-check attempt reported "GUARD DID NOT FIRE" and was wrong — it
swept `users`, and the sentinel environments hold no user rows, so the trigger
never fired because no row matched. A guard check that sweeps an empty table is
the same vacuous pass `no-trigger-in-migrations.test.ts` guards against by
asserting its own patterns can match.

## T065 — the connect path, measured, and the plan was wrong

Four runs of the shipped build against T004a's single baseline:

  concurrency    before (T004a)   after, two reads      after, folded to one
  1              6.807            6.4–7.2               4.9–7.2
  8              5.490            5.7–6.7               5.8–6.2
  32             15.001           17.4 / 17.4 / 17.5 / 18.1   16.6 / 16.8 / 17.0

**Research R7 chose the wrong shape and the measurement says so.** It reasoned
that chapter 3.10's H2 forbade extending `environmentLimits`, so the cap should be
"a second call on the same request rather than a heavier version of the first".
H2 is still right — `environmentLimits` has a SECOND caller, the rate-limit
middleware, on every `/v1` request. But a second call costs what a join would
have: 15.0ms to 17.6ms at 32-way, four runs clustered inside 0.7ms, so a real
regression rather than noise. Folding the connect path's two reads into one
`connectPolicy` recovered 0.8ms of it. `environmentLimits` is untouched.

**And T004a's method was wrong**: one measurement is not a baseline. Three runs of
the shipped build sit inside 0.4ms, so the AFTER is tight — but the 15.001 is a
single sample and the residual 1.8ms cannot be cleanly attributed. The instrument
was built to avoid chapter 3.10's mistake and then used once.

SC-012's actual claim — no scan proportional to traffic — is satisfied by the
`EXPLAIN` in `session.perf.itest.ts`, which is committed rather than ad-hoc.

## T066 — what a third dimension actually cost

Seven places, against chapter 3.10's written prediction of "a new dimension is a
new key … a one-line constraint change":

  1  quotaConfigSchema                    one key
  2  environments_quota_config_shape      1 typeof + 2 regex clauses, constraint
                                          dropped and rebuilt (no ALTER CONSTRAINT)
  3  quota_notifications_dimension_check  one IN value, dropped and rebuilt
  4  usage_periods                        one column + one CHECK, in two files
  5  quota.error.ts                       a two-way ternary became two Record tables
  6  quota-email.ts                       a key in NOUN, and a new STOPPAGE table
  7  usageFor's return shape              two fields

The prediction was right about the shape and light about the size. Two of the
seven are places it did not anticipate at all — and place 5 is the one that
mattered, because `Dimension` is `keyof QuotaConfig` and widened itself, so the
ternary would have rendered "monthly ACTIVE USER quota exhausted" with the
compiler silent.

## T067 — coverage

  TOTAL   90.32 / 83.98 / 89.51 / 91.53   (from 89.45 / 82.73 / 88.94 / 90.86)
  repository.ts        97.5 / 90.6 / 100 / 99.45   ratchet 97 / 90 / 100 / 99
  credit.ts            100 / 100 / 100 / 100
  usage.controller.ts  100 / 100 / 100 / 100
  meter.ts             100 / 93.75 / 100 / 100

All ratchets hold, including the three this chapter added.

## T069 — the traceability pass

**Chapter 3.10 ships ZERO bare feature-local ids.** T047's fix removed them
rather than annotating them, and checking that convention first is what stopped
this chapter shipping ~60 of its own: an `FR-014` in a test name resolves only
inside `specs/032-chapter-3-11/` and means something else in every other feature.

FR ids mapped to the SRS id a reader can actually resolve (FR-RTL-05/06/07/08,
NFR-SEC-06, EIR-WS-06, ADR-05, constitution III and VI); SC ids dropped, because
they identify an acceptance criterion and the test's own name already says what
it checks.

Done line-by-line against `part3-ch10` so no pre-existing line was rewritten —
T047's own fix made that mistake, altering six comments belonging to chapters
3.5, 3.6 and 3.9. Four removed lines DO cite earlier chapters and all four are
deliberate: the 4008 tripwire in `session.test.ts`, the platform-credential
comment in `authenticate.middleware.ts`, and `bait-size.test.ts`'s stale
cross-reference.

**The first attempt at this pass broke the build.** A regex tidying trailing
`()` after id removal deleted legitimate empty parameter lists — `next: () =>
void` became `next:  => void` — across 36 files. Everything through Phase 7 was
committed, so the repair was `git checkout f605840 -- .` and a rewrite of the two
uncommitted pieces. Committing each phase is what made a bad regex a five-minute
loss instead of an afternoon.

## T070 — credential scan

Patterns searched (10), recorded because the conclusion is worthless without them:

  rk_svc_                                    rk_live_
  rk_dev_                                    rk_svc_local_development_credential_0000
  rk_svc_local_development_gateway_00000     RELAY_INTERNAL_CREDENTIAL
  RELAY_WEBHOOK_SECRET_KEY                   Bearer ey
  [0-9a-f]{32,}                              [A-Za-z0-9+/]{40,}={0,2}

Searched: all seven spec artifacts, plus every line this chapter added to source.

Findings, each classified rather than counted:

  research.md:29         "the `rk_svc_` prefix"      — a prefix, not a secret
  contracts §1           `Bearer rk_svc_…`           — elided
  tasks.md T070          the pattern list itself
  baseline.txt           4 × 40-hex                  — git SHAs, T001 provenance
  source, 9 fixtures     rk_svc_..._0123456789abcdef, ..._nobody_knows_this_one,
                         ..._tooshort                 — obviously-fake test bodies
  `Bearer ey`            one occurrence, in an assertion that the report's
                         credential is NOT a JWT

**One real decision.** `rk_svc_local_development_gateway_00000` is the compose
default and `compose.yaml` carries three fences, so it will publish. Its sibling
`rk_svc_local_development_credential_0000` is already published in chapter 3.5,
the file says "Dev-only credentials — this file never ships anywhere" three lines
above it, and both are placeholders a deployment overrides. Published on that
precedent, deliberately, and recorded here so it is a decision rather than an
oversight.

No signing secret, encryption key or real internal credential appears in any spec
artifact or any added line.
