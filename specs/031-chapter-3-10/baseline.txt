Chapter 3.10 — baseline
================================================================================

T001 — PROVENANCE
--------------------------------------------------------------------------------

  parent repo      6344b1c  docs: fence titles carry no relay-platform prefix (Q1)
  relay-platform   d3002fa  fix: notifications.itest.ts declares the budget its 
  relay-tutorial   24a72f4  docs: chapter 3.11 is connection-minutes, gauntlet m

  platform tags at HEAD: feature-030-global-operation-guard 
  This chapter starts from feature-030-global-operation-guard plus its
  documentation commits. Chapter 3.9 shipped at part3-ch9.

  submodule pins match HEADs:
     d3002fa16dd041b06a2de7d1a5517c3570c66113 relay-platform (feature-030-global-operation-guard)
     24a72f49d3bbccac5f37ec8792a877bdd5793198 relay-tutorial (heads/main)

T003 — SITE BASELINE
--------------------------------------------------------------------------------

  check:fences   exit=0  check-fence-chain: 165 fenced files replay onto relay-platform across 26 chapters (26 translated, fe
  check:docs     exit=0  check-docs-drift: all mirrored docs match their sources
  lint           exit=0  
  build          exit=0  ƒ  (Dynamic)  server-rendered on demand

T004a — THE QUERY THE CHAPTER ARGUES AGAINST, RE-MEASURED
--------------------------------------------------------------------------------

  table sizes: messages 198690 | channels 10217 | environments 26331
  busiest environment: 36c3dc58-c9d8-4600-854b-9c996cc87e71 (507 messages)

  ONE ENVIRONMENT — the shape a send-path check would take:

                                                                                   QUERY PLAN                                                                               
    ------------------------------------------------------------------------------------------------------------------------------------------------------------------------
     Aggregate  (cost=433.88..433.89 rows=1 width=16) (actual time=0.211..0.211 rows=1.00 loops=1)
       Buffers: shared hit=38
       ->  Sort  (cost=433.58..433.68 rows=40 width=16) (actual time=0.188..0.197 rows=507.00 loops=1)
             Sort Key: m.user_id
             Sort Method: quicksort  Memory: 25kB
             Buffers: shared hit=38
             ->  Nested Loop  (cost=9.17..432.51 rows=40 width=16) (actual time=0.036..0.150 rows=507.00 loops=1)
                   Buffers: shared hit=35
                   ->  Bitmap Heap Scan on channels c  (cost=4.30..11.70 rows=2 width=16) (actual time=0.022..0.023 rows=5.00 loops=1)
                         Recheck Cond: (environment_id = '36c3dc58-c9d8-4600-854b-9c996cc87e71'::uuid)
                         Heap Blocks: exact=2
                         Buffers: shared hit=4
                         ->  Bitmap Index Scan on channels_environment_id_external_id_unique  (cost=0.00..4.30 rows=2 width=0) (actual time=0.012..0.013 rows=5.00 loops=1)
                               Index Cond: (environment_id = '36c3dc58-c9d8-4600-854b-9c996cc87e71'::uuid)
                               Index Searches: 1
                               Buffers: shared hit=2
                   ->  Bitmap Heap Scan on messages m  (cost=4.87..209.83 rows=58 width=32) (actual time=0.008..0.022 rows=101.40 loops=5)
                         Recheck Cond: (c.id = channel_id)
                         Filter: (created_at >= date_trunc('month'::text, (now() AT TIME ZONE 'utc'::text)))
                         Heap Blocks: exact=11
                         Buffers: shared hit=31
                         ->  Bitmap Index Scan on messages_channel_id_sequence_unique  (cost=0.00..4.85 rows=58 width=0) (actual time=0.006..0.006 rows=101.40 loops=5)
                               Index Cond: (channel_id = c.id)
                               Index Searches: 5
                               Buffers: shared hit=20
     Planning:
       Buffers: shared hit=255
     Planning Time: 0.743 ms
     Execution Time: 0.266 ms
    (29 rows)
    

  EVERY ENVIRONMENT — the shape a monthly report would take:

                   ->  Seq Scan on messages m  (cost=0.00..6837.46 rows=201253 width=32) (actual time=0.010..29.124 rows=198690.00 loops=1)
     Planning Time: 0.761 ms
     Execution Time: 92.517 ms

  READ THE PLAN, NOT THE CLOCK.

    ->  Bitmap Heap Scan on messages m
          Recheck Cond: (c.id = channel_id)
          Filter: (created_at >= date_trunc('month'::text, ...))

  The month predicate is a **Filter**, not an Index Cond. Every message the
  environment has ever sent is read off the heap and then discarded if it is not
  this month's. 507 rows today, and the number only goes up — there is no index on
  `messages.created_at`, and `messages` carries no `environment_id` at all.

  THE TIMING MOVED AND THE ARGUMENT DID NOT. Research R1 recorded 1.189ms with
  3.035ms of planning; this run measures **0.266ms with 0.743ms of planning**. The
  difference is a cold cache versus a warm one — R1's was the first execution of a
  query the planner had never seen. Neither number is wrong and neither is the
  point: at 507 messages both are fast, and the chapter's case is the `Filter`
  line, which does not depend on a clock.

  The chapter quotes the plan. If it quotes a duration it says which cache state
  produced it, because an unqualified 1.189ms invites a reader to check it, get
  0.266ms, and conclude the argument was inflated.

T004 / T005 — THREE LANE RUNS, AND WHAT THE THIRD FOUND
--------------------------------------------------------------------------------

  run 1  exit=0  187s  failing-files=0
  run 2  exit=0  183s  failing-files=0
  run 3  exit=1  115s  failing-files=1

  FAIL src/webhooks/deliveries.itest.ts >
       counts what is pending and stops counting it once it is delivered
  AssertionError: expected 22741 to be 22742

  **INSTANCE 12, AND IT IS THE BASELINE'S, NOT THIS CHAPTER'S.** The test read the
  global `pendingDeliveryDepth`, delivered one row, and asserted the count fell by
  exactly one. Its own comment defended the shape:

      // GLOBAL, and asserted as a delta for that reason — this is the number an
      // operator watches, so it counts every tenant's backlog, and another suite
      // seeding rows beside this one must not be able to break it.

  A delta is two reads with a gap. Another suite delivering one of its own rows in
  that gap moves the second read by one more, which is what 22741-against-22742
  is. Seeding was defended against; delivering was not.

  That makes three of the twelve occurrences whose defence was a comment saying
  why this one was fine. Feature 030's spec opens on the same observation — the
  sixth instance was written by someone who had recorded the other five.

  Fixed forward in its own commit, ahead of any chapter work: the global function
  keeps a caller and is asked only what cannot race (there is a backlog); the
  delta is asserted against this environment's own rows.

  Twenty runs of feature 030's battery passed with this in the tree, and three
  runs here found it. Nothing about the fault is rare — the lane had simply not
  been run with two suites landing in the same gap.

T002 — THE PRE-CHANGE PLATFORM BASELINE
--------------------------------------------------------------------------------


  unit lane        exit=0    5.932s
      @relay/api               123
      @relay/gateway            64
      @relay/protocol           37
      @relay/test-harness        9
      @relay/dispatcher          8
      @relay/config              7
      @relay/service-kit         3
      TOTAL                    251

  integration lane exit=0  189s
      @relay/api               177
      @relay/gateway            21
      @relay/dispatcher         16
      @relay/e2e                 9
      @relay/test-harness        8
      TOTAL                    231

  coverage lane    exit=0  247s
      Test Files  54 passed (54)
      Tests      473 passed (473)
      All files          89.08 | 82.35 | 89.25 | 90.58
      repository.ts      97.27 | 90.90 |  100  | 99.24   (ratchet 97 | 90 | 100 | 99)

  Identical to feature 030's close-out on every figure. SC-006 compares against
  these, and T035's floor is 89.08 statements and 82.35 branches.

  THE GUARD FIRED THREE TIMES DURING THE COVERAGE RUN, AND ALL THREE ARE CORRECT.
  Counted by diffing the Postgres log across the run:

      the bait planted by packages/test-harness/src/guard.itest.ts
      the bait planted by packages/test-harness/src/guard.itest.ts
      the bait planted by packages/test-harness/src/guard.itest.ts#narrow

  Those are the guard's own refusal tests, which exist to produce exactly this.

  The log held **276 refusals in total**, which looked alarming until they were
  attributed: 136 from `guard.itest.ts` (deliberate), 123 naming
  `dispatcher.itest.ts`'s bait, 17 with no owner. The 123 and the 17 are residue
  from feature 030's own development, when the bait design changed three times —
  stale sentinel endpoints left enabled by a superseded shape, swept once, refused,
  and then disabled for good. Re-running the dispatcher lane produces **zero** new
  refusals, and the coverage lane produces only the three above.

  Recorded because the count is the interesting part: **a database log is the only
  place a swallowed refusal shows up**, which is the hazard research R13 named and
  chapter 3.10's design avoids by performing no global operation at all. Anyone
  auditing that claim should diff this count, not read the lane's exit code.

T016b — THE FLUSH TEST CAN FAIL
--------------------------------------------------------------------------------

  Built chapter 3.8's way for one run — the count's home moved to Redis, written
  by `sendMessage` and read by `usageFor` — and the lane said:

      × reports identical figures across a FLUSHALL      102ms
      Tests  1 failed | 6 passed (7)

  ONE test failed and the other six did not, which is the signature worth having:
  the flush test is the only one that distinguishes the two designs. Every other
  assertion in the file passes under both, because both count correctly until
  something clears the store.

  Reverted with `git checkout --`; `md5sum` matches the committed file
  (9430c7389d86f56384d6577bc346b5bb). Committed before the mutation, per T017.

T018 — WHAT THE SEND PATH GAINED
--------------------------------------------------------------------------------

  FR-020 forbids a query that SCANS the message table. The transaction does gain
  two statements, and neither scans anything:

    1. the roll-up increment
       Insert on usage_periods
         Conflict Resolution: UPDATE
         Conflict Arbiter Indexes: usage_periods_pkey
         Buffers: shared hit=25
       Execution Time: 0.357 ms

    2. the distinct-user membership
       Insert on usage_active_users
         Conflict Resolution: NOTHING
         Buffers: shared hit=60
       Trigger for constraint usage_active_users_environment_id_fkey: time=0.687
       Trigger for constraint usage_active_users_user_id_fkey:        time=0.311
       Execution Time: 1.304 ms

  No `Seq Scan` in either plan. The primary key is the arbiter index, which is
  what storing the period rather than computing it buys.

  THE HONEST LINE IS THE SECOND STATEMENT'S. Two foreign-key constraint triggers
  cost 0.998ms of its 1.304ms — more than the insert itself. That is the price of
  the membership row being a real row with real references, and it is paid on
  every ATTRIBUTED send. An unattributed send skips it entirely.

  Compare against the query this design refused: 0.266ms warm for one environment
  of 507 messages, growing with the tenant's lifetime traffic. The two new
  statements are flat.

PHASE 4 — US2, AND WHAT THE GUARD SAID
--------------------------------------------------------------------------------

  integration lane  exit=0  191s
      @relay/api             194      (+17)
      @relay/e2e              10      (+1)
      @relay/dispatcher       16
      @relay/gateway          21
      @relay/test-harness      8
      TOTAL                  249      (from 231)

  GUARD REFUSALS DURING THE RUN: 9, and every one of them is on
  `webhook_endpoints`:

      3  packages/test-harness/src/guard.itest.ts   (its own refusal tests)
      5  services/dispatcher/src/dispatcher.itest.ts (bait)
      1  __shared__/sweep-bait

  **NOT ONE NAMES A QUOTA TABLE**, which is what research R5 predicted and T034
  will confirm formally: `usage_periods`, `usage_active_users` and
  `quota_notifications` are written only for the environment doing the sending, so
  nothing in this design performs a cross-environment mutation.

  The other six are feature 030's residue and are NOT this chapter's: a suite
  exempt for `webhook_deliveries` but not for `webhook_endpoints` drives a
  delivery relay that disables a sentinel endpoint, the guard refuses, and the
  relay logs and continues — research R13's swallowed refusal, live. Chapter 3.10
  touches no webhook table, so it neither caused this nor can fix it here.

  Worth writing down for the shape rather than the count: **the coverage lane
  produces three refusals and the integration lane produces nine**, because they
  run different suites against the same database. A lane's exit code says nothing
  about either number.

PHASE 5 — US3, THE SEAM
--------------------------------------------------------------------------------

  integration lane  exit=0  191s
      @relay/api             200      (+6 from Phase 4, +23 from the baseline)
      @relay/gateway          21
      @relay/dispatcher       16
      @relay/e2e              10
      @relay/test-harness      8
      TOTAL                  255      (from 231 at T002)

  GUARD REFUSALS DURING THE RUN: 3, all `webhook_endpoints`, all
  `guard.itest.ts`'s own. **NONE NAMES A QUOTA TABLE.** Research R5 predicted this
  design engages the guard nowhere, and after three phases of writing to
  `usage_periods`, `usage_active_users` and `quota_notifications` it still does.

  Down from 9 in Phase 4, because the six residual `dispatcher.itest.ts` refusals
  need a sentinel endpoint that is still enabled, and by now the sweeps have
  disabled them for good. The number moves with the database's history, not with
  the tree — which is the reason T034 runs against a freshly migrated one.

T028a — THE CONSTRAINT IS WHAT MAKES IT AT-MOST-ONCE
--------------------------------------------------------------------------------

  Two identical crossings inserted directly, bypassing the code path entirely:

      NOTICE:  refused by quota_notifications_once_per_threshold
      NOTICE:  rows for that threshold: 1

  FR-015 is a schema property, not a promise the writing code makes. A concurrent
  double-crossing resolves to one row rather than two emails, and it would do so
  even if `recordCrossings` were wrong.

TWO TEST EXPECTATIONS I GOT WRONG, AND WHAT THEY WERE
--------------------------------------------------------------------------------

  Both in the Mailpit tests, both mine rather than the code's:

  1. **"Two crossings on the way"** for a cap of 4 over four sends. It is three:
     80% of 4 is 3.2, so no message lands on 80 and the fourth clears 80 and 100
     in one step. The test's own NAME said three emails while its assertion said
     two.
  2. **Sorting subjects as strings** puts "100%" before "50%". The assertion now
     extracts the percentages and compares them as numbers, which is what it
     meant all along.

  Neither was a defect in the implementation, and both are the kind of thing that
  would have passed unnoticed had the numbers been rounder.

T033 — WHAT THE QUOTA PATH COSTS, AND THREE WRONG ANSWERS FIRST
--------------------------------------------------------------------------------

  THE RESULT. Same environment, same channels, same user, quota toggled between
  two identical loops, with the send path instrumented:

    32-way concurrent, one channel per sender
      unconfigured           1.77ms per send    assertWithinQuota = 1.246ms
      configured             2.33ms per send    assertWithinQuota = 1.586ms
      back to unconfigured   1.77ms per send    assertWithinQuota = 1.121ms

      configured vs unconfigured: +0.56ms per send
      after reverting the config: -0.00ms

  Configuring a quota costs about half a millisecond per send. The ~1.2ms
  `assertWithinQuota` spends is the joined read, and US1 needs it whether or not
  a cap exists.

  THE CROSSING BLOCK COSTS 0.000ms, because `thresholdsCrossed` is pure
  arithmetic on two numbers the transaction already holds and answers "nothing"
  for almost every send. It was not always: the first version looked the
  organisation up and counted the period's users BEFORE consulting the
  arithmetic, which put two queries on every send by an environment that merely
  HAS a quota. Reordered so the free question is asked first.

  THREE WRONG ANSWERS BEFORE THAT ONE, and they are the point of writing this up.

  An uncontrolled benchmark — one environment per case, run in sequence —
  reported 273%, then 341%, then 303%, then 411%. Each time the number stayed
  large, and each time a different cause was inferred and acted on:

    1. "The `FOR UPDATE` is serialising the environment."  Removed it. No change.
    2. "The organisation lookup and the user count are on every send."  Deferred
       them behind the arithmetic. No change. (A real improvement, kept.)
    3. "Two round-trips hold a pooled connection above the pool size."  Folded
       them into one joined read. No change. (Also a real improvement, kept.)

  The cause was the benchmark. Each case built its own environment and ran in a
  fixed slot, so the measurement carried warm-up and table growth. Toggling the
  quota on ONE environment and instrumenting the phases produced 0.56ms, and the
  three "fixes" had been changes made to a number that was never real.

  Two of the three are better code and stay. The lesson is the ordering: this
  project's whole subject is not reasoning where a measurement is cheap, and the
  measurement here was cheap the entire time.

  AND THE LOCK CANNOT COME BACK, for a reason that has nothing to do with speed.
  Once the caps and the usage are one joined read, Postgres refuses to lock it:

      ERROR:  FOR UPDATE cannot be applied to the nullable side of an outer join

  So the overshoot is bounded by the number of sends in flight rather than by
  one, which is what `spec.md`'s edge case asked for in the first place — "the
  overshoot is bounded by concurrency, not unbounded, and this is stated rather
  than defended against". Research R8 defended against it; T033 was scheduled to
  find out whether that was worth it, and the honest answer is that the question
  was moot by the time it could be asked cleanly.

T034 / T034a — THE GUARD'S PREDICTION, ON A FRESHLY BAITED DATABASE
--------------------------------------------------------------------------------

  pnpm test:integration against a database created and migrated for the run:

      exit=0        255 tests
      guard refusals during the run: 15

      15  sentinel row public.webhook_endpoints
      ---
      10  planted by services/dispatcher/src/dispatcher.itest.ts
       3  planted by packages/test-harness/src/guard.itest.ts
       2  planted by __shared__/sweep-bait

  **NOT ONE NAMES `usage_periods`, `usage_active_users` OR `quota_notifications`.**
  Research R5 predicted that a design whose usage only rises on a send needs no
  global operation, and after three phases of writing to three new tables it still
  needs none. T034 existed to find out the prediction was wrong; it is not.

  T034a: `packages/test-harness/src/exempt.ts` and `eslint.config.mjs` are
  byte-identical to `feature-030-global-operation-guard`. No file joined either
  list.

  Higher than the 3 a used database gives, and the reason is worth keeping: a
  fresh database has every sentinel endpoint still ENABLED, so the first sweep of
  the run meets them. On an aged one they were disabled long ago and the sweep
  finds nothing. The count moves with the database's history, which is why this
  task specifies a fresh one.

T035 / T035a — COVERAGE, AND TWO GAPS THE FIRST RUN FOUND
--------------------------------------------------------------------------------

  First run:   88.81 | 82.32 | 88.02 | 90.27      BELOW T035's floor
  After:       89.51 | 82.73 | 88.94 | 90.92      exit=0, every ratchet green
  Baseline:    89.08 | 82.35 | 89.25 | 90.58

  The lane's own global threshold is 70%, so the first run exited 0 while sitting
  under the floor this task set. A gate that passes is not the same as a number
  that holds, which is the whole reason T035 names figures rather than an exit
  code.

  Two gaps, both mine:

  1. **`period.ts` at 50% functions.** `currentPeriod()` was written "so callers
     that already hold an instant do not have to invent one" and nothing ever
     called it. Deleted. An unused export is precisely what feature 030's R48
     found as `repository.ts`'s last uncovered function.
  2. **`quota-relay.ts` at 56.66% statements, 62.5% functions.** `start`, `stop`
     and the `run` loop — the part of every relay in this codebase that no test
     enters. Chapters 3.3, 3.5 and 3.9 all have the hole, and
     `notification-relay.ts` sits at 58.06% because of it. Four lines of test
     closed it here: **96.66 statements, 100 functions.** The fourth relay does
     not repeat the gap the first three have.

  T035a's mechanism, checked before the coverage run rather than after:
  `repository.ts` gained two exported functions, `usageFor` and
  `drainQuotaNotifications`. Both are exercised, and the file's **100% functions**
  ratchet holds.

  The overall statements and branches now read 89.51 and 82.73 — the figures
  chapter 3.8 recorded and feature 030's baseline corrected downward as
  unreachable. They are reachable; two test gaps were holding them down.

T040 / T041 / T041b / T041c — THE CHAPTER, AND THE SIZE GATE
--------------------------------------------------------------------------------

  PROSE WORDS: 2,548        gate: 2,000 to 4,000 (SC-009)     PASS
  FENCES:      31           counted by reading the page, not estimated

  Comparable: chapter 3.9 at 2,472 words. The plan estimated 3,000 to 3,600 for
  this one, so the estimate was 18% high — recorded because a written estimate
  that goes unchecked is the thing SC-009 exists to replace, and this is the
  fourth Part 3 chapter where the number on the page differed from the number in
  the plan.

  THE FIRST DRAFT CAME IN AT 2,053 AND WAS TOO THIN, in a way the count could not
  see. Two things the chapter is actually about were missing: the FLUSH TEST,
  which is its entire spine — a quota that a cache flush can erase is not a quota
  — and the reason a DISTINCT-USER COUNT CANNOT BE AN INCREMENT, which is the one
  genuinely interesting piece of arithmetic in the feature. Both are now sections.
  The gate measures length; it does not measure whether the length is spent on
  the right thing.

  FENCE ROUTING (T041c). Ten fenced files changed, and they did not all go to the
  same place:

    IN THE CHAPTER — five diffs, because the chapter teaches them:
      services/api/src/db/schema.ts
      services/api/src/db/repository.ts
      services/api/src/messages/messages.service.ts
      services/api/src/app.module.ts
      services/api/src/main.ts

    IN THE CHAPTER — eight whole files, all new:
      services/api/migrations/0009_quotas.sql
      services/api/src/quotas/{period,policy,config,quota.error,quota-email,
                               quota-relay,quotas.module}.ts

    POST-SERIES — five, because the chapter explains none of them:
      turbo.json                                   RELAY_QUOTA_RELAY declared
      services/api/vitest.integration.config.mts   the flag
      relay-platform/vitest.coverage.config.mts    the flag
      packages/e2e/src/harness.ts                  setQuota, for a lane that may not import pg
      services/api/src/webhooks/deliveries.itest.ts instance 12, found at this chapter's baseline

  T041b's task text said seven files and 53 fences. It is ten files: `turbo.json`,
  `main.ts` and `app.module.ts` were not on the list, and `harness.ts` joined
  during Phase 4. The list was written from the plan rather than from
  `check:fences`, which is the only thing that knows.

  THE CHAIN AFTER: 173 fenced files replay onto relay-platform across 27 chapters,
  from 165 across 26.

  THREE FENCE TITLES WERE WRONG AT FIRST. An illustrative fence — a query, a JSON
  shape, terminal output — is not a file, and the checker treats every `title=` as
  one unless it contains `(excerpt)`:

      the query this chapter argues against does not exist in relay-platform
      EXPLAIN (ANALYZE, BUFFERS), 198,690 messages does not exist in relay-platform
      environments.quota_config does not exist in relay-platform

  Fixed by following the convention the series already had: `(excerpt)` in the
  title, or no title at all for command output.

  GATES: check:fences, check:docs, lint, build — all exit 0.
