# Chapter 3.3 — pre-change baseline (T001)
# Measured 2026-08-08 at the part3-ch2 state, before any 3.3 edit.
# Stores: RELAY_POSTGRES_PORT=15432 RELAY_REDIS_PORT=16379 RELAY_NATS_PORT=14222
#
# EXIT CODES, not just counts. Chapter 3.2 shipped two failures past a grep over
# a build log; both were caught only when $? was read.

## Docker-free gate
pnpm lint       exit 0
pnpm typecheck  exit 0
pnpm build      exit 0

## Unit lane — `pnpm test`   TOTAL 109   exit 0
@relay/service-kit     3
@relay/config          6
@relay/protocol       26
@relay/api            40
@relay/gateway        34

## Integration lane — `pnpm test:integration`   TOTAL 76   exit 0
@relay/api            55
@relay/gateway        13
@relay/e2e             8

## Broker
nats:2.12-alpine with JetStream (-js -sd /data), healthy on 14222.
No stream exists yet; no service connects to it. This chapter is the first.

## What T008 and T021 compare against
Both lanes must still total at least these counts with exit code 0, every
listed suite present, and assertions unchanged in substance (spec FR-016,
SC-008). This chapter adds files; it must not shrink or weaken any row above.
