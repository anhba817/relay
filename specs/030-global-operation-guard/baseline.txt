Feature 030 — baseline
======================

Measured before anything changed. Exit codes and numbers, not greps over output.

--------------------------------------------------------------------------------
T001 — PROVENANCE
--------------------------------------------------------------------------------

  parent repo HEAD          4ae8c80
  relay-platform HEAD       b060056   tags: part3-ch8 AND part3-ch9
  relay-tutorial HEAD       684db42
  submodule pins            both match their submodule HEADs
  platform working tree     clean, 0 modified files
  commits after part3-ch9   none — HEAD is the tag

BOTH TAGS POINT AT ONE COMMIT, which is not a mistake. Chapter 3.9's split moved
prose and no code, so the limiter chapter and the transport chapter share a tree;
`specs/029-chapter-3-8/battery.txt` records the decision that made that true.

Stores up on the off-default ports before measuring: postgres, redis, nats,
clickhouse, mailpit — all running.

--------------------------------------------------------------------------------
T002 — THE PRE-CHANGE BASELINE
--------------------------------------------------------------------------------

  unit lane          10 tasks, 242 tests passed
  integration lane    9 tasks, 223 tests passed      3m13.852s
  coverage lane      456 tests passed                4m6.745s

  api 177 · dispatcher 16 · gateway 21 · e2e 9  = 223

  coverage   89.08 statements | 82.39 branches | 89.03 functions | 90.58 lines

**3m13.852s IS THE BASELINE SC-004 MEASURES AGAINST.** Not chapter 3.9's recorded
`3m15s`, which was taken at 213 integration tests against a chapter that finished
on 223 (research R28). Same machine, same test count, measured here.

**COVERAGE IS LOWER THAN THE DOCUMENTS SAY**, and the reason is worth more than
the number. `quickstart.md` V8 and T033 cite a floor of 89.50% statements and
82.73% branches, from `specs/029-chapter-3-8/captured-output.md`. The tree now
measures **89.08 / 82.39** — 0.42 and 0.34 lower — across 456 tests rather than 441.

More tests and less coverage looks like a contradiction and is not. Chapter 3.9's
coverage was measured *before* its last three commits, and one of them was the fix
research R46 records: `notifications.itest.ts` stopped calling
`sweepDisabledEndpoints` and drove the scoped `recordAttemptOutcome` path instead,
because the global sweep was reaching into a neighbouring suite's fixture. That was
the right change and it left a global function with one fewer caller.

So a correctness fix lowered a coverage figure. The floor in `quickstart.md` and
T033 is corrected to these numbers.

**"EVERY PER-FILE RATCHET STILL PASSES" WAS WRONG, and this paragraph used to say
it.** It was inferred from the overall figures rather than read off the run. One
ratchet was already red — `repository.ts` measured 98.7% functions against a
threshold of 100 — and it had been red before this feature began, because
`drainDisableNotifications`'s `onError` default has never had a caller that omits
it. Found during T033 and recorded in research R48; the correction is to require
the handler.

  20 ratchet entries in force, of which 14 are per-file:
    repository.ts · expand.ts · deliver.ts · resume.ts · user-token.ts
    disable.ts · analytics.ts · bucket.ts · policy.ts · fallback.ts
    store.ts · auth-limiter.ts · client-address.ts · rate-limit.middleware.ts
    gateway/limits.ts

  lowest-covered files, for context rather than action:
    packages/service-kit/src/index.ts                 20.00 stmt
    services/api/src/db/migrate.ts                    40.74 stmt
    services/api/src/notifications/notification-relay.ts  58.06 stmt / 50.00 br

`notification-relay.ts` is the file chapter 3.9 shipped, and its `start`/`stop`/`run`
loop is never entered — every suite drives `drainOnce()` directly, which is the same
shape chapter 3.3's relay has. Named here because a reader comparing 89.08 against
89.50 deserves to know which file the difference lives in.

--------------------------------------------------------------------------------
T003 — THE POLLUTED-DATABASE CENSUS
--------------------------------------------------------------------------------

The development database, on the machine that shipped chapters 3.5 through 3.9:

  outbox unpublished           285
  deliveries due             8,543
  notifications undelivered     12
  endpoints sweepable            0
  environments total        17,803

Against research R1's figures, taken four days and several lane runs earlier —
190 / 8,364 / 14 / 35 / 17,542 — every count that accumulates has gone up and
**sweepable endpoints has fallen from 35 to zero**, because the lane keeps sweeping
them. The adversarial condition this database supplies by accident is therefore
partial: it has far more than enough due deliveries and unpublished events to fill
any default batch, and nothing at all for a sweep to catch.

That is the argument for planting rather than relying on accumulation. An aged
database is adversarial in the dimensions that only grow, and benign in the one
that gets consumed — which is the dimension instance 6 lived in.

--------------------------------------------------------------------------------
T004 — THE FRESH DATABASE CURRENTLY PROVES NOTHING
--------------------------------------------------------------------------------

Created and migrated a scratch database with `fresh-db.sh`, then ran the api
integration lane against it:

  177 passed (177)

Green. That is the number SC-001 is a delta against: **today zero of the six
recorded instances fail under the conditions this feature promises**, because a
freshly migrated database is the easy condition and every one of the six needs a
neighbour's rows to be visible before it breaks.

The whole feature is the gap between this line and T003's census.

--------------------------------------------------------------------------------
T005 — fresh-db.sh
--------------------------------------------------------------------------------

`specs/030-global-operation-guard/fresh-db.sh`, with `create`, `drop` and `url`.
Verified: creates, migrates, reports 0 environments, prints its URL.

It fails with a sentence rather than a stack trace when the api has not been built,
because the migration runner it calls is build output.

--------------------------------------------------------------------------------
T009b — THE PLANTING DID NOT FAIL, BECAUSE THE CONSTRAINTS WERE READ FIRST
--------------------------------------------------------------------------------

T009b says to watch the planting fail once before trusting it, because research
hit `webhook_endpoints_disabled_check` while re-planting an endpoint by hand.

It did not fail. The four check constraints were read off the live schema before a
single `INSERT` was written:

  environments_kind_check              kind IN ('development','production')
  humans_provider_check                provider IN ('github','google')
  memberships_role_check               role IN ('owner','admin','member')
  webhook_endpoints_disabled_check     (disabled_at IS NULL) = (disabled_reason IS NULL)
  webhook_endpoints_failure_run_check  (failure_run_started_at IS NULL) = (failure_run_attempts IS NULL)

First run against a fresh database:

  {"endpoints":1,"due":200,"unpublished":200,"undelivered":200,"unaddressable":1}

Re-planted: due deliveries still 200, not 400. Idempotent (FR-003).

The task's instinct was right and its remedy was one step too late. "Watch it fail"
is what you do when you have guessed at a schema; reading `pg_constraint` first is
cheaper than a failure and produces the same knowledge. The five constraint
definitions are now comments beside the inserts they govern, which is where the
next person editing them will look.

## T023 — callers broken by removing sweepDisabledEndpoints's default

One, in production code: `services/api/src/webhooks/delivery-relay.ts:169`,
`sweepDisabledEndpoints(db)` -> `sweepDisabledEndpoints(db, 100)`. The four call
sites in `deliveries.itest.ts` already passed `10_000` — chapter 3.7's fix for
instance 1 — so the compiler found exactly one.

## V7 — the lint rule fires

    services/api/src/messages/history.itest.ts
      1:10  error  'outboxDepth' import from '../db/repository' is restricted. …
    ✖ 1 problem (1 error, 0 warnings)

    services/api/src/db/repository.itest.ts
      6:41  error  'outboxDepth' import from './repository' is restricted. …
    ✖ 1 problem (1 error, 0 warnings)

Two spellings, two entries. The same import added to `outbox/outbox.itest.ts`,
which is on the ignores list, exits 0.

================================================================================
T033 — THE POST-CHANGE MEASUREMENT
================================================================================

  unit lane          11 tasks, 306 tests passed              3.781s
  integration lane   10 tasks, 231 tests passed           3m10.218s
  coverage lane      53 files, 472 tests passed           4m 5.770s

  api 177 · dispatcher 16 · gateway 21 · e2e 9 · test-harness 8  = 231

  coverage   89.08 statements | 82.35 branches | 89.25 functions | 90.58 lines
  repository.ts   97.27 | 90.90 | 100 | 99.24     (ratchet: 97 | 90 | 100 | 99)

Against T002's baseline of 3m13.852s at 223 tests: **3m10.218s at 231 tests**, a
lane that is 3.6 seconds FASTER carrying 8 more tests. SC-004 asked for growth
under 10 seconds and the growth is negative. Two things paid for the bait: the
dispatcher lane no longer plants any (research R44), and the api lane's relays are
off, so nine suites stopped running four background loops each (research R39).

Coverage moved 82.39 -> 82.35 branches and 89.03 -> 89.25 functions. The functions
figure rose because `drainDisableNotifications`'s unused `onError` default is gone;
that is also what returned `repository.ts` to the 100 its ratchet has demanded
since chapter 3.5, and had not met since before this feature began (research R48).

T039 — POST-SERIES AMENDMENTS
--------------------------------------------------------------------------------

Nine fenced files, five new sections plus one subsection appended to the existing
`consumer.itest.ts` entry:

  services/api/src/db/repository.ts
  services/api/src/webhooks/delivery-relay.ts
  eslint.config.mjs
  services/api/vitest.integration.config.mts
  services/gateway/vitest.integration.config.mts
  packages/e2e/vitest.integration.config.mts
  vitest.coverage.config.mts
  services/api/src/outbox/outbox.itest.ts
  services/api/src/consumer/consumer.itest.ts        (### Three)

`services/dispatcher/vitest.integration.config.mts` changed and needs no amendment:
no chapter fences it. `packages/test-harness/**` is new and unfenced, because
post-series entries are diffs against a chapter's state and a new file has none.

    check-fence-chain: 165 fenced files replay onto relay-platform across
    26 chapters (26 translated, fences mirrored, 1 retired, plus post-series
    amendments)

T042 — TRACEABILITY
--------------------------------------------------------------------------------

Searched for `FR-0\d\d[a-c]?`, `NFR-[A-Z]+-\d+` and `ADR-\d+` in every line this
feature added, across `packages/test-harness/**`, the five vitest configs,
`eslint.config.mjs`, `repository.ts`, `delivery-relay.ts`, `outbox.itest.ts` and
`consumer.itest.ts`.

**Fourteen feature-local ids had leaked into source comments** — FR-002, FR-003,
FR-009, FR-012a, FR-012b, FR-015, FR-020, FR-022 (x5), FR-023, FR-024 (x4),
FR-025, FR-026 — written by someone who had just read the task warning that
chapter 3.8 did exactly this. All replaced with what they meant, or with
`feature 030`. `FR-WHK-07` stays: it resolves in `docs/04-srs.md`.

Not fixed, and named rather than left silent: `repository.ts` carries about
twenty-two bare `FR-0xx` citations from EARLIER features' specs — FR-006, FR-007,
FR-008, FR-009, FR-011, FR-013, FR-014, FR-015, FR-016, FR-017 — and none of them
resolves anywhere in `docs/`. The SRS numbers requirements `FR-XXX-NN` with a
three-letter domain. `vitest.coverage.config.mts:187` has one more. That is the
same leak at four times the size, it predates this feature, and fixing it would
rewrite prose inside regions five chapters fence.

T034 — TWENTY RUNS, INTERRUPTED AT SIX (resume point)
--------------------------------------------------------------------------------

Stopped by request part-way; to be re-run from scratch, not continued — twenty
CONSECUTIVE runs is the claim, and six plus fourteen tomorrow is not that.

  relay-platform frozen at faa97cc
  run  1  exit=0  187s  failing-files=0
  run  2  exit=0  187s  failing-files=0
  run  3  exit=0  187s  failing-files=0
  run  4  exit=0  186s  failing-files=0
  run  5  exit=0  186s  failing-files=0
  run  6  exit=0  188s  failing-files=0

Six green, 186-188s each — flat to within two seconds, so the lane does not slow
as the database ages across runs. That flatness is worth as much as the green.

The EARLIER attempt at this step failed on run 1 and is the reason the tree moved:
`notifications.itest.ts` timed out at 5,000ms because 3,400 bait notification rows
cost about 1.4ms each to drain (research R49). Three full lanes had passed on that
same tree beforehand.

To resume:

  bash <scratchpad>/030/twenty.sh          # wipes and re-runs all twenty
  # or, from the repository:
  for i in $(seq 1 20); do pnpm test:integration || break; done

Then T036 (quickstart V0-V11), the remaining transcripts in T037, and T045's tag.
