BASELINE — chapter 3.23, editing and deleting a message

THE LANE ENVIRONMENT, PINNED WHERE THE TASKS CAN SEE IT (T004)

Nine variables and one compose line. Chapter 3.22 spent two full lane runs learning that
they apply to `pnpm coverage` as well as `test:integration`, one of them predicted verbatim
by its own quickstart.

    RELAY_POSTGRES_PORT=15432 docker compose up -d --wait

    DATABASE_URL=postgres://relay:relay@localhost:15432/relay
    RELAY_REDIS_URL=redis://localhost:6379
    RELAY_NATS_URL=nats://localhost:4222
    RELAY_INTERNAL_CREDENTIAL=rk_svc_local_development_credential_0000
    RELAY_INTERNAL_CREDENTIAL_GATEWAY=rk_svc_local_development_gateway_00000
    RELAY_WEBHOOK_SECRET_KEY=BpDal75yBZp7Fc2GtGS3D1vh7qOKgCWJkF6/d0XWxBU=
    RELAY_OUTBOX_RELAY=off
    RELAY_EVENT_CONSUMER=off
    RELAY_DELIVERY_RELAY=off

All local development values. Before a timing battery:
`RELAY_POSTGRES_PORT=15432 docker compose stop api gateway dispatcher`, and nothing else runs
on the machine — including your own tooling.

VERIFY EVERY EXIT CODE INTO A VARIABLE, NEVER THROUGH A PIPE. `pnpm -s check:x 2>&1 | tail -3;
echo $?` reads `tail`'s status. This session hit that three times while ANALYSING, on the one
gate that was red — knowing the hazard is not the same as not doing it.

THE INSTRUMENTS, AND WHAT COPYING THEM COST (T001, T002)

`check-refs.py` and `sweep.py` are per-chapter copies by decision, not by default — chapter
3.22's `gaps.md` item 8 recorded that for the fourth chapter running, and its one argument is
that an instrument can be improved mid-chapter without breaking another chapter's record.
**Both halves of that bargain showed up in the first ten minutes.**

The improving half:

    check-refs.py  every id must be cited by a task that DOES WORK, not by a commit task
                   -> tested red two ways: "cited only by a commit task", "cited by no task"
    check-refs.py  the success-criteria pattern gains `[a-z]?`
                   -> 11 -> 13, and SC-002a and SC-006a were never required to have a
                      traceability row at all, which is the half that mattered
    sweep.py       the same hole, in the same field, in the sibling instrument
                   -> 11 -> 13
    check-refs.py  the checklist-format message now names the id grammar

The inheriting half, which nothing warned about:

    check-refs.py arrived with **31 stale FOREIGN pairs** — chapter 3.22's task ids,
    declared foreign in chapter 3.22's records. Carried here they would have masked a real
    citation of a task id that does not exist in this chapter. Emptied deliberately.

    sweep.py's docstring said "chapter 3.21" — it had been copied 3.21 -> 3.22 with partial
    re-pointing, and a naive replace of "3.22" would have compounded it.

**A COPY INHERITS STATE AS WELL AS CODE.** That sentence is the cost of the decision, and it
is not in chapter 3.22's argument for it.

AND ONE ANALYSIS FINDING WAS WRONG (T001b)

Pass 7 reported that a malformed task id like `T033c1` would slip past `check-refs.py` with
"ids sequential" printed, because the sequence rule reads length-4 ids and the suffix rule
reads length-5. **The two branches do miss it and the run fails anyway**: `TASK.match` fails
first, the format problem is appended, and the line `continue`s before the id list is built.

A rule iterating that id list could never see such an id. It was written, tested, found to be
dead code, and **removed rather than covered** — the ratchet's discipline applied to a
checker. What was kept is the message.


THE FOUR PREMISES, RUN RATHER THAN ASSERTED (T005–T008)

P1 — the history read has no predicate on `messages.text` (T005)

    services/api/src/db/repository.ts:4686   async listMessages
      its three `.where(` clauses, at +40, +78 and +95 of that function, are the
      channel-visibility predicate and the sequence bounds. `text` appears once,
      at +60, as a SELECTED column: `text: messages.text`.
    services/api/src/messages/messages.service.ts:176   history()
      returns `repo.listMessages(...)` rows unchanged; the only validation on that
      route is `ZodValidationPipe(historyQuerySchema)` on the QUERY.

**So a tombstone already crosses the REST history route and always has.** That is what the
spec's resume decision rests on and what T009 tests before any writer exists.

P2 — the published message payload cannot express a tombstone (T006)

    packages/protocol/src/frames.ts:20                text: z.string()
    services/api/src/messages/messages.controller.ts:194
      "`messageSchema.text` is `z.string()`, not nullable, so a tombstone could not
       be published anyway"
    services/api/src/internal/backfill.controller.ts:83
      "A tombstone (FR-MSG-08) is not a creation. When deletes arrive in Part 4 they
       get `message.deleted`, and resume will carry that frame instead"

Two comments already name the constraint. The second is a promise this chapter keeps by half:
deletes do get `message.deleted`, and resume does **not** carry it — `check-prose.py` holds
that sentence.

P3 — the event-type set is a pinned place, and the prediction is four (T007)

    event.ts:55       OUTBOX_EVENT_TYPES = [            the array
    event.ts:61       type OutboxEventType = …[number]   derived; no edit needed
    event.ts:188      "Adding a type … now forces a branch here"
    event.test.ts:144 expect([...OUTBOX_EVENT_TYPES]).toEqual([…])
    event.test.ts:149 expect(OUTBOX_EVENT_TYPES).toHaveLength(3)
    event.test.ts:255 for (const type of OUTBOX_EVENT_TYPES)   iterates; no edit needed

Seven mentions, of which **four need an edit**: the array, the branch at :188, the exact-set
assertion and the count. The two derived ones do not. **Predicted four, re-measured at the
phase that changes it** — chapter 3.22 predicted two for one close code and found four.

P4 — `message_edits` exists in exactly one place, and it is a note about its absence (T008)

    services/api/src/db/schema.ts:26   "absent, with named arrivals: message_edits (edit
                                        chapter), emoji/media tables, …"
    docs/05-sad.md                     1 occurrence — the DDL at :435

No table, no migration, no reference in any query. The absence is dated and this is the date.


T009 AND T010 — THE READER PASSED, AND THE FIRST FALSIFICATION DID NOT WORK

    repository.itest.ts  "returns a tombstone in its original position, with the run
                          unbroken (FR-011 (3.23), SC-003 (3.23))"    1 passed

Green against unchanged code, which is the whole point: the repair path this chapter's resume
decision rests on already works and nothing had said so.

**THEN THE FALSIFICATION CAME BACK GREEN.** Adding `isNotNull(messages.text)` to the history
query left the test passing. The reason is not a subtlety:

    listMessages is a TERNARY OVER TWO QUERIES
      beforeSeq branch   .orderBy(desc(...))   a backward page
      afterSeq  branch   .orderBy(asc(...))    a forward page

The test called it with no cursor, which takes the backward branch alone, and the filter went
into the forward one. **A test that covers one of two query branches passes with half its
subject applied** — chapter 3.17's T047c in a different file, and it took running the
falsification to see it, not reading the test.

The test now reads both directions in a loop with the direction in every assertion message,
and both branches turn it red:

    filter the forward branch    1 failed
    filter the backward branch   1 failed

TRACEABILITY, GENERATED RATHER THAN TYPED (T011)

43 rows — 30 requirements, 13 criteria — every id and every task read out of `spec.md` and
`tasks.md` by a script. **All 43 have at least one task that does work**, which is the check
four analysis passes could not perform because the instrument counted commit lines.

AND THE MAP FORCED A DECISION ABOUT ITS OWN CHECKER

`check-refs.py` forbids a task id in any artifact but `tasks.md`, because ids go stale on a
renumber. **`traceability.md`'s whole subject is task ids.** Exempting it would be weakening a
rule to fit an artifact — the failure this chapter has caught itself in three times — so the
prohibition became a **consistency check**: every id the map names must exist in `tasks.md`.
A renumber now turns it red instead of leaving it quietly wrong.

That rule then caught its own author twice: the improved format message used `T000`/`T000a` as
examples, and both were parsed as references to tasks that do not exist. They are `Tnnn` now.

PHASE 1 GATES

    check-refs.py   exit 0   142 tasks, ids sequential, 30 requirements, 13 criteria, all traced
    sweep.py        exit 0   no superseded claim, no placeholder, no count disagreement
    check-prose.py  exit 1   6 claims, 6 still present — RED ON PURPOSE until their phases
    typecheck       exit 0


PHASE 2 — THE PROTOCOL, AND TWO PINNED-PLACE PREDICTIONS THAT BOTH HELD

`message.deleted` GETS ITS OWN PAYLOAD (T013)

    id, channel, seq, user, deleted_at   and NO text field

`messageSchema` is untouched. Widening its `text` to nullable would make a null legal on a
CREATION — which the send path deliberately refuses — and would edit a contract published
since chapter 1.3. **The event that has no message is the one that stops carrying one.**

An empty string was the third option and is worse than both: a client could not tell a
deleted message from one somebody sent blank, so the platform would be asserting something
false rather than declining to say it.

`MessageDeleted` is exported beside `Message`, so a producer does not re-declare the shape
inline and the schema stays the single statement of it.

THE PINNED PLACES, COUNTED BEFORE AND MEASURED AFTER (T014, T017)

    frames.test.ts predicted   1   the `valid` fixture entry at :30
    frames.test.ts measured    1   plus one mechanical import line
      NOT moved: toHaveLength(11) at :150 and the inbound-set test at :157 — the union's
      MEMBERSHIP is unchanged, so both stay green, which is correct and is why analysis
      pass 3 had to correct the task that named them as pinned places

    codes.test.ts  predicted   1   counted before editing: the file names a total once
    codes.test.ts  measured    1   toHaveLength(18) -> 19

**Chapter 3.22 predicted two and found four for its close code**, which is why both numbers
here were counted rather than assumed. Both held.

`not_message_author`, AND WHY NOT `forbidden` (T017a)

`docs/08-error-reference.md`'s entry for the generic 403 rules itself out twice: it calls
itself *"the generic case: where a more specific code exists … that one is sent instead"*, and
its client action is *"nothing the client can retry. This is a change of credential or of
permission."* **Authorship is neither.** No credential grants it and no permission change
makes a message yours.

One code for both refusals — an end user editing somebody else's message and a tenant key
editing anybody's have the same cause and the same answer. `codes.ts` had already made this
argument twice, for `wrong_credential_type` and `wrong_credential_service`; this is the third
time and the first where the reason is not about credentials at all.

**Left undecided the default was `forbidden`**, because `ProtocolErrorFilter` maps a bare 403
to it. Analysis pass 3 found the task whose condition nobody had evaluated.

`check:errors` IS RED ON PURPOSE UNTIL PHASE 10 (T017c)

    check-error-codes: these codes have no section in the reference, so their docs_url 404s:
      not_message_author

**It reads the BUILT `packages/protocol/dist/codes.js`**, so a stale build makes it green for
the wrong reason — `pnpm build` ran before this reading. Chapter 3.22 held the same
deliberate red from its Phase 3 to its Phase 9.

AND A TASK WAS IN THE WRONG PHASE

T017a, T017b and T017c were sitting in **Phase 3**, under the fabric heading. Analysis pass
3 created them and anchored the insert on Phase 3's first task instead of Phase 2's commit,
so the error code would have been committed under a heading about subject grammars, after
Phase 2 had closed. Eight analysis passes did not catch it because **no check compares a
task's content with its phase** — the id sequence was legal, the file path was named, the
requirement was cited. Moved before T018.

THE TYPED POINTS, RE-COUNTED — AND R13'S NUMBER CANNOT BE REPRODUCED (T018a)

T018a said to re-count and carry nothing, because ADR-19's record had said three typed
points and a re-derivation found eight. The re-count did not agree with R13, and **the
reason is that R13 never said what a typed point is.** Four definitions were tried against
its 1 / 3 / 7 / 4 and none of them returns it:

    occurrences of `Message` as a whole word           1  2  4  3
    LINES mentioning it                                1  2  4  3
    adding `messageSchema`, `messageCreatedSchema`      2  2  6  3
    the same, comment lines excluded                    0  2  6  2

**A count with no definition is not a measurement**, and R18 wrote "the numbers are R13's
unchanged" — a claim about a quantity that could not be re-derived. This is the chapter's own
recorded failure mode arriving in its own research note: an instrument fitted to the examples
in front of it. R18 has been corrected in place.

The definition pinned from here on: **occurrences, on non-comment lines, of the identifiers
that name the fabric's payload type** — `Message`, `messageSchema`, `messageCreatedSchema`,
`messageDeletedPayloadSchema`, `RevisionFabric`, `revisionFabricSchema`. Comment lines are
excluded because they were the whole of R13's `1` for `packages/protocol/src/fanout.ts`,
which declares `subjectForChannel` and **no payload type at all** — the file's own docstring
says the fan-out "has always carried a wire frame's payload rather than a shape of its own".

    file                                       before  after
    packages/protocol/src/fanout.ts                 0      0
    packages/protocol/src/revision.ts               0      7   (new)
    services/api/src/fanout/publisher.ts            2      4
    services/gateway/src/fanout.ts                  6     12
    services/gateway/src/session.ts                 2      4
    total                                          10     27

`before` is `git show HEAD:` at the start of the phase, not a number carried from research.

THE NEW GRAMMAR DID NOT GO WHERE THE TASK SAID (T018c, T018d)

T018c named `packages/protocol/src/fanout.ts` and T018d named its test. **The repository's
own practice is one module per grammar** and the grep says so in one line: `presence.ts`,
`typing.ts` and `membership.ts` each declare their subject function and their fabric schema,
each with a test file of its own. `fanout.ts` holds `subjectForChannel` and nothing else.

So the fifth grammar is `packages/protocol/src/revision.ts` with
`packages/protocol/src/revision.test.ts` beside it. Another task premise that named a file
without checking where its three predecessors live — the fourth of this chapter's tasks to
be written against a plausible location rather than a measured one.

`REVISION_SUBJECT_PREFIX` AND `isChannelRevisionSubject` WERE NOT IN THE PLAN

The gateway holds **one** Redis subscriber for both `chan:{channel_id}` and
`revision:{channel_id}`, so `subscriber.on("message")` has to route on the subject. The first
version wrote `subject.startsWith("revision:")` in `services/gateway/src/fanout.ts` — a
second place that knows the grammar, which is the thing "a fabric owns its subject grammar"
forbids. `internal.ts` had already set the precedent with `EVENT_SUBJECT_PREFIX`. The
predicate now lives in `revision.ts` with a test whose negative cases are the ones that
matter: `chan:revision:c1` and `revisionc1` are both false.

THE DISCRIMINATOR'S FALSIFICATION IS NOT DEFERRED AFTER ALL (T018i)

T018i's premise: the falsification needs a delivery test to go red against, and the first one
does not exist until US1. **That stopped being true inside this phase.** T018h added three
unit tests to `services/gateway/src/session.test.ts` — the two arms and the buffering
refusal — so the distinction became observable two phases early. Five falsifications ran, all
red for the stated reason and all restored:

    F1  the subscriber does not route on the prefix        4 of 8 fanout.itest red
    F2  `subscribe` takes only the message subject         4 of 8 red
    F3  `unsubscribe` leaves the revision subject open     1 red, in 37 ms
    F4  `session.ts` stamps `message.created` again        2 session.test red
    F5  the buffering check removed                        1 session.test red

    publisher.test: two windows instead of one shared      1 red
    publisher.test: the revision on `chan:` instead        1 red ("expected 'chan:c1'")

**T033a still stands and is not redundant.** F4 falsifies the kind against a stub fanout in
one process; T033a falsifies it against two connected sockets and a real Redis, which is
where a shared reference count or a mis-scoped subject would show. A falsification at the
unit seam does not retire one at the integration seam — chapter 3.21's inert module passed
1,174 unit tests.

TWO GATES ARE RED ON PURPOSE FROM HERE, AND ONE WAS FIXED INSTEAD (T018j)

    check:errors    RED until T075 (phase 10) — `not_message_author` has no reference
                    section yet. Chapter 3.22 held the same red from its phase 3 to its 9.
    check:fences    RED until T088 (phase 11) — twelve fenced files now differ from the
                    chapters that teach them. The list, so phase 11 re-derives nothing:

        chapter 3.17  services/api/src/db/repository.itest.ts
        chapter 3.18  services/gateway/src/fanout.ts
                      services/gateway/src/fanout.itest.ts
                      services/api/src/fanout/publisher.ts
                      services/api/src/fanout/publisher.test.ts
        chapter 3.21  packages/protocol/src/frames.ts
                      packages/protocol/src/frames.test.ts
                      packages/protocol/src/index.ts
                      services/gateway/src/session.test.ts
        chapter 3.22  packages/protocol/src/codes.ts
                      packages/protocol/src/codes.test.ts
                      services/gateway/src/session.ts

    check:docs      was red and is now GREEN. `pnpm -s sync:docs` was scheduled for T076 in
                    phase 10, which would have left the gate red for eight phases over an
                    edit made in this one. ADR-24 landed here, so its mirror belongs here.
                    T076 still runs — phase 10 edits `docs/` again — and it is idempotent.

**A gate held red for eight phases catches nothing during them.** That is chapter 3.17's
recorded finding about a checker that cries wolf, one level up: the reason to fix a red gate
in the phase that broke it is not tidiness, it is that a red gate is a blind gate.

check-refs.py WALKS `*.md` ONLY, AND THIS FILE IS `.txt`

Its rule — a task id in any artifact but `tasks.md` is a defect, because it survives a
renumber attached to an unrelated task — never reaches `baseline.txt`. Every heading here is
keyed to one, `(T018a)` included, and the checker has printed "no undeclared task ids outside
tasks.md" the whole time.

The file stays as it is, deliberately: it is a lab notebook, and a record of what was run
belongs beside the id that ran it. **What was wrong is the sentence the checker printed**,
which claimed more than it checked, and that is now `no undeclared task ids in any .md
outside tasks.md`. A checker that overstates its reach is the failure this chapter has hit
four times; the reach itself is fine.

Found by the rule firing on `research.md` for two citations added in this phase — the same
two ids, in a `.md` file. The instrument caught the copy it could see.

THE ONE NEW TABLE'S SHAPE WAS NOT THE SHAPE THE SAD PUBLISHED (T019)

`data-model.md:39` says *"Its shape is the SAD's, not this chapter's"* and then gives a shape
the SAD does not have. T019 repeated it. What `docs/05-sad.md:435` actually publishes:

    CREATE TABLE message_edits (
        message_id  UUID NOT NULL REFERENCES messages(id),
        edited_at   TIMESTAMPTZ NOT NULL,
        prior_text  TEXT NOT NULL,                              -- FR-MSG-07
        PRIMARY KEY (message_id, edited_at)
    );

Three columns and a **composite primary key**. Both documents invented a fourth column —
`id UUID PRIMARY KEY` — and dropped the key. Eleven analysis passes went past it, and the
reason is the one this chapter keeps writing down: **every checker here compares identifiers,
and this was a clause.** `sweep.py` verified that the table is named in the plan, the tasks
and the data model; nothing read the DDL under the name.

Both documents corrected, and the table is built to §6.1.

WHAT THE COMPOSITE KEY COSTS, WHICH THE SAD DOES NOT SAY

`PRIMARY KEY (message_id, edited_at)` means **two edits to one message at the same timestamp
collide** — a unique violation on the second insert rather than a second history row.
Postgres timestamps hold microseconds, so this needs two edits inside one microsecond on the
same message.

Not worked around, and the reason is not that it is unlikely. A surrogate `id` would accept
both rows and leave the history with two entries claiming the same instant, which is a
silent wrong answer; the key refuses instead. **A published constraint is a decision until an
ADR supersedes it** (Constitution VII), and this chapter has no evidence to supersede it
with — the largest fixture here is five channels.

drizzle-kit GENERATED A MIGRATION THAT WOULD HAVE FAILED, AND T020 IS WHY IT DID NOT (T020)

`pnpm exec drizzle-kit generate --name message_edits` produced
**`migrations/0008_message_edits.sql`** — a number `0008_limit_policy.sql` has held since
chapter 3.9 — containing `message_edits` and then six whole `CREATE TABLE`s and fourteen
`ALTER TABLE`s replayed from migrations 0008 through 0013:

    quota_notifications  read_positions  usage_active_users
    usage_connections    usage_periods   (+ channels.last_activity_at, members.role,
                                            users.kind, three environments limits, …)

**Its snapshot sits at 0007 while the directory sits at 0013**, because those six were
hand-written and nobody regenerated a snapshot. `migrate.ts` keys its ledger on the full
filename, so the file would have been treated as new and applied — failing on
`CREATE TABLE "quota_notifications"` and rolling back the whole transaction.

Hand-written as `0014_message_edits.sql` instead, and reviewed against §6.1 line by line —
identical column for column, the only difference being the named constraint
`message_edits_message_id_edited_at_pk`, which is both the house pattern
(`read_positions_channel_id_user_id_pk`) and the name drizzle-kit itself generated.

**Bringing the snapshots forward is not this chapter's work.** Six migrations and four
chapters would have to be reconciled, and ADR-16 already says generation is a draft and the
reviewed file is the artifact. `gaps.md` gets it.

THE CATALOGUE REFUSED THE NEW TABLE, IN 4 ms, AND ALL THREE OF ITS REMEDIES WERE WRONG (T021)

    AssertionError: these tables have no path to an environment: message_edits.
    Add environment_id, add a foreign key to a table that has one, or add it to
    SPINE in db/catalogue.ts with a reason.

`classifyTables` accepted a path of length **one**: a foreign key landing directly on a table
carrying `environment_id`. `message_edits` is the first table two links out —
`message_edits → messages → channels` — and it classified as having no tenant at all.

The three remedies the message named, and why each is wrong here:

    environment_id on the table      the SAD does not publish the column; deviating from a
                                     published DDL needs an ADR, and there is no evidence
                                     to supersede it with (Constitution VII)
    a second foreign key             the same objection, plus a denormalisation
    SPINE with a reason              the row holds message text. SPINE's own comment says
                                     an entry should be an edit somebody has to justify,
                                     and "a table of message text is platform bookkeeping"
                                     cannot be justified

**So the query was wrong, not the table.** The rule the check states is that every table has
*a path* back to one environment; the implementation accepted only paths of length one, which
covered every table that existed. That is this project's most-repeated failure — a pattern
matching the examples in front of it rather than the set the rule names — arriving in the
check that CLAUDE.md calls the highest-yield instrument in the repository.

`WITH RECURSIVE` now walks the foreign-key graph and reports **the direct tables the chain
arrives at**, never the intermediate ones. That keeps `tenant-scope.itest.ts`'s existing
invariant — every entry in `via` is itself `direct` — literally unchanged:

    tenant paths: 24 base tables — 13 direct, 3 hop, 8 spine
      hop: members       → channels, users
      hop: message_edits → channels, users        (through messages)
      hop: messages      → channels, users

**"This is not a weakening" is a claim, so it was falsified twice against the live database:**

    F7a  a table with no foreign key at all       zz_orphan     red, named in the message
    F7b  a table whose only chain reaches the     zz_spineward  red, named in the message
         spine (→ organisations → nothing)

Both dropped afterwards. And the failure message was corrected: it said *"a foreign key to a
table that has one"*, which is what the query used to mean and is now advice that would send
the next chapter to denormalise a column it does not need.

TWO TRAPS THIS FILE HAD ALREADY WRITTEN DOWN, AND ONE OF THEM STILL FIRED

`catalogue.ts`'s query carries the note *"(No backticks in here: this is inside a template
literal.)"*. The new comment block went in with five of them and the parse failed at
`catalogue.ts(150,11): error TS1005`. Reading a warning is not the same as heeding it — the
same shape as chapter 3.22's `tail` trap, hit three times by somebody who had just documented
it.

The second was caught before running: a self-referencing CTE needs `WITH RECURSIVE`, and the
keyword belongs on the first CTE in the chain even though `base` and `direct` are not
recursive.

WHAT T021 ACTUALLY MEASURED, WHICH IS NOT WHAT IT SAID

T021 says to run "the suite that executes the new SQL". **Nothing writes to `message_edits`
until US1** — this phase creates the table and no query touches it. The suites that did
exercise the new SQL were the migration runner and `tenant-scope.itest.ts`, which reads
`information_schema` and is the one that refused it. `repository.itest.ts` (30 tests) and
`packages/test-harness`'s guard suite (17 tests) ran green and proved the FK and the
tenancy trigger catalogue survived, which is worth having and is not what the task claimed.

`prior_text` IS NOT NULL, AND WHAT FOLLOWS (T022)

A deletion writes **no** edit-history row: a tombstone has no text to preserve, and the
column will not take a null. That is why FR-010 (3.23) refuses an edit on a tombstone rather
than defining what its history would say — the alternative is a row whose `prior_text` is the
text of a message the caller has already been told does not exist.

It also settles a question US2 would otherwise have to answer: **a deletion is not an edit**,
in the schema and not only in the wording. The two operations touch different tables.
