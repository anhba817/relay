BASELINE — feature 057, chapter 4.11, "the half of the union that was refused"

Every phase's measurements, in the order they were taken. Where a figure appears it is what
came back, including the ones that came back from a broken instrument.

================================================================================
PHASE 1 — SETUP                                                      2026-09-19
================================================================================

T001 · The stores, on this machine's documented ports
------------------------------------------------------------------------------
  RELAY_POSTGRES_PORT=15432 docker compose up -d --wait \
    postgres redis nats clickhouse minio mailpit          EXIT 0, six healthy

  Published ports, read back rather than assumed (056-3: a restarted container
  can come back healthy publishing nothing):

    clickhouse   8123->8123, 9000->9000
    minio        9100->9000          <- 9100 on the host, 9000 in the container
    nats         4222->4222, 8222->8222
    postgres     15432->5432
    redis        6379->6379
    mailpit      1025->1025, 8025->8025

    minio  /minio/health/live   200      from the host
    clickhouse /ping            200      from the host

T002 · The fence chain, absolute
------------------------------------------------------------------------------
    check-fence-chain: 290 fenced files replay onto relay-platform
                       across 53 chapters                    EXIT 0

  ZERO PROBLEMS, and the problem line is ABSENT rather than reading zero. The
  counted success line is the one to read (055-4); an exit code would have said
  the same thing for a checker that never looked.

T003 · Runtime dependencies — and the first instrument was broken
------------------------------------------------------------------------------
  THE FIRST ATTEMPT READ 0 AND 0 WAS NOT A MEASUREMENT. A shell one-liner with
  nested quotes produced `SyntaxError: missing ) after argument list` eleven
  times and summed the eleven failures to `TOTAL runtime dependencies: 0`. The
  errors were on stderr, the total was on stdout, and a reader skimming for the
  total would have recorded a dependency count of zero for a platform with 29.
  Rewritten as a file rather than a one-liner:

      0  ./package.json                    14  ./services/api/package.json
      0  ./packages/config                  3  ./services/dispatcher
      1  ./packages/e2e                     6  ./services/gateway
      0  ./packages/outsider                3  ./services/ingester
      1  ./packages/protocol
      0  ./packages/service-kit
      1  ./packages/test-harness

    TOTAL 29 across 11 package.json       matches part4-ch10 exactly (SC-010)

T004 · The three lanes at the opening
------------------------------------------------------------------------------
    pnpm test              12 of 12 tasks, 71 files            EXIT 0
    pnpm test:integration  12 of 12 tasks, 58 files            EXIT 0
    pnpm check:errors      33 codes, 33 sections, 6 close codes EXIT 0

  Each read off its counted line. `pnpm build` ran first, because `check:errors`
  reads the built `dist`.

T001a · THE COMPOSED API COULD NOT ISSUE A SLOT, AND IT WAS MEASURED NOT INFERRED
------------------------------------------------------------------------------
  Analysis pass 8 predicted this from `compose.yaml` and never ran it. Run:

    docker compose --profile services build                   EXIT 0
    node services/api/dist/db/migrate.js                      nothing to apply
    docker compose --profile services up -d --wait            api HEALTHY

    POST localhost:4000/v1/media  ->  HTTP 503
      {"code":"media_storage_unavailable",
       "message":"the object store is not reachable; this is temporary and the
                  request can be retried"}

  A HEALTHY CONTAINER ANSWERING 503 TO EVERY SLOT REQUEST. The message says
  "temporary" and nothing was temporary about it.

  THE CONTROL, which is what makes it the address and not the store:

    from inside the api container   localhost:9100 -> ECONNREFUSED
                                    minio:9000     -> 200
    from the host                   localhost:9100 -> 200

  `compose.yaml`'s api names postgres:5432, nats:4222, redis:6379 and clickhouse
  and names MinIO nowhere, while `depends_on` waits on it. The block directly
  above the omission says *"Container names, not localhost"* — the file
  documents the class and then omits one service.

T001b · Two consumers, two addresses, because the host is in the signature
------------------------------------------------------------------------------
  `presign.ts` signs `X-Amz-SignedHeaders: host` with `host:${host}` in the
  canonical request, so a URL signed for one origin is refused at another. One
  `endpoint` field cannot serve both consumers once they disagree, and they have
  agreed until now only because every lane runs the api as a host process.

    StoreConfig.endpoint          where the CLIENT reaches the store — signed
                                  into the upload URL
    StoreConfig.internalEndpoint  where THIS SERVICE reaches it — the probe and
                                  ensureBucket. Defaults to `endpoint`.

  The default is what keeps every existing lane unchanged: unset, the two are
  the same string and nothing has to know the field exists.

  compose.yaml now sets both:
    RELAY_MINIO_ENDPOINT          http://localhost:9100
    RELAY_MINIO_INTERNAL_ENDPOINT http://minio:9000

  AFTER, the same request against a rebuilt and force-recreated api:

    POST localhost:4000/v1/media  ->  201
      media_id  9940ad63-…
      state     pending
      upload_url signed for http://localhost:9100

    PUT that URL from the host    ->  200        2,097,152 bytes

  SC-012 discharged: a slot from the COMPOSED api, and a URL usable from outside
  the compose network. tsc --noEmit EXIT 0; 39 unit files, 409 tests, green.

  store.test.ts needed two edits and both are the finding in miniature: the
  suite redirects the store to a fake server and overrode `endpoint` alone,
  which after the split would have pointed the fake at the client's address and
  left the probe talking to the real store.

T004a · The ten validators, re-verified against the tree rather than the table
------------------------------------------------------------------------------
  Seven name `attachmentSchema` — internal.ts:34, internal.ts:70, frames.ts:45,
  frames.ts:93, event.ts:373, event.ts:412, messages.schema.ts:40. Three reach
  it through `messageSchema` — revision.ts:62, internal.ts:115, frames.ts:122.
  All ten present as `data-model.md` §4b records them.

T005 · What the chain charges so far
------------------------------------------------------------------------------
    check-fence-chain: 2 problem(s) — HEAD 2
      compose.yaml                     differs at line 202   (15 titled fences)
      services/api/src/media/store.ts  differs at line 10    (1)
      services/api/src/media/store.test.ts                    (0, free)

  0 -> 2 from phase 1's edits alone. Repaired in phase 7 with `diff` hunks, not
  now: the chain is a per-chapter artifact and a hunk written against a moving
  tree is written against the wrong state.

================================================================================
PHASE 2 — THE REGISTRY AND THE ARM                                   2026-09-19
================================================================================

The registry: one code deleted, two added
------------------------------------------------------------------------------
    media_not_available     DELETED, on its own entry's instruction
    media_not_attachable    the operation, not the cause — three conditions,
                            one answer, because naming the cause is an
                            existence oracle (FR-005)
    unprocessable_request   the 422 rung's fallback, with no thrower

    check-error-codes: 33 codes, 33 sections  ->  34 codes, 34 sections
                       both directions, run by hand (no CI job — 055-3)

  `codes.test.ts` asserts the DELETION as well as the additions. "We removed it"
  is not a property anything checks: `check:errors` compares the registry to the
  reference in both directions and would catch a leftover SECTION, and nothing
  but that test catches a leftover ENTRY no section documents.

T011b · The 422 rung, run red by deleting it
------------------------------------------------------------------------------
    green                        21 of 21
    rung deleted                 2 failed

      × answers 422 with unprocessable_request, from a throw that names nothing
        AssertionError: expected 'internal_error' to be 'unprocessable_request'
      × does not fall 422 through to internal_error
        AssertionError: expected 'internal_error' not to be 'internal_error'

  The second failure is the defect in one line. A ladder that answers
  `internal_error` everywhere satisfies "every rung answers something", which is
  why the negative half is asserted separately.

THE ARM, AND WHAT THE COMPILER ASKED FOR
------------------------------------------------------------------------------
  `.refine(() => false)` and its `protocolCode` params are gone; `media_id` is
  `z.uuid()` where it was `z.string().min(1)`.

  ONE PERMISSIVE ELEMENT, NOT FOUR COPIES. `forwardedAttachmentSchema` is
  exported once and used by all five forwarding readers. The strict union is
  unchanged and still refuses an arm it does not know, which is what a request
  schema is for.

  AND THE COMPILER NAMED SEVEN SITES, WHICH IS THE `attachments: required`
  COMMENT EARNING ITS KEEP. Making the schemas permissive left hand-written
  types narrow, and `tsc` listed every place a forwarded value met a strict one:

    consumer/runtime.ts:166      OutboxEvent's attachments was Attachment[]
    session.ts:399, 1377         send(frame: Frame) — the outbound relay
    session.ts:1365              highWaterMarks(… messages: Message[])
    session.ts:1628              fanout.publish(message: Message)

  EACH ONE WAS FIXED BY WIDENING THE TYPE, NOT BY CASTING. A consumer really can
  be handed an arm its binary does not know — that is what the schema now says —
  and a type that denied it would move the lie one layer in. Two of them got
  something better than a wider type:

    highWaterMarks   typed by what it READS: `{ messages: { seq: number }[] }`.
                     It touches one field and never an attachment.
    send             takes `RelayedFrame`, a new type: the published `Frame`
                     contract is unchanged, because what is happening is not
                     "this platform intends to send unknown arms" but "two
                     versions of the server briefly disagree", and the honest
                     place to say that is the type of the process between them.

FIVE READERS, FIVE RED-FIRST PROBES, ALL FIRED
------------------------------------------------------------------------------
    outbox consumer      strict -> expected [] to include '<event id>'
                         (the message was TERMINATED and never came back)
    send response        strict -> "send returned a payload the contract does
                         not allow"
    backfill page        strict -> same
    fanout delivery      strict -> deadline; the frame was dropped
    fanout revision      strict -> deadline; the edit was dropped

**AND THE TASKS' OWN PREMISE HAD MOVED UNDER THEM.** T017b, T017e and T017g each
said to test with `{ "type": "media", "media_id": "<uuid>" }` and to run it red
"against the arm as it stands today". That was the right probe when the media arm
REFUSED. This chapter makes it accept — so a media attachment now parses under the
strict union too, and every one of those tests would have passed with or without
the permissive reader. **They would have asserted nothing.** What discriminates
the two is an arm from a writer NEWER than this binary, so each test sends
`{ type: "audio_clip", clip_id, duration_ms }` beside the media arm, and the red
probe reverts the READER rather than the arm.

  A fourth premise held: the negative control still passes. A payload that is not
  a message (`seq: -1`) is still dropped. Permissive about the ARM is not
  permissive about being an attachment — `forwardedAttachmentSchema` still
  refuses a value with no `type`, and the pair of tests is what says so.

AND ONE TEST FIXTURE WAS WRONG IN A WAY THE DESIGN CAUGHT
------------------------------------------------------------------------------
  The send-response test first used the WIRE frame's spelling — `channel` — where
  the internal contract takes `channel_id`. The strict object refused it: *"send
  returned a payload the contract does not allow."* That is the half of this
  design that is supposed to stay loud, so the fixture was wrong and the schema
  was right. Two shapes for one message, and a test that conflates them is how
  the permissive half would have been blamed for the strict half's work.

T029 PULLED FORWARD FROM PHASE 3, BY THE COMPILER
------------------------------------------------------------------------------
  `messages.itest.ts` cited `media_not_available` in a `docsUrl()` call, so the
  api stopped typechecking the moment the code was deleted. Both tests are
  CONVERTED rather than deleted, each keeping its subject:

    "refuses a media_id that really exists"  ->  "accepts …", 422 -> 201
    "answers a media_id with its own code"   ->  a 400 naming
                                                 attachments.0.media_id

  The second is the test that would have been a 500 (R3): with the old
  `z.string().min(1)` and an accepting arm, `m_1` reaches the driver and the
  filter calls it `internal_error`.

GATES AT THE CLOSE OF PHASE 2
------------------------------------------------------------------------------
    pnpm lint            0 errors   (1 pre-existing warning, session.perf.itest.ts)
    pnpm typecheck       14 of 14
    pnpm test            12 of 12
    pnpm check:errors    34 codes, 34 sections, both directions
    pnpm check:fences    2 -> 17 problems, absolute

  Seventeen is the running cost of thirteen edited fenced files and is repaired
  in phase 7 with `diff` hunks (T062-T065), not now: a hunk written against a
  tree that is still moving is written against the wrong state.

================================================================================
PHASE 3 — THE PREDICATE AND THE ACCEPT PATH (US1)                    2026-09-19
================================================================================

The predicate: one query, the difference taken in order
------------------------------------------------------------------------------
  `assertAttachableMedia`, inside `sendMessage`'s transaction, after the ban,
  the channel and the quota and before the insert. Three clauses in the clause's
  own order: environment, uploader, state.

  ONE `IN` AND NOT N. Up to ten round trips inside a write transaction for a
  question one query answers. What it costs is that "which one failed" becomes a
  set difference — and the difference preserves POSITION, because the refusal's
  field is `attachments.<n>.media_id`.

  T020a · `senderMustBeBot` IS REUSED AND THE ALTERNATIVE IS THE VIOLATION. The
  predicate needs one fact — whether the caller is an application credential —
  and `sendMessage` already carries it under a name about the sender. A second
  boolean called `callerIsApplication` would read better here and is exactly what
  research R5 forbids: *"the repository must not learn what a credential is."*

  `state IN ('pending','ready')` IS WRITTEN IN FULL AND ONE ARM CANNOT OCCUR.
  The CHECK constraint is `state = 'pending'`. The clause names both, so the
  predicate names both, and whoever builds the scanner does not have to find it.

TESTS · a new file, 13 of 13
------------------------------------------------------------------------------
  `services/api/src/media/attach.itest.ts`. Its own file because FR-MED-06 needs
  BOTH credential classes and two users in one tenant, which neither existing
  suite has — and **a new file costs the fence chain nothing**, which is the
  cheap direction 051 recorded.

    accept  own slot · API key's own slot · a TENANT slot attached by a user
            (R1's case, the one the specification would have refused)
            one media + one url in order · no text · the ten cap over the union
    read    the send response AND history, both `toEqual` on the whole array
    edit    the attachment survives (FR-020)
    delete  the column is nulled and the media row stands (FR-024)
    refuse  three conditions, byte-identical bodies · `attachments.2.media_id`
            · this code in the envelope · no row and no sequence movement

  THE RED PROBE FIRED FOR ITS OWN REASON. Deleting the environment clause:
  `expected 201 to be 422` on the foreign tenant's id, with the other nine green —
  so the test fails for the tenancy reason and not for a shared refusal.

AND THE FIXTURE WAS WRONG THREE TIMES, EACH TIME ONLY THE DATABASE COULD SAY
------------------------------------------------------------------------------
    createChannel({name,type})   -> 23502, `external_id` null; it takes positionals
    addMember(channel, "a")      -> 22P02 string_to_uuid; it takes the row's UUID
    a bot with no description    -> 23514 users_bot_description_check

  **Reading cannot find what the schema refuses** — 046's rule, three times in
  one `beforeAll`. None of the three is about this chapter and all three would
  have read as the predicate failing.

THE SOCKET DOOR, AND A STALE BUILD ONE LAYER OUT
------------------------------------------------------------------------------
  `session.itest.ts:415` is CONVERTED, not deleted: it asserted *"refuses a
  media_id and SAYS hosted media is unavailable"* and now asserts that `m_1` is
  refused for being a non-UUID and that the message is **not** the old sentence —
  the assertion that catches a gateway still carrying 3.24's schema.

  Two new socket tests: a frame carrying the sender's own `media_id` COMMITS, and
  a frame carrying a foreign one gets `media_not_attachable` rather than
  `invalid_frame`.

  T032c FAILED FIRST WITH `no error within 5s`, AND IT WAS THE STALE `dist`.
  The gateway suite spawns `services/api/dist`, which predated the predicate.
  `pnpm build` and all three pass. Third recorded instance of that trap.

THE SEALED SUITE, 19 OF 19 — AND A THIRD KIND OF STALE BUILD
------------------------------------------------------------------------------
  `integrate.itest.ts` now uploads a file and attaches it from outside: slot,
  PUT to the presigned URL, send, and both arms delivered to a socket in order,
  with nothing but a published credential.

  IT FAILED FIRST WITH `expected 422 to be 201`, AND THE HOST BUILD WAS ALREADY
  FRESH. The composed api runs its own IMAGE, built at T001b before the arm
  accepted — so `pnpm build` fixes the spawned-dist trap and does nothing for
  this one. `docker compose --profile services build` is a third thing that can
  be stale, and the suite that found it is the only one that talks to a container.

T031 · The send's latency, and the first measurement was the pacing
------------------------------------------------------------------------------
    no attachment          p50  7.452   p95  9.490   mean 7.528
    one media attachment   p50  7.908   p95 10.872   mean 8.196
    delta at p50          +0.456 ms  (+6.1%)          n = 30 a side, interleaved

  WHAT IT IS A MEASUREMENT OF: one indexed lookup inside a transaction, against a
  lane whose `media_objects` holds a few hundred rows. It is not a production
  figure and the shape of the query — `IN` over a primary key — is what makes the
  small table uninformative rather than reassuring.

  THE FIRST RUN REPORTED THE MEDIA SEND **35.4% FASTER**, which is impossible and
  was the instrument. The pause sat AFTER each pair, so the plain send always
  followed a quiet gap and the media send always followed a warm one. Fixed by
  putting the pause first and alternating the order within the pair. 4.1's
  quiet-then-busy confound, reproduced exactly.

  AND THE PLATFORM'S OWN LIMITER BOUNDS THE METHOD. 200 samples a side is 600
  requests and the tenant's budget answered `429 … retry after 34 seconds`. The
  sample is 30 a side for that reason, said rather than quietly chosen — 4.8
  found the same wall from the other direction, where reading the request log
  spends the budget the reader is investigating.

AND ONE RED IN THE FULL LANE WAS MINE
------------------------------------------------------------------------------
    pnpm test:integration   11 of 12 — outbox invariant 8 red
    the same test alone     1 of 1 green
    with `--profile services` stopped, the whole lane   12 of 12

  *"Two concurrent relays publish every row exactly once"* — and I had left the
  composed api, gateway and dispatcher running, which is a SECOND set of relays
  polling the same outbox. 056 recorded a test taking a shared service away from
  its neighbours; this is the same class from the other side, a neighbour taking a
  shared table into contention with the lane. **The composed stack and the
  integration lane cannot both run.**

GATES AT THE CLOSE OF PHASE 3
------------------------------------------------------------------------------
    pnpm lint            0 errors
    pnpm typecheck       14 of 14
    pnpm test:integration 12 of 12
    pnpm test:outsider   19 of 19        against the composed platform

================================================================================
PHASE 4 — SOMEBODY ELSE'S MEDIA CANNOT BE ATTACHED (US2)             2026-09-19
================================================================================

  Most of this phase's surface landed with the accept path, because a refusal
  test and an accept test share a fixture and splitting them across two phases
  would have meant building the tenant twice. Two additions were real:

T038 · A REFUSED SEND LEAVES THREE FIGURES UNCHANGED, NOT TWO
------------------------------------------------------------------------------
  The first version counted rows and the sequence. The third is the OUTBOX, and
  each can move without the others: a platform that inserted and rolled back
  while `channels.last_sequence` kept its increment leaves a gap a resume cursor
  reads as a lost message, and an outbox row for a message that does not exist is
  worse than either — the consumer publishes it, a subscriber renders it, and
  nothing in the database agrees.

T039 · NINE GOOD ATTACHMENTS AND ONE FOREIGN STORES NOTHING
------------------------------------------------------------------------------
    field   attachments.9.media_id
    rows    0 with that text

  The nine are the assertion. A platform validating each attachment as it wrote
  would leave a message carrying nine — one the sender never wrote, delivered to
  every member, with no error anybody saw.

T041 · THE RED PROBE, RE-RUN AGAINST THE FULLER SUITE
------------------------------------------------------------------------------
    environment clause deleted   1 failed | 13 passed

  It fails for the TENANCY reason and nothing else does. A probe that turned the
  whole file red would have proved only that the predicate is load-bearing, not
  that this test is about tenancy.

T042 · THE NON-UUID IS A 400 AND NOT A 500
------------------------------------------------------------------------------
  Green in `messages.itest.ts`, the test converted in phase 2. R3 measured what
  the alternative costs: `z.string().min(1)` sends `m_1` to the driver, Postgres
  answers `invalid input syntax for type uuid`, and the filter calls it
  `internal_error` — a 500 any caller can produce with one request.

    attach.itest.ts   14 of 14

================================================================================
PHASE 5 — THE CLAUSE'S UNREACHABLE HALF (US3)                        2026-09-19
================================================================================

T046 · The database's own refusal, quoted rather than described
------------------------------------------------------------------------------
    INSERT … state = 'ready'      violates check constraint
                                  "media_objects_state_check"
    INSERT … state = 'rejected'   the same

  THE TEXT IS THE ASSERTION AND NOT THE THROW. "The insert failed" would pass
  against a typo in the column list, a missing environment or a closed pool —
  three things that are not this chapter's evidence. Both transitions FR-MED-07
  names are asserted, which is what makes the first a fact about the CHECK rather
  than a fact about the string `ready`.

  T047 HELD: the CHECK is not widened so a fixture can plant `ready`. It would buy
  a green assertion about a transition no code performs.

T048/T049 · The pins, read out of `coverage-summary.json`
------------------------------------------------------------------------------
    126 files · 1,814 tests · all passing

    attachments.ts             100 / 100 / 100 / 100     pin holds
    frames.ts                  100 / 100 / 100 / 100     forwardedMessageSchema
    revision.ts                100 / 100 / 100 / 100
    media/store.ts             100 / 100 / 100 / 100     the endpoint split
    media/presign.ts           100 / 100 / 100 / 100
    outbox/event.ts            100 / 100 / 100 / 100
    db/repository.ts          97.73 / 92.91 / 100 / 99.27   pin 92, headroom 0.91
    zod-validation.pipe.ts       90 / 52.17 / 100 / 90      NO PIN

  `zod-validation.pipe.ts` AT 52.17% BRANCHES IS T015'S COST, MEASURED. Keeping
  `protocolCode` with no producer leaves its arm uncovered and the file carries no
  pin, so nothing reports it. That was stated as the price when the decision was
  made; this is the number.

  NO PIN IS RAISED. `repository.ts` measures 92.91 against 92 and the config's own
  comment warns against moving a ratchet on one chapter's evidence. The one
  threshold failure in the run is `services/ingester/src/shape.ts` at **95.12%**
  against a 100 pin — the identical figure 4.7 recorded and deliberately left.

T050a · CONSTITUTION VI'S BRANCH CLAUSE, ANSWERED PER ARM
------------------------------------------------------------------------------
  *"Message ordering, idempotency, and tenant isolation MUST have 100% branch
  coverage (NFR-MNT-02)."* The clause is MET rather than pinned, and the evidence
  is four deletions rather than a percentage — because `repository.ts` holds
  hundreds of branches and an uncovered arm here clears the pin with room to spare.

    the three SQL clauses      no JavaScript branch at all (048's shape)
    senderMustBeBot ? …        forced -> exactly ONE test red
    refused !== undefined      never fires -> five red
    wanted.length === 0        deleted -> **17 of 17 still pass**

  TWO OF THE FOUR PROBES TOLD US SOMETHING A NUMBER COULD NOT.

  The credential arm turned out to discriminate almost nothing: an API key's own
  slot records `user_id IS NULL` and the USER predicate admits NULL, so forcing the
  user predicate on an application credential changes nothing for its own objects.
  **The suite as written would have passed with that arm deleted.** The one case
  that moves is an API key attaching a USER's object, which FR-MED-06 permits
  because it qualifies the uploader clause with *"(for user tokens)"*. That test
  did not exist until this probe asked what the arm was for; it does now, and it is
  the only thing the probe turns red.

  And the early return has **no behavioural consequence**: with no media the `IN`
  is empty, nothing comes back, nothing is refused. It is kept because most sends
  carry no media and none of them should pay a round trip. **A coverage number
  would have called all four arms covered and told nobody that one does nothing
  observable.**

================================================================================
PHASE 6 — THE GAUNTLET, AND WHAT THIS CHAPTER CANNOT FIX             2026-09-19
================================================================================

T051 · The derivation found nothing, and that was predicted
------------------------------------------------------------------------------
    targets.itest.ts   9 of 9 green

  Eight chapters running, the derivation named the new route before the
  classification did. This chapter adds NO route — the send path is already a
  target and is the derivation's own `CANARY_TARGET` — so a green run here is the
  expected answer rather than a clean bill. **The direction that still applied is
  the second one**: a route that is classified and attacked can still be attacked
  for only one of its boundaries, and nothing asks that automatically.

T052/T053 · A second boundary on one path
------------------------------------------------------------------------------
    gauntlet.itest.ts   59 of 59

  The existing attack forges a CHANNEL id; this one forges a MEDIA id and leaves
  the channel honest. Different walls, different queries, different code —
  `channels.environment_id` and `media_objects.environment_id` — and a platform
  could hold one and not the other. **Naming a route is not covering it, and
  attacking it once is not either.**

  THE ATTACK PLANTS A ROW FOR EACH TENANT, which no other attack in that file has
  to: both tables are otherwise empty and an empty table passes a leak check for
  the same reason an empty page does (4.8). And it runs a CONTROL first — the
  attacker attaching its OWN object must answer 201, or the refusal below says the
  feature is broken rather than that the boundary holds.

T054 · Run red, and the leak is the whole finding
------------------------------------------------------------------------------
    predicate removed:  status 201 (foreign) vs 422 (absent)

    {"id":"281d3ece-…","seq":3,"text":"from the attacker",
     "attachments":[{"type":"media","media_id":"094efd45-…"}]}

  **The attacker committed a message carrying the victim's object.** One test red
  and fifty-eight green, so it fails for the tenancy reason and not for a shared
  refusal — the trap the existing attack's own comment records.

T057 · check-lane-scope
------------------------------------------------------------------------------
    60 integration files · 0 unscoped reads · controls 10 of 10 fired

  59 at 056's close. The number had to rise: a run that reads nothing exits 2, and
  a control that fires proves the checker works rather than that it looked.

T055 / T055a / T056 · Written into `gaps.md` as 057-1, 057-3 and 057-2
------------------------------------------------------------------------------
  Nothing counts references to a media object (grep: zero hits, verified). A
  message can attach an object nobody uploaded to — made reachable by this chapter
  rather than caused by it. And `attachment_count` changes meaning under
  `load-analytics.mjs:178,187,200` with no clause naming the distinction.

================================================================================
PHASE 7 — THE CHAPTER                                                2026-09-19
================================================================================

T058/T059 · The prose
------------------------------------------------------------------------------
    2,396 words outside code fences        bound 2,000-4,000
    1 TRAP box · 3 figures · 1 Why · 1 ForwardRef · 1 Checkpoint

  The TRAP is the loose validator: `z.string().min(1)` was correct for two
  chapters and became a caller-triggered 500 the moment the arm accepted. **The
  cost of a loose schema is paid by whoever makes it reachable**, which is not
  the chapter that wrote it.

T062/T063 · Twenty-two files, forty-four hunks, every one anchored at -U6
------------------------------------------------------------------------------
  Generated from `--dump`, which is the checker's own replay (rule 1a), and
  verified by EXACT OCCURRENCE COUNT rather than `patch --dry-run` — `patch`
  applies with fuzz and said yes to seven hunks the checker refused at 056 (056-7).
  The generator walked -U6, 8, 10, 4, 3, 2 and stopped at the first width where
  every pre-image occurred exactly once. All twenty-two settled at 6.

    1,576 diff lines in total — the largest fence bill of any chapter so far

T064/T065 · Eight in the chapter, fourteen in the appendix
------------------------------------------------------------------------------
  The chapter publishes what carries the argument: the predicate, the arm, the
  forwarding reader, the registry excerpt, the filter rung, the service mapping,
  and the store's two addresses. **A chapter that printed all twenty-two would
  have shown a reader 1,576 diff lines to make one point about a predicate.**

  Seven of the fourteen are test files, whose assertions the chapter quotes in
  prose — the form a reader can follow — with the hunks in the appendix because
  the chain still replays them. Three more are relays the COMPILER named rather
  than the author: `session.ts`, `resume.ts` and `zod-validation.pipe.ts` changed
  because a forwarded value met a strict type.

AND ONE GOOD HUNK FAILED, FOR 053'S REASON EXACTLY
------------------------------------------------------------------------------
    hunk pre-image matched 0 times — starts "  // ONLY THE AUTHOR EVER SEES IT."

  `codes.ts` was generated against the dump's END state, and `fences/post-series.md`
  **already amends that file twice**. The appendix applies after every chapter, so
  a file it touches has one shape a reader sees at chapter N and another the chain
  reaches at the end — and the hunk was right about the wrong one.

  Moved to the appendix, placed after the two hunks that made it fail, and the
  chapter shows an untitled excerpt instead with the reason written down. 4.8 paid
  for this sentence first: **check which state a hunk is written against before
  blaming the hunk.**

T066 · The Vietnamese twin, CHECKED rather than described
------------------------------------------------------------------------------
    app/(vi)/vi/part-4/   chapter-01 chapter-02 chapter-03
    app/(en)/part-4/      chapter-01 … chapter-10

  A lag of SEVEN before this chapter and eight after. The task said eight and the
  tree said seven; 050 and 056 both recorded a task that described a corpus instead
  of checking one, so the figure here is the `ls`.

T067 · The chain
------------------------------------------------------------------------------
    22 -> 0 · EXIT 0
    290 fenced files replay onto relay-platform across 54 chapters (was 53)

THE GATES, AND ONE CAUGHT WHAT THE BUILD COULD NOT
------------------------------------------------------------------------------
    lint            exit 0     (no counted line — eslint prints nothing on success)
    build           exit 0     125 pages
    check:docs      exit 1 -> 0  after `pnpm sync:docs`
    check:srs       exit 0
    check:figures   exit 1 -> 0
    check:fences    exit 0     290 files, 54 chapters

  **`check:figures` FOUND THREE DEAD DIAGRAMS AND `pnpm build` DID NOT.** The
  figures were passed as `chart={…}` where `<Figure>` reads `code` — so all three
  would have rendered nothing, on a page that compiled, generated and served. The
  gate's whole sentence is *"every diagram passed as `code`"*, and it is the only
  thing in the repository that asks.

  And `check:docs` was red for a reason this chapter caused honestly: it edits
  `docs/08-error-reference.md`, and the tutorial serves its own mirrored copy.
  `pnpm sync:docs` is the answer, and the gate exists because the two drifting is
  invisible from either side.

================================================================================
PHASE 8 — THE RECORD                                                 2026-09-19
================================================================================

T068a · SC-010, measured twice rather than asserted once
------------------------------------------------------------------------------
    29 runtime dependencies at T003 · 29 at T068a · 11 package.json

T070 · SRS 1.18 — two clauses, and one of them was silent rather than strict
------------------------------------------------------------------------------
  FR-MED-06 gains the case it left undefined. The uploader clause read *"(for
  user tokens) was uploaded by the sending user"*; an object whose uploader is
  unrecorded was taken by an application credential acting for the environment,
  and **the first sentence withheld permission for it while the second mandated
  refusal for neither case.** The clause was silent on the commonest shape a
  customer's backend produces, and 4.10 made that column nullable expressly so
  this chapter could ask. It now states three cases and requires the three
  refusals to be indistinguishable.

  FR-MED-07 is recorded UNMET BY DECISION. It cost nothing while no attachment
  had a state; this chapter delivers the first that do and asserts the state is
  absent. **Vacuous and unmet are different facts** and nothing said which.

T070a/T070b · Both copies of the Part 4 table, for the first time
------------------------------------------------------------------------------
  `docs/12` row 12 amended, as 4.7 through 4.10 each amended their own row.

  AND `docs/07`'s COPY, WHICH HAD RECEIVED NONE OF THOSE FOUR. Four corrections,
  three of them inherited: row 12, row 11's *"four distinct refusals"* (056
  amended `docs/12` alone), the count stated as **23** when the second contraction
  took it to 22 on 2026-09-13, and *"milestones at 10, 18 and 23"* — original
  ordinals presented as current, which is how this project put hosted media at 4.5
  and 4.6 for eight analysis passes. The pre-contraction note `docs/12` carries is
  now in both.

T073b · THE QUICKSTART WAS RUN, AND IT WAS WRONG FIVE TIMES
------------------------------------------------------------------------------
  NFR-USE-03 makes *"the quickstart shall run without modification"* a `T` clause
  at 100% pass, and `ci.yml` contains the word `quickstart` zero times. This run
  is the whole of its verification, and it earned the sentence 4.10's opens with.

    1  nothing started the api          `profiles: ["services"]`; the prerequisite
                                        block starts stores and no list is the same
                                        as a list of store names
    2  `$USER_TOKEN` had no source      `ci.yml:305`: *"There is no public way to
                                        obtain one"*
    3  IT IS NOT A USER TOKEN           the seeder yields an APPLICATION credential,
                                        every send omitted `user`, and the platform
                                        answered **400** — *"name the sender in
                                        `user`"*. An application credential may send
                                        only as a bot, so §0 now creates one.
    4  re-running the seeder gives      it REUSES the existing demo environment:
       you your own tenant              two credential strings, one environment_id.
                                        The "foreign" object was mine and the send
                                        answered **201** — a reader watching for a
                                        refusal would conclude the platform leaks.
    5  `uuidgen` is not installed       three `command not found`, so the loop ran
                                        with EMPTY ids and refused them for the
                                        wrong reason. And `psql -tAc` leaves a
                                        newline `tr -d ' '` does not strip, which
                                        put a control character in the JSON body:
                                        *"Bad control character … position 107"*.

  **FOUR OF THE FIVE PRODUCED A RED THAT LOOKED LIKE A PLATFORM DEFECT.** Two of
  them — the 201 on a "foreign" id, and three refusals for empty strings — would
  have been read as this chapter's predicate failing. Running it is the only thing
  that could have told them apart, and `environments` has no `name` column either,
  which `information_schema` said and memory did not.

  After the corrections, every section:

    §1  slot pending · PUT 200 · send 201
    §2  three identical 422 `media_not_attachable` bodies
    §3  400 `invalid_request`, field `attachments.0.media_id`
    §4  violates check constraint "media_objects_state_check"
    §5  2|2 before a refusal and 2|2 after

CLOSING GATES
------------------------------------------------------------------------------
    pnpm lint              0 errors
    pnpm typecheck         14 of 14
    pnpm test              12 of 12
    pnpm test:integration  12 of 12      composed services stopped (057-5)
    pnpm test:outsider     19 of 19      against the composed platform
    pnpm check:errors      34 codes, 34 sections, both directions
    pnpm check:docs        all mirrored docs match · 19 revisions, 1.0 to 1.18
    pnpm check:figures     287 figures, every diagram passed as `code`
    pnpm check:fences      290 files, 54 chapters, **0 problems**, EXIT 0
    pnpm build             125 pages

T076 · CI, COMPARED PER ERROR — AND THE COMPARISON FOUND A JOB THAT NEVER RAN
------------------------------------------------------------------------------
    relay-tutorial — build, docs drift, fence chain      SUCCESS   (SC-009 met)
    relay-platform — the Docker-free gate                SUCCESS
    relay-platform — the sealed integration              failure, and it was
                                                         failing at the baseline too
    relay-platform — integration lanes and coverage      in progress at the time
                                                         of writing

  **THE TUTORIAL JOB SUCCEEDED ON THIS CHAPTER'S PUSH**, which is SC-009 and the thing
  the fence work was for.

  THE SEALED JOB WAS RED BEFORE AND AFTER, AND COMPARING THE COLOUR WOULD HAVE
  ENDED THERE. Its error, at the baseline and unchanged:

    Error: Cannot find module
      '/home/runner/work/relay/relay/services/api/dist/db/migrate.js'

  The path is missing `relay-platform/`. `ci.yml:281`'s migrate step carries no
  `working-directory` where **every other step in that job does** — two of the
  three jobs set it once under `defaults` and this one repeats it per step, which
  is what made one easy to miss. **The sealed suite has therefore never run in CI**:
  the job exists, it is red, and it has never reached the thing it is named for.

  This chapter is what made it matter — FR-021 puts a media test in that suite, and
  T073a's premise was that CI runs it. Fixed in one line and pushed. `gaps.md` 057-7.

  4.9 found the same shape (*"the gate was already red, so a planted drift deepened
  a red rather than turning one"*) and 4.10 found `pnpm test:integration` skipped
  for nine features. **Three chapters running have found a gate whose colour was
  true and whose meaning was not.**

  THE PER-ERROR COMPARISON, WHICH IS THE ONLY THING THAT COULD SAY THIS
------------------------------------------------------------------------------
    baseline (e8b5c3c)    4 distinct ##[error] lines
    this chapter (e539403) 4 distinct

  Raw, one difference: `cost=0.15..12…` against `cost=0.15..11…` — a Postgres
  planner ESTIMATE inside a query-plan assertion, moving because the lane's
  tables hold different row counts. Normalising the estimate:

    diff baseline chapter   EMPTY, both directions   3 distinct errors

  **THE ERROR SET IS IDENTICAL TO THE PRE-CHAPTER BASELINE.** All three are
  inherited: `session.perf.itest.ts`'s query-plan assertion, which holds only at
  volume, and the two lines its failure produces. The chapter introduced no new
  CI failure, and **a colour could not have said that** — the workflow was red
  before and is red after.
